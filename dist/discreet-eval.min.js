var __require = /* @__PURE__ */ ((x) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x, {
  get: (a, b) => (typeof require !== "undefined" ? require : a)[b]
}) : x)(function(x) {
  if (typeof require !== "undefined") return require.apply(this, arguments);
  throw Error('Dynamic require of "' + x + '" is not supported');
});

// node_modules/@nyariv/sandboxjs/dist/Sandbox.min.js
var AsyncFunction = Object.getPrototypeOf(async function() {
}).constructor;
var GeneratorFunction = Object.getPrototypeOf(function* () {
}).constructor;
var AsyncGeneratorFunction = Object.getPrototypeOf(async function* () {
}).constructor;
var SandboxGlobal = function(e) {
  for (const t in e) this[t] = e[t];
};
var ExecContext = class {
  constructor(e, t, n, o, s, r, i, a, c, p, l, d) {
    this.ctx = e, this.constants = t, this.tree = n, this.getSubscriptions = o, this.setSubscriptions = s, this.changeSubscriptions = r, this.setSubscriptionsGlobal = i, this.changeSubscriptionsGlobal = a, this.evals = c, this.registerSandboxFunction = p, this.allowJit = l, this.evalContext = d;
  }
};
function createContext(e, t) {
  const n = new SandboxGlobal(t.globals), o = { sandbox: e, globalsWhitelist: new Set(Object.values(t.globals)), prototypeWhitelist: new Map([...t.prototypeWhitelist].map((e2) => [e2[0].prototype, e2[1]])), options: t, globalScope: new Scope(null, t.globals, n), sandboxGlobal: n, ticks: { ticks: 0n, tickLimit: t.executionQuota }, sandboxedFunctions: /* @__PURE__ */ new WeakSet() };
  return o.prototypeWhitelist.set(Object.getPrototypeOf([][Symbol.iterator]()), /* @__PURE__ */ new Set()), o;
}
function createExecContext(sandbox, executionTree, evalContext) {
  const evals = /* @__PURE__ */ new Map(), execContext = new ExecContext(sandbox.context, executionTree.constants, executionTree.tree, /* @__PURE__ */ new Set(), /* @__PURE__ */ new WeakMap(), /* @__PURE__ */ new WeakMap(), sandbox.setSubscriptions, sandbox.changeSubscriptions, evals, (e) => sandbox.sandboxFunctions.set(e, execContext), !!evalContext, evalContext);
  if (evalContext) {
    const func = evalContext.sandboxFunction(execContext), asyncFunc = evalContext.sandboxAsyncFunction(execContext);
    evals.set(Function, func), evals.set(AsyncFunction, asyncFunc), evals.set(GeneratorFunction, func), evals.set(AsyncGeneratorFunction, asyncFunc), evals.set(eval, evalContext.sandboxedEval(func)), evals.set(setTimeout, evalContext.sandboxedSetTimeout(func, execContext)), evals.set(setInterval, evalContext.sandboxedSetInterval(func, execContext)), evals.set(clearTimeout, evalContext.sandboxedClearTimeout(execContext)), evals.set(clearInterval, evalContext.sandboxedClearInterval(execContext));
    for (const [e, t] of evals) sandbox.context.prototypeWhitelist.set(t.prototype, /* @__PURE__ */ new Set()), sandbox.context.prototypeWhitelist.set(e.prototype, /* @__PURE__ */ new Set());
  }
  return execContext;
}
var CodeString = class _CodeString {
  constructor(e) {
    this.ref = { str: "" }, e instanceof _CodeString ? (this.ref = e.ref, this.start = e.start, this.end = e.end) : (this.ref.str = e, this.start = 0, this.end = e.length);
  }
  substring(e, t) {
    if (!this.length) return this;
    (e = this.start + e) < 0 && (e = 0), e > this.end && (e = this.end), (t = void 0 === t ? this.end : this.start + t) < 0 && (t = 0), t > this.end && (t = this.end);
    const n = new _CodeString(this);
    return n.start = e, n.end = t, n;
  }
  get length() {
    const e = this.end - this.start;
    return e < 0 ? 0 : e;
  }
  char(e) {
    if (this.start !== this.end) return this.ref.str[this.start + e];
  }
  toString() {
    return this.ref.str.substring(this.start, this.end);
  }
  trimStart() {
    const e = /^\s+/.exec(this.toString()), t = new _CodeString(this);
    return e && (t.start += e[0].length), t;
  }
  slice(e, t) {
    return e < 0 && (e = this.end - this.start + e), e < 0 && (e = 0), void 0 === t && (t = this.end - this.start), t < 0 && (t = this.end - this.start + t), t < 0 && (t = 0), this.substring(e, t);
  }
  trim() {
    const e = this.trimStart(), t = /\s+$/.exec(e.toString());
    return t && (e.end -= t[0].length), e;
  }
  valueOf() {
    return this.toString();
  }
};
function keysOnly(e) {
  const t = Object.assign({}, e);
  for (const e2 in t) t[e2] = true;
  return t;
}
var reservedWords = /* @__PURE__ */ new Set(["await", "break", "case", "catch", "class", "const", "continue", "debugger", "default", "delete", "do", "else", "enum", "export", "extends", "false", "finally", "for", "function", "if", "implements", "import", "in", "instanceof", "let", "new", "null", "return", "super", "switch", "this", "throw", "true", "try", "typeof", "var", "void", "while", "with"]);
var Scope = class {
  constructor(e, t = {}, n) {
    this.const = {}, this.let = {}, this.var = {};
    const o = void 0 !== n || null === e;
    this.parent = e, this.allVars = t, this.let = o ? this.let : keysOnly(t), this.var = o ? keysOnly(t) : this.var, this.globals = null === e ? keysOnly(t) : {}, this.functionThis = n;
  }
  get(e) {
    const t = "this" === e, n = this.getWhereValScope(e, t);
    return n && t ? new Prop({ this: n.functionThis }, e, false, false, true) : n ? new Prop(n.allVars, e, e in n.const, e in n.globals, true) : new Prop(void 0, e);
  }
  set(e, t) {
    if ("this" === e) throw new SyntaxError('"this" cannot be assigned');
    if (reservedWords.has(e)) throw new SyntaxError("Unexepected token '" + e + "'");
    const n = this.get(e);
    if (void 0 === n.context) throw new ReferenceError(`Variable '${e}' was not declared.`);
    if (null === n.context) throw new TypeError(`Cannot set properties of null, (setting '${e}')`);
    if (n.isConst) throw new TypeError(`Cannot assign to const variable '${e}'`);
    if (n.isGlobal) throw new SandboxError(`Cannot override global variable '${e}'`);
    return n.context[n.prop] = t, n;
  }
  getWhereValScope(e, t) {
    return t ? void 0 !== this.functionThis ? this : this.parent?.getWhereValScope(e, t) || null : !(e in this.allVars) || e in {} && !hasOwnProperty(this.allVars, e) ? this.parent?.getWhereValScope(e, t) || null : this;
  }
  getWhereVarScope(e, t = false) {
    return !(e in this.allVars) || e in {} && !hasOwnProperty(this.allVars, e) ? null === this.parent || t || void 0 !== this.functionThis ? this : this.parent.getWhereVarScope(e, t) : this;
  }
  declare(e, t, n = void 0, o = false) {
    if ("this" === e) throw new SyntaxError('"this" cannot be declared');
    if (reservedWords.has(e)) throw new SyntaxError("Unexepected token '" + e + "'");
    const s = this.getWhereVarScope(e, "var" !== t);
    if ("var" === t) {
      if (s.var[e]) return s.allVars[e] = n, o ? s.globals[e] = true : delete s.globals[e], new Prop(s.allVars, e, false, s.globals[e], true);
      if (e in s.allVars) throw new SyntaxError(`Identifier '${e}' has already been declared`);
    }
    if (e in s.allVars) throw new SyntaxError(`Identifier '${e}' has already been declared`);
    return o && (s.globals[e] = true), s[t][e] = true, s.allVars[e] = n, new Prop(this.allVars, e, "const" === t, o, true);
  }
};
var LocalScope = class {
};
var SandboxError = class extends Error {
};
var SandboxExecutionQuotaExceededError = class extends SandboxError {
};
var SandboxExecutionTreeError = class extends SandboxError {
};
var SandboxCapabilityError = class extends SandboxError {
};
var SandboxAccessError = class extends SandboxError {
};
function isLisp(e) {
  return Array.isArray(e) && "number" == typeof e[0] && 0 !== e[0] && 88 !== e[0];
}
var Prop = class {
  constructor(e, t, n = false, o = false, s = false) {
    this.context = e, this.prop = t, this.isConst = n, this.isGlobal = o, this.isVariable = s;
  }
  get(e) {
    const t = this.context;
    if (void 0 === t) throw new ReferenceError(`${this.prop.toString()} is not defined`);
    if (null === t) throw new TypeError(`Cannot read properties of null, (reading '${this.prop.toString()}')`);
    return e.getSubscriptions.forEach((e2) => e2(t, this.prop.toString())), t[this.prop];
  }
};
function hasOwnProperty(e, t) {
  return Object.prototype.hasOwnProperty.call(e, t);
}
var ExecReturn = class {
  constructor(e, t, n, o = false, s = false) {
    this.auditReport = e, this.result = t, this.returned = n, this.breakLoop = o, this.continueLoop = s;
  }
};
var optional = {};
function generateArgs(e, t) {
  const n = {};
  return e.forEach((e2, o) => {
    e2.startsWith("...") ? n[e2.substring(3)] = t.slice(o) : n[e2] = t[o];
  }), n;
}
function createFunction(e, t, n, o, s, r) {
  if (o.ctx.options.forbidFunctionCreation) throw new SandboxCapabilityError("Function creation is forbidden");
  let i;
  return i = void 0 === r ? (...r2) => {
    const i2 = generateArgs(e, r2);
    return executeTree(n, o, t, void 0 === s ? [] : [new Scope(s, i2)]).result;
  } : function(...r2) {
    const i2 = generateArgs(e, r2);
    return executeTree(n, o, t, void 0 === s ? [] : [new Scope(s, i2, this)]).result;
  }, o.registerSandboxFunction(i), o.ctx.sandboxedFunctions.add(i), i;
}
function createFunctionAsync(e, t, n, o, s, r) {
  if (o.ctx.options.forbidFunctionCreation) throw new SandboxCapabilityError("Function creation is forbidden");
  if (!o.ctx.prototypeWhitelist?.has(Promise.prototype)) throw new SandboxCapabilityError("Async/await not permitted");
  let i;
  return i = void 0 === r ? async (...r2) => {
    const i2 = generateArgs(e, r2);
    return (await executeTreeAsync(n, o, t, void 0 === s ? [] : [new Scope(s, i2)])).result;
  } : async function(...r2) {
    const i2 = generateArgs(e, r2);
    return (await executeTreeAsync(n, o, t, void 0 === s ? [] : [new Scope(s, i2, this)])).result;
  }, o.registerSandboxFunction(i), o.ctx.sandboxedFunctions.add(i), i;
}
function assignCheck(e, t, n = "assign") {
  if (void 0 === e.context) throw new ReferenceError(`Cannot ${n} value to undefined.`);
  if (e.isConst) throw new TypeError("Assignment to constant variable.");
  if (e.isGlobal) throw new SandboxAccessError(`Cannot ${n} property '${e.prop.toString()}' of a global object`);
  if (null === e.context) throw new TypeError("Cannot set properties of null");
  if ("function" == typeof e.context[e.prop] && !hasOwnProperty(e.context, e.prop)) throw new SandboxAccessError(`Override prototype property '${e.prop.toString()}' not allowed`);
  "delete" === n ? hasOwnProperty(e.context, e.prop) && (t.changeSubscriptions.get(e.context)?.forEach((t2) => t2({ type: "delete", prop: e.prop.toString() })), t.changeSubscriptionsGlobal.get(e.context)?.forEach((t2) => t2({ type: "delete", prop: e.prop.toString() }))) : hasOwnProperty(e.context, e.prop) ? (t.setSubscriptions.get(e.context)?.get(e.prop.toString())?.forEach((e2) => e2({ type: "replace" })), t.setSubscriptionsGlobal.get(e.context)?.get(e.prop.toString())?.forEach((e2) => e2({ type: "replace" }))) : (t.changeSubscriptions.get(e.context)?.forEach((t2) => t2({ type: "create", prop: e.prop.toString() })), t.changeSubscriptionsGlobal.get(e.context)?.forEach((t2) => t2({ type: "create", prop: e.prop.toString() })));
}
var arrayChange = /* @__PURE__ */ new Set([[].push, [].pop, [].shift, [].unshift, [].splice, [].reverse, [].sort, [].copyWithin]);
var KeyVal = class {
  constructor(e, t) {
    this.key = e, this.val = t;
  }
};
var SpreadObject = class {
  constructor(e) {
    this.item = e;
  }
};
var SpreadArray = class {
  constructor(e) {
    this.item = e;
  }
};
var If = class {
  constructor(e, t) {
    this.t = e, this.f = t;
  }
};
var literalRegex = /(\$\$)*(\$)?\${(\d+)}/g;
var ops = /* @__PURE__ */ new Map();
function addOps(e, t) {
  ops.set(e, t);
}
var prorptyKeyTypes = ["string", "number", "symbol"];
function isPropertyKey(e) {
  return prorptyKeyTypes.includes(typeof e);
}
function hasPossibleProperties(e) {
  return null != e;
}
function getGlobalProp(e, t, n) {
  if (!e) return;
  const o = "function" == typeof e;
  e instanceof Prop && (n || (n = e), e = e.get(t));
  const s = n?.prop || "prop";
  if (e === globalThis) return new Prop({ [s]: t.ctx.sandboxGlobal }, s, n?.isConst || false, false, n?.isVariable || false);
  const r = o && t.evals.get(e);
  return r ? new Prop({ [s]: r }, s, n?.isConst || false, true, n?.isVariable || false) : void 0;
}
function valueOrProp(e, t) {
  return e instanceof Prop ? e.get(t) : e !== optional ? e : void 0;
}
function execMany(e, t, n, o, s, r, i) {
  t === execSync ? _execManySync(e, n, o, s, r, i) : _execManyAsync(e, n, o, s, r, i).catch(o);
}
function _execManySync(e, t, n, o, s, r) {
  const i = [];
  for (let a = 0; a < t.length; a++) {
    let c = syncDone((n2) => execSync(e, t[a], o, s, n2, r)).result;
    if (c instanceof ExecReturn && (c.returned || c.breakLoop || c.continueLoop)) return void n(void 0, c);
    if (isLisp(t[a]) && 8 === t[a][0]) return void n(void 0, new ExecReturn(s.ctx.auditReport, c, true));
    i.push(c);
  }
  n(void 0, i);
}
async function _execManyAsync(e, t, n, o, s, r) {
  const i = [];
  for (let a = 0; a < t.length; a++) {
    let c;
    try {
      let n2;
      c = true === (n2 = asyncDone((n3) => execAsync(e, t[a], o, s, n3, r))).isInstant ? n2.instant : (await n2.p).result;
    } catch (e2) {
      return void n(e2);
    }
    if (c instanceof ExecReturn && (c.returned || c.breakLoop || c.continueLoop)) return void n(void 0, c);
    if (isLisp(t[a]) && 8 === t[a][0]) return void n(void 0, new ExecReturn(s.ctx.auditReport, c, true));
    i.push(c);
  }
  n(void 0, i);
}
function asyncDone(e) {
  let t, n = false;
  const o = new Promise((o2, s) => {
    e((...e2) => {
      1 === e2.length ? s(e2[0]) : (n = true, t = e2[1], o2({ result: e2[1] }));
    });
  });
  return { isInstant: n, instant: t, p: o };
}
function syncDone(e) {
  let t, n;
  if (e((...e2) => {
    n = 1 === e2.length ? { error: e2[0] } : void 0, t = e2[1];
  }), n) throw n.error;
  return { result: t };
}
async function execAsync(e, t, n, o, s, r) {
  let i = s;
  const a = new Promise((e2) => {
    i = (...t2) => {
      s(...t2), e2();
    };
  });
  if (!_execNoneRecurse(e, t, n, o, i, true, r) && isLisp(t)) {
    let s2, a2 = t[0];
    try {
      let i2;
      s2 = true === (i2 = asyncDone((s3) => execAsync(e, t[1], n, o, s3, r))).isInstant ? i2.instant : (await i2.p).result;
    } catch (e2) {
      return void i(e2);
    }
    let c, p = s2;
    try {
      p = s2 instanceof Prop ? s2.get(o) : s2;
    } catch (e2) {
      return void i(e2);
    }
    if (20 === a2 || 21 === a2) {
      if (null == p) return void i(void 0, optional);
      a2 = 20 === a2 ? 1 : 5;
    }
    if (p === optional) {
      if (1 === a2 || 5 === a2) return void i(void 0, p);
      p = void 0;
    }
    if (89 === a2 && null != p) return void i(void 0, p);
    try {
      let s3;
      c = true === (s3 = asyncDone((s4) => execAsync(e, t[2], n, o, s4, r))).isInstant ? s3.instant : (await s3.p).result;
    } catch (e2) {
      return void i(e2);
    }
    let l = c;
    try {
      l = c instanceof Prop ? c.get(o) : c;
    } catch (e2) {
      return void i(e2);
    }
    l === optional && (l = void 0), performOp({ op: a2, exec: execAsync, done: i, ticks: e, a: p, b: l, obj: s2, context: o, scope: n, bobj: c, inLoopOrSwitch: r, tree: t });
  }
  await a;
}
function execSync(e, t, n, o, s, r) {
  if (!_execNoneRecurse(e, t, n, o, s, false, r) && isLisp(t)) {
    let i = t[0], a = syncDone((s2) => execSync(e, t[1], n, o, s2, r)).result, c = a instanceof Prop ? a.get(o) : a;
    if (20 === i || 21 === i) {
      if (null == c) return void s(void 0, optional);
      i = 20 === i ? 1 : 5;
    }
    if (c === optional) {
      if (1 === i || 5 === i) return void s(void 0, c);
      c = void 0;
    }
    if (89 === i && null != c) return void s(void 0, c);
    let p = syncDone((s2) => execSync(e, t[2], n, o, s2, r)).result, l = p instanceof Prop ? p.get(o) : p;
    l === optional && (l = void 0), performOp({ op: i, exec: execSync, done: s, ticks: e, a: c, b: l, obj: a, context: o, scope: n, bobj: p, inLoopOrSwitch: r, tree: t });
  }
}
function checkHaltExpectedTicks(e, t = 0) {
  const n = e.context.ctx.sandbox, o = e.context.ctx.options, { ticks: s, scope: r, context: i, done: a, op: c } = e;
  if (n.halted) {
    const t2 = n.subscribeResume(() => {
      t2.unsubscribe();
      try {
        const t3 = ops.get(c);
        if (!t3) return void a(new SyntaxError("Unknown operator: " + c));
        t3(e);
      } catch (e2) {
        if (o.haltOnSandboxError && e2 instanceof SandboxError) {
          const t3 = n.subscribeResume(() => {
            t3.unsubscribe(), a(e2);
          });
          n.haltExecution({ error: e2, ticks: s, scope: r, context: i });
        } else a(e2);
      }
    });
    return true;
  }
  if (s.tickLimit && s.tickLimit <= s.ticks + BigInt(t)) {
    const t2 = n.subscribeResume(() => {
      t2.unsubscribe();
      try {
        const t3 = ops.get(c);
        if (!t3) return void a(new SyntaxError("Unknown operator: " + c));
        t3(e);
      } catch (e2) {
        if (i.ctx.options.haltOnSandboxError && e2 instanceof SandboxError) {
          const t3 = n.subscribeResume(() => {
            t3.unsubscribe(), a(e2);
          });
          n.haltExecution({ error: e2, ticks: s, scope: r, context: i });
        } else a(e2);
      }
    }), o2 = new SandboxExecutionQuotaExceededError("Execution quota exceeded");
    return n.haltExecution({ error: o2, ticks: s, scope: r, context: i }), true;
  }
  return false;
}
function performOp(e) {
  const { done: t, op: n, ticks: o, context: s, scope: r } = e;
  o.ticks++;
  const i = s.ctx.sandbox;
  if (!checkHaltExpectedTicks(e)) try {
    const o2 = ops.get(n);
    if (!o2) return void t(new SandboxExecutionTreeError("Unknown operator: " + n));
    o2(e);
  } catch (e2) {
    if (s.ctx.options.haltOnSandboxError && e2 instanceof SandboxError) {
      const n2 = i.subscribeResume(() => {
        n2.unsubscribe(), t(e2);
      });
      i.haltExecution({ error: e2, ticks: o, scope: r, context: s });
    } else t(e2);
  }
}
addOps(1, ({ done: e, a: t, b: n, obj: o, context: s, scope: r }) => {
  if (null === t) throw new TypeError(`Cannot read properties of null (reading '${n?.toString()}')`);
  if (isPropertyKey(n) || (n = `${n}`), void 0 === t && void 0 === o && "string" == typeof n) {
    const t2 = r.get(n);
    t2.context === s.ctx.sandboxGlobal && s.ctx.options.audit && s.ctx.auditReport?.globalsAccess.add(n);
    return void e(void 0, getGlobalProp(t2.context ? t2.context[t2.prop] : void 0, s, t2) || t2);
  }
  if (void 0 === t) throw new TypeError(`Cannot read properties of undefined (reading '${n.toString()}')`);
  if (!hasPossibleProperties(t)) return void e(void 0, new Prop(void 0, n));
  const i = "function" == typeof t || !hasOwnProperty(t, n);
  if (s.ctx.options.audit && i) {
    let e2 = Object.getPrototypeOf(t);
    do {
      hasOwnProperty(e2, n) && (s.ctx.auditReport && !s.ctx.auditReport.prototypeAccess[e2.constructor.name] && (s.ctx.auditReport.prototypeAccess[e2.constructor.name] = /* @__PURE__ */ new Set()), s.ctx.auditReport?.prototypeAccess[e2.constructor.name].add(n));
    } while (e2 = Object.getPrototypeOf(e2));
  }
  if (i) {
    if ("function" == typeof t && hasOwnProperty(t, n)) {
      const o3 = s.ctx.prototypeWhitelist.get(t.prototype), r2 = s.ctx.options.prototypeReplacements.get(t);
      if (r2) return void e(void 0, new Prop(r2(t, true), n));
      if ((!o3 || o3.size && !o3.has(n)) && !s.ctx.sandboxedFunctions.has(t)) throw new SandboxAccessError(`Static method or property access not permitted: ${t.name}.${n.toString()}`);
    }
    let o2 = t;
    for (; o2 = Object.getPrototypeOf(o2); ) if (hasOwnProperty(o2, n) || "__proto__" === n) {
      const r2 = s.ctx.prototypeWhitelist.get(o2), i2 = s.ctx.options.prototypeReplacements.get(o2.constructor);
      if (i2) return void e(void 0, new Prop(i2(t, false), n));
      if (r2 && (!r2.size || r2.has(n)) || s.ctx.sandboxedFunctions.has(o2.constructor)) break;
      if ("__proto__" === n) throw new SandboxAccessError("Access to prototype of global object is not permitted");
      throw new SandboxAccessError(`Method or property access not permitted: ${o2.constructor.name}.${n.toString()}`);
    }
  }
  const a = t[n];
  if ("function" == typeof t && "prototype" === n && !s.ctx.sandboxedFunctions.has(t)) throw new SandboxAccessError("Access to prototype of global object is not permitted");
  if ("__proto__" === n && !s.ctx.sandboxedFunctions.has(a?.constructor)) throw new SandboxAccessError("Access to prototype of global object is not permitted");
  const c = getGlobalProp(a, s);
  if (c) return void e(void 0, c);
  const p = o instanceof Prop && o.isGlobal || "function" == typeof t && !s.ctx.sandboxedFunctions.has(t) || s.ctx.globalsWhitelist.has(t);
  e(void 0, new Prop(t, n, false, p, false));
}), addOps(5, ({ done: e, a: t, b: n, obj: o, context: s }) => {
  if (s.ctx.options.forbidFunctionCalls) throw new SandboxCapabilityError("Function invocations are not allowed");
  if ("function" != typeof t) throw new TypeError(`${"symbol" == typeof o.prop ? "Symbol" : o.prop} is not a function`);
  const r = n.map((e2) => e2 instanceof SpreadArray ? [...e2.item] : [e2]).flat().map((e2) => valueOrProp(e2, s));
  if ("function" == typeof o) {
    let t2 = o(...r);
    return t2 = getGlobalProp(t2, s) || t2, void e(void 0, t2);
  }
  if (o.context[o.prop] === JSON.stringify && s.getSubscriptions.size) {
    const e2 = /* @__PURE__ */ new Set(), t2 = (n2) => {
      if (n2 && "object" == typeof n2 && !e2.has(n2)) {
        e2.add(n2);
        for (const e3 of Object.keys(n2)) s.getSubscriptions.forEach((t3) => t3(n2, e3)), t2(n2[e3]);
      }
    };
    t2(r[0]);
  }
  if (o.context instanceof Array && arrayChange.has(o.context[o.prop]) && (s.changeSubscriptions.get(o.context) || s.changeSubscriptionsGlobal.get(o.context))) {
    let e2, t2 = false;
    if ("push" === o.prop) e2 = { type: "push", added: r }, t2 = !!r.length;
    else if ("pop" === o.prop) e2 = { type: "pop", removed: o.context.slice(-1) }, t2 = !!e2.removed.length;
    else if ("shift" === o.prop) e2 = { type: "shift", removed: o.context.slice(0, 1) }, t2 = !!e2.removed.length;
    else if ("unshift" === o.prop) e2 = { type: "unshift", added: r }, t2 = !!r.length;
    else if ("splice" === o.prop) e2 = { type: "splice", startIndex: r[0], deleteCount: void 0 === r[1] ? o.context.length : r[1], added: r.slice(2), removed: o.context.slice(r[0], void 0 === r[1] ? void 0 : r[0] + r[1]) }, t2 = !!e2.added.length || !!e2.removed.length;
    else if ("reverse" === o.prop || "sort" === o.prop) e2 = { type: o.prop }, t2 = !!o.context.length;
    else if ("copyWithin" === o.prop) {
      const n2 = void 0 === r[2] ? o.context.length - r[1] : Math.min(o.context.length, r[2] - r[1]);
      e2 = { type: "copyWithin", startIndex: r[0], endIndex: r[0] + n2, added: o.context.slice(r[1], r[1] + n2), removed: o.context.slice(r[0], r[0] + n2) }, t2 = !!e2.added.length || !!e2.removed.length;
    }
    t2 && (s.changeSubscriptions.get(o.context)?.forEach((t3) => t3(e2)), s.changeSubscriptionsGlobal.get(o.context)?.forEach((t3) => t3(e2)));
  }
  o.get(s);
  let i = o.context[o.prop](...r);
  i = getGlobalProp(i, s) || i, e(void 0, i);
}), addOps(22, ({ done: e, b: t }) => {
  let n = {};
  for (const e2 of t) e2.key instanceof SpreadObject ? n = { ...n, ...e2.key.item } : n[e2.key] = e2.val;
  e(void 0, n);
}), addOps(6, ({ done: e, a: t, b: n }) => e(void 0, new KeyVal(t, n))), addOps(12, ({ done: e, b: t, context: n }) => {
  e(void 0, t.map((e2) => e2 instanceof SpreadArray ? [...e2.item] : [e2]).flat().map((e2) => valueOrProp(e2, n)));
}), addOps(23, ({ done: e, b: t }) => e(void 0, t)), addOps(35, ({ done: e, b: t }) => {
  switch (t) {
    case "true":
      return e(void 0, true);
    case "false":
      return e(void 0, false);
    case "null":
      return e(void 0, null);
    case "undefined":
      return e(void 0, void 0);
    case "NaN":
      return e(void 0, NaN);
    case "Infinity":
      return e(void 0, 1 / 0);
  }
  e(new Error("Unknown symbol: " + t));
}), addOps(7, ({ done: e, b: t }) => e(void 0, Number(t.replace(/_/g, "")))), addOps(83, ({ done: e, b: t }) => e(void 0, BigInt(t.replace(/_/g, "")))), addOps(2, ({ done: e, b: t, context: n }) => e(void 0, n.constants.strings[parseInt(t)])), addOps(85, ({ done: e, b: t, context: n }) => {
  const o = n.constants.regexes[parseInt(t)];
  if (!n.ctx.globalsWhitelist.has(RegExp)) throw new SandboxCapabilityError("Regex not permitted");
  e(void 0, new RegExp(o.regex, o.flags));
}), addOps(84, ({ exec: e, done: t, ticks: n, b: o, context: s, scope: r }) => {
  const i = s.constants.literals[parseInt(o)], [, a, c] = i, p = [];
  let l;
  const d = [];
  for (; l = literalRegex.exec(a); ) l[2] || (p.push(c[parseInt(l[3], 10)]), d.push(l[3]));
  e(n, p, r, s, (...e2) => {
    const n2 = {};
    if (1 === e2.length) return void t(e2[0]);
    const o2 = e2[1];
    for (const e3 of Object.keys(o2)) {
      const t2 = d[e3];
      n2[t2] = o2[e3];
    }
    t(void 0, a.replace(/(\\\\)*(\\)?\${(\d+)}/g, (e3, t2, o3, r2) => {
      if (o3) return e3;
      return (t2 || "") + `${valueOrProp(n2[r2], s)}`;
    }));
  });
}), addOps(18, ({ done: e, b: t }) => {
  e(void 0, new SpreadArray(t));
}), addOps(17, ({ done: e, b: t }) => {
  e(void 0, new SpreadObject(t));
}), addOps(24, ({ done: e, b: t }) => e(void 0, !t)), addOps(64, ({ done: e, b: t }) => e(void 0, ~t)), addOps(25, ({ done: e, obj: t, context: n }) => {
  assignCheck(t, n), e(void 0, ++t.context[t.prop]);
}), addOps(26, ({ done: e, obj: t, context: n }) => {
  assignCheck(t, n), e(void 0, t.context[t.prop]++);
}), addOps(27, ({ done: e, obj: t, context: n }) => {
  assignCheck(t, n), e(void 0, --t.context[t.prop]);
}), addOps(28, ({ done: e, obj: t, context: n }) => {
  assignCheck(t, n), e(void 0, t.context[t.prop]--);
}), addOps(9, ({ done: e, b: t, obj: n, context: o, scope: s, bobj: r }) => {
  if (assignCheck(n, o), n.isGlobal = r?.isGlobal || false, n.isVariable) {
    const o2 = s.getWhereValScope(n.prop, "this" === n.prop);
    if (null === o2) throw new ReferenceError(`Cannot assign to undeclared variable '${n.prop.toString()}'`);
    return o2.set(n.prop, t), n.isGlobal ? o2.globals[n.prop.toString()] = true : delete o2.globals[n.prop.toString()], void e(void 0, t);
  }
  e(void 0, n.context[n.prop] = t);
}), addOps(66, ({ done: e, b: t, obj: n, context: o }) => {
  assignCheck(n, o), e(void 0, n.context[n.prop] += t);
}), addOps(65, ({ done: e, b: t, obj: n, context: o }) => {
  assignCheck(n, o), e(void 0, n.context[n.prop] -= t);
}), addOps(67, ({ done: e, b: t, obj: n, context: o }) => {
  assignCheck(n, o), e(void 0, n.context[n.prop] /= t);
}), addOps(69, ({ done: e, b: t, obj: n, context: o }) => {
  assignCheck(n, o), e(void 0, n.context[n.prop] *= t);
}), addOps(68, ({ done: e, b: t, obj: n, context: o }) => {
  assignCheck(n, o), e(void 0, n.context[n.prop] **= t);
}), addOps(70, ({ done: e, b: t, obj: n, context: o }) => {
  assignCheck(n, o), e(void 0, n.context[n.prop] %= t);
}), addOps(71, ({ done: e, b: t, obj: n, context: o }) => {
  assignCheck(n, o), e(void 0, n.context[n.prop] ^= t);
}), addOps(72, ({ done: e, b: t, obj: n, context: o }) => {
  assignCheck(n, o), e(void 0, n.context[n.prop] &= t);
}), addOps(73, ({ done: e, b: t, obj: n, context: o }) => {
  assignCheck(n, o), e(void 0, n.context[n.prop] |= t);
}), addOps(76, ({ done: e, b: t, obj: n, context: o }) => {
  assignCheck(n, o), e(void 0, n.context[n.prop] <<= t);
}), addOps(75, ({ done: e, b: t, obj: n, context: o }) => {
  assignCheck(n, o), e(void 0, n.context[n.prop] >>= t);
}), addOps(74, ({ done: e, b: t, obj: n, context: o }) => {
  assignCheck(n, o), e(void 0, n.context[n.prop] >>>= t);
}), addOps(57, ({ done: e, a: t, b: n }) => e(void 0, t > n)), addOps(56, ({ done: e, a: t, b: n }) => e(void 0, t < n)), addOps(55, ({ done: e, a: t, b: n }) => e(void 0, t >= n)), addOps(54, ({ done: e, a: t, b: n }) => e(void 0, t <= n)), addOps(52, ({ done: e, a: t, b: n }) => e(void 0, t == n)), addOps(32, ({ done: e, a: t, b: n }) => e(void 0, t === n)), addOps(53, ({ done: e, a: t, b: n }) => e(void 0, t != n)), addOps(31, ({ done: e, a: t, b: n }) => e(void 0, t !== n)), addOps(29, ({ done: e, a: t, b: n }) => e(void 0, t && n)), addOps(30, ({ done: e, a: t, b: n }) => e(void 0, t || n)), addOps(89, ({ done: e, a: t, b: n }) => e(void 0, t ?? n)), addOps(77, ({ done: e, a: t, b: n }) => e(void 0, t & n)), addOps(78, ({ done: e, a: t, b: n }) => e(void 0, t | n)), addOps(33, ({ done: e, a: t, b: n }) => e(void 0, t + n)), addOps(47, ({ done: e, a: t, b: n }) => e(void 0, t - n)), addOps(59, ({ done: e, b: t }) => e(void 0, +t)), addOps(58, ({ done: e, b: t }) => e(void 0, -t)), addOps(48, ({ done: e, a: t, b: n }) => e(void 0, t / n)), addOps(49, ({ done: e, a: t, b: n }) => e(void 0, t ** n)), addOps(79, ({ done: e, a: t, b: n }) => e(void 0, t ^ n)), addOps(50, ({ done: e, a: t, b: n }) => e(void 0, t * n)), addOps(51, ({ done: e, a: t, b: n }) => e(void 0, t % n)), addOps(80, ({ done: e, a: t, b: n }) => e(void 0, t << n)), addOps(81, ({ done: e, a: t, b: n }) => e(void 0, t >> n)), addOps(82, ({ done: e, a: t, b: n }) => e(void 0, t >>> n)), addOps(60, ({ exec: e, done: t, ticks: n, b: o, context: s, scope: r }) => {
  e(n, o, r, s, (e2, n2) => {
    t(void 0, typeof valueOrProp(n2, s));
  });
}), addOps(62, ({ done: e, a: t, b: n }) => e(void 0, t instanceof n)), addOps(63, ({ done: e, a: t, b: n }) => e(void 0, t in n)), addOps(61, ({ done: e, context: t, bobj: n }) => {
  n instanceof Prop ? (assignCheck(n, t, "delete"), n.isVariable ? e(void 0, false) : e(void 0, delete n.context?.[n.prop])) : e(void 0, true);
}), addOps(8, ({ done: e, b: t }) => e(void 0, t)), addOps(34, ({ done: e, a: t, b: n, scope: o, bobj: s }) => {
  e(void 0, o.declare(t, "var", n, s?.isGlobal || false));
}), addOps(3, ({ done: e, a: t, b: n, scope: o, bobj: s }) => {
  e(void 0, o.declare(t, "let", n, s?.isGlobal || false));
}), addOps(4, ({ done: e, a: t, b: n, scope: o, bobj: s }) => {
  e(void 0, o.declare(t, "const", n, s?.isGlobal || false));
}), addOps(11, ({ done: e, ticks: t, a: n, b: o, obj: s, context: r, scope: i }) => {
  if (n = [...n], "string" == typeof s[2] || s[2] instanceof CodeString) {
    if (!r.allowJit || !r.evalContext) throw new SandboxCapabilityError("Unevaluated code detected, JIT not allowed");
    s[2] = o = r.evalContext.lispifyFunction(new CodeString(s[2]), r.constants);
  }
  n.shift() ? e(void 0, createFunctionAsync(n, o, t, r, i)) : e(void 0, createFunction(n, o, t, r, i));
}), addOps(37, ({ done: e, ticks: t, a: n, b: o, obj: s, context: r, scope: i }) => {
  if ("string" == typeof s[2] || s[2] instanceof CodeString) {
    if (!r.allowJit || !r.evalContext) throw new SandboxCapabilityError("Unevaluated code detected, JIT not allowed");
    s[2] = o = r.evalContext.lispifyFunction(new CodeString(s[2]), r.constants);
  }
  const a = n.shift(), c = n.shift();
  let p;
  p = 88 === a ? createFunctionAsync(n, o, t, r, i, c) : createFunction(n, o, t, r, i, c), c && i.declare(c, "var", p), e(void 0, p);
}), addOps(10, ({ done: e, ticks: t, a: n, b: o, obj: s, context: r, scope: i }) => {
  if ("string" == typeof s[2] || s[2] instanceof CodeString) {
    if (!r.allowJit || !r.evalContext) throw new SandboxCapabilityError("Unevaluated code detected, JIT not allowed");
    s[2] = o = r.evalContext.lispifyFunction(new CodeString(s[2]), r.constants);
  }
  const a = n.shift(), c = n.shift();
  let p;
  c && (i = new Scope(i, {})), p = 88 === a ? createFunctionAsync(n, o, t, r, i, c) : createFunction(n, o, t, r, i, c), c && i.declare(c, "let", p), e(void 0, p);
}), addOps(38, ({ exec: e, done: t, ticks: n, a: o, b: s, context: r, scope: i }) => {
  const [a, c, p, l, d, u, f] = o;
  let x = true;
  const h = new Scope(i, {}), b = { $$obj: void 0 }, g = new Scope(h, b);
  if (e === execAsync) (async () => {
    let o2;
    for (o2 = asyncDone((t2) => e(n, l, h, r, t2)), b.$$obj = true === (o2 = asyncDone((t2) => e(n, p, h, r, t2))).isInstant ? o2.instant : (await o2.p).result, o2 = asyncDone((t2) => e(n, c, g, r, t2)), a && (x = true === (o2 = asyncDone((t2) => e(n, u, g, r, t2))).isInstant ? o2.instant : (await o2.p).result); x; ) {
      const i2 = {};
      o2 = asyncDone((t2) => e(n, f, new Scope(g, i2), r, t2)), true === o2.isInstant ? o2.instant : (await o2.p).result;
      const a2 = await executeTreeAsync(n, r, s, [new Scope(h, i2)], "loop");
      if (a2 instanceof ExecReturn && a2.returned) return void t(void 0, a2);
      if (a2 instanceof ExecReturn && a2.breakLoop) break;
      o2 = asyncDone((t2) => e(n, d, g, r, t2)), x = true === (o2 = asyncDone((t2) => e(n, u, g, r, t2))).isInstant ? o2.instant : (await o2.p).result;
    }
    t();
  })().catch(t);
  else {
    for (syncDone((t2) => e(n, l, h, r, t2)), b.$$obj = syncDone((t2) => e(n, p, h, r, t2)).result, syncDone((t2) => e(n, c, g, r, t2)), a && (x = syncDone((t2) => e(n, u, g, r, t2)).result); x; ) {
      const o2 = {};
      syncDone((t2) => e(n, f, new Scope(g, o2), r, t2));
      const i2 = executeTree(n, r, s, [new Scope(h, o2)], "loop");
      if (i2 instanceof ExecReturn && i2.returned) return void t(void 0, i2);
      if (i2 instanceof ExecReturn && i2.breakLoop) break;
      syncDone((t2) => e(n, d, g, r, t2)), x = syncDone((t2) => e(n, u, g, r, t2)).result;
    }
    t();
  }
}), addOps(86, ({ done: e, a: t, context: n, inLoopOrSwitch: o }) => {
  if ("switch" === o && "continue" === t || !o) throw new TypeError("Illegal " + t + " statement");
  e(void 0, new ExecReturn(n.ctx.auditReport, void 0, false, "break" === t, "continue" === t));
}), addOps(13, ({ exec: e, done: t, ticks: n, a: o, b: s, context: r, scope: i, inLoopOrSwitch: a }) => {
  e(n, valueOrProp(o, r) ? s.t : s.f, i, r, t, a);
}), addOps(15, ({ exec: e, done: t, ticks: n, a: o, b: s, context: r, scope: i }) => {
  e(n, valueOrProp(o, r) ? s.t : s.f, i, r, t, void 0);
}), addOps(16, ({ done: e, a: t, b: n }) => e(void 0, new If(t, n))), addOps(14, ({ done: e, a: t, b: n }) => e(void 0, new If(t, n))), addOps(40, ({ exec: e, done: t, ticks: n, a: o, b: s, context: r, scope: i }) => {
  e(n, o, i, r, (...o2) => {
    if (1 === o2.length) return void t(o2[0]);
    let a = o2[1];
    if (a = valueOrProp(a, r), e === execSync) {
      let o3, c = false;
      for (const p of s) if (c || (c = !p[1] || a === valueOrProp(syncDone((t2) => e(n, p[1], i, r, t2)).result, r))) {
        if (!p[2]) continue;
        if (o3 = executeTree(n, r, p[2], [i], "switch"), o3.breakLoop) break;
        if (o3.returned) return void t(void 0, o3);
        if (!p[1]) break;
      }
      t();
    } else (async () => {
      let o3, c = false;
      for (const p of s) {
        let s2;
        if (c || (c = !p[1] || a === valueOrProp(true === (s2 = asyncDone((t2) => e(n, p[1], i, r, t2))).isInstant ? s2.instant : (await s2.p).result, r))) {
          if (!p[2]) continue;
          if (o3 = await executeTreeAsync(n, r, p[2], [i], "switch"), o3.breakLoop) break;
          if (o3.returned) return void t(void 0, o3);
          if (!p[1]) break;
        }
      }
      t();
    })().catch(t);
  });
}), addOps(39, ({ exec: e, done: t, ticks: n, a: o, b: s, context: r, scope: i, inLoopOrSwitch: a }) => {
  const [c, p, l] = s;
  executeTreeWithDone(e, (...o2) => {
    executeTreeWithDone(e, (...s2) => {
      if (1 === s2.length) t(s2[0]);
      else if (1 === o2.length) {
        const s3 = {};
        c && (s3[c] = o2[0]), executeTreeWithDone(e, t, n, r, p, [new Scope(i, s3)], a);
      } else t(void 0, o2[1]);
    }, n, r, l, [new Scope(i, {})]);
  }, n, r, o, [new Scope(i)], a);
}), addOps(87, ({ done: e }) => {
  e();
}), addOps(45, ({ done: e, a: t, b: n, context: o }) => {
  if (!o.ctx.globalsWhitelist.has(t) && !o.ctx.sandboxedFunctions.has(t)) throw new SandboxAccessError(`Object construction not allowed: ${t.constructor.name}`);
  e(void 0, new t(...n));
}), addOps(46, ({ done: e, b: t }) => {
  e(t);
}), addOps(43, ({ done: e, a: t }) => e(void 0, t.pop())), addOps(0, ({ done: e }) => e());
var unexecTypes = /* @__PURE__ */ new Set([11, 37, 10, 38, 39, 40, 14, 16, 60]);
var currentTicks = { current: { ticks: BigInt(0) } };
function _execNoneRecurse(e, t, n, o, s, r, i) {
  const a = r ? execAsync : execSync;
  if (currentTicks.current = e, t instanceof Prop) s(void 0, t.get(o));
  else if (t === optional) s();
  else if (Array.isArray(t) && !isLisp(t)) 0 === t[0] ? s() : execMany(e, a, t, s, n, o, i);
  else if (isLisp(t)) if (42 === t[0]) execMany(e, a, t[1], s, n, o, i);
  else if (44 === t[0]) r ? o.ctx.prototypeWhitelist?.has(Promise.prototype) ? execAsync(e, t[1], n, o, async (...e2) => {
    if (1 === e2.length) s(e2[0]);
    else try {
      s(void 0, await valueOrProp(e2[1], o));
    } catch (e3) {
      s(e3);
    }
  }, i).catch(s) : s(new SandboxCapabilityError("Async/await is not permitted")) : s(new SyntaxError("Illegal use of 'await', must be inside async function"));
  else {
    if (!unexecTypes.has(t[0])) return false;
    performOp({ op: t[0], exec: a, done: s, ticks: e, a: t[1], b: t[2], obj: t, tree: t, context: o, scope: n, bobj: void 0, inLoopOrSwitch: i });
  }
  else s(void 0, t);
  return true;
}
function executeTree(e, t, n, o = [], s) {
  return syncDone((r) => executeTreeWithDone(execSync, r, e, t, n, o, s)).result;
}
async function executeTreeAsync(e, t, n, o = [], s) {
  let r;
  return true === (r = asyncDone((r2) => executeTreeWithDone(execAsync, r2, e, t, n, o, s))).isInstant ? r.instant : (await r.p).result;
}
function executeTreeWithDone(e, t, n, o, s, r = [], i) {
  if (!s) return void t();
  if (!(s instanceof Array)) throw new SyntaxError("Bad execution tree");
  let a, c = o.ctx.globalScope;
  for (; a = r.shift(); ) "object" == typeof a && (c = a instanceof Scope ? a : new Scope(c, a, a instanceof LocalScope ? void 0 : null));
  o.ctx.options.audit && !o.ctx.auditReport && (o.ctx.auditReport = { globalsAccess: /* @__PURE__ */ new Set(), prototypeAccess: {} }), e === execSync ? _executeWithDoneSync(t, n, o, s, c, i) : _executeWithDoneAsync(t, n, o, s, c, i).catch(t);
}
function _executeWithDoneSync(e, t, n, o, s, r) {
  if (!(o instanceof Array)) throw new SyntaxError("Bad execution tree");
  let i = 0;
  for (i = 0; i < o.length; i++) {
    let a, c;
    const p = o[i];
    try {
      execSync(t, p, s, n, (...e2) => {
        1 === e2.length ? c = { error: e2[0] } : a = e2[1];
      }, r);
    } catch (e2) {
      c = { error: e2 };
    }
    if (c) return void e(c.error);
    if (a instanceof ExecReturn) return void e(void 0, a);
    if (isLisp(p) && 8 === p[0]) return void e(void 0, new ExecReturn(n.ctx.auditReport, a, true));
  }
  e(void 0, new ExecReturn(n.ctx.auditReport, void 0, false));
}
async function _executeWithDoneAsync(e, t, n, o, s, r) {
  if (!(o instanceof Array)) throw new SyntaxError("Bad execution tree");
  let i = 0;
  for (i = 0; i < o.length; i++) {
    let a, c;
    const p = o[i];
    try {
      await execAsync(t, p, s, n, (...e2) => {
        1 === e2.length ? c = { error: e2[0] } : a = e2[1];
      }, r);
    } catch (e2) {
      c = { error: e2 };
    }
    if (c) return void e(c.error);
    if (a instanceof ExecReturn) return void e(void 0, a);
    if (isLisp(p) && 8 === p[0]) return void e(void 0, new ExecReturn(n.ctx.auditReport, a, true));
  }
  e(void 0, new ExecReturn(n.ctx.auditReport, void 0, false));
}
function parseHexToInt(e) {
  return !e.match(/[^a-f0-9]/i) ? parseInt(e, 16) : NaN;
}
function validateAndParseHex(e, t, n) {
  const o = parseHexToInt(e);
  if (Number.isNaN(o) || void 0 !== n && n !== e.length) throw new SyntaxError(t + ": " + e);
  return o;
}
function parseHexadecimalCode(e) {
  const t = validateAndParseHex(e, "Malformed Hexadecimal", 2);
  return String.fromCharCode(t);
}
function parseUnicodeCode(e, t) {
  const n = validateAndParseHex(e, "Malformed Unicode", 4);
  if (void 0 !== t) {
    const e2 = validateAndParseHex(t, "Malformed Unicode", 4);
    return String.fromCharCode(n, e2);
  }
  return String.fromCharCode(n);
}
function isCurlyBraced(e) {
  return "{" === e.charAt(0) && "}" === e.charAt(e.length - 1);
}
function parseUnicodeCodePointCode(e) {
  if (!isCurlyBraced(e)) throw new SyntaxError("Malformed Unicode: +" + e);
  const t = validateAndParseHex(e.slice(1, -1), "Malformed Unicode");
  try {
    return String.fromCodePoint(t);
  } catch (e2) {
    throw e2 instanceof RangeError ? new SyntaxError("Code Point Limit:" + t) : e2;
  }
}
var singleCharacterEscapes = /* @__PURE__ */ new Map([["b", "\b"], ["f", "\f"], ["n", "\n"], ["r", "\r"], ["t", "	"], ["v", "\v"], ["0", "\0"]]);
function parseSingleCharacterCode(e) {
  return singleCharacterEscapes.get(e) || e;
}
var escapeMatch = /\\(?:(\\)|x([\s\S]{0,2})|u(\{[^}]*\}?)|u([\s\S]{4})\\u([^{][\s\S]{0,3})|u([\s\S]{0,4})|([0-3]?[0-7]{1,2})|([\s\S])|$)/g;
function unraw(e) {
  return e.replace(escapeMatch, function(e2, t, n, o, s, r, i, a, c) {
    if (void 0 !== t) return "\\";
    if (void 0 !== n) return parseHexadecimalCode(n);
    if (void 0 !== o) return parseUnicodeCodePointCode(o);
    if (void 0 !== s) return parseUnicodeCode(s, r);
    if (void 0 !== i) return parseUnicodeCode(i);
    if ("0" === a) return "\0";
    if (void 0 !== a) throw new SyntaxError("Octal Deprecation: " + a);
    if (void 0 !== c) return parseSingleCharacterCode(c);
    throw new SyntaxError("End of string");
  });
}
function createLisp(e) {
  return [e.op, e.a, e.b];
}
var NullLisp = createLisp({ op: 0, a: 0, b: 0 });
var lispTypes = /* @__PURE__ */ new Map();
var ParseError = class extends Error {
  constructor(e, t) {
    super(e + ": " + t.substring(0, 40)), this.code = t;
  }
};
var lastType;
var inlineIfElse = /^:/;
var elseIf = /^else(?![\w$])/;
var ifElse = /^if(?![\w$])/;
var space = /^\s/;
var expectTypes = { splitter: { types: { opHigh: /^(\/|\*\*|\*(?!\*)|%)(?!=)/, op: /^(\+(?!(\+))|-(?!(-)))(?!=)/, comparitor: /^(<=|>=|<(?!<)|>(?!>)|!==|!=(?!=)|===|==)/, bitwiseShift: /^(<<|>>(?!>)|>>>)(?!=)/, bitwiseAnd: /^(&(?!&))(?!=)/, bitwiseXor: /^(\^)(?!=)/, bitwiseOr: /^(\|(?!\|))(?!=)/, boolOpAnd: /^(&&)/, boolOpOr: /^(\|\||instanceof(?![\w$])|in(?![\w$]))/, nullishCoalescing: /^\?\?/ }, next: ["modifier", "value", "prop", "incrementerBefore"] }, inlineIf: { types: { inlineIf: /^\?(?!\.(?!\d))/ }, next: ["expEnd"] }, assignment: { types: { assignModify: /^(-=|\+=|\/=|\*\*=|\*=|%=|\^=|&=|\|=|>>>=|>>=|<<=)/, assign: /^(=)(?!=)/ }, next: ["modifier", "value", "prop", "incrementerBefore"] }, incrementerBefore: { types: { incrementerBefore: /^(\+\+|--)/ }, next: ["prop"] }, expEdge: { types: { call: /^(\?\.)?[(]/, incrementerAfter: /^(\+\+|--)/ }, next: ["splitter", "expEdge", "dot", "inlineIf", "expEnd"] }, modifier: { types: { not: /^!/, inverse: /^~/, negative: /^-(?!-)/, positive: /^\+(?!\+)/, typeof: /^typeof(?![\w$])/, delete: /^delete(?![\w$])/ }, next: ["modifier", "value", "prop", "incrementerBefore"] }, dot: { types: { arrayProp: /^(\?\.)?\[/, dot: /^(\?)?\.(?=\s*[a-zA-Z$_])/ }, next: ["splitter", "assignment", "expEdge", "dot", "inlineIf", "expEnd"] }, prop: { types: { prop: /^[a-zA-Z$_][a-zA-Z\d$_]*/ }, next: ["splitter", "assignment", "expEdge", "dot", "inlineIf", "expEnd"] }, value: { types: { createObject: /^\{/, createArray: /^\[/, number: /^(0b[01]+(_[01]+)*|0o[0-7]+(_[0-7]+)*|0x[\da-f]+(_[\da-f]+)*|(\d+(_\d+)*(\.\d+(_\d+)*)?|\.\d+(_\d+)*))(e[+-]?\d+(_\d+)*)?(n)?(?!\d)/i, string: /^"(\d+)"/, literal: /^`(\d+)`/, regex: /^\/(\d+)\/r(?![\w$])/, boolean: /^(true|false)(?![\w$])/, null: /^null(?![\w$])/, und: /^undefined(?![\w$])/, arrowFunctionSingle: /^(async\s+)?([a-zA-Z$_][a-zA-Z\d$_]*)\s*=>\s*({)?/, arrowFunction: /^(async\s*)?\(\s*((\.\.\.)?\s*[a-zA-Z$_][a-zA-Z\d$_]*(\s*,\s*(\.\.\.)?\s*[a-zA-Z$_][a-zA-Z\d$_]*)*)?\s*\)\s*=>\s*({)?/, inlineFunction: /^(async\s+)?function(\s*[a-zA-Z$_][a-zA-Z\d$_]*)?\s*\(\s*((\.\.\.)?\s*[a-zA-Z$_][a-zA-Z\d$_]*(\s*,\s*(\.\.\.)?\s*[a-zA-Z$_][a-zA-Z\d$_]*)*)?\s*\)\s*{/, group: /^\(/, NaN: /^NaN(?![\w$])/, Infinity: /^Infinity(?![\w$])/, void: /^void(?![\w$])\s*/, await: /^await(?![\w$])\s*/, new: /^new(?![\w$])\s*/ }, next: ["splitter", "expEdge", "dot", "inlineIf", "expEnd"] }, initialize: { types: { initialize: /^(var|let|const)\s+([a-zA-Z$_][a-zA-Z\d$_]*)\s*(=)?/, return: /^return(?![\w$])/, throw: /^throw(?![\w$])\s*/ }, next: ["modifier", "value", "prop", "incrementerBefore", "expEnd"] }, spreadObject: { types: { spreadObject: /^\.\.\./ }, next: ["value", "prop"] }, spreadArray: { types: { spreadArray: /^\.\.\./ }, next: ["value", "prop"] }, expEnd: { types: {}, next: [] }, expFunction: { types: { function: /^(async\s+)?function(\s*[a-zA-Z$_][a-zA-Z\d$_]*)\s*\(\s*((\.\.\.)?\s*[a-zA-Z$_][a-zA-Z\d$_]*(\s*,\s*(\.\.\.)?\s*[a-zA-Z$_][a-zA-Z\d$_]*)*)?\s*\)\s*{/ }, next: ["expEdge", "expEnd"] }, expSingle: { types: { for: /^(([a-zA-Z$_][\w$]*)\s*:)?\s*for\s*\(/, do: /^(([a-zA-Z$_][\w$]*)\s*:)?\s*do(?![\w$])\s*(\{)?/, while: /^(([a-zA-Z$_][\w$]*)\s*:)?\s*while\s*\(/, loopAction: /^(break|continue)(?![\w$])\s*([a-zA-Z$_][\w$]*)?/, if: /^((([a-zA-Z$_][\w$]*)\s*:)?\s*)if\s*\(/, try: /^try\s*{/, block: /^{/, switch: /^(([a-zA-Z$_][\w$]*)\s*:)?\s*switch\s*\(/ }, next: ["expEnd"] } };
var closings = { "(": ")", "[": "]", "{": "}", "'": "'", '"': '"', "`": "`" };
function testMultiple(e, t) {
  let n = null;
  for (let o = 0; o < t.length; o++) {
    if (n = t[o].exec(e), n) break;
  }
  return n;
}
var emptyString = new CodeString("");
var okFirstChars = /^[+\-~ !]/;
var aNumber = expectTypes.value.types.number;
var wordReg = /^((if|for|else|while|do|function)(?![\w$])|[\w$]+)/;
var semiColon = /^;/;
var insertedSemicolons = /* @__PURE__ */ new WeakMap();
var quoteCache = /* @__PURE__ */ new WeakMap();
function restOfExp(e, t, n, o, s, r, i = {}) {
  if (!t.length) return t;
  i.words = i.words || [];
  let a = true;
  const c = (n = n || []).includes(semiColon);
  c && (n = n.filter((e2) => e2 !== semiColon));
  const p = insertedSemicolons.get(t.ref) || [], l = quoteCache.get(t.ref) || /* @__PURE__ */ new Map();
  if (quoteCache.set(t.ref, l), o && l.has(t.start - 1)) return t.substring(0, l.get(t.start - 1) - t.start);
  let d, u = false, f = false, x = "", h = false, b = false;
  for (d = 0; d < t.length && !f; d++) {
    let g = t.char(d);
    if ('"' === o || "'" === o || "`" === o) {
      if ("`" !== o || "$" !== g || "{" !== t.char(d + 1) || u) {
        if (g === o && !u) return t.substring(0, d);
      } else {
        d += restOfExp(e, t.substring(d + 2), [], "{").length + 2;
      }
      u = !u && "\\" === g;
    } else if (closings[g]) {
      if (!b && p[d + t.start]) {
        if (b = true, c) break;
        d--, x = ";";
        continue;
      }
      if (h && "{" === g && (h = false), g === s) {
        f = true;
        break;
      }
      {
        const n2 = restOfExp(e, t.substring(d + 1), [], g);
        if (l.set(n2.start - 1, n2.end), d += n2.length + 1, a = false, r) {
          let e2;
          (e2 = testMultiple(t.substring(d).toString(), r)) && (i.regRes = e2, f = true);
        }
      }
    } else if (o) {
      if (g === closings[o]) return t.substring(0, d);
    } else {
      let e2, o2, s2 = t.substring(d).toString();
      if (r) {
        let e3;
        if (e3 = testMultiple(s2, r)) {
          i.regRes = e3, d++, f = true;
          break;
        }
      }
      if (o2 = aNumber.exec(s2)) d += o2[0].length - 1, s2 = t.substring(d).toString();
      else if (x != g) {
        let o3 = null;
        if (";" === g || p[d + t.start] && !a && !b) {
          if (c) o3 = [";"];
          else if (p[d + t.start]) {
            b = true, d--, x = ";";
            continue;
          }
          g = s2 = ";";
        } else b = false;
        o3 || (o3 = testMultiple(s2, n)), o3 && (f = true), !f && (e2 = wordReg.exec(s2)) && (h = true, e2[0].length > 1 && (i.words.push(e2[1]), i.lastAnyWord = e2[1], e2[2] && (i.lastWord = e2[2])), e2[0].length > 2 && (d += e2[0].length - 2));
      }
      if (a && (okFirstChars.test(s2) ? f = false : a = false), f) break;
    }
    x = g;
  }
  if (o) throw new SyntaxError("Unclosed '" + o + "'");
  return i && (i.oneliner = h), t.substring(0, d);
}
restOfExp.next = ["splitter", "expEnd", "inlineIf"];
var startingExecpted = ["initialize", "expSingle", "expFunction", "value", "modifier", "prop", "incrementerBefore", "expEnd"];
var setLispType = (e, t) => {
  e.forEach((e2) => {
    lispTypes.set(e2, t);
  });
};
var closingsCreate = { createArray: /^\]/, createObject: /^\}/, group: /^\)/, arrayProp: /^\]/, call: /^\)/ };
var typesCreate = { createArray: 12, createObject: 22, group: 23, arrayProp: 19, call: 5, prop: 1, "?prop": 20, "?call": 21 };
setLispType(["createArray", "createObject", "group", "arrayProp", "call"], (e, t, n, o, s, r) => {
  let i = emptyString;
  const a = [];
  let c = false, p = o[0].length;
  const l = p;
  for (; p < n.length && !c; ) i = restOfExp(e, n.substring(p), [closingsCreate[t], /^,/]), p += i.length, i.trim().length && a.push(i), "," !== n.char(p) ? c = true : p++;
  const d = ["value", "modifier", "prop", "incrementerBefore", "expEnd"];
  let u, f;
  switch (t) {
    case "group":
    case "arrayProp":
      u = lispifyExpr(e, n.substring(l, p));
      break;
    case "call":
    case "createArray":
      u = a.map((t2) => lispify(e, t2, [...d, "spreadArray"]));
      break;
    case "createObject":
      u = a.map((t2) => {
        let n2;
        t2 = t2.trimStart();
        let o2 = "";
        if (f = expectTypes.expFunction.types.function.exec("function " + t2), f) o2 = f[2].trimStart(), n2 = lispify(e, new CodeString("function " + t2.toString().replace(o2, "")));
        else {
          const s2 = restOfExp(e, t2, [/^:/]);
          o2 = lispify(e, s2, [...d, "spreadObject"]), 1 === o2[0] && (o2 = o2[2]), n2 = lispify(e, t2.substring(s2.length + 1));
        }
        return createLisp({ op: 6, a: o2, b: n2 });
      });
  }
  const x = "arrayProp" === t ? o[1] ? 20 : 1 : "call" === t ? o[1] ? 21 : 5 : typesCreate[t];
  r.lispTree = lispify(e, n.substring(p + 1), expectTypes[s].next, createLisp({ op: x, a: r.lispTree, b: u }));
});
var modifierTypes = { inverse: 64, not: 24, positive: 59, negative: 58, typeof: 60, delete: 61 };
setLispType(["inverse", "not", "negative", "positive", "typeof", "delete"], (e, t, n, o, s, r) => {
  const i = restOfExp(e, n.substring(o[0].length), [/^([^\s.?\w$]|\?[^.])/]);
  r.lispTree = lispify(e, n.substring(i.length + o[0].length), restOfExp.next, createLisp({ op: modifierTypes[t], a: r.lispTree, b: lispify(e, i, expectTypes[s].next) }));
});
var incrementTypes = { "++$": 25, "--$": 27, "$++": 26, "$--": 28 };
setLispType(["incrementerBefore"], (e, t, n, o, s, r) => {
  const i = restOfExp(e, n.substring(2), [/^[^\s.\w$]/]);
  r.lispTree = lispify(e, n.substring(i.length + 2), restOfExp.next, createLisp({ op: incrementTypes[o[0] + "$"], a: lispify(e, i, expectTypes[s].next), b: 0 }));
}), setLispType(["incrementerAfter"], (e, t, n, o, s, r) => {
  r.lispTree = lispify(e, n.substring(o[0].length), expectTypes[s].next, createLisp({ op: incrementTypes["$" + o[0]], a: r.lispTree, b: 0 }));
});
var adderTypes = { "&&": 29, "||": 30, "??": 89, instanceof: 62, in: 63, "=": 9, "-=": 65, "+=": 66, "/=": 67, "**=": 68, "*=": 69, "%=": 70, "^=": 71, "&=": 72, "|=": 73, ">>>=": 74, "<<=": 76, ">>=": 75 };
setLispType(["assign", "assignModify", "nullishCoalescing"], (e, t, n, o, s, r) => {
  r.lispTree = createLisp({ op: adderTypes[o[0]], a: r.lispTree, b: lispify(e, n.substring(o[0].length), expectTypes[s].next) });
}), setLispType(["boolOpOr"], (e, t, n, o, s, r) => {
  const i = [expectTypes.inlineIf.types.inlineIf, inlineIfElse, expectTypes.splitter.types.boolOpOr], a = restOfExp(e, n.substring(o[0].length), i);
  r.lispTree = lispify(e, n.substring(a.length + o[0].length), restOfExp.next, createLisp({ op: adderTypes[o[0]], a: r.lispTree, b: lispify(e, a, expectTypes[s].next) }));
}), setLispType(["boolOpAnd"], (e, t, n, o, s, r) => {
  const i = [expectTypes.inlineIf.types.inlineIf, inlineIfElse, expectTypes.splitter.types.boolOpAnd, expectTypes.splitter.types.boolOpOr], a = restOfExp(e, n.substring(o[0].length), i);
  r.lispTree = lispify(e, n.substring(a.length + o[0].length), restOfExp.next, createLisp({ op: adderTypes[o[0]], a: r.lispTree, b: lispify(e, a, expectTypes[s].next) }));
});
var opTypes = { "&": 77, "|": 78, "^": 79, "<<": 80, ">>": 81, ">>>": 82, "<=": 54, ">=": 55, "<": 56, ">": 57, "!==": 31, "!=": 53, "===": 32, "==": 52, "+": 33, "-": 47, "/": 48, "**": 49, "*": 50, "%": 51 };
function extractIfElse(e, t) {
  let n, o, s = 0, r = t.substring(0, 0), i = emptyString, a = true, c = {};
  for (; (r = restOfExp(e, t.substring(r.end - t.start), [elseIf, ifElse, semiColon], void 0, void 0, void 0, c)).length || a; ) {
    a = false;
    const p = t.substring(r.end - t.start).toString();
    if (p.startsWith("if")) r.end++, s++;
    else if (p.startsWith("else")) n = t.substring(0, r.end - t.start), r.end++, s--, s || r.end--;
    else {
      if (!(o = /^;?\s*else(?![\w$])/.exec(p))) {
        n = i.length ? n : t.substring(0, r.end - t.start);
        break;
      }
      n = t.substring(0, r.end - t.start), r.end += o[0].length - 1, s--, s || (r.end -= o[0].length - 1);
    }
    if (!s) {
      i = extractIfElse(e, t.substring(r.end - t.start + (/^;?\s*else(?![\w$])/.exec(p)?.[0].length || 0))).all;
      break;
    }
    c = {};
  }
  return n = n || t.substring(0, r.end - t.start), { all: t.substring(0, Math.max(n.end, i.end) - t.start), true: n, false: i };
}
setLispType(["opHigh", "op", "comparitor", "bitwiseShift", "bitwiseAnd", "bitwiseXor", "bitwiseOr"], (e, t, n, o, s, r) => {
  const i = [expectTypes.inlineIf.types.inlineIf, inlineIfElse];
  switch (t) {
    case "opHigh":
      i.push(expectTypes.splitter.types.opHigh);
    case "op":
      i.push(expectTypes.splitter.types.op);
    case "comparitor":
      i.push(expectTypes.splitter.types.comparitor);
    case "bitwiseShift":
      i.push(expectTypes.splitter.types.bitwiseShift);
    case "bitwiseAnd":
      i.push(expectTypes.splitter.types.bitwiseAnd);
    case "bitwiseXor":
      i.push(expectTypes.splitter.types.bitwiseXor);
    case "bitwiseOr":
      i.push(expectTypes.splitter.types.bitwiseOr), i.push(expectTypes.splitter.types.boolOpAnd), i.push(expectTypes.splitter.types.boolOpOr);
  }
  const a = restOfExp(e, n.substring(o[0].length), i);
  r.lispTree = lispify(e, n.substring(a.length + o[0].length), restOfExp.next, createLisp({ op: opTypes[o[0]], a: r.lispTree, b: lispify(e, a, expectTypes[s].next) }));
}), setLispType(["inlineIf"], (e, t, n, o, s, r) => {
  let i = false;
  const a = n.substring(0, 0);
  let c = 1;
  for (; !i && a.length < n.length; ) a.end = restOfExp(e, n.substring(a.length + 1), [expectTypes.inlineIf.types.inlineIf, inlineIfElse]).end, "?" === n.char(a.length) ? c++ : c--, c || (i = true);
  a.start = n.start + 1, r.lispTree = createLisp({ op: 15, a: r.lispTree, b: createLisp({ op: 16, a: lispifyExpr(e, a), b: lispifyExpr(e, n.substring(o[0].length + a.length + 1)) }) });
}), setLispType(["if"], (e, t, n, o, s, r) => {
  let i = restOfExp(e, n.substring(o[0].length), [], "(");
  const a = extractIfElse(e, n.substring(o[1].length)), c = o[0].length - o[1].length + i.length + 1;
  let p = a.true.substring(c), l = a.false;
  i = i.trim(), p = p.trim(), l = l.trim(), "{" === p.char(0) && (p = p.slice(1, -1)), "{" === l.char(0) && (l = l.slice(1, -1)), r.lispTree = createLisp({ op: 13, a: lispifyExpr(e, i), b: createLisp({ op: 14, a: lispifyBlock(p, e), b: lispifyBlock(l, e) }) });
}), setLispType(["switch"], (e, t, n, o, s, r) => {
  const i = restOfExp(e, n.substring(o[0].length), [], "(");
  let a = n.toString().indexOf("{", o[0].length + i.length + 1);
  if (-1 === a) throw new SyntaxError("Invalid switch");
  let c, p = insertSemicolons(e, restOfExp(e, n.substring(a + 1), [], "{"));
  const l = /^\s*(case\s|default)\s*/, d = [];
  let u = false;
  for (; c = l.exec(p.toString()); ) {
    if ("default" === c[1]) {
      if (u) throw new SyntaxError("Only one default switch case allowed");
      u = true;
    }
    const t2 = restOfExp(e, p.substring(c[0].length), [/^:/]);
    let n2 = emptyString, o2 = a = c[0].length + t2.length + 1;
    const s2 = /^\s*\{/.exec(p.substring(o2).toString());
    let r2 = [];
    if (s2) o2 += s2[0].length, n2 = restOfExp(e, p.substring(o2), [], "{"), o2 += n2.length + 1, r2 = lispifyBlock(n2, e);
    else {
      const t3 = restOfExp(e, p.substring(o2), [l]);
      if (t3.trim().length) {
        for (; (n2 = restOfExp(e, p.substring(o2), [semiColon])).length && (o2 += n2.length + (";" === p.char(o2 + n2.length) ? 1 : 0), !l.test(p.substring(o2).toString())); ) ;
        r2 = lispifyBlock(p.substring(a, n2.end - p.start), e);
      } else r2 = [], o2 += t3.length;
    }
    p = p.substring(o2), d.push(createLisp({ op: 41, a: "default" === c[1] ? 0 : lispifyExpr(e, t2), b: r2 }));
  }
  r.lispTree = createLisp({ op: 40, a: lispifyExpr(e, i), b: d });
}), setLispType(["dot", "prop"], (e, t, n, o, s, r) => {
  let i = o[0], a = o[0].length, c = "prop";
  if ("dot" === t) {
    o[1] && (c = "?prop");
    const e2 = n.substring(o[0].length).toString().match(expectTypes.prop.types.prop);
    if (!e2 || !e2.length) throw new SyntaxError("Hanging dot");
    i = e2[0], a = i.length + o[0].length;
  } else if (reservedWords.has(i) && "this" !== i) throw new SyntaxError(`Unexpected token '${i}'`);
  r.lispTree = lispify(e, n.substring(a), expectTypes[s].next, createLisp({ op: typesCreate[c], a: r.lispTree, b: i }));
}), setLispType(["spreadArray", "spreadObject"], (e, t, n, o, s, r) => {
  r.lispTree = createLisp({ op: "spreadArray" === t ? 18 : 17, a: 0, b: lispify(e, n.substring(o[0].length), expectTypes[s].next) });
}), setLispType(["return", "throw"], (e, t, n, o, s, r) => {
  r.lispTree = createLisp({ op: "return" === t ? 8 : 46, a: 0, b: lispifyExpr(e, n.substring(o[0].length)) });
}), setLispType(["number", "boolean", "null", "und", "NaN", "Infinity"], (e, t, n, o, s, r) => {
  r.lispTree = lispify(e, n.substring(o[0].length), expectTypes[s].next, createLisp({ op: "number" === t ? o[12] ? 83 : 7 : 35, a: 0, b: o[12] ? o[1] : o[0] }));
}), setLispType(["string", "literal", "regex"], (e, t, n, o, s, r) => {
  r.lispTree = lispify(e, n.substring(o[0].length), expectTypes[s].next, createLisp({ op: "string" === t ? 2 : "literal" === t ? 84 : 85, a: 0, b: o[1] }));
}), setLispType(["initialize"], (e, t, n, o, s, r) => {
  const i = "var" === o[1] ? 34 : "let" === o[1] ? 3 : 4;
  o[3] ? r.lispTree = createLisp({ op: i, a: o[2], b: lispify(e, n.substring(o[0].length), expectTypes[s].next) }) : r.lispTree = lispify(e, n.substring(o[0].length), expectTypes[s].next, createLisp({ op: i, a: o[2], b: 0 }));
}), setLispType(["function", "inlineFunction", "arrowFunction", "arrowFunctionSingle"], (e, t, n, o, s, r) => {
  const i = "function" !== t && "inlineFunction" !== t, a = i && !o[o.length - 1], c = i ? 2 : 3, p = o[1] ? 88 : 0, l = o[c] ? o[c].replace(/\s+/g, "").split(/,/g) : [];
  i || l.unshift((o[2] || "").trimStart());
  let d = false;
  l.forEach((e2) => {
    if (d) throw new SyntaxError("Rest parameter must be last formal parameter");
    e2.startsWith("...") && (d = true);
  });
  const u = restOfExp(e, n.substring(o[0].length), a ? [/^[,)}\]]/, semiColon] : [/^}/]), f = a ? "return " + u : u.toString();
  l.forEach((e2) => {
    if (reservedWords.has(e2.replace(/^\.\.\./, ""))) throw new SyntaxError(`Unexpected token '${e2}'`);
  }), r.lispTree = lispify(e, n.substring(o[0].length + f.length + 1), expectTypes[s].next, createLisp({ op: i ? 11 : "function" === t ? 37 : 10, a: [p, ...l], b: e.eager ? lispifyFunction(new CodeString(f), e) : f }));
});
var iteratorRegex = /^((let|var|const)\s+)?\s*([a-zA-Z$_][a-zA-Z\d$_]*)\s+(in|of)(?![\w$])/;
setLispType(["for", "do", "while"], (e, t, n, o, s, r) => {
  let i, a, c = 0, p = 88, l = [], d = 0, u = 0, f = 88, x = 88;
  switch (t) {
    case "while": {
      c = n.toString().indexOf("(") + 1;
      const t2 = restOfExp(e, n.substring(c), [], "(");
      i = lispifyReturnExpr(e, t2), a = restOfExp(e, n.substring(c + t2.length + 1)).trim(), "{" === a.char(0) && (a = a.slice(1, -1));
      break;
    }
    case "for": {
      c = n.toString().indexOf("(") + 1;
      const t2 = [];
      let o2, s2 = emptyString;
      for (let o3 = 0; o3 < 3 && (s2 = restOfExp(e, n.substring(c), [/^[;)]/]), t2.push(s2.trim()), c += s2.length + 1, ")" !== n.char(c - 1)); o3++) ;
      if (1 === t2.length && (o2 = iteratorRegex.exec(t2[0].toString()))) "of" === o2[4] ? (d = lispifyReturnExpr(e, t2[0].substring(o2[0].length)), l = [ofStart2, ofStart3], i = ofCondition, x = ofStep, u = lispify(e, new CodeString((o2[1] || "let ") + o2[3] + " = $$next.value"), ["initialize"])) : (d = lispifyReturnExpr(e, t2[0].substring(o2[0].length)), l = [inStart2, inStart3], x = inStep, i = inCondition, u = lispify(e, new CodeString((o2[1] || "let ") + o2[3] + " = $$keys[$$keyIndex]"), ["initialize"]));
      else {
        if (3 !== t2.length) throw new SyntaxError("Invalid for loop definition");
        p = lispifyExpr(e, t2.shift(), startingExecpted), i = lispifyReturnExpr(e, t2.shift()), x = lispifyExpr(e, t2.shift());
      }
      a = restOfExp(e, n.substring(c)).trim(), "{" === a.char(0) && (a = a.slice(1, -1));
      break;
    }
    case "do": {
      f = 0;
      const t2 = !!o[3];
      a = restOfExp(e, n.substring(o[0].length), t2 ? [/^\}/] : [semiColon]), i = lispifyReturnExpr(e, restOfExp(e, n.substring(n.toString().indexOf("(", o[0].length + a.length) + 1), [], "("));
      break;
    }
  }
  const h = [f, l, d, p, x, i, u];
  r.lispTree = createLisp({ op: 38, a: h, b: lispifyBlock(a, e) });
}), setLispType(["block"], (e, t, n, o, s, r) => {
  r.lispTree = createLisp({ op: 42, a: lispifyBlock(restOfExp(e, n.substring(1), [], "{"), e), b: 0 });
}), setLispType(["loopAction"], (e, t, n, o, s, r) => {
  r.lispTree = createLisp({ op: 86, a: o[1], b: 0 });
});
var catchReg = /^\s*(catch\s*(\(\s*([a-zA-Z$_][a-zA-Z\d$_]*)\s*\))?|finally)\s*\{/;
setLispType(["try"], (e, t, n, o, s, r) => {
  const i = restOfExp(e, n.substring(o[0].length), [], "{");
  let a, c, p = catchReg.exec(n.substring(o[0].length + i.length + 1).toString()), l = "", d = 0;
  p[1].startsWith("catch") ? (p = catchReg.exec(n.substring(o[0].length + i.length + 1).toString()), l = p[2], c = restOfExp(e, n.substring(o[0].length + i.length + 1 + p[0].length), [], "{"), d = o[0].length + i.length + 1 + p[0].length + c.length + 1, (p = catchReg.exec(n.substring(d).toString())) && p[1].startsWith("finally") && (a = restOfExp(e, n.substring(d + p[0].length), [], "{"))) : a = restOfExp(e, n.substring(o[0].length + i.length + 1 + p[0].length), [], "{");
  const u = [l, lispifyBlock(insertSemicolons(e, c || emptyString), e), lispifyBlock(insertSemicolons(e, a || emptyString), e)];
  r.lispTree = createLisp({ op: 39, a: lispifyBlock(insertSemicolons(e, i), e), b: u });
}), setLispType(["void", "await"], (e, t, n, o, s, r) => {
  const i = restOfExp(e, n.substring(o[0].length), [/^([^\s.?\w$]|\?[^.])/]);
  r.lispTree = lispify(e, n.substring(o[0].length + i.length), expectTypes[s].next, createLisp({ op: "void" === t ? 87 : 44, a: lispify(e, i), b: 0 }));
}), setLispType(["new"], (e, t, n, o, s, r) => {
  let i = o[0].length;
  const a = restOfExp(e, n.substring(i), [], void 0, "(");
  i += a.length + 1;
  const c = [];
  if ("(" === n.char(i - 1)) {
    const t2 = restOfExp(e, n.substring(i), [], "(");
    let o2;
    i += t2.length + 1;
    let s2 = 0;
    for (; (o2 = restOfExp(e, t2.substring(s2), [/^,/])).length; ) s2 += o2.length + 1, c.push(o2.trim());
  }
  r.lispTree = lispify(e, n.substring(i), expectTypes.expEdge.next, createLisp({ op: 45, a: lispify(e, a, expectTypes.initialize.next), b: c.map((t2) => lispify(e, t2, expectTypes.initialize.next)) }));
});
var ofStart2 = lispify(void 0, new CodeString("let $$iterator = $$obj[Symbol.iterator]()"), ["initialize"]);
var ofStart3 = lispify(void 0, new CodeString("let $$next = $$iterator.next()"), ["initialize"]);
var ofCondition = lispify(void 0, new CodeString("return !$$next.done"), ["initialize"]);
var ofStep = lispify(void 0, new CodeString("$$next = $$iterator.next()"));
var inStart2 = lispify(void 0, new CodeString("let $$keys = Object.keys($$obj)"), ["initialize"]);
var inStart3 = lispify(void 0, new CodeString("let $$keyIndex = 0"), ["initialize"]);
var inStep = lispify(void 0, new CodeString("$$keyIndex++"));
var inCondition = lispify(void 0, new CodeString("return $$keyIndex < $$keys.length"), ["initialize"]);
function lispify(e, t, n, o, s = false) {
  if (o = o || NullLisp, n = n || expectTypes.initialize.next, void 0 === t) return o;
  const r = (t = t.trimStart()).toString();
  if (!t.length && !n.includes("expEnd")) throw new SyntaxError("Unexpected end of expression");
  if (!t.length) return o;
  const i = { lispTree: o };
  let a;
  for (const o2 of n) if ("expEnd" !== o2) {
    for (const n2 in expectTypes[o2].types) if ("expEnd" !== n2 && (a = expectTypes[o2].types[n2].exec(r))) {
      lastType = n2;
      try {
        lispTypes.get(n2)?.(e, n2, t, a, o2, i);
      } catch (e2) {
        if (s && e2 instanceof SyntaxError) throw new ParseError(e2.message, r);
        throw e2;
      }
      break;
    }
    if (a) break;
  }
  if (!a && t.length) {
    if (s) throw new ParseError(`Unexpected token after ${lastType}: ${t.char(0)}`, r);
    throw new SyntaxError(`Unexpected token after ${lastType}: ${t.char(0)}`);
  }
  return i.lispTree;
}
var startingExpectedWithoutSingle = startingExecpted.filter((e) => "expSingle" !== e);
function lispifyExpr(e, t, n) {
  if (!t.trimStart().length) return NullLisp;
  const o = [];
  let s, r = 0;
  if ((n = n || expectTypes.initialize.next).includes("expSingle") && testMultiple(t.toString(), Object.values(expectTypes.expSingle.types))) return lispify(e, t, ["expSingle"], void 0, true);
  for (n === startingExecpted && (n = startingExpectedWithoutSingle); (s = restOfExp(e, t.substring(r), [/^,/])).length; ) o.push(s.trimStart()), r += s.length + 1;
  if (1 === o.length) return lispify(e, t, n, void 0, true);
  if (n.includes("initialize")) {
    const s2 = expectTypes.initialize.types.initialize.exec(o[0].toString());
    if (s2) return createLisp({ op: 42, a: o.map((t2, n2) => lispify(e, n2 ? new CodeString(s2[1] + " " + t2) : t2, ["initialize"], void 0, true)), b: 0 });
    if (expectTypes.initialize.types.return.exec(o[0].toString())) return lispify(e, t, n, void 0, true);
  }
  const i = o.map((t2) => lispify(e, t2, n, void 0, true));
  return createLisp({ op: 43, a: i, b: 0 });
}
function lispifyReturnExpr(e, t) {
  return createLisp({ op: 8, a: 0, b: lispifyExpr(e, t) });
}
function lispifyBlock(e, t, n = false) {
  if (!(e = insertSemicolons(t, e)).trim().length) return [];
  const o = [];
  let s, r = 0, i = 0, a = {}, c = false, p = false;
  for (; (s = restOfExp(t, e.substring(r), [semiColon], void 0, void 0, void 0, a)).length && (p = !(!e.char(r + s.length) || ";" === e.char(r + s.length)), r += s.length + (p ? 0 : 1), /^\s*else(?![\w$])/.test(e.substring(r).toString()) || a.words?.includes("do") && /^\s*while(?![\w$])/.test(e.substring(r).toString()) ? c = true : (c = false, o.push(e.substring(i, r - (p ? 0 : 1))), i = r), a = {}, !n); ) ;
  return c && o.push(e.substring(i, r - (p ? 0 : 1))), o.map((e2) => e2.trimStart()).filter((e2) => e2.length).map((e2) => lispifyExpr(t, e2.trimStart(), startingExecpted));
}
function lispifyFunction(e, t, n = false) {
  if (!e.trim().length) return [];
  const o = lispifyBlock(e, t, n);
  return hoist(o), o;
}
function hoist(e, t = []) {
  if (isLisp(e)) {
    if (!isLisp(e)) return false;
    const [n, o, s] = e;
    if (39 === n || 13 === n || 38 === n || 40 === n) hoist(o, t), hoist(s, t);
    else if (34 === n) t.push(createLisp({ op: 34, a: o, b: 0 }));
    else if (37 === n && o[1]) return t.push(e), true;
  } else if (Array.isArray(e)) {
    const n = [];
    for (const o of e) hoist(o, t) || n.push(o);
    n.length !== e.length && (e.length = 0, e.push(...t, ...n));
  }
  return false;
}
var closingsNoInsertion = /^(\})\s*(catch|finally|else|while|instanceof)(?![\w$])/;
var colonsRegex = /^((([\w$\])"'`]|\+\+|--)\s*\r?\n\s*([\w$+\-!~]))|(\}\s*[\w$!~+\-{("'`]))/;
function insertSemicolons(e, t) {
  let n = t, o = emptyString, s = {};
  const r = insertedSemicolons.get(t.ref) || new Array(t.ref.str.length);
  for (; (o = restOfExp(e, n, [], void 0, void 0, [colonsRegex], s)).length; ) {
    let e2 = false, t2 = o, i = o.length;
    if (s.regRes) {
      e2 = true;
      const [, , r2, , , a] = s.regRes;
      if (i = "++" === s.regRes[3] || "--" === s.regRes[3] ? o.length + 1 : o.length, t2 = n.substring(0, i), a) {
        const t3 = closingsNoInsertion.exec(n.substring(o.length - 1).toString());
        t3 ? e2 = "while" === t3[2] && "do" !== s.lastWord : "function" === s.lastWord && "}" === s.regRes[5][0] && "(" === s.regRes[5].slice(-1) && (e2 = false);
      } else r2 && ("if" !== s.lastWord && "while" !== s.lastWord && "for" !== s.lastWord && "else" !== s.lastWord || (e2 = false));
    }
    e2 && (r[t2.end] = true), n = n.substring(i), s = {};
  }
  return insertedSemicolons.set(t.ref, r), t;
}
function checkRegex(e) {
  let t = 1, n = false, o = false, s = false;
  for (; t < e.length && !o && !s; ) o = "/" === e[t] && !n, n = "\\" === e[t] && !n, s = "\n" === e[t], t++;
  const r = e.substring(t);
  if (s = s || !o || /^\s*\d/.test(r), s) return null;
  const i = /^[a-z]*/.exec(r);
  return /^\s+[\w$]/.test(e.substring(t + i[0].length)) ? null : { regex: e.substring(1, t - 1), flags: i && i[0] || "", length: t + (i && i[0].length || 0) };
}
var notDivide = /(typeof|delete|instanceof|return|in|of|throw|new|void|do|if)$/;
var possibleDivide = /^([\w$\])]|\+\+|--)[\s/]/;
function extractConstants(e, t, n = "") {
  let o, s, r = [], i = false, a = "", c = -1, p = [], l = "";
  const d = [], u = [];
  let f = null, x = 0;
  for (x = 0; x < t.length; x++) if (l = t[x], a) l === a && ("*" === a && "/" === t[x + 1] ? (a = "", x++) : "\n" === a && (a = ""));
  else {
    if (i) {
      i = false, r.push(l);
      continue;
    }
    if (o) if ("`" === o && "$" === l && "{" === t[x + 1]) {
      const n2 = extractConstants(e, t.substring(x + 2), "{");
      p.push(n2.str), r.push("${", p.length - 1, "}"), x += n2.length + 2;
    } else if (o === l) {
      if ("`" === o) {
        const t2 = createLisp({ op: 36, a: unraw(r.join("")), b: [] });
        t2.tempJsStrings = p, e.literals.push(t2), d.push("`", e.literals.length - 1, "`");
      } else e.strings.push(unraw(r.join(""))), d.push('"', e.strings.length - 1, '"');
      o = null, r = [];
    } else r.push(l);
    else {
      if ("'" === l || '"' === l || "`" === l) p = [], o = l;
      else {
        if (closings[n] === l && !u.length) return { str: d.join(""), length: x };
        closings[l] ? (u.push(l), d.push(l)) : closings[u[u.length - 1]] === l ? (u.pop(), d.push(l)) : "/" !== l || "*" !== t[x + 1] && "/" !== t[x + 1] ? "/" === l && !f && (s = checkRegex(t.substring(x))) ? (e.regexes.push(s), d.push("/", e.regexes.length - 1, "/r"), x += s.length - 1) : d.push(l) : (a = "*" === t[x + 1] ? "*" : "\n", c = x);
      }
      f && space.test(l) || (f = possibleDivide.exec(t.substring(x))) && notDivide.test(t.substring(0, x + f[1].length)) && (f = null);
    }
    i = !(!o || "\\" !== l);
  }
  if (a && "*" === a) throw new SyntaxError(`Unclosed comment '/*': ${t.substring(c)}`);
  return { str: d.join(""), length: x };
}
function parse(e, t = false, n = false) {
  if ("string" != typeof e) throw new ParseError(`Cannot parse ${e}`, e);
  let o = " " + e;
  const s = { strings: [], literals: [], regexes: [], eager: t };
  o = extractConstants(s, o).str;
  for (const e2 of s.literals) e2[2] = e2.tempJsStrings.map((e3) => lispifyExpr(s, new CodeString(e3))), delete e2.tempJsStrings;
  return { tree: lispifyFunction(new CodeString(o), s, n), constants: s };
}
function createEvalContext() {
  return { sandboxFunction, sandboxAsyncFunction, sandboxedEval, sandboxedSetTimeout, sandboxedSetInterval, sandboxedClearTimeout, sandboxedClearInterval, lispifyFunction };
}
function SB() {
}
function sandboxFunction(e, t) {
  return SandboxFunction.prototype = SB.prototype, SandboxFunction;
  function SandboxFunction(...n) {
    const o = parse(n.pop() || "");
    return createFunction(n, o.tree, t || currentTicks.current, { ...e, constants: o.constants, tree: o.tree }, void 0, "anonymous");
  }
}
function SAF() {
}
function sandboxAsyncFunction(e, t) {
  return n.prototype = SAF.prototype, n;
  function n(...n2) {
    const o = parse(n2.pop() || "");
    return createFunctionAsync(n2, o.tree, t || currentTicks.current, { ...e, constants: o.constants, tree: o.tree }, void 0, "anonymous");
  }
}
function SE() {
}
function sandboxedEval(e) {
  return t.prototype = SE.prototype, t;
  function t(t2) {
    return e(t2)();
  }
}
function sST() {
}
function sandboxedSetTimeout(e, t) {
  return n.prototype = sST.prototype, n;
  function n(n2, o, ...s) {
    const r = t.ctx.sandbox, i = (...t2) => {
      const o2 = "string" == typeof n2 ? e(n2) : n2;
      return d.unsubscribe(), u.unsubscribe(), r.setTimeoutHandles.delete(a), o2(...t2);
    }, a = ++r.timeoutHandleCounter;
    let c = Date.now(), p = setTimeout(i, o, ...s), l = 0;
    const d = r.subscribeHalt(() => {
      l = Date.now() - c + l, clearTimeout(p);
    }), u = r.subscribeResume(() => {
      c = Date.now();
      const e2 = Math.floor((o || 0) - l);
      p = setTimeout(i, e2, ...s), r.setTimeoutHandles.set(a, { handle: p, haltsub: d, contsub: u });
    });
    return r.setTimeoutHandles.set(a, { handle: p, haltsub: d, contsub: u }), a;
  }
}
function sCT() {
}
function sandboxedClearTimeout(e) {
  return t.prototype = sCT.prototype, t;
  function t(t2) {
    const n = e.ctx.sandbox, o = n.setTimeoutHandles.get(t2);
    o && (clearTimeout(o.handle), o.haltsub.unsubscribe(), o.contsub.unsubscribe(), n.setTimeoutHandles.delete(t2));
  }
}
function sCI() {
}
function sandboxedClearInterval(e) {
  return t.prototype = sCI.prototype, t;
  function t(t2) {
    const n = e.ctx.sandbox, o = n.setIntervalHandles.get(t2);
    o && (clearInterval(o.handle), o.haltsub.unsubscribe(), o.contsub.unsubscribe(), n.setIntervalHandles.delete(t2));
  }
}
function sSI() {
}
function sandboxedSetInterval(e, t) {
  return n.prototype = sSI.prototype, n;
  function n(n2, o, ...s) {
    const r = t.ctx.sandbox, i = "string" == typeof n2 ? e(n2) : n2, a = (...e2) => (p = Date.now(), d = 0, i(...e2)), c = ++r.timeoutHandleCounter;
    let p = Date.now(), l = setInterval(a, o, ...s), d = 0;
    const u = r.subscribeHalt(() => {
      d = Date.now() - p + d, clearInterval(l);
    }), f = r.subscribeResume(() => {
      p = Date.now(), l = setTimeout(() => {
        p = Date.now(), d = 0, l = setInterval(a, o, ...s), a(...s);
      }, Math.floor((o || 0) - d), ...s), x.handle = l;
    }), x = { handle: l, haltsub: u, contsub: f };
    return r.setIntervalHandles.set(c, x), c;
  }
}
function subscribeSet(e, t, n, o) {
  const s = o.setSubscriptions.get(e) || /* @__PURE__ */ new Map();
  o.setSubscriptions.set(e, s);
  const r = s.get(t) || /* @__PURE__ */ new Set();
  let i;
  s.set(t, r), r.add(n);
  const a = e[t];
  return a instanceof Object && (i = o.changeSubscriptions.get(a) || /* @__PURE__ */ new Set(), i.add(n), o.changeSubscriptions.set(a, i)), { unsubscribe: () => {
    r.delete(n), i?.delete(n);
  } };
}
var SandboxExec = class _SandboxExec {
  constructor(e, t) {
    this.evalContext = t, this.setSubscriptions = /* @__PURE__ */ new WeakMap(), this.changeSubscriptions = /* @__PURE__ */ new WeakMap(), this.sandboxFunctions = /* @__PURE__ */ new WeakMap(), this.haltSubscriptions = /* @__PURE__ */ new Set(), this.resumeSubscriptions = /* @__PURE__ */ new Set(), this.halted = false, this.timeoutHandleCounter = 0, this.setTimeoutHandles = /* @__PURE__ */ new Map(), this.setIntervalHandles = /* @__PURE__ */ new Map();
    const n = Object.assign({ audit: false, forbidFunctionCalls: false, forbidFunctionCreation: false, globals: _SandboxExec.SAFE_GLOBALS, prototypeWhitelist: _SandboxExec.SAFE_PROTOTYPES, prototypeReplacements: /* @__PURE__ */ new Map() }, e || {});
    this.context = createContext(this, n);
  }
  static get SAFE_GLOBALS() {
    return { globalThis, Function, eval, console: { debug: console.debug, error: console.error, info: console.info, log: console.log, table: console.table, warn: console.warn }, isFinite, isNaN, parseFloat, parseInt, decodeURI, decodeURIComponent, encodeURI, encodeURIComponent, escape, unescape, Boolean, Number, BigInt, String, Object, Array, Symbol, Error, EvalError, RangeError, ReferenceError, SyntaxError, TypeError, URIError, Int8Array, Uint8Array, Uint8ClampedArray, Int16Array, Uint16Array, Int32Array, Uint32Array, Float32Array, Float64Array, Map, Set, WeakMap, WeakSet, Promise, Intl, JSON, Math, Date, RegExp };
  }
  static get SAFE_PROTOTYPES() {
    const e = [SandboxGlobal, Function, Boolean, Number, BigInt, String, Date, Error, Array, Int8Array, Uint8Array, Uint8ClampedArray, Int16Array, Uint16Array, Int32Array, Uint32Array, Float32Array, Float64Array, Map, Set, WeakMap, WeakSet, Promise, Symbol, Date, RegExp], t = /* @__PURE__ */ new Map();
    return e.forEach((e2) => {
      t.set(e2, /* @__PURE__ */ new Set());
    }), t.set(Object, /* @__PURE__ */ new Set(["constructor", "name", "entries", "fromEntries", "getOwnPropertyNames", "is", "keys", "hasOwnProperty", "isPrototypeOf", "propertyIsEnumerable", "toLocaleString", "toString", "valueOf", "values"])), t;
  }
  subscribeGet(e, t) {
    return t.getSubscriptions.add(e), { unsubscribe: () => t.getSubscriptions.delete(e) };
  }
  subscribeSet(e, t, n, o) {
    return subscribeSet(e, t, n, o);
  }
  subscribeSetGlobal(e, t, n) {
    return subscribeSet(e, t, n, this);
  }
  subscribeHalt(e) {
    return this.haltSubscriptions.add(e), { unsubscribe: () => {
      this.haltSubscriptions.delete(e);
    } };
  }
  subscribeResume(e) {
    return this.resumeSubscriptions.add(e), { unsubscribe: () => {
      this.resumeSubscriptions.delete(e);
    } };
  }
  haltExecution(e) {
    if (!this.halted) {
      this.halted = true;
      for (const t of this.haltSubscriptions) t(e);
    }
  }
  resumeExecution() {
    if (this.halted) {
      if (this.context.ticks.tickLimit && this.context.ticks.ticks >= this.context.ticks.tickLimit) throw new SandboxExecutionQuotaExceededError("Cannot resume execution: tick limit exceeded");
      this.halted = false;
      for (const e of this.resumeSubscriptions) e();
    }
  }
  getContext(e) {
    return this.sandboxFunctions.get(e);
  }
  executeTree(e, t = []) {
    return executeTree(e.ctx.ticks, e, e.tree, t);
  }
  executeTreeAsync(e, t = []) {
    return executeTreeAsync(e.ctx.ticks, e, e.tree, t);
  }
};
var Sandbox = class extends SandboxExec {
  constructor(e) {
    super(e, createEvalContext());
  }
  static audit(e, t = []) {
    const n = {};
    for (const e2 of Object.getOwnPropertyNames(globalThis)) n[e2] = globalThis[e2];
    const o = new SandboxExec({ globals: n, audit: true });
    return o.executeTree(createExecContext(o, parse(e, true), createEvalContext()), t);
  }
  static parse(e) {
    return parse(e);
  }
  compile(e, t = false) {
    const n = parse(e, t);
    return (...e2) => {
      const t2 = createExecContext(this, n, this.evalContext);
      return { context: t2, run: () => this.executeTree(t2, [...e2]).result };
    };
  }
  compileAsync(e, t = false) {
    const n = parse(e, t);
    return (...e2) => {
      const t2 = createExecContext(this, n, this.evalContext);
      return { context: t2, run: () => this.executeTreeAsync(t2, [...e2]).then((e3) => e3.result) };
    };
  }
  compileExpression(e, t = false) {
    const n = parse(e, t, true);
    return (...e2) => {
      const t2 = createExecContext(this, n, this.evalContext);
      return { context: t2, run: () => this.executeTree(t2, [...e2]).result };
    };
  }
  compileExpressionAsync(e, t = false) {
    const n = parse(e, t, true);
    return (...e2) => {
      const t2 = createExecContext(this, n, this.evalContext);
      return { context: t2, run: () => this.executeTreeAsync(t2, [...e2]).then((e3) => e3.result) };
    };
  }
};

// node_modules/async-mutex/index.mjs
var E_TIMEOUT = new Error("timeout while waiting for mutex to become available");
var E_ALREADY_LOCKED = new Error("mutex already locked");
var E_CANCELED = new Error("request for lock canceled");
var __awaiter$2 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var Semaphore = class {
  constructor(_value, _cancelError = E_CANCELED) {
    this._value = _value;
    this._cancelError = _cancelError;
    this._queue = [];
    this._weightedWaiters = [];
  }
  acquire(weight = 1, priority = 0) {
    if (weight <= 0)
      throw new Error(`invalid weight ${weight}: must be positive`);
    return new Promise((resolve, reject) => {
      const task = { resolve, reject, weight, priority };
      const i = findIndexFromEnd(this._queue, (other) => priority <= other.priority);
      if (i === -1 && weight <= this._value) {
        this._dispatchItem(task);
      } else {
        this._queue.splice(i + 1, 0, task);
      }
    });
  }
  runExclusive(callback_1) {
    return __awaiter$2(this, arguments, void 0, function* (callback, weight = 1, priority = 0) {
      const [value, release] = yield this.acquire(weight, priority);
      try {
        return yield callback(value);
      } finally {
        release();
      }
    });
  }
  waitForUnlock(weight = 1, priority = 0) {
    if (weight <= 0)
      throw new Error(`invalid weight ${weight}: must be positive`);
    if (this._couldLockImmediately(weight, priority)) {
      return Promise.resolve();
    } else {
      return new Promise((resolve) => {
        if (!this._weightedWaiters[weight - 1])
          this._weightedWaiters[weight - 1] = [];
        insertSorted(this._weightedWaiters[weight - 1], { resolve, priority });
      });
    }
  }
  isLocked() {
    return this._value <= 0;
  }
  getValue() {
    return this._value;
  }
  setValue(value) {
    this._value = value;
    this._dispatchQueue();
  }
  release(weight = 1) {
    if (weight <= 0)
      throw new Error(`invalid weight ${weight}: must be positive`);
    this._value += weight;
    this._dispatchQueue();
  }
  cancel() {
    this._queue.forEach((entry) => entry.reject(this._cancelError));
    this._queue = [];
  }
  _dispatchQueue() {
    this._drainUnlockWaiters();
    while (this._queue.length > 0 && this._queue[0].weight <= this._value) {
      this._dispatchItem(this._queue.shift());
      this._drainUnlockWaiters();
    }
  }
  _dispatchItem(item) {
    const previousValue = this._value;
    this._value -= item.weight;
    item.resolve([previousValue, this._newReleaser(item.weight)]);
  }
  _newReleaser(weight) {
    let called = false;
    return () => {
      if (called)
        return;
      called = true;
      this.release(weight);
    };
  }
  _drainUnlockWaiters() {
    if (this._queue.length === 0) {
      for (let weight = this._value; weight > 0; weight--) {
        const waiters = this._weightedWaiters[weight - 1];
        if (!waiters)
          continue;
        waiters.forEach((waiter) => waiter.resolve());
        this._weightedWaiters[weight - 1] = [];
      }
    } else {
      const queuedPriority = this._queue[0].priority;
      for (let weight = this._value; weight > 0; weight--) {
        const waiters = this._weightedWaiters[weight - 1];
        if (!waiters)
          continue;
        const i = waiters.findIndex((waiter) => waiter.priority <= queuedPriority);
        (i === -1 ? waiters : waiters.splice(0, i)).forEach((waiter) => waiter.resolve());
      }
    }
  }
  _couldLockImmediately(weight, priority) {
    return (this._queue.length === 0 || this._queue[0].priority < priority) && weight <= this._value;
  }
};
function insertSorted(a, v) {
  const i = findIndexFromEnd(a, (other) => v.priority <= other.priority);
  a.splice(i + 1, 0, v);
}
function findIndexFromEnd(a, predicate) {
  for (let i = a.length - 1; i >= 0; i--) {
    if (predicate(a[i])) {
      return i;
    }
  }
  return -1;
}
var __awaiter$1 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var Mutex = class {
  constructor(cancelError) {
    this._semaphore = new Semaphore(1, cancelError);
  }
  acquire() {
    return __awaiter$1(this, arguments, void 0, function* (priority = 0) {
      const [, releaser] = yield this._semaphore.acquire(1, priority);
      return releaser;
    });
  }
  runExclusive(callback, priority = 0) {
    return this._semaphore.runExclusive(() => callback(), 1, priority);
  }
  isLocked() {
    return this._semaphore.isLocked();
  }
  waitForUnlock(priority = 0) {
    return this._semaphore.waitForUnlock(1, priority);
  }
  release() {
    if (this._semaphore.isLocked())
      this._semaphore.release();
  }
  cancel() {
    return this._semaphore.cancel();
  }
};

// src-web/dsl.ts
var DslErrors;
((DslErrors2) => {
  class PerfectHedgeError extends Error {
    state;
    amount;
    partyIdx;
    pair;
    constructor(msg, st, amount, partyIdx, pair) {
      super(msg);
      this.state = st;
      this.amount = amount;
      this.partyIdx = partyIdx;
      this.pair = pair;
    }
  }
  DslErrors2.PerfectHedgeError = PerfectHedgeError;
  class InfinityError extends Error {
    state = void 0;
    constructor(msg, st) {
      super(msg);
      this.state = st;
    }
  }
  DslErrors2.InfinityError = InfinityError;
  class InfinityCountError extends Error {
  }
  DslErrors2.InfinityCountError = InfinityCountError;
  class PartyAtAdvantage extends Error {
    amount;
    partyIdx;
    pair;
    constructor(msg, amount, partyIdx, pair) {
      super(msg);
      this.amount = amount;
      this.partyIdx = partyIdx;
      this.pair = pair;
    }
  }
  DslErrors2.PartyAtAdvantage = PartyAtAdvantage;
  class OnePayPerCondition extends Error {
    amount;
    partyIdx;
    pair;
    state;
    constructor(msg, amount, partyIdx, pair, state) {
      super(msg);
      this.amount = amount;
      this.partyIdx = partyIdx;
      this.pair = pair;
      this.state = state;
    }
  }
  DslErrors2.OnePayPerCondition = OnePayPerCondition;
  class ComplexConditions extends Error {
    amount;
    partyIdx;
    pair;
    constructor(msg, amount, partyIdx, pair) {
      super(msg);
      this.amount = amount;
      this.partyIdx = partyIdx;
      this.pair = pair;
    }
  }
  DslErrors2.ComplexConditions = ComplexConditions;
  class SafeModeError extends Error {
  }
  DslErrors2.SafeModeError = SafeModeError;
  class EmptyDslOutput extends Error {
  }
  DslErrors2.EmptyDslOutput = EmptyDslOutput;
})(DslErrors || (DslErrors = {}));
var Dsl = class {
  state = {};
  template() {
    const model = {
      id: "",
      bet: [0, 0],
      betOn: void 0,
      oracles: [],
      question: "",
      status: "matching",
      blockchain: "",
      role: "initiator"
    };
    return model;
  }
  root = this.template();
  cursor = this.root;
  prev = void 0;
  lastOutcome = void 0;
  flag = false;
  alicePayCounter = 0;
  bobPayCounter = 0;
  aliceTrackers = {};
  bobTrackers = {};
  pay(idx, amount) {
    if (this.flagSameAssetSwap) {
      throw Error(`Cannot swap same type of asset between ${this.selected[0]} and ${this.selected[1]}`);
    }
    if (this.unssafeInifnityCtx) {
      throw new Error("Payouts are disabled in unsafe infinity context. Specify cashflows in return instead");
    }
    if (idx === void 0) {
      throw new Error("party undefined");
    }
    if (!this.protect) {
      throw new Error("should not call outside of body; use `new Dsl((dsl) => handler).enumerate()`");
    }
    if (this.prev === void 0 || this.lastOutcome === void 0) {
      new Error("cannot pay unconditionally!");
    }
    if (amount <= 0) {
      throw Error("Pay amount must be positive! found: " + amount);
    }
    if (this.lastOutcome !== null) {
      if (this.lastOutcome && idx === 0) {
        if (this.prev.betOn === void 0 || this.prev.betOn === true) {
          this.prev.betOn = true;
        } else {
          throw new DslErrors.PerfectHedgeError("Perfect hedge! Trader not allowed to benefit regardless of outcome. Your trade is overcollaterized!", this.state, amount, idx, this.selected);
        }
      }
      if (!this.lastOutcome && idx === 0) {
        if (this.prev.betOn === void 0 || this.prev.betOn === false) {
          this.prev.betOn = false;
        } else {
          throw new DslErrors.PerfectHedgeError("Perfect hedge! Trader not allowed to benefit regardless of outcome. Your trade is overcollaterized!", this.state, amount, idx, this.selected);
        }
      }
    }
    if (this.lastOutcome !== null) {
      if (this.lastOutcome && idx === 1) {
        if (this.prev.betOn === void 0 || this.prev.betOn === false) {
          this.prev.betOn = false;
        } else {
          throw new DslErrors.PerfectHedgeError("Perfect hedge! Trader not allowed to benefit regardless of outcome. Your trade is overcollaterized!", this.state, amount, idx, this.selected);
        }
      }
      if (!this.lastOutcome && idx === 1) {
        if (this.prev.betOn === void 0 || this.prev.betOn === true) {
          this.prev.betOn = true;
        } else {
          throw new DslErrors.PerfectHedgeError("Perfect hedge! Trader not allowed to benefit regardless of outcome. Your trade is overcollaterized!", this.state, amount, idx, this.selected);
        }
      }
    }
    if (this.flag) {
      throw new Error(`one pay per condition check! and pay before checking out next condition too, please!
at amount = ${amount}; idx = ${idx}; pair = ${this.selected}`);
    }
    const id = this.megaMode ? this.currentPub : this.currentPub + JSON.stringify(this.currentArgs);
    if (idx == 0) {
      this.alicePayCounter++;
      if (!this.aliceTrackers[id]) {
        this.aliceTrackers[id] = 0;
      }
      this.aliceTrackers[id] += 1;
      this.prev.bet[1] = Math.round(amount);
    } else {
      if (!this.bobTrackers[id]) {
        this.bobTrackers[id] = 0;
      }
      this.bobTrackers[id] += 1;
      this.bobPayCounter++;
      this.prev.bet[0] = Math.round(amount);
    }
    this.flag = true;
    if (idx === 0) {
      this.collateral1 += amount;
      if (this.collateral1 > this.budgetBound1) {
        const party = this.selected[0] ?? "Alice";
        throw new Error(`exceeded budget ${this.budgetBound1} for ${party}, for outcomes:` + JSON.stringify(this.state));
      }
    }
    if (idx === 1) {
      this.collateral2 += amount;
      if (this.collateral2 > this.budgetBound2) {
        const party = this.selected[1] ?? "Bob";
        throw new Error(`exceeded budget ${this.budgetBound2} for ${party}, for outcomes:` + JSON.stringify(this.state));
      }
    }
  }
  currentPub = void 0;
  currentArgs = void 0;
  enrichAndProgress(aliceOutcome, pubkey, yes, no, args = {}) {
    this.lastOutcome = aliceOutcome;
    this.flag = false;
    if (aliceOutcome === false) {
      if (this.cursor.ifCounterPartyWins === void 0) {
        this.cursor.ifCounterPartyWins = this.template();
      } else {
        const x = this.cursor.ifCounterPartyWins.bet;
        this.cursor.ifCounterPartyWins.bet = [x[0] === null ? 0 : x[0], x[0] === null ? 0 : x[0]];
      }
      this.prev = this.cursor;
      this.cursor = this.cursor.ifCounterPartyWins;
    } else if (aliceOutcome === true) {
      if (this.cursor.ifPartyWins === void 0) {
        this.cursor.ifPartyWins = this.template();
      } else {
        const x = this.cursor.ifPartyWins.bet;
        this.cursor.ifPartyWins.bet = [x[0] === null ? 0 : x[0], x[0] === null ? 0 : x[0]];
      }
      this.prev = this.cursor;
      this.cursor = this.cursor.ifPartyWins;
    }
    if (this.prev) {
      this.prev.oracles[0] = {
        capabilityPub: pubkey.replaceAll(/-###-.*/g, ""),
        params: args
      };
      this.prev.yesOutcomes = yes;
      this.prev.noOutcomes = no;
    }
  }
  counter = 0;
  memoize = [];
  checked = [];
  superMode = false;
  safeMode = false;
  megaMode = false;
  safeModeStarted = false;
  megaModeStarted = false;
  superModeStarted = false;
  fairModeStarted = false;
  strictModeStarted = false;
  security = {
    startSafeMode: () => {
      if (this.collateral1 > 0 && this.collateral2 > 0 && this.counter > 0) {
        throw new Error("Safe mode has to be started before any payouts or observations happen!");
      }
      if (this.safeMode) {
        throw new Error("Safe mode already enabled manually!");
      }
      this.safeModeStarted = true;
      this.safeMode = true;
    },
    startMegaMode: () => {
      if (this.collateral1 > 0 && this.collateral2 > 0 && this.counter > 0) {
        throw new Error("Mega mode has to be started before any payouts or observations happen!");
      }
      if (this.megaMode) {
        throw new Error("Mega mode already enabled manually!");
      }
      this.megaModeStarted = true;
      this.megaMode = true;
    },
    startSuperMode: () => {
      if (this.collateral1 > 0 && this.collateral2 > 0 && this.counter > 0) {
        throw new Error("Super mode has to be started before any payouts or observations happen!");
      }
      if (this.superMode) {
        throw new Error("Super mode already enabled manually!");
      }
      this.superModeStarted = true;
      this.superMode = true;
    },
    startFairMode: () => {
      if (this.collateral1 > 0 && this.collateral2 > 0 && this.counter > 0) {
        throw new Error("Fair mode has to be started before any payouts or observations happen!");
      }
      if (this.megaMode) {
        throw new Error("Fair mode already enabled manually!");
      }
      this.fairModeStarted = true;
      this.strictlyFair = true;
    },
    startStrictMode: () => {
      if (this.collateral1 > 0 && this.collateral2 > 0 && this.counter > 0) {
        throw new Error("Strict mode has to be started before any payouts or observations happen!");
      }
      if (this.megaMode) {
        throw new Error("Strict mode already enabled manually!");
      }
      this.strictModeStarted = true;
      this.strictlyStrict = true;
    }
  };
  insecurity = {
    open: {
      disableSafeMode: () => {
        if (this.safeModeStarted) {
          throw new Error("Safe mode has to be started with `startSafeMode`!");
        }
        if (!this.safeMode) {
          throw new Error("Safe mode has to be enabled first!");
        }
        this.safeMode = false;
      },
      disableMegaMode: () => {
        if (this.megaModeStarted) {
          throw new Error("Mega mode has to be started with `startMegaMode`!");
        }
        if (!this.megaMode) {
          throw new Error("Mega mode has to be enabled first!");
        }
        this.megaMode = false;
      },
      disableSuperMode: () => {
        if (this.superModeStarted) {
          throw new Error("Super mode has to be started with `startSuperMode`!");
        }
        if (!this.superMode) {
          throw new Error("Super mode has to be enabled first!");
        }
        this.superMode = false;
      }
    },
    close: {
      reEnableSafeMode: () => {
        if (this.safeModeStarted) {
          throw new Error("Safe mode has to be started with `startSafeMode`!");
        }
        if (this.safeMode) {
          throw new Error("Safe mode has to be disabled first!");
        }
        this.safeMode = false;
      },
      reEnableMegaMode: () => {
        if (this.megaModeStarted) {
          throw new Error("Mega mode has to be started with `startMegaMode`!");
        }
        if (this.megaMode) {
          throw new Error("Mega mode has to be disabled first!");
        }
        this.megaMode = false;
      },
      reEnableSuperMode: () => {
        if (this.superModeStarted) {
          throw new Error("Mega mode has to be started with `startSuperMode`!");
        }
        if (this.superMode) {
          throw new Error("Mega mode has to be disabled first!");
        }
        this.superMode = false;
      }
    }
  };
  strictlyOneLeafPays = false;
  strictlyOneLeafPairPays = false;
  flagSameAssetSwap = false;
  outcome(pubkey, yes, no, args = {}, allowTruth = false, strict = true, ignoreObserveChecksSuperUnsafe = false) {
    this.currentPub = pubkey;
    this.currentArgs = args;
    const pubkeyUnique = pubkey + "-###-" + JSON.stringify(yes) + JSON.stringify(no) + JSON.stringify(args);
    if (ignoreObserveChecksSuperUnsafe) {
      if (this.state[pubkeyUnique] === void 0) {
        const memoized = this.memoize.find((x) => x.id === pubkey && JSON.stringify(x.yes.sort()) === JSON.stringify(yes.sort()) && JSON.stringify(x.args) === JSON.stringify(args));
        const pubkeyUnique2 = pubkey + "-###-" + JSON.stringify(yes) + JSON.stringify(memoized.no) + JSON.stringify(args);
        if (!this.state[pubkeyUnique2]) {
          throw new Error("Did not find memoized state " + pubkeyUnique2 + "\n" + this.state);
        }
        this.enrichAndProgress(this.state[pubkeyUnique2][1], pubkeyUnique2, yes, no, args);
        return this.state[pubkeyUnique2][1];
      }
      this.enrichAndProgress(this.state[pubkeyUnique][1], pubkeyUnique, yes, no, args);
      return this.state[pubkeyUnique][1];
    }
    if (strict) {
      this.alicePayCounter = 0;
      this.bobPayCounter = 0;
    }
    if (allowTruth && this.selected !== void 0) {
      const tokens1 = this.selected[0].split("_");
      const asset1 = tokens1.length === 1 ? "btc" : tokens1[tokens1.length - 1];
      const tokens2 = this.selected[1].split("_");
      const asset2 = tokens2.length === 1 ? "btc" : tokens2[tokens2.length - 1];
      if (asset1 === asset2) {
        this.flagSameAssetSwap = true;
      }
    }
    yes.sort();
    no.sort();
    this.counter++;
    if (new Set(yes).size !== yes.length) {
      throw Error("Duplicate outcomes:" + JSON.stringify(yes) + "; pubkey: " + pubkey);
    }
    if (new Set(no).size !== no.length) {
      throw Error("Duplicate outcomes: " + JSON.stringify(no)) + "; pubkey: " + pubkey;
    }
    if (strict && (yes.length === 0 || no.length === 0)) {
      throw Error("One of the outcome sets is empty. Trader would possibly benefit regardless of outcome. Use `dsl.unsafe` to allow!; pubkey: " + pubkey);
    }
    if (yes.length === 0 && no.length === 0) {
      throw Error("Transaction race! Outcomes are empty. Trader cannot benefit regardless of outcome. Use `dsl.unsafe` with synthetic outcome (convention: btc script generated outcomes should start with $, e.g. $(thisTx.utxo[0])) to allow!; pubkey: " + pubkey);
    }
    if (JSON.stringify(yes) === JSON.stringify(no) && !allowTruth) {
      throw Error("Contradiction! Outcomes are not mutually exclusive!; pubkey: " + pubkey);
    }
    const yesSet = new Set(yes);
    const noSet = new Set(no);
    if (yes.find((x) => noSet.has(x)) || no.find((x) => yesSet.has(x))) {
      if (strict) {
        throw Error("Partial contradiction! Some outcomes are not mutually exclusive!; pubkey: " + pubkey);
      }
    }
    if (!this.protect) {
      throw "should not call outside of body; use `new Dsl((dsl) => handler).enumerate()`; pubkey: " + pubkey;
    }
    if (this.state[pubkeyUnique] === void 0) {
      const max = Object.values(this.state).length === 0 ? -1 : Math.max(...Object.values(this.state).map((x) => x[0]));
      this.state[pubkeyUnique] = [max + 1, null, args];
      this.checked.push(this.state[pubkeyUnique][0]);
      this.memoize.push({
        id: pubkey,
        yes,
        no,
        args,
        pubkeyUnique
      });
      throw "uninitialized";
    } else {
      this.checked.push(this.state[pubkeyUnique][0]);
      if (this.memoize.find((x) => x.id === pubkey && JSON.stringify(x.yes.sort()) === JSON.stringify(yes.sort()) && JSON.stringify(x.no.sort()) === JSON.stringify(no.sort()) && JSON.stringify(x.args) === JSON.stringify(args)) !== void 0) {
        throw new Error("Cannot query same observation twice. Save it into const instead: const obs1 = outcome(...); args=" + JSON.stringify(args) + "; pubkey: " + pubkey);
      }
      const sameQuery = this.memoize.find((x) => x.id === pubkey);
      if (strict && sameQuery && JSON.stringify(sameQuery.yes.concat(sameQuery.no).sort()) !== JSON.stringify(yes.concat(no).sort())) {
        throw new Error("Set of overall outcomes must be same, regardless of parameters! " + sameQuery.yes.concat(sameQuery.no).sort() + " != " + yes.concat(no).sort() + "; pubkey: " + pubkey);
      }
      if (this.superMode && sameQuery && JSON.stringify(sameQuery.args) === JSON.stringify(sameQuery.args)) {
        throw new Error("Cannot query same observation twice! Super strictly! Arguments are allowed to vary; pubkey: " + pubkey);
      }
      if (this.megaMode && sameQuery) {
        throw new Error("Cannot query same observation twice! MEGA strictly!");
      }
      const contradiction = this.memoize.find((x) => x.id === pubkey && JSON.stringify(x.yes.sort()) === JSON.stringify(no.sort()) && JSON.stringify(x.no.sort()) === JSON.stringify(yes.sort()) && JSON.stringify(x.args) === JSON.stringify(args));
      if (contradiction !== void 0) {
        throw new Error("Cannot query the opposite of checked observation. Save it into const and inverse instead: const obs1 = outcome(...); const obs2 = !obs1; pubkey: " + pubkey);
      }
      this.enrichAndProgress(this.state[pubkeyUnique][1], pubkeyUnique, yes, no, args);
      this.memoize.push({
        id: pubkey,
        yes,
        no,
        args,
        pubkeyUnique
      });
      return this.state[pubkeyUnique][1];
    }
  }
  next() {
    this.cursor = this.root;
    this.prev = void 0;
    let i = 0;
    let cursor = true;
    let entry = void 0;
    while (cursor) {
      if (Object.values(this.state).find((x) => x[0] === i) === void 0) {
        return false;
      }
      entry = Object.values(this.state).find((x) => x[0] === i);
      if (entry[1] === null && cursor) {
        entry[1] = false;
        return true;
      }
      if (!cursor) {
        return true;
      }
      if (entry[1] === true) {
        if (cursor === true) {
          entry[1] = false;
        }
      } else {
        entry[1] = true;
        cursor = false;
      }
      i++;
    }
    return true;
  }
  body;
  constructor(body) {
    this.body = body;
  }
  protect = false;
  static Party = 0;
  static CounterParty = 1;
  static Alice = 0;
  static Bob = 1;
  collateral1 = 0;
  collateral2 = 0;
  budgetBound1 = 0;
  budgetBound2 = 0;
  leafsFiltered = false;
  strictlyStrict = false;
  filterLeafs(model, assertNoZeros = false) {
    if (model === void 0) {
      throw new DslErrors.EmptyDslOutput("Empty DSL model output!");
    }
    if (!model.bet[0] && !model.bet[1] && !model.ifPartyWins && !model.ifCounterPartyWins) {
      this.leafsFiltered = true;
      return void 0;
    }
    if (this.strictlyFair) {
      if (!model.bet[0] && model.bet[1]) {
        throw new DslErrors.PartyAtAdvantage("Party at advantage - no premium/discount introduced", model.bet[1], 0, this.selected);
      }
      if (model.bet[0] && !model.bet[1]) {
        throw new DslErrors.PartyAtAdvantage("Party at advantage - no premium/discount introduced", model.bet[0], 1, this.selected);
      }
    }
    if (assertNoZeros) {
      if (!model.bet[0] && !model.bet[1]) {
        throw new DslErrors.ComplexConditions("Strict Semantics of observation: complex conditions are disallowed!", model.bet[0], 1, this.selected);
      }
    }
    if (model.ifPartyWins) {
      model.ifPartyWins = this.filterLeafs(model.ifPartyWins);
    }
    if (model.ifCounterPartyWins) {
      model.ifCounterPartyWins = this.filterLeafs(model.ifCounterPartyWins);
    }
    if (!model.ifPartyWins) {
      delete model.ifPartyWins;
    }
    if (!model.ifCounterPartyWins) {
      delete model.ifCounterPartyWins;
    }
    return model;
  }
  multiparty = [];
  selected = [void 0, void 0];
  isSelected0 = (party) => {
    return this.selected[0] && this.selected[0] === party;
  };
  isSelected1 = (party) => {
    return this.selected[0] && this.selected[1] === party;
  };
  multiple = (...parties) => {
    if (this.multiparty.length > 0) {
      throw Error("parties can be specified only once! Adding more parties ad-hoc is equivalent to early termination since consensus is required for that! \n It is advised to create a new contract. \n Note: you can freely parametrize Discreet contracts (terms) and use factories to easily spawn contracts with additional parties.");
    }
    if (parties.length < 2) {
      throw Error("Commmon sense! Minimum of 2 parties required for a contract!");
    }
    const set = [...new Set(parties)];
    if (set.length < parties.length) {
      throw Error("Tractable relations! parties have to be unique!");
    }
    this.multiparty = parties;
    return this;
  };
  party = (partyName, partyAsset) => ({
    pays: (counterpartyName, counterpartyAsset) => ({
      amount: (amount, asset) => {
        if (partyName === void 0) {
          throw Error("Party undefined");
        }
        if (counterpartyName === void 0) {
          throw Error("Counter-party undefined");
        }
        const party = partyName + (partyAsset ? "_" + partyAsset : "");
        const counterparty = counterpartyName + (counterpartyAsset ? "_" + counterpartyAsset : "");
        if (partyAsset !== asset) {
          throw Error(`Trying to pay ${asset} from collateral denominated in ${partyAsset}`);
        }
        if (!this.multiparty.find((x) => x === party)) {
          throw Error("party " + party + " not registered! Use .multiple to register parties");
        }
        if (!this.multiparty.find((x) => x === counterparty)) {
          throw Error("counterparty " + counterparty + " not registered! Use .multiple to register parties");
        }
        if (this.isSelected0(party) && this.isSelected1(counterparty)) {
          this.pay(0, amount);
        } else if (this.isSelected0(counterparty) && this.isSelected1(party)) {
          this.pay(1, amount);
        }
      }
    })
  });
  static account(partyName, partyAsset) {
    return partyName + "_" + partyAsset;
  }
  static mutual(...parties) {
    return parties.join("+");
  }
  static refund(party) {
    return party + "_refund";
  }
  unfinalized = 0;
  static track = {};
  static recurse = {
    bounded: (fn) => ({
      attempts: (attempts) => ({
        otherwiseYield: (defaultValue) => {
          if (!this.track[fn.toString()]) {
            this.track[fn.toString()] = 0;
          }
          return () => {
            this.track[fn.toString()] = this.track[fn.toString()] + 1;
            if (this.track[fn.toString()] > attempts) {
              return defaultValue;
            }
            return fn();
          };
        }
      })
    })
  };
  strictlyFair = false;
  unssafeInifnityCtx = false;
  atomicSwap = {
    ifTruth: (lock = "TRUTH", unlockOutcome = "true", args = {}, allowMisplacedPay = true) => {
      return this.if(lock, [unlockOutcome], [unlockOutcome], args, true, allowMisplacedPay, false);
    },
    truth: (lock = "TRUTH", unlockOutcome = "true", args = {}) => {
      return this.outcome(lock, [unlockOutcome], [unlockOutcome], args, true);
    }
  };
  unsafe = {
    if: (pubkey, yes, no, args = {}, allowSwaps = false, allowMisplacedPay = true, strict = false, ignoreObserveChecksSuperUnsafe = false) => {
      if (this.safeMode) {
        throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
      }
      return this.if(pubkey, yes, no, args, allowSwaps, allowMisplacedPay, strict, ignoreObserveChecksSuperUnsafe);
    },
    numeric: {
      outcome: (pubkey, from, to, step = 1, args = {}, allowMisplacedPay = true, allowUnsafe = true, ignoreObserveChecksSuperUnsafe = false) => {
        if (this.safeMode) {
          throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
        }
        return this.numeric.outcome(pubkey, from, to, step, args, allowMisplacedPay, allowUnsafe, ignoreObserveChecksSuperUnsafe);
      },
      infinity: {
        bounded: (maxInfinity = 1e7, maxCount = 1e9) => ({
          progress: (start, forward = (x) => x + 1) => ({
            perpetual: (init, step) => {
              if (this.safeMode) {
                throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
              }
              this.unsafe.infinity.bounded(maxInfinity, maxCount).compare((a, b) => a - b).progress(start, forward).perpetual(init, step);
            }
          }),
          perpetual: (init, step) => {
            if (this.safeMode) {
              throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
            }
            this.unsafe.infinity.bounded(maxInfinity, maxCount).compare((a, b) => b - a).progress(0, (x) => x + 1).perpetual(init, step);
          }
        })
      }
    },
    set: {
      outcome: (pubkey, set, args = {}, allowMisplacedPay = true, allowUnsafe = true) => {
        if (this.safeMode) {
          throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
        }
        return this.set.outcome(pubkey, set, args, allowMisplacedPay, allowUnsafe);
      },
      outcomeT: (pubkey, set, renderer, args = {}, allowMisplacedPay = true, allowUnsafe = true) => {
        if (this.safeMode) {
          throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
        }
        return this.set.outcomeT(pubkey, set, renderer, args, allowMisplacedPay, allowUnsafe);
      }
    },
    outcome: (pubkey, yes, no, args = {}, allowTruth = false, strict = false, ignoreObserveChecksSuperUnsafe = false) => {
      if (this.safeMode) {
        throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
      }
      return this.outcome(pubkey, yes, no, args, allowTruth, strict, ignoreObserveChecksSuperUnsafe);
    },
    infinity: {
      move: (x) => {
        if (x === void 0) {
          throw new Error("Cannot move with undefined state!");
        }
        return x;
      },
      stop: (cashflows) => [void 0, cashflows],
      bounded: (maxInfinity, maxCount = 1e4) => ({
        compare: (cmp) => ({
          progress: (start, forward) => ({
            perpetual: (init, step) => {
              if (this.safeMode) {
                throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
              }
              let cursor = start;
              let counter = 0;
              let state = init;
              this.unssafeInifnityCtx = true;
              try {
                while (cmp(cursor, maxInfinity) > 0 && counter < maxCount) {
                  const saveState = state;
                  let cashflows = void 0;
                  [state, cashflows] = step(cursor, state);
                  cashflows.forEach((cashflow) => {
                    try {
                      this.party(
                        cashflow.from[0],
                        cashflow.from[1]
                      ).pays(
                        cashflow.to[0],
                        cashflow.to[1]
                      ).amount(
                        cashflow.amount[0],
                        cashflow.amount[1]
                      );
                    } catch (e) {
                      if (e instanceof DslErrors.PerfectHedgeError) {
                        const party = e.pair[e.partyIdx];
                        state[party] = saveState[party];
                      }
                    }
                  });
                  if (state === void 0) {
                    return;
                  }
                  if (state === saveState) {
                    throw new DslErrors.InfinityError("Infinity Inferred! State did not progress! Collaterals are not decreasing?", state);
                  }
                  cursor = forward(cursor);
                  counter++;
                }
                if (counter >= maxCount) {
                  throw new DslErrors.InfinityCountError("Max count reached!");
                }
                if (cmp(cursor, maxInfinity) <= 0) {
                  throw new DslErrors.InfinityError("Infinity Reached! Collaterals are not decreasing?", state);
                }
              } finally {
                this.unssafeInifnityCtx = false;
              }
            }
          })
        })
      })
    }
  };
  infinity = {
    move: (x) => {
      if (x === void 0) {
        throw new Error("Cannot move with undefined state!");
      }
      return x;
    },
    stop: void 0,
    bounded: (maxInfinity, maxCount = 1e4) => ({
      compare: (cmp) => ({
        progress: (start, forward) => ({
          perpetual: (init, step) => {
            let cursor = start;
            let counter = 0;
            let state = init;
            while (cmp(cursor, maxInfinity) > 0 && counter < maxCount) {
              const saveState = state;
              state = step(cursor, state);
              if (state === void 0) {
                return;
              }
              if (state === saveState) {
                throw new DslErrors.InfinityError("Infinity Inferred! State did not progress! Collaterals are not decreasing?", state);
              }
              cursor = forward(cursor);
              counter++;
            }
            if (counter >= maxCount) {
              throw new DslErrors.InfinityCountError("Max count reached!");
            }
            if (cmp(cursor, maxInfinity) <= 0) {
              throw new DslErrors.InfinityError("Infinity Reached! Collaterals are not decreasing?", state);
            }
          }
        })
      })
    })
  };
  bool = {
    safe: {
      outcome: (pubkey, yes, no, args = {}) => {
        return this.outcome(pubkey, [yes], [no], args);
      }
    }
  };
  numeric = {
    infinity: {
      bounded: (maxInfinity = 1e7, maxCount = 1e9) => ({
        progress: (start, forward = (x) => x + 1) => ({
          perpetual: (init, step) => {
            this.infinity.bounded(maxInfinity, maxCount).compare((a, b) => a - b).progress(start, forward).perpetual(init, step);
          }
        }),
        perpetual: (init, step) => {
          this.infinity.bounded(maxInfinity, maxCount).compare((a, b) => b - a).progress(0, (x) => x + 1).perpetual(init, step);
        }
      })
    },
    safe: {
      outcome: (pubkey, yes, no, args = {}) => {
        if (this.outcome(pubkey, [`${yes}`], [`${no}`], args)) {
          return yes;
        } else {
          return no;
        }
      },
      if: (pubkey, yes, no, args = {}, allowSwaps = false, allowMisplacedPay = false, strict = true) => {
        const iff = this.if(pubkey, [`${yes}`], [`${no}`], args, allowSwaps, allowMisplacedPay, strict);
        return {
          then: (handler) => {
            const thenn = iff.then((h) => handler(yes, h));
            return {
              else: (handler2) => {
                return thenn.else((h) => handler2(no, h));
              }
            };
          }
        };
      }
    },
    outcome: (pubkey, from, to, step = 1, args = {}, allowMisplacedPay = false, allowUnsafe = false, ignoreObserveChecksSuperUnsafe = false) => ({
      evaluate: (handler) => {
        let numbers = [];
        for (let i = from; i <= to; i += step) {
          numbers.push(i);
        }
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          if (this.outcome(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args, false, false)) {
            if (l.length === 1) {
              if (this.outcome(pubkey, l.map((x) => x.toString()), [], args, false, false, true)) {
                handler(l[0]);
              }
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          } else {
            if (r.length === 1) {
              handler(r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          }
        };
        recurse(numbers.slice(0, numbers.length / 2), numbers.slice(numbers.length / 2));
        const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
        if (!allowUnsafe) {
          if (this.aliceTrackers[id] > numbers.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
          }
          if (this.bobTrackers[id] > numbers.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
          }
        }
        if (this.strictlyOneLeafPays && (this.aliceTrackers[id] > 1 || this.bobPayCounter[id] > 1)) {
          throw new DslErrors.OnePayPerCondition("Only one leaf in a tree is alowed to pay", void 0, void 0, this.selected, this.state);
        }
        if (this.strictlyOneLeafPairPays && (this.aliceTrackers[id] > 1 || this.bobPayCounter[id] > 1)) {
          throw new DslErrors.PerfectHedgeError("Only one pair in a tree is alowed to pay", this.state, void 0, void 0, this.selected);
        }
      },
      evaluateWithPaymentCtx: (payhandler) => {
        let numbers = [];
        for (let i = from; i <= to; i += step) {
          numbers.push(i);
        }
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          this.unsafe.if(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args, false, allowMisplacedPay).then((h) => {
            if (l.length === 1) {
              this.unsafe.if(pubkey, l.map((x) => x.toString()), [], args, false, allowMisplacedPay, false, true).then((h2) => {
                payhandler(h2, l[0]);
              });
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          }).else((h) => {
            if (r.length === 1) {
              payhandler(h, l[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          });
        };
        recurse(numbers.slice(0, numbers.length / 2), numbers.slice(numbers.length / 2));
        const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
        if (!allowUnsafe) {
          if (this.aliceTrackers[id] > numbers.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
          }
          if (this.bobTrackers[id] > numbers.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
          }
        }
      },
      value: () => {
        let numbers = [];
        for (let i = from; i <= to; i += step) {
          numbers.push(i);
        }
        const recurse = (l, r) => {
          if (l.length === 1 && r.length === 0) {
            return l[0];
          }
          if (r.length === 1 && l.length === 0) {
            return r[0];
          }
          if (this.unsafe.outcome(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args)) {
            if (l.length === 1) {
              if (this.unsafe.outcome(pubkey, l.map((x) => x.toString()), [], args, false, false, true)) {
                return l[0];
              } else {
                throw "skip";
              }
            } else {
              return recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          } else {
            if (r.length === 1) {
              return r[0];
            } else {
              return recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          }
        };
        if (!allowUnsafe) {
          throw new Error("Only available in unsafe mode");
        }
        return recurse(numbers.slice(0, numbers.length / 2), numbers.slice(numbers.length / 2));
      },
      valueWithPaymentCtxUnsafe: () => {
        let numbers = [];
        for (let i = from; i <= to; i += step) {
          numbers.push(i);
        }
        let nn = numbers[0];
        let hh = void 0;
        let payhandler = (h, n) => {
          nn = n;
          hh = h;
        };
        let argument = args;
        const recurse = (l, r, payhandlerOut) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          this.unsafe.if(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), argument, false, allowMisplacedPay).then((h) => {
            if (l.length === 1) {
              this.unsafe.if(pubkey, l.map((x) => x.toString()), [], args, false, allowMisplacedPay, false, true).then((h2) => {
                payhandler(h2, l[0]);
              }).else((_) => {
                throw "skip";
              });
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2), h);
            }
          }).else((h) => {
            if (r.length === 1) {
              payhandler(h, r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2), h);
            }
          });
        };
        recurse(numbers.slice(0, numbers.length / 2), numbers.slice(numbers.length / 2), null);
        this.unfinalized++;
        const saveRelease = hh.release;
        hh.release = () => {
          const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
          if (!allowUnsafe) {
            if (this.aliceTrackers[id] > numbers.length - 1) {
              throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
            }
            if (this.bobTrackers[id] > numbers.length - 1) {
              throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
            }
          }
          saveRelease();
        };
        if (hh === void 0) {
          this.unfinalized--;
          throw "skip";
        }
        return [nn, hh];
      }
    })
  };
  set = {
    safe: {
      outcome: (pubkey, yes, no, args = {}, allowMisplacedPay = false) => {
        if (this.outcome(pubkey, [yes], [no], args)) {
          return yes;
        } else {
          return no;
        }
      },
      outcomeT: (pubkey, yes, no, renderer = (x) => x.toString(), args = {}) => {
        if (this.outcome(pubkey, [renderer(yes)], [renderer(no)], args)) {
          return yes;
        } else {
          return no;
        }
      },
      if: (pubkey, yes, no, args = {}) => {
        const iff = this.if(pubkey, [yes], [no], args);
        return {
          then: (handler) => {
            const thenn = iff.then((h) => handler(yes, h));
            return {
              else: (handler2) => {
                return thenn.else((h) => handler2(no, h));
              }
            };
          }
        };
      },
      ifT: (pubkey, yes, no, renderer = (x) => x.toString(), args = {}) => {
        const iff = this.if(pubkey, [renderer(yes)], [renderer(no)], args);
        return {
          then: (handler) => {
            const thenn = iff.then((h) => handler(yes, h));
            return {
              else: (handler2) => {
                return thenn.else((h) => handler2(no, h));
              }
            };
          }
        };
      }
    },
    outcome: (pubkey, set, args = {}, allowMisplacedPay = false, allowUnsafe = true) => ({
      evaluate: (handler) => {
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          if (this.unsafe.outcome(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args)) {
            if (l.length === 1) {
              if (this.unsafe.outcome(pubkey, l, [], args, false, true, true)) {
                handler(l[0]);
              }
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          } else {
            if (r.length === 1) {
              handler(r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          }
        };
        recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
        const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
        if (!allowUnsafe) {
          if (this.aliceTrackers[id] > set.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
          }
          if (this.bobTrackers[id] > set.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
          }
        }
        if (this.strictlyOneLeafPays && (this.aliceTrackers[id] > 1 || this.bobPayCounter[id] > 1)) {
          throw new DslErrors.OnePayPerCondition("Only one leaf in a tree is alowed to pay", void 0, void 0, this.selected, this.state);
        }
        if (this.strictlyOneLeafPairPays && (this.aliceTrackers[id] > 1 || this.bobPayCounter[id] > 1)) {
          throw new DslErrors.PerfectHedgeError("Only one pair in a tree is alowed to pay", this.state, void 0, void 0, this.selected);
        }
      },
      evaluateWithPaymentCtx: (payhandler) => {
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          this.if(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args, false, allowMisplacedPay).then((h) => {
            if (l.length === 1) {
              this.if(pubkey, l.map((x) => x.toString()), [], args, false, allowMisplacedPay, false, true).then((h2) => {
                payhandler(h2, l[0]);
              });
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          }).else((h) => {
            if (r.length === 1) {
              payhandler(h, r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          });
        };
        if (this.strictlyOneLeafPays || this.strictlyOneLeafPairPays) {
          throw Error("Leaf strictness tracking is not available in this mode. Use `evaluate`");
        }
        recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
        const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
        if (!allowUnsafe) {
          if (this.aliceTrackers[id] > set.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
          }
          if (this.bobTrackers[id] > set.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
          }
        }
      },
      value: () => {
        const recurse = (l, r) => {
          if (l.length === 1 && r.length === 0) {
            return l[0];
          }
          if (r.length === 1 && l.length === 0) {
            return r[0];
          }
          if (this.outcome(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args)) {
            if (l.length === 1) {
              if (this.outcome(pubkey, l.map((x) => x.toString()), [], args, false, false, true)) {
                return l[0];
              } else {
                throw "skip";
              }
            } else {
              return recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          } else {
            if (r.length === 1) {
              return r[0];
            } else {
              return recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          }
        };
        if (this.strictlyOneLeafPays || this.strictlyOneLeafPairPays) {
          throw Error("Leaf strictness tracking is not available in this mode. Use `evaluate`");
        }
        return recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
      },
      valueWithPaymentCtxUnsafe: () => {
        let nn = set[0];
        let hh = void 0;
        const payhandler = (h, n) => {
          nn = n;
          hh = h;
        };
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          this.if(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args, false, allowMisplacedPay).then((h) => {
            if (l.length === 1) {
              this.if(pubkey, l.map((x) => x.toString()), [], args, false, allowMisplacedPay, true).then((h2) => {
                payhandler(h2, l[0]);
              });
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          }).else((h) => {
            if (r.length === 1) {
              payhandler(h, r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          });
        };
        if (this.strictlyOneLeafPays || this.strictlyOneLeafPairPays) {
          throw Error("Leaf strictness tracking is not available in this mode. Use `evaluate`");
        }
        recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
        this.unfinalized++;
        const saveRelease = hh.release;
        hh.release = () => {
          const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
          if (!allowUnsafe) {
            if (this.aliceTrackers[id] > set.length - 1) {
              throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
            }
            if (this.bobTrackers[id] > set.length - 1) {
              throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
            }
          }
          saveRelease();
        };
        if (hh === void 0) {
          this.unfinalized--;
          throw "skip";
        }
        return [nn, hh];
      }
    }),
    outcomeT: (pubkey, set, renderer, args = {}, allowMisplacedPay = false, allowUnsafe = false) => ({
      evaluate: (handler) => {
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          if (this.unsafe.outcome(pubkey, l.map((x) => renderer(x)), r.map((x) => renderer(x)), args)) {
            if (l.length === 1) {
              if (this.unsafe.outcome(pubkey, l.map((x) => renderer(x)), [], args, false, false, true)) {
                handler(l[0]);
              }
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          } else {
            if (r.length === 1) {
              handler(r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          }
        };
        recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
        const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
        if (!allowUnsafe) {
          if (this.aliceTrackers[id] > set.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
          }
          if (this.bobTrackers[id] > set.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
          }
        }
        if (this.strictlyOneLeafPays && (this.aliceTrackers[id] > 1 || this.bobPayCounter[id] > 1)) {
          throw new DslErrors.OnePayPerCondition("Only one leaf in a tree is alowed to pay", void 0, void 0, this.selected, this.state);
        }
        if (this.strictlyOneLeafPairPays && (this.aliceTrackers[id] > 1 || this.bobPayCounter[id] > 1)) {
          throw new DslErrors.PerfectHedgeError("Only one pair in a tree is alowed to pay", this.state, void 0, void 0, this.selected);
        }
      },
      evaluateWithPaymentCtx: (payhandler) => {
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          this.if(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args, false, allowMisplacedPay).then((h) => {
            if (l.length === 1) {
              this.if(pubkey, l.map((x) => x.toString()), [], args, false, allowMisplacedPay, false, true).then((h2) => {
                payhandler(h2, l[0]);
              });
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          }).else((h) => {
            if (r.length === 1) {
              payhandler(h, r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          });
        };
        if (this.strictlyOneLeafPays || this.strictlyOneLeafPairPays) {
          throw Error("Leaf strictness tracking is not available in this mode. Use `evaluate`");
        }
        recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
      },
      value: () => {
        const recurse = (l, r) => {
          if (l.length === 1 && r.length === 0) {
            return l[0];
          }
          if (r.length === 1 && l.length === 0) {
            return r[0];
          }
          if (this.outcome(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args)) {
            if (l.length === 1) {
              if (this.outcome(pubkey, l.map((x) => x.toString()), [], args, false, false, true)) {
                return l[0];
              }
            } else {
              return recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          } else {
            if (r.length === 1) {
              return r[0];
            } else {
              return recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          }
        };
        if (this.strictlyOneLeafPays || this.strictlyOneLeafPairPays) {
          throw Error("Leaf strictness tracking is not available in this mode. Use `evaluate`");
        }
        return recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
      },
      valueWithPaymentCtxUnsafe: () => {
        let nn = set[0];
        let hh = void 0;
        let payhandler = (h, n) => {
          nn = n;
          hh = h;
        };
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          const rt = this.if(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args, false, allowMisplacedPay).then((h) => {
            if (l.length === 1) {
              this.if(pubkey, l.map((x) => x.toString()), [], args, false, allowMisplacedPay, false, true).then((h2) => {
                payhandler(h2, l[0]);
              });
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          }).else((h) => {
            if (r.length === 1) {
              payhandler(h, r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          });
        };
        if (this.strictlyOneLeafPays || this.strictlyOneLeafPairPays) {
          throw Error("Leaf strictness tracking is not available in this mode. Use `evaluate`");
        }
        recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
        this.unfinalized++;
        const saveRelease = hh.release;
        hh.release = () => {
          const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
          if (!allowUnsafe) {
            if (this.aliceTrackers[id] > set.length - 1) {
              throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
            }
            if (this.bobTrackers[id] > set.length - 1) {
              throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
            }
          }
          saveRelease();
        };
        if (hh === void 0) {
          this.unfinalized--;
          throw "skip";
        }
        return [nn, hh];
      }
    })
  };
  assert = {
    sum: {
      budget: (amount) => {
        if (this.collateral1 + this.collateral2 > amount) {
          throw Error(`Max mutual budget assertion: ${this.collateral1} > ${amount}`);
        }
      },
      parties: (party1, party2) => ({
        budget: (amount) => {
          if (this.isSelected0(party1) && this.isSelected1(party2)) {
            if (this.collateral1 + this.collateral2 > amount) {
              throw Error(`Max mutual budget assertion: ${this.collateral1} > ${amount}`);
            }
          }
          if (this.isSelected0(party2) && this.isSelected1(party1)) {
            if (this.collateral1 + this.collateral2 > amount) {
              throw Error(`Max mutual budget assertion: ${this.collateral1} > ${amount}`);
            }
          }
        }
      })
    },
    budget: (idx, amount) => {
      if (idx == 0) {
        if (this.collateral1 > amount) {
          throw Error(`Max Alice budget assertion: ${this.collateral1} > ${amount}`);
        }
      } else {
        if (this.collateral1 > amount) {
          throw Error(`Max Bob budget assertion: ${this.collateral2} > ${amount}`);
        }
      }
    },
    parties: (party1, party2) => ({
      budget: (idx, amount) => {
        if (this.isSelected0(party1) && this.isSelected1(party2)) {
          if (idx == 0) {
            if (this.collateral1 > amount) {
              throw Error(`Max ${party1} budget assertion (against ${party2}): ${this.collateral1} > ${amount}`);
            }
          } else {
            if (this.collateral1 > amount) {
              throw Error(`Max ${party2} budget assertion (against ${party1}): ${this.collateral2} > ${amount}`);
            }
          }
        }
        if (this.isSelected0(party2) && this.isSelected1(party1)) {
          if (idx == 0) {
            if (this.collateral1 > amount) {
              throw Error(`Max ${party2} budget assertion (against ${party1}): ${this.collateral1} > ${amount}`);
            }
          } else {
            if (this.collateral1 > amount) {
              throw Error(`Max ${party1} budget assertion (against ${party2}): ${this.collateral2} > ${amount}`);
            }
          }
        }
      }
    })
  };
  disablePartyRoleReversal = false;
  if = (pubkey, yes, no, args = {}, allowSwaps = false, allowMisplacedPay = false, strict = true, ignoreObserveChecksSuperUnsafe = false) => {
    let contradiction = false;
    const yesSet = new Set(yes);
    const noSet = new Set(no);
    if (yes.find((x) => noSet.has(x)) || no.find((x) => yesSet.has(x))) {
      contradiction = true;
    }
    const observation = this.outcome(pubkey, yes, no, args, allowSwaps, strict, ignoreObserveChecksSuperUnsafe);
    const currentNode = this.cursor;
    const currentPrevNode = this.prev;
    const currentLastOutcome = this.lastOutcome;
    return {
      then: (handler) => {
        let party = void 0;
        let sum = 0;
        const funds = {
          pay: (idx, amount) => {
            if (party === void 0 || idx === party) {
              sum += amount;
              party = idx;
            } else {
              if (party !== void 0 && idx !== party) {
                if (this.disablePartyRoleReversal) {
                  throw new Error("Party role reversal is disabled!");
                }
                sum -= amount;
              } else {
                throw new DslErrors.PerfectHedgeError("Perfect Hedge! Party cannot benefit regardless of outcome!", this.state, amount, idx, this.selected);
              }
            }
          },
          party: (partyName, partyAsset) => ({
            pays: (counterpartyName, counterpartyAsset) => ({
              amount: (amount, asset) => {
                const party2 = partyName + (partyAsset ? "_" + partyAsset : "");
                const counterparty = counterpartyName + (counterpartyAsset ? "_" + counterpartyAsset : "");
                if (currentNode !== this.cursor && !allowMisplacedPay) {
                  throw Error("Possibly trying to pay nondeterministically! You tried to use outer account context to pay: use the closest `if(...).then/else(account => ...)` please! This also happens when you pay after checking unrelated observations: pay before checking next outcome! You can turn this off by using `dsl.unsafe`");
                }
                if (partyAsset !== asset) {
                  throw Error(`Trying to pay ${asset} from collateral denominated in ${partyAsset}`);
                }
                if (!this.multiparty.find((x) => x === party2)) {
                  throw Error("party " + party2 + " not registered! Use .multiple to register parties");
                }
                if (!this.multiparty.find((x) => x === counterparty)) {
                  throw Error("counterparty " + counterparty + " not registered! Use .multiple to register parties");
                }
                if (this.isSelected0(party2) && this.isSelected1(counterparty)) {
                  funds.pay(0, amount);
                } else if (this.isSelected0(counterparty) && this.isSelected1(party2)) {
                  funds.pay(1, amount);
                }
                if (sum < 0 && partyAsset !== counterpartyAsset) {
                  throw new Error(`Semantics: ${partyName} cannot pay negative amount of ${partyAsset} units. It is only allowed if assets are of the same type.`);
                }
                if (contradiction && partyAsset === counterpartyAsset) {
                  throw Error("Contradiction! Outcomes are not mutually exclusive! Cannot allow swaps in same currency!");
                }
              }
            })
          }),
          release: () => {
            if (currentNode !== this.cursor && !allowMisplacedPay) {
              throw Error("Trying to release nondeterministically! You tried to release using outer account context: use the closest `if(...).then/else(account => ...)` please! This also happens if you release after checking unrelated observations: release before next `outcome; use `allowMisplacedPay` in `dsl.if` to disable this check`");
            }
            const saveCursor = this.cursor;
            const savePrev = this.prev;
            const saveFlag = this.flag;
            const saveLastOutcome = this.lastOutcome;
            if (allowMisplacedPay) {
              this.cursor = currentNode;
              this.prev = currentPrevNode;
              this.flag = false;
              this.lastOutcome = currentLastOutcome;
            }
            this.unfinalized--;
            funds.party = void 0;
            funds.pay = void 0;
            if (party !== void 0 && sum !== 0) {
              if (sum > 0) {
                this.pay(party, sum);
              } else if (sum < 0) {
                this.pay(party === 0 ? 1 : 0, -sum);
              }
            }
            this.cursor = saveCursor;
            this.prev = savePrev;
            this.flag = saveFlag;
            this.lastOutcome = saveLastOutcome;
          }
        };
        const finalizeUnsafeInternal = () => {
          if (observation) {
            handler(funds);
            const saveCursor = this.cursor;
            const savePrev = this.prev;
            const saveFlag = this.flag;
            const saveLastOutcome = this.lastOutcome;
            if (allowMisplacedPay) {
              this.cursor = currentNode;
              this.prev = currentPrevNode;
              this.flag = false;
              this.lastOutcome = currentLastOutcome;
            }
            if (party !== void 0 && sum !== 0) {
              if (sum > 0) {
                this.pay(party, sum);
              } else if (sum < 0) {
                this.pay(party === 0 ? 1 : 0, -sum);
              }
            }
            this.cursor = saveCursor;
            this.prev = savePrev;
            this.flag = saveFlag;
            this.lastOutcome = saveLastOutcome;
          }
        };
        finalizeUnsafeInternal();
        return {
          else: (handler2) => {
            let counterparty = void 0;
            let sum2 = 0;
            const funds2 = {
              pay: (idx, amount) => {
                if (counterparty === void 0 || idx === counterparty) {
                  if (party !== void 0 && counterparty !== void 0 && party === counterparty) {
                    throw new DslErrors.PerfectHedgeError("Perfect Hedge! Party cannot benefit regardless of outcome!", this.state, amount, idx, this.selected);
                  }
                  counterparty = idx;
                  sum2 += amount;
                } else {
                  if (counterparty !== void 0 && idx !== counterparty) {
                    if (this.disablePartyRoleReversal) {
                      throw new Error("Party role reversal is disabled!");
                    }
                    sum2 -= amount;
                  } else {
                    throw new DslErrors.PerfectHedgeError("Perfect Hedge! Party cannot benefit regardless of outcome!", this.state, amount, idx, this.selected);
                  }
                }
              },
              party: (partyName, partyAsset) => ({
                pays: (counterpartyName, counterpartyAsset) => ({
                  amount: (amount, asset) => {
                    const party2 = partyName + (partyAsset ? "_" + partyAsset : "");
                    const counterparty2 = counterpartyName + (counterpartyAsset ? "_" + counterpartyAsset : "");
                    if (currentNode !== this.cursor && !allowMisplacedPay) {
                      throw Error("Possibly trying to pay nondeterministically! You tried to use outer account context to pay: use the closest `if(...).then/else(account => ...)` please! This also happens when you pay after checking unrelated observations: pay before checking next outcome! You can turn this off by using `dsl.unsafe`");
                    }
                    if (partyAsset !== asset) {
                      throw Error(`Trying to pay ${asset} from collateral denominated in ${partyAsset}`);
                    }
                    if (!this.multiparty.find((x) => x === party2)) {
                      throw Error("party " + party2 + " not registered! Use .multiple to register parties");
                    }
                    if (!this.multiparty.find((x) => x === counterparty2)) {
                      throw Error("counterparty " + counterparty2 + " not registered! Use .multiple to register parties");
                    }
                    if (!this.multiparty.find((x) => x === party2)) {
                      throw Error("party " + party2 + " not registered! Use .multiple to register parties in " + this.multiparty);
                    }
                    if (!this.multiparty.find((x) => x === counterparty2)) {
                      throw Error("counterparty " + counterparty2 + " not registered! Use .multiple to register parties in " + this.multiparty);
                    }
                    if (this.isSelected0(party2) && this.isSelected1(counterparty2)) {
                      funds2.pay(0, amount);
                    } else if (this.isSelected0(counterparty2) && this.isSelected1(party2)) {
                      funds2.pay(1, amount);
                    }
                  }
                })
              }),
              release: () => {
                if (currentNode !== this.cursor && !allowMisplacedPay) {
                  throw Error("Possibly trying to release nondeterministically! You tried to release using outer account context: use the closest `if(...).then/else(account => ...)` please! This also happens if you release after checking unrelated observations: release before next `outcome`; use `allowMisplacedPay` in `dsl.if` to disable this check");
                }
                const saveCursor = this.cursor;
                const savePrev = this.prev;
                const saveFlag = this.flag;
                const saveLastOutcome = this.lastOutcome;
                if (allowMisplacedPay) {
                  this.cursor = currentNode;
                  this.prev = currentPrevNode;
                  this.flag = false;
                  this.lastOutcome = currentLastOutcome;
                }
                this.unfinalized--;
                funds2.party = void 0;
                funds2.pay = void 0;
                if (counterparty !== void 0 && sum2 !== 0) {
                  if (sum2 > 0) {
                    this.pay(counterparty, sum2);
                  } else if (sum2 < 0) {
                    this.pay(counterparty === 0 ? 1 : 0, -sum2);
                  }
                }
                this.cursor = saveCursor;
                this.prev = savePrev;
                this.flag = saveFlag;
                this.lastOutcome = saveLastOutcome;
              }
            };
            const finalizeUnsafeInternal2 = () => {
              if (!observation) {
                handler2(funds2);
                const saveCursor = this.cursor;
                const savePrev = this.prev;
                const saveFlag = this.flag;
                const saveLastOutcome = this.lastOutcome;
                if (allowMisplacedPay) {
                  this.cursor = currentNode;
                  this.prev = currentPrevNode;
                  this.flag = false;
                  this.lastOutcome = currentLastOutcome;
                }
                if (counterparty !== void 0 && sum2 !== 0) {
                  if (sum2 > 0) {
                    this.pay(counterparty, sum2);
                  } else if (sum2 < 0) {
                    this.pay(counterparty === 0 ? 1 : 0, -sum2);
                  }
                }
                this.cursor = saveCursor;
                this.prev = savePrev;
                this.flag = saveFlag;
                this.lastOutcome = saveLastOutcome;
              }
            };
            finalizeUnsafeInternal2();
          }
        };
      }
    };
  };
  ifAtomicSwapLeg1(lock = "TRUTH", unlockOutcome = "true", allowMisplacedPay = false) {
    return this.if(lock, [unlockOutcome], [unlockOutcome], {}, true, allowMisplacedPay);
  }
  multiflag = false;
  async enumerateWithBoundMulti(collateralBounds) {
    this.multiflag = true;
    const pairs = this.multiparty.map((x) => this.multiparty.map((y) => [x, y]).filter((pair) => pair[0] !== pair[1])).flat().map((x) => x.sort());
    const ids = /* @__PURE__ */ new Set();
    const uniquepairs = pairs.filter((id) => !ids.has(JSON.stringify(id)) && ids.add(JSON.stringify(id)));
    const mutex = new Mutex();
    const res = await Promise.all(uniquepairs.map(async (pair, i) => {
      return await mutex.runExclusive(async () => {
        this.selected = pair;
        if (!collateralBounds[i]) {
          throw Error("Specify bounds for a pair " + pair + " at index: " + i);
        }
        try {
          const subcontract = await this.enumerateWithBound(collateralBounds[i][0], collateralBounds[i][1]);
          return [pair[0], pair[1], subcontract];
        } catch (e) {
          if (e instanceof DslErrors.EmptyDslOutput) {
            return [pair[0], pair[1], void 0];
          } else {
            throw e;
          }
        }
      });
    }));
    this.multiflag = false;
    return res;
  }
  async enumerateWithBound(collateralBound1, collateralBound2) {
    if (this.multiparty.length > 0 && !this.multiflag) {
      throw Error("use `enumerateWithBoundMulti` for multiparty contracts!");
    }
    if (this.protect) {
      throw "Don't call enumerate inside of the body of your script!";
    }
    this.protect = true;
    let next = true;
    const mutex = new Mutex();
    this.aliceTrackers = {};
    this.bobTrackers = {};
    while (next) {
      try {
        this.flagSameAssetSwap = false;
        this.collateral1 = 0;
        this.collateral2 = 0;
        this.budgetBound1 = collateralBound1;
        this.budgetBound2 = collateralBound2;
        this.memoize = [];
        this.checked = [];
        this.counter = 0;
        this.lastOutcome = void 0;
        this.flag = false;
        await mutex.runExclusive(async () => await this.body(this));
        if (this.unfinalized !== 0) {
          throw new Error("" + this.unfinalized + " resource locks are not released! Every `[v, payments] = valueWithPaymentCtxUnsafe` must have a corresponding `payments.release()`");
        }
        if (this.safeModeStarted && !this.safeMode) {
          throw new Error("All insecure safe mode escape sections must be closed! Forgot `insecure.close.reEnableMode`?");
        }
        if (this.megaModeStarted && !this.megaMode) {
          throw new Error("All insecure mega mode escape sections must be closed! Forgot `insecure.close.reEnableMode`?");
        }
        if (this.strictModeStarted && !this.strictlyStrict) {
          throw new Error("All insecure strict mode escape sections must be closed! Forgot `insecure.close.reEnableMode`?");
        }
        if (this.superModeStarted && !this.superMode) {
          throw new Error("All insecure super mode escape sections must be closed! Forgot `insecure.close.reEnableMode`?");
        }
        if (this.fairModeStarted && !this.strictlyFair) {
          throw new Error("All insecure fair mode escape sections must be closed! Forgot `insecure.close.reEnableMode`?");
        }
      } catch (e) {
        if (e === "uninitialized" || e === "skip") {
        } else {
          throw e;
        }
      }
      next = this.next();
    }
    this.protect = false;
    this.leafsFiltered = true;
    let result = this.root;
    while (this.leafsFiltered) {
      this.leafsFiltered = false;
      result = this.filterLeafs(result);
    }
    if (this.strictlyStrict) {
      this.filterLeafs(result, true);
    }
    return result;
  }
};
if (typeof window === "undefined" && __require.main === module) {
  (async () => {
    const model = await new Dsl(async (dsl) => {
      const a = 60;
      if (dsl.outcome("really?", ["YES"], ["NO"])) {
        dsl.pay(Dsl.Bob, a + 100);
        const out1 = dsl.outcome("is it?", ["YES"], ["NO", "DON'T KNOW"]);
        if (out1) {
          dsl.pay(Dsl.Alice, 40);
          if (dsl.outcome("is it?", ["DON'T KNOW"], ["NO", "YES"])) {
            dsl.pay(Dsl.Bob, 40);
            dsl.if("lol?", ["yup"], ["nope"]).then((funds) => {
              funds.pay(Dsl.Bob, 20);
              funds.pay(Dsl.Alice, 30);
            }).else((funds) => {
              funds.pay(Dsl.Bob, 40);
              funds.pay(Dsl.Alice, 10);
            });
          }
        } else {
          dsl.pay(Dsl.Bob, 50);
        }
      } else {
        dsl.pay(Dsl.Alice, 20);
      }
    }).multiple("alice", "bob").enumerateWithBoundMulti([[1e3, 2e4]]);
    console.log(model);
    const multi = await new Dsl(async (dsl) => {
      if (dsl.outcome("really?", ["YES"], ["NO"])) {
        dsl.party("alice").pays("bob").amount(100);
        dsl.party("bob").pays("carol").amount(20);
      } else {
        dsl.party("carol").pays("alice").amount(40);
        dsl.party("bob").pays("alice").amount(40);
        dsl.if("wow?", ["yup"], ["nope"]).then((account) => {
          account.party("alice").pays("carol").amount(30);
          account.party("carol").pays("alice").amount(5);
        }).else((account) => {
          account.party("carol").pays("alice").amount(30);
        });
      }
    }).multiple("alice", "bob", "carol").enumerateWithBoundMulti([[1e3, 2e3], [1e3, 2e3], [1e3, 2e3]]);
    console.log(multi);
    const multi2 = await new Dsl(async (dsl) => {
      const dates = ["today", "tomorrow", "next week", "next month"];
      const capitalisationDates = /* @__PURE__ */ new Set(["next week"]);
      const notional = 1e4;
      const floatingLegIndex = "interest rate index?";
      const fixedRate = 0.990382835;
      const quantisationStep = 1;
      dates.reduce(([capitalisation1, capitalisation2], date) => {
        const [floatingRate, accounts] = dsl.unsafe.numeric.outcome(floatingLegIndex, 0, 1, quantisationStep, { date }).valueWithPaymentCtxUnsafe();
        if (capitalisationDates.has(date)) {
          const floatingPayout = (notional + capitalisation1) * (floatingRate / 100);
          const fixedPayout = (notional + capitalisation2) * (fixedRate / 100);
          accounts.party("alice").pays("bob").amount(floatingPayout);
          accounts.party("bob").pays("alice").amount(fixedPayout);
          console.log("!!!!!" + (floatingPayout - fixedPayout) + " ==> " + JSON.stringify(dsl["aliceTrackers"]));
          accounts.release();
          return [0, 0];
        } else {
          accounts.release();
          return [
            notional * (floatingRate / 100) + capitalisation1,
            notional * (fixedRate / 100) + capitalisation2
          ];
        }
      }, [0, 0]);
    }).multiple("alice", "bob").enumerateWithBoundMulti([[5e4, 2e4]]);
    console.log(multi2);
    const assets = await new Dsl(async (dsl) => {
      if (dsl.outcome("really?", ["YES"], ["NO"])) {
        dsl.party("alice", "usd").pays("bob", "btc").amount(1e7, "usd");
      } else {
        dsl.party("bob", "btc").pays("alice", "usd").amount(10, "btc");
      }
    }).multiple(Dsl.account("alice", "usd"), Dsl.account("bob", "btc")).enumerateWithBoundMulti([[1e9, 2e4]]);
    console.log(assets);
    const swap = await new Dsl(async (dsl) => {
      dsl.atomicSwap.ifTruth("lock1", "allowed").then((pay) => {
        dsl.atomicSwap.ifTruth("lock12", "allowed").then((pay2) => {
          pay2.party("bob", "btc").pays("alice", "usd").amount(10, "btc");
        }).else(() => {
        });
        pay.party("alice", "usd").pays("bob", "btc").amount(1e7, "usd");
      }).else((pay) => {
        pay.party("bob", "btc").pays("alice", "usd").amount(10, "btc");
      });
      dsl.unsafe.numeric.outcome("???", 0, 3).evaluate((x) => {
        dsl.pay(Dsl.Alice, 10);
      });
      dsl.numeric.infinity.bounded(100).perpetual(0, (x, st) => {
        return dsl.infinity.stop;
      });
      dsl.unsafe.numeric.infinity.bounded(100).perpetual({ "alice": 100, "bob": 100 }, (x, st) => {
        const shouldnot = dsl.unsafe.infinity.move([{
          alice: st.alice - 1,
          bob: st.bob - 2
        }, [
          {
            from: ["alice", "usd"],
            to: ["bob", "btc"],
            amount: 1
          },
          {
            from: ["bob", "btc"],
            to: ["alice", "usd"],
            amount: 2
          }
        ]]);
        return dsl.unsafe.infinity.stop([]);
      });
    }).multiple(Dsl.account("alice", "usd"), Dsl.account("bob", "btc")).enumerateWithBoundMulti([[1e10, 2e5]]);
    console.log(swap);
    const turing = (a) => () => {
      if (a > 5) {
        return 7;
      } else {
        return Dsl.recurse.bounded(turing(a)).attempts(30).otherwiseYield(50)();
      }
    };
    console.log(turing(7)());
    console.log(turing(1)());
    console.log("OK!");
  })();
}

// src-web/util/dsl-runtime.ts
var evalDiscreet = async (expression, parties, bounds) => {
  const model = await new Dsl(async (dsl) => {
    const prototypeWhitelist = Sandbox.SAFE_PROTOTYPES;
    const globals = { ...Sandbox.SAFE_GLOBALS, alert };
    prototypeWhitelist.set(Dsl, /* @__PURE__ */ new Set());
    const sandbox = new Sandbox({ globals, prototypeWhitelist });
    const exec = sandbox.compile(expression);
    exec({ dsl, Dsl }).run();
  }).multiple(...parties).enumerateWithBoundMulti(bounds);
  return model;
};
window.evalDiscreet = evalDiscreet;
export {
  evalDiscreet
};
//# sourceMappingURL=discreet-eval.min.js.map
