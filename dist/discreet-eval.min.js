var __require = /* @__PURE__ */ ((x) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x, {
  get: (a, b) => (typeof require !== "undefined" ? require : a)[b]
}) : x)(function(x) {
  if (typeof require !== "undefined") return require.apply(this, arguments);
  throw Error('Dynamic require of "' + x + '" is not supported');
});

// node_modules/@nyariv/sandboxjs/dist/esm/utils/errors.js
var SandboxError = class extends Error {
};
var SandboxExecutionQuotaExceededError = class extends SandboxError {
};
var SandboxExecutionTreeError = class extends SandboxError {
};
var SandboxCapabilityError = class extends SandboxError {
};
var SandboxAccessError = class extends SandboxError {
};

// node_modules/@nyariv/sandboxjs/dist/esm/utils/types.js
var AsyncFunction = Object.getPrototypeOf(async function() {
}).constructor;
var GeneratorFunction = Object.getPrototypeOf(function* () {
}).constructor;
var AsyncGeneratorFunction = Object.getPrototypeOf(async function* () {
}).constructor;
var NON_BLOCKING_THRESHOLD = 50000n;
var reservedWords = /* @__PURE__ */ new Set([
  "await",
  "break",
  "case",
  "catch",
  "class",
  "const",
  "continue",
  "debugger",
  "default",
  "delete",
  "do",
  "else",
  "enum",
  "export",
  "extends",
  "false",
  "finally",
  "for",
  "function",
  "if",
  "implements",
  "import",
  "in",
  "instanceof",
  "let",
  "new",
  "null",
  "return",
  "super",
  "switch",
  "this",
  "throw",
  "true",
  "try",
  "typeof",
  "var",
  "void",
  "while",
  "with"
]);
var VarType = /* @__PURE__ */ function(VarType2) {
  VarType2["let"] = "let";
  VarType2["const"] = "const";
  VarType2["var"] = "var";
  VarType2["internal"] = "internal";
  return VarType2;
}({});
var LispType = /* @__PURE__ */ function(LispType2) {
  LispType2[LispType2["None"] = 0] = "None";
  LispType2[LispType2["Prop"] = 1] = "Prop";
  LispType2[LispType2["StringIndex"] = 2] = "StringIndex";
  LispType2[LispType2["Let"] = 3] = "Let";
  LispType2[LispType2["Const"] = 4] = "Const";
  LispType2[LispType2["Call"] = 5] = "Call";
  LispType2[LispType2["KeyVal"] = 6] = "KeyVal";
  LispType2[LispType2["Number"] = 7] = "Number";
  LispType2[LispType2["Return"] = 8] = "Return";
  LispType2[LispType2["Assign"] = 9] = "Assign";
  LispType2[LispType2["InlineFunction"] = 10] = "InlineFunction";
  LispType2[LispType2["ArrowFunction"] = 11] = "ArrowFunction";
  LispType2[LispType2["CreateArray"] = 12] = "CreateArray";
  LispType2[LispType2["If"] = 13] = "If";
  LispType2[LispType2["IfCase"] = 14] = "IfCase";
  LispType2[LispType2["InlineIf"] = 15] = "InlineIf";
  LispType2[LispType2["InlineIfCase"] = 16] = "InlineIfCase";
  LispType2[LispType2["SpreadObject"] = 17] = "SpreadObject";
  LispType2[LispType2["SpreadArray"] = 18] = "SpreadArray";
  LispType2[LispType2["ArrayProp"] = 19] = "ArrayProp";
  LispType2[LispType2["PropOptional"] = 20] = "PropOptional";
  LispType2[LispType2["CallOptional"] = 21] = "CallOptional";
  LispType2[LispType2["CreateObject"] = 22] = "CreateObject";
  LispType2[LispType2["Group"] = 23] = "Group";
  LispType2[LispType2["Not"] = 24] = "Not";
  LispType2[LispType2["IncrementBefore"] = 25] = "IncrementBefore";
  LispType2[LispType2["IncrementAfter"] = 26] = "IncrementAfter";
  LispType2[LispType2["DecrementBefore"] = 27] = "DecrementBefore";
  LispType2[LispType2["DecrementAfter"] = 28] = "DecrementAfter";
  LispType2[LispType2["And"] = 29] = "And";
  LispType2[LispType2["Or"] = 30] = "Or";
  LispType2[LispType2["StrictNotEqual"] = 31] = "StrictNotEqual";
  LispType2[LispType2["StrictEqual"] = 32] = "StrictEqual";
  LispType2[LispType2["Plus"] = 33] = "Plus";
  LispType2[LispType2["Var"] = 34] = "Var";
  LispType2[LispType2["GlobalSymbol"] = 35] = "GlobalSymbol";
  LispType2[LispType2["Literal"] = 36] = "Literal";
  LispType2[LispType2["Function"] = 37] = "Function";
  LispType2[LispType2["Loop"] = 38] = "Loop";
  LispType2[LispType2["Try"] = 39] = "Try";
  LispType2[LispType2["Switch"] = 40] = "Switch";
  LispType2[LispType2["SwitchCase"] = 41] = "SwitchCase";
  LispType2[LispType2["InternalBlock"] = 42] = "InternalBlock";
  LispType2[LispType2["Expression"] = 43] = "Expression";
  LispType2[LispType2["Await"] = 44] = "Await";
  LispType2[LispType2["New"] = 45] = "New";
  LispType2[LispType2["Throw"] = 46] = "Throw";
  LispType2[LispType2["Minus"] = 47] = "Minus";
  LispType2[LispType2["Divide"] = 48] = "Divide";
  LispType2[LispType2["Power"] = 49] = "Power";
  LispType2[LispType2["Multiply"] = 50] = "Multiply";
  LispType2[LispType2["Modulus"] = 51] = "Modulus";
  LispType2[LispType2["Equal"] = 52] = "Equal";
  LispType2[LispType2["NotEqual"] = 53] = "NotEqual";
  LispType2[LispType2["SmallerEqualThan"] = 54] = "SmallerEqualThan";
  LispType2[LispType2["LargerEqualThan"] = 55] = "LargerEqualThan";
  LispType2[LispType2["SmallerThan"] = 56] = "SmallerThan";
  LispType2[LispType2["LargerThan"] = 57] = "LargerThan";
  LispType2[LispType2["Negative"] = 58] = "Negative";
  LispType2[LispType2["Positive"] = 59] = "Positive";
  LispType2[LispType2["Typeof"] = 60] = "Typeof";
  LispType2[LispType2["Delete"] = 61] = "Delete";
  LispType2[LispType2["Instanceof"] = 62] = "Instanceof";
  LispType2[LispType2["In"] = 63] = "In";
  LispType2[LispType2["Inverse"] = 64] = "Inverse";
  LispType2[LispType2["SubractEquals"] = 65] = "SubractEquals";
  LispType2[LispType2["AddEquals"] = 66] = "AddEquals";
  LispType2[LispType2["DivideEquals"] = 67] = "DivideEquals";
  LispType2[LispType2["PowerEquals"] = 68] = "PowerEquals";
  LispType2[LispType2["MultiplyEquals"] = 69] = "MultiplyEquals";
  LispType2[LispType2["ModulusEquals"] = 70] = "ModulusEquals";
  LispType2[LispType2["BitNegateEquals"] = 71] = "BitNegateEquals";
  LispType2[LispType2["BitAndEquals"] = 72] = "BitAndEquals";
  LispType2[LispType2["BitOrEquals"] = 73] = "BitOrEquals";
  LispType2[LispType2["UnsignedShiftRightEquals"] = 74] = "UnsignedShiftRightEquals";
  LispType2[LispType2["ShiftRightEquals"] = 75] = "ShiftRightEquals";
  LispType2[LispType2["ShiftLeftEquals"] = 76] = "ShiftLeftEquals";
  LispType2[LispType2["BitAnd"] = 77] = "BitAnd";
  LispType2[LispType2["BitOr"] = 78] = "BitOr";
  LispType2[LispType2["BitNegate"] = 79] = "BitNegate";
  LispType2[LispType2["BitShiftLeft"] = 80] = "BitShiftLeft";
  LispType2[LispType2["BitShiftRight"] = 81] = "BitShiftRight";
  LispType2[LispType2["BitUnsignedShiftRight"] = 82] = "BitUnsignedShiftRight";
  LispType2[LispType2["BigInt"] = 83] = "BigInt";
  LispType2[LispType2["LiteralIndex"] = 84] = "LiteralIndex";
  LispType2[LispType2["RegexIndex"] = 85] = "RegexIndex";
  LispType2[LispType2["LoopAction"] = 86] = "LoopAction";
  LispType2[LispType2["Void"] = 87] = "Void";
  LispType2[LispType2["True"] = 88] = "True";
  LispType2[LispType2["NullishCoalescing"] = 89] = "NullishCoalescing";
  LispType2[LispType2["AndEquals"] = 90] = "AndEquals";
  LispType2[LispType2["OrEquals"] = 91] = "OrEquals";
  LispType2[LispType2["NullishCoalescingEquals"] = 92] = "NullishCoalescingEquals";
  LispType2[LispType2["Block"] = 93] = "Block";
  LispType2[LispType2["Labeled"] = 94] = "Labeled";
  LispType2[LispType2["Internal"] = 95] = "Internal";
  LispType2[LispType2["Yield"] = 96] = "Yield";
  LispType2[LispType2["YieldDelegate"] = 97] = "YieldDelegate";
  LispType2[LispType2["Hole"] = 98] = "Hole";
  LispType2[LispType2["LispEnumSize"] = 99] = "LispEnumSize";
  return LispType2;
}({});

// node_modules/@nyariv/sandboxjs/dist/esm/utils/functionReplacements.js
function checkTicksAndThrow(ctx, expectTicks) {
  const { ticks } = ctx.ctx;
  if (ticks.tickLimit !== void 0 && ticks.tickLimit <= ticks.ticks + expectTicks) throw new SandboxExecutionQuotaExceededError("Execution quota exceeded");
  ticks.ticks += expectTicks;
}
var typedArrayProtos = new Set([
  Int8Array,
  Uint8Array,
  Uint8ClampedArray,
  Int16Array,
  Uint16Array,
  Int32Array,
  Uint32Array,
  Float32Array,
  Float64Array
].map((T) => Object.getPrototypeOf(T.prototype)));
function isTypedArray(obj) {
  return ArrayBuffer.isView(obj) && !(obj instanceof DataView) && typedArrayProtos.has(Object.getPrototypeOf(Object.getPrototypeOf(obj)));
}
function makeReplacement(original, getTicks) {
  return (ctx) => function(...args) {
    checkTicksAndThrow(ctx, getTicks(this, args));
    return original.apply(this, args);
  };
}
var arr = [];
var arrProto = Array.prototype;
function arrayTicks(complexity, original) {
  return (thisArg, args) => {
    if (!Array.isArray(thisArg)) return 0n;
    const n = BigInt(thisArg.length);
    switch (complexity) {
      case "one":
        return 1n;
      case "n":
        return n;
      case "nlogn":
        return thisArg.length <= 1 ? 1n : n * BigInt(Math.round(Math.log2(thisArg.length)));
      case "arrs": {
        let ticks = 0n;
        const maxDepth = original === arr.flat ? typeof args[0] === "number" ? args[0] : 1 : 1;
        const recurse = (a, depth = 0) => {
          ticks += BigInt(a.length);
          if (depth >= maxDepth) return;
          for (const item of a) if (Array.isArray(item)) recurse(item, depth + 1);
        };
        recurse(thisArg);
        return ticks;
      }
    }
  };
}
var arrayReplacementDefs = [
  [arr.push, "one"],
  [arr.pop, "one"],
  [arrProto.at, "one"],
  [arr.fill, "n"],
  [arr.includes, "n"],
  [arr.indexOf, "n"],
  [arr.lastIndexOf, "n"],
  [arr.find, "n"],
  [arr.findIndex, "n"],
  [arrProto.findLast, "n"],
  [arrProto.findLastIndex, "n"],
  [arr.forEach, "n"],
  [arr.map, "n"],
  [arr.filter, "n"],
  [arr.reduce, "n"],
  [arr.reduceRight, "n"],
  [arr.every, "n"],
  [arr.some, "n"],
  [arr.join, "n"],
  [arr.reverse, "n"],
  [arr.shift, "n"],
  [arr.unshift, "n"],
  [arr.splice, "n"],
  [arr.slice, "n"],
  [arr.copyWithin, "n"],
  [arr.entries, "n"],
  [arr.keys, "n"],
  [arr.values, "n"],
  [arrProto.toReversed, "n"],
  [arrProto.toSpliced, "n"],
  [arrProto.with, "n"],
  [arr.toString, "n"],
  [arr.toLocaleString, "n"],
  [arr.sort, "nlogn"],
  [arrProto.toSorted, "nlogn"],
  [arr.flat, "arrs"],
  [arr.flatMap, "arrs"],
  [arr.concat, "arrs"]
];
var str = "";
var strProto = String.prototype;
function stringTicks(complexity) {
  return (thisArg) => {
    if (typeof thisArg !== "string") return 0n;
    return complexity === "one" ? 1n : BigInt(thisArg.length);
  };
}
var stringReplacementDefs = [
  [str.charAt, "one"],
  [str.charCodeAt, "one"],
  [str.codePointAt, "one"],
  [strProto.at, "one"],
  [str.indexOf, "n"],
  [str.lastIndexOf, "n"],
  [str.includes, "n"],
  [str.startsWith, "n"],
  [str.endsWith, "n"],
  [str.slice, "n"],
  [str.substring, "n"],
  [str.padStart, "n"],
  [str.padEnd, "n"],
  [str.repeat, "n"],
  [str.split, "n"],
  [str.replace, "n"],
  [strProto.replaceAll, "n"],
  [str.match, "n"],
  [str.matchAll, "n"],
  [str.search, "n"],
  [str.trim, "n"],
  [str.trimStart, "n"],
  [str.trimEnd, "n"],
  [str.toLowerCase, "n"],
  [str.toUpperCase, "n"],
  [str.toLocaleLowerCase, "n"],
  [str.toLocaleUpperCase, "n"],
  [str.normalize, "n"],
  [str.concat, "n"],
  [str.toString, "n"],
  [str.valueOf, "n"]
];
var _map = /* @__PURE__ */ new Map();
var mapReplacementDefs = [
  [_map.get, "one"],
  [_map.set, "one"],
  [_map.has, "one"],
  [_map.delete, "one"],
  [_map.keys, "n"],
  [_map.values, "n"],
  [_map.entries, "n"],
  [_map.forEach, "n"],
  [_map.clear, "n"]
];
function mapTicks(complexity) {
  return (thisArg) => {
    if (!(thisArg instanceof Map)) return 0n;
    return complexity === "one" ? 1n : BigInt(thisArg.size);
  };
}
var _set = /* @__PURE__ */ new Set();
var setReplacementDefs = [
  [_set.add, "one"],
  [_set.has, "one"],
  [_set.delete, "one"],
  [_set.values, "n"],
  [_set.keys, "n"],
  [_set.entries, "n"],
  [_set.forEach, "n"],
  [_set.clear, "n"]
];
function setTicks(complexity) {
  return (thisArg) => {
    if (!(thisArg instanceof Set)) return 0n;
    return complexity === "one" ? 1n : BigInt(thisArg.size);
  };
}
var typedArrayReplacementDefs = [];
for (const proto of typedArrayProtos) {
  if (proto.at) typedArrayReplacementDefs.push([proto.at, "one"]);
  if (proto.set) typedArrayReplacementDefs.push([proto.set, "one"]);
  for (const m of [
    "fill",
    "find",
    "findIndex",
    "findLast",
    "findLastIndex",
    "includes",
    "indexOf",
    "lastIndexOf",
    "forEach",
    "map",
    "filter",
    "reduce",
    "reduceRight",
    "every",
    "some",
    "join",
    "reverse",
    "slice",
    "subarray",
    "copyWithin",
    "entries",
    "keys",
    "values",
    "toReversed",
    "with",
    "toString",
    "toLocaleString"
  ]) if (proto[m]) typedArrayReplacementDefs.push([proto[m], "n"]);
  if (proto.sort) typedArrayReplacementDefs.push([proto.sort, "nlogn"]);
  if (proto.toSorted) typedArrayReplacementDefs.push([proto.toSorted, "nlogn"]);
}
function typedArrayTicks(complexity) {
  return (thisArg) => {
    if (!isTypedArray(thisArg)) return 0n;
    const n = BigInt(thisArg.length);
    switch (complexity) {
      case "one":
        return 1n;
      case "n":
        return n;
      case "nlogn":
        return thisArg.length <= 1 ? 1n : n * BigInt(Math.round(Math.log2(thisArg.length)));
    }
  };
}
var mathReplacementDefs = [
  [Math.max],
  [Math.min],
  [Math.hypot]
];
var _re = /x/;
var regexpReplacementDefs = [
  _re.exec,
  _re.test,
  _re[Symbol.match],
  _re[Symbol.matchAll],
  _re[Symbol.replace],
  _re[Symbol.search],
  _re[Symbol.split]
];
var promiseReplacementDefs = [
  Promise.all,
  Promise.allSettled,
  Promise.race,
  ...typeof Promise.any === "function" ? [Promise.any] : []
];
var objectReplacementDefs = [
  [Object.prototype.hasOwnProperty, "one"],
  [Object.prototype.propertyIsEnumerable, "one"],
  [Object.prototype.isPrototypeOf, "one"],
  [Object.create, "one"],
  [Object.getPrototypeOf, "one"],
  [Object.setPrototypeOf, "one"],
  [Object.is, "one"],
  [Object.defineProperty, "one"],
  [Object.getOwnPropertyDescriptor, "one"],
  [Object.isExtensible, "one"],
  [Object.preventExtensions, "one"],
  [Object.keys, "n"],
  [Object.values, "n"],
  [Object.entries, "n"],
  [Object.assign, "n"],
  [Object.fromEntries, "n"],
  [Object.getOwnPropertyNames, "n"],
  [Object.getOwnPropertySymbols, "n"],
  [Object.getOwnPropertyDescriptors, "n"],
  [Object.freeze, "n"],
  [Object.seal, "n"],
  [Object.isFrozen, "n"],
  [Object.isSealed, "n"]
];
function objectTicks(complexity, isStatic) {
  return (thisArg, args) => {
    if (complexity === "one") return 1n;
    const target = isStatic ? args[0] : thisArg;
    if (target !== null && typeof target === "object") return BigInt(Object.keys(target).length);
    return 1n;
  };
}
var staticObjectMethods = /* @__PURE__ */ new Set([
  Object.keys,
  Object.values,
  Object.entries,
  Object.assign,
  Object.fromEntries,
  Object.getOwnPropertyNames,
  Object.getOwnPropertySymbols,
  Object.getOwnPropertyDescriptors,
  Object.freeze,
  Object.seal,
  Object.isFrozen,
  Object.isSealed
]);
var DEFAULT_FUNCTION_REPLACEMENTS = /* @__PURE__ */ new Map();
var THIS_DEPENDENT_FUNCTION_REPLACEMENTS = /* @__PURE__ */ new Set();
for (const [original, complexity] of arrayReplacementDefs) {
  if (!original) continue;
  DEFAULT_FUNCTION_REPLACEMENTS.set(original, makeReplacement(original, arrayTicks(complexity, original)));
  THIS_DEPENDENT_FUNCTION_REPLACEMENTS.add(original);
}
for (const [original, complexity] of stringReplacementDefs) {
  if (!original) continue;
  DEFAULT_FUNCTION_REPLACEMENTS.set(original, makeReplacement(original, stringTicks(complexity)));
  THIS_DEPENDENT_FUNCTION_REPLACEMENTS.add(original);
}
for (const [original, complexity] of mapReplacementDefs) {
  if (!original) continue;
  DEFAULT_FUNCTION_REPLACEMENTS.set(original, makeReplacement(original, mapTicks(complexity)));
  THIS_DEPENDENT_FUNCTION_REPLACEMENTS.add(original);
}
for (const [original, complexity] of setReplacementDefs) {
  if (!original) continue;
  DEFAULT_FUNCTION_REPLACEMENTS.set(original, makeReplacement(original, setTicks(complexity)));
  THIS_DEPENDENT_FUNCTION_REPLACEMENTS.add(original);
}
for (const [original, complexity] of typedArrayReplacementDefs) {
  if (!original) continue;
  DEFAULT_FUNCTION_REPLACEMENTS.set(original, makeReplacement(original, typedArrayTicks(complexity)));
  THIS_DEPENDENT_FUNCTION_REPLACEMENTS.add(original);
}
for (const [original] of mathReplacementDefs) {
  if (!original) continue;
  DEFAULT_FUNCTION_REPLACEMENTS.set(original, makeReplacement(original, (_thisArg, args) => BigInt(args.length)));
}
for (const original of [JSON.parse, JSON.stringify]) DEFAULT_FUNCTION_REPLACEMENTS.set(original, makeReplacement(original, (_thisArg, args) => {
  const target = args[0];
  if (typeof target === "string") return BigInt(target.length);
  if (target !== null && typeof target === "object") return BigInt(Object.keys(target).length);
  return 1n;
}));
for (const original of regexpReplacementDefs) {
  if (!original) continue;
  DEFAULT_FUNCTION_REPLACEMENTS.set(original, makeReplacement(original, (_thisArg, args) => {
    const input = args[0];
    return typeof input === "string" ? BigInt(input.length) : 1n;
  }));
  THIS_DEPENDENT_FUNCTION_REPLACEMENTS.add(original);
}
for (const original of promiseReplacementDefs) {
  if (!original) continue;
  DEFAULT_FUNCTION_REPLACEMENTS.set(original, makeReplacement(original, (_thisArg, args) => {
    const iterable = args[0];
    return Array.isArray(iterable) ? BigInt(iterable.length) : 0n;
  }));
}
for (const [original, complexity] of objectReplacementDefs) {
  if (!original) continue;
  const isStatic = staticObjectMethods.has(original);
  DEFAULT_FUNCTION_REPLACEMENTS.set(original, makeReplacement(original, objectTicks(complexity, isStatic)));
  if (!isStatic) THIS_DEPENDENT_FUNCTION_REPLACEMENTS.add(original);
}
if (Array.from) DEFAULT_FUNCTION_REPLACEMENTS.set(Array.from, makeReplacement(Array.from, (_thisArg, args) => {
  const source = args[0];
  if (source != null && typeof source.length === "number") return BigInt(source.length);
  return 0n;
}));
if (typeof Array.fromAsync === "function") {
  const fromAsync = Array.fromAsync;
  DEFAULT_FUNCTION_REPLACEMENTS.set(fromAsync, makeReplacement(fromAsync, (_thisArg, args) => {
    const source = args[0];
    if (source != null && typeof source.length === "number") return BigInt(source.length);
    return 0n;
  }));
}

// node_modules/@nyariv/sandboxjs/dist/esm/utils/Prop.js
var boundFunctionCache = /* @__PURE__ */ new WeakMap();
var replacementReceiver = /* @__PURE__ */ new WeakMap();
var Prop = class {
  constructor(context, prop, isConst = false, isGlobal = false, isVariable = false, isInternal = false) {
    this.context = context;
    this.prop = prop;
    this.isConst = isConst;
    this.isGlobal = isGlobal;
    this.isVariable = isVariable;
    this.isInternal = isInternal;
  }
  get(context) {
    const ctx = this.context;
    if (ctx === void 0) throw new ReferenceError(`${this.prop.toString()} is not defined`);
    if (ctx === null) throw new TypeError(`Cannot read properties of null, (reading '${this.prop.toString()}')`);
    context.getSubscriptions.forEach((cb) => cb(ctx, this.prop.toString()));
    const val = ctx[this.prop];
    return getReplacementValue(val, context, ctx);
  }
};
function hasOwnProperty(obj, prop) {
  return Object.prototype.hasOwnProperty.call(obj, prop);
}
function getReplacementReceiver(fn) {
  return replacementReceiver.get(fn);
}
function resolveSandboxProp(val, context, prop) {
  if (!val) return;
  if (val instanceof Prop) {
    if (!prop) prop = val;
    val = val.get(context);
  }
  const p = prop?.prop || "prop";
  if (val === globalThis) return new Prop({ [p]: context.ctx.sandboxGlobal }, p, prop?.isConst || false, false, prop?.isVariable || false);
  if (prop && !prop.isVariable) return;
  const replacement = getReplacementValue(val, context, prop?.context);
  if (replacement !== val) return new Prop({ [p]: replacement }, p, prop?.isConst || false, prop?.isGlobal || false, prop?.isVariable || false);
}
function getReplacementValue(val, context, bindContext) {
  if (typeof val !== "function") return val;
  const replacement = context.evals.get(val);
  if (replacement === void 0) return val;
  if (!shouldBindReplacement(val, bindContext, context)) return replacement;
  return bindReplacement(replacement, bindContext, val, context);
}
function shouldBindReplacement(original, bindContext, context) {
  return THIS_DEPENDENT_FUNCTION_REPLACEMENTS.has(original) && bindContext !== null && (typeof bindContext === "object" || typeof bindContext === "function") && bindContext !== context.ctx.sandboxGlobal && !context.ctx.globalsWhitelist.has(bindContext);
}
function bindReplacement(replacement, bindContext, original, context) {
  let cache = boundFunctionCache.get(replacement);
  if (!cache) {
    cache = /* @__PURE__ */ new WeakMap();
    boundFunctionCache.set(replacement, cache);
  }
  let bound = cache.get(bindContext);
  if (bound) return bound;
  bound = function(...args) {
    return replacement.apply(this, args);
  };
  redefineFunctionMetadata(bound, original);
  context.ctx.sandboxedFunctions.add(bound);
  replacementReceiver.set(bound, bindContext);
  cache.set(bindContext, bound);
  return bound;
}
function redefineFunctionMetadata(target, source, overrides = {}) {
  for (const key of ["name", "length"]) {
    const descriptor = Object.getOwnPropertyDescriptor(source, key);
    if (descriptor?.configurable) Object.defineProperty(target, key, {
      ...descriptor,
      value: overrides[key] ?? source[key]
    });
  }
}

// node_modules/@nyariv/sandboxjs/dist/esm/utils/Scope.js
function keysOnly(obj) {
  const ret = Object.assign({}, obj);
  for (const key in ret) ret[key] = true;
  return ret;
}
var Scope = class {
  constructor(parent, vars = {}, functionThis) {
    this.const = {};
    this.let = {};
    this.var = {};
    this.internal = {};
    this.internalVars = {};
    const isFuncScope = functionThis !== void 0 || parent === null;
    this.parent = parent;
    this.allVars = vars;
    this.let = isFuncScope ? this.let : keysOnly(vars);
    this.var = isFuncScope ? keysOnly(vars) : this.var;
    this.globals = parent === null ? keysOnly(vars) : {};
    this.functionThis = functionThis;
  }
  get(key, internal) {
    const isThis = key === "this";
    const scope = this.getWhereValScope(key, isThis, internal);
    if (scope && isThis) return new Prop({ this: scope.functionThis }, key, false, false, true);
    if (!scope) return new Prop(void 0, key);
    if (internal && scope.internalVars[key]) return new Prop(scope.internalVars, key, false, false, true, true);
    return new Prop(scope.allVars, key, hasOwnProperty(scope.const, key), hasOwnProperty(scope.globals, key), true);
  }
  set(key, val, internal) {
    if (key === "this") throw new SyntaxError('"this" cannot be assigned');
    if (reservedWords.has(key)) throw new SyntaxError("Unexepected token '" + key + "'");
    const prop = this.get(key, internal);
    if (prop.context === void 0) throw new ReferenceError(`Variable '${key}' was not declared.`);
    if (prop.context === null) throw new TypeError(`Cannot set properties of null, (setting '${key}')`);
    if (prop.isConst) throw new TypeError(`Assignment to constant variable`);
    if (prop.isGlobal) throw new SandboxError(`Cannot override global variable '${key}'`);
    prop.context[prop.prop] = val;
    return prop;
  }
  getWhereValScope(key, isThis, internal) {
    let scope = this;
    if (isThis) {
      do {
        if (scope.functionThis !== void 0) return scope;
        scope = scope.parent;
      } while (scope !== null);
      return null;
    }
    do {
      if (internal && key in scope.internalVars && !(key in {} && !hasOwnProperty(scope.internalVars, key))) return scope;
      if (key in scope.allVars && !(key in {} && !hasOwnProperty(scope.allVars, key))) return scope;
      scope = scope.parent;
    } while (scope !== null);
    return null;
  }
  getWhereVarScope(key, localScope, internal) {
    let scope = this;
    do {
      if (internal && key in scope.internalVars && !(key in {} && !hasOwnProperty(scope.internalVars, key))) return scope;
      if (key in scope.allVars && !(key in {} && !hasOwnProperty(scope.allVars, key))) return scope;
      if (scope.parent === null || localScope || scope.functionThis !== void 0) return scope;
      scope = scope.parent;
    } while (scope !== null);
    return scope;
  }
  declare(key, type, value, isGlobal, internal) {
    if (key === "this") throw new SyntaxError('"this" cannot be declared');
    if (reservedWords.has(key)) throw new SyntaxError("Unexepected token '" + key + "'");
    const existingScope = this.getWhereVarScope(key, type !== VarType.var, internal);
    if (type === VarType.var) {
      if (existingScope.var[key]) {
        existingScope.allVars[key] = value;
        if (!isGlobal) delete existingScope.globals[key];
        else existingScope.globals[key] = true;
        return new Prop(existingScope.allVars, key, false, existingScope.globals[key], true);
      } else if (key in existingScope.allVars) throw new SyntaxError(`Identifier '${key}' has already been declared`);
    }
    if (key in existingScope.allVars || key in existingScope.internalVars) throw new SyntaxError(`Identifier '${key}' has already been declared`);
    if (isGlobal) existingScope.globals[key] = true;
    existingScope[type][key] = true;
    if (type === VarType.internal) existingScope.internalVars[key] = value;
    else existingScope.allVars[key] = value;
    return new Prop(type === VarType.internal ? this.internalVars : this.allVars, key, type === VarType.const, isGlobal, true, type === VarType.internal);
  }
};
var LocalScope = class {
};
var optional = {};
var DelayedSynchronousResult = class {
  constructor(cb) {
    this.result = cb();
  }
};
function sanitizeProp(value, context, cache = /* @__PURE__ */ new WeakSet()) {
  if (value === null || typeof value !== "object" && typeof value !== "function") return value;
  value = resolveSandboxProp(value, context) || value;
  if (value instanceof Prop) value = value.get(context);
  if (value === optional) return;
  return value;
}
function sanitizeScope(scope, context, cache = /* @__PURE__ */ new WeakSet()) {
  if (cache.has(scope)) return;
  cache.add(scope);
  for (const key in scope) {
    const val = scope[key];
    if (val !== null && typeof val === "object") sanitizeScope(val, context, cache);
    scope[key] = sanitizeProp(val, context);
  }
}
function sanitizeScopes(scopes, context, cache = /* @__PURE__ */ new WeakSet()) {
  for (const scope of scopes) sanitizeScope(scope, context, cache);
}

// node_modules/@nyariv/sandboxjs/dist/esm/utils/ExecContext.js
var ExecContext = class {
  constructor(ctx, constants, tree, getSubscriptions, setSubscriptions, changeSubscriptions, setSubscriptionsGlobal, changeSubscriptionsGlobal, evals, registerSandboxFunction, allowJit, evalContext) {
    this.ctx = ctx;
    this.constants = constants;
    this.tree = tree;
    this.getSubscriptions = getSubscriptions;
    this.setSubscriptions = setSubscriptions;
    this.changeSubscriptions = changeSubscriptions;
    this.setSubscriptionsGlobal = setSubscriptionsGlobal;
    this.changeSubscriptionsGlobal = changeSubscriptionsGlobal;
    this.evals = evals;
    this.registerSandboxFunction = registerSandboxFunction;
    this.allowJit = allowJit;
    this.evalContext = evalContext;
  }
};
function createSandboxSymbolContext(symbolWhitelist) {
  return {
    registry: /* @__PURE__ */ new Map(),
    reverseRegistry: /* @__PURE__ */ new Map(),
    whitelist: { ...symbolWhitelist }
  };
}
var RESERVED_SYMBOL_PROPERTIES = /* @__PURE__ */ new Set([
  "length",
  "name",
  "prototype",
  "for",
  "keyFor"
]);
function copyWhitelistedSymbols(target, symbolWhitelist) {
  for (const [key, value] of Object.entries(symbolWhitelist)) {
    if (RESERVED_SYMBOL_PROPERTIES.has(key)) continue;
    const descriptor = Object.getOwnPropertyDescriptor(Symbol, key);
    if (descriptor) Object.defineProperty(target, key, descriptor);
  }
}
function getSandboxSymbolCtor(symbols) {
  if (symbols.ctor) return symbols.ctor;
  function SandboxSymbol(description) {
    if (new.target) throw new TypeError("Symbol is not a constructor");
    return Symbol(description === void 0 ? void 0 : String(description));
  }
  copyWhitelistedSymbols(SandboxSymbol, symbols.whitelist);
  Object.defineProperties(SandboxSymbol, {
    prototype: {
      value: Symbol.prototype,
      enumerable: false,
      configurable: false,
      writable: false
    },
    for: {
      value(key) {
        const stringKey = String(key);
        let symbol = symbols.registry.get(stringKey);
        if (!symbol) {
          symbol = Symbol(stringKey);
          symbols.registry.set(stringKey, symbol);
          symbols.reverseRegistry.set(symbol, stringKey);
        }
        return symbol;
      },
      enumerable: false,
      configurable: true,
      writable: true
    },
    keyFor: {
      value(symbol) {
        return typeof symbol === "symbol" ? symbols.reverseRegistry.get(symbol) : void 0;
      },
      enumerable: false,
      configurable: true,
      writable: true
    }
  });
  symbols.ctor = SandboxSymbol;
  return SandboxSymbol;
}
function SandboxGlobal() {
}
function sandboxedGlobal(globals) {
  SG.prototype = SandboxGlobal.prototype;
  return SG;
  function SG() {
    for (const i in globals) this[i] = globals[i];
  }
}
function createContext(sandbox, options) {
  const sandboxSymbols = createSandboxSymbolContext(options.symbolWhitelist);
  const sandboxGlobal = new (sandboxedGlobal(options.globals))();
  const context = {
    sandbox,
    globalsWhitelist: new Set(Object.values(options.globals)),
    prototypeWhitelist: new Map([...options.prototypeWhitelist].map((a) => [a[0].prototype, a[1]])),
    sandboxSymbols,
    options,
    globalScope: new Scope(null, sandboxGlobal, sandboxGlobal),
    sandboxGlobal,
    ticks: {
      ticks: 0n,
      tickLimit: options.executionQuota,
      nextYield: options.nonBlocking ? NON_BLOCKING_THRESHOLD : void 0
    },
    sandboxedFunctions: /* @__PURE__ */ new WeakSet()
  };
  context.prototypeWhitelist.set(Object.getPrototypeOf(sandboxGlobal), /* @__PURE__ */ new Set());
  context.prototypeWhitelist.set(Object.getPrototypeOf([][Symbol.iterator]()), /* @__PURE__ */ new Set());
  const genProto = Object.getPrototypeOf(function* () {
  }());
  context.prototypeWhitelist.set(Object.getPrototypeOf(genProto), /* @__PURE__ */ new Set());
  const asyncGenProto = Object.getPrototypeOf(async function* () {
  }());
  context.prototypeWhitelist.set(Object.getPrototypeOf(asyncGenProto), /* @__PURE__ */ new Set());
  return context;
}
function createExecContext(sandbox, executionTree, evalContext) {
  const evals = /* @__PURE__ */ new Map();
  const execContext = new ExecContext(sandbox.context, executionTree.constants, executionTree.tree, /* @__PURE__ */ new Set(), /* @__PURE__ */ new WeakMap(), /* @__PURE__ */ new WeakMap(), sandbox.setSubscriptions, sandbox.changeSubscriptions, evals, (fn) => sandbox.sandboxFunctions.set(fn, execContext), !!evalContext, evalContext);
  if (evalContext) {
    const func = evalContext.sandboxFunction(execContext);
    const asyncFunc = evalContext.sandboxAsyncFunction(execContext);
    const genFunc = evalContext.sandboxGeneratorFunction(execContext);
    const asyncGenFunc = evalContext.sandboxAsyncGeneratorFunction(execContext);
    const sandboxSymbol = evalContext.sandboxedSymbol(execContext);
    evals.set(Function, func);
    evals.set(AsyncFunction, asyncFunc);
    evals.set(GeneratorFunction, genFunc);
    evals.set(AsyncGeneratorFunction, asyncGenFunc);
    evals.set(Symbol, sandboxSymbol);
    evals.set(eval, evalContext.sandboxedEval(func, execContext));
    evals.set(setTimeout, evalContext.sandboxedSetTimeout(func, execContext));
    evals.set(setInterval, evalContext.sandboxedSetInterval(func, execContext));
    evals.set(clearTimeout, evalContext.sandboxedClearTimeout(execContext));
    evals.set(clearInterval, evalContext.sandboxedClearInterval(execContext));
    for (const [original, factory] of DEFAULT_FUNCTION_REPLACEMENTS) evals.set(original, factory(execContext));
    for (const [original, factory] of sandbox.context.options.functionReplacements) evals.set(original, factory(execContext, evals.get(original)));
    const ptwl = sandbox.context.prototypeWhitelist;
    for (const [key, value] of evals) {
      if (!ptwl.has(key.prototype)) ptwl.set(key.prototype, /* @__PURE__ */ new Set());
      if (!ptwl.has(value.prototype)) ptwl.set(value.prototype, ptwl.get(key.prototype) || /* @__PURE__ */ new Set());
      if (sandbox.context.globalsWhitelist.has(key)) sandbox.context.globalsWhitelist.add(value);
      if (hasOwnProperty(sandbox.context.sandboxGlobal, key.name)) sandbox.context.sandboxGlobal[key.name] = value;
    }
    if (sandbox.context.sandboxGlobal.globalThis) sandbox.context.sandboxGlobal.globalThis = sandbox.context.sandboxGlobal;
  }
  return execContext;
}
function isLisp(item) {
  return Array.isArray(item) && typeof item[0] === "number" && item[0] !== LispType.None && item[0] !== LispType.True;
}

// node_modules/@nyariv/sandboxjs/dist/esm/utils/unraw.js
function parseHexToInt(hex) {
  return !hex.match(/[^a-f0-9]/i) ? parseInt(hex, 16) : NaN;
}
function validateAndParseHex(hex, errorName, enforcedLength) {
  const parsedHex = parseHexToInt(hex);
  if (Number.isNaN(parsedHex) || enforcedLength !== void 0 && enforcedLength !== hex.length) throw new SyntaxError(errorName + ": " + hex);
  return parsedHex;
}
function parseHexadecimalCode(code) {
  const parsedCode = validateAndParseHex(code, "Malformed Hexadecimal", 2);
  return String.fromCharCode(parsedCode);
}
function parseUnicodeCode(code, surrogateCode) {
  const parsedCode = validateAndParseHex(code, "Malformed Unicode", 4);
  if (surrogateCode !== void 0) {
    const parsedSurrogateCode = validateAndParseHex(surrogateCode, "Malformed Unicode", 4);
    return String.fromCharCode(parsedCode, parsedSurrogateCode);
  }
  return String.fromCharCode(parsedCode);
}
function isCurlyBraced(text) {
  return text.charAt(0) === "{" && text.charAt(text.length - 1) === "}";
}
function parseUnicodeCodePointCode(codePoint) {
  if (!isCurlyBraced(codePoint)) throw new SyntaxError("Malformed Unicode: +" + codePoint);
  const parsedCode = validateAndParseHex(codePoint.slice(1, -1), "Malformed Unicode");
  try {
    return String.fromCodePoint(parsedCode);
  } catch (err) {
    throw err instanceof RangeError ? /* @__PURE__ */ new SyntaxError("Code Point Limit:" + parsedCode) : err;
  }
}
var singleCharacterEscapes = /* @__PURE__ */ new Map([
  ["b", "\b"],
  ["f", "\f"],
  ["n", "\n"],
  ["r", "\r"],
  ["t", "	"],
  ["v", "\v"],
  ["0", "\0"]
]);
function parseSingleCharacterCode(code) {
  return singleCharacterEscapes.get(code) || code;
}
var escapeMatch = /\\(?:(\\)|x([\s\S]{0,2})|u(\{[^}]*\}?)|u([\s\S]{4})\\u([^{][\s\S]{0,3})|u([\s\S]{0,4})|([0-3]?[0-7]{1,2})|([\s\S])|$)/g;
function unraw(raw) {
  return raw.replace(escapeMatch, function(_, backslash, hex, codePoint, unicodeWithSurrogate, surrogate, unicode, octal, singleCharacter) {
    if (backslash !== void 0) return "\\";
    if (hex !== void 0) return parseHexadecimalCode(hex);
    if (codePoint !== void 0) return parseUnicodeCodePointCode(codePoint);
    if (unicodeWithSurrogate !== void 0) return parseUnicodeCode(unicodeWithSurrogate, surrogate);
    if (unicode !== void 0) return parseUnicodeCode(unicode);
    if (octal === "0") return "\0";
    if (octal !== void 0) throw new SyntaxError("Octal Deprecation: " + octal);
    if (singleCharacter !== void 0) return parseSingleCharacterCode(singleCharacter);
    throw new SyntaxError("End of string");
  });
}

// node_modules/@nyariv/sandboxjs/dist/esm/utils/CodeString.js
var CodeString = class CodeString2 {
  constructor(str2) {
    this.ref = { str: "" };
    if (str2 instanceof CodeString2) {
      this.ref = str2.ref;
      this.start = str2.start;
      this.end = str2.end;
    } else {
      this.ref.str = str2;
      this.start = 0;
      this.end = str2.length;
    }
  }
  substring(start, end) {
    if (!this.length) return this;
    start = this.start + start;
    if (start < 0) start = 0;
    if (start > this.end) start = this.end;
    end = end === void 0 ? this.end : this.start + end;
    if (end < 0) end = 0;
    if (end > this.end) end = this.end;
    const code = new CodeString2(this);
    code.start = start;
    code.end = end;
    return code;
  }
  get length() {
    const len = this.end - this.start;
    return len < 0 ? 0 : len;
  }
  char(i) {
    if (this.start === this.end) return void 0;
    return this.ref.str[this.start + i];
  }
  toString() {
    return this.ref.str.substring(this.start, this.end);
  }
  trimStart() {
    const found = /^\s+/.exec(this.toString());
    const code = new CodeString2(this);
    if (found) code.start += found[0].length;
    return code;
  }
  slice(start, end) {
    if (start < 0) start = this.end - this.start + start;
    if (start < 0) start = 0;
    if (end === void 0) end = this.end - this.start;
    if (end < 0) end = this.end - this.start + end;
    if (end < 0) end = 0;
    return this.substring(start, end);
  }
  trim() {
    const code = this.trimStart();
    const found = /\s+$/.exec(code.toString());
    if (found) code.end -= found[0].length;
    return code;
  }
  valueOf() {
    return this.toString();
  }
};

// node_modules/@nyariv/sandboxjs/dist/esm/parser/lispTypes/conditionals.js
function registerConditionalLispTypes({ createLisp: createLisp2, emptyString: emptyString2, extractStatementLabels: extractStatementLabels2, insertSemicolons: insertSemicolons2, lispifyBlock: lispifyBlock2, lispifyExpr: lispifyExpr2, restOfExp: restOfExp2, semiColon: semiColon2, setLispType: setLispType2, wrapLabeledStatement: wrapLabeledStatement2 }) {
  const elseIf = /^else(?![\w$])/;
  const ifElse = /^if(?![\w$])/;
  function extractIfElse(constants, part, depth = 0) {
    if (depth > constants.maxDepth) throw new SandboxCapabilityError("Maximum expression depth exceeded");
    let count = 0;
    let found = part.substring(0, 0);
    let foundElse = emptyString2;
    let foundTrue;
    let first = true;
    let elseReg;
    let details = {};
    while ((found = restOfExp2(constants, part.substring(found.end - part.start), [
      elseIf,
      ifElse,
      semiColon2
    ], void 0, void 0, void 0, details)).length || first) {
      first = false;
      const f = part.substring(found.end - part.start).toString();
      if (f.startsWith("if")) {
        found.end++;
        count++;
      } else if (f.startsWith("else")) {
        foundTrue = part.substring(0, found.end - part.start);
        found.end++;
        count--;
        if (!count) found.end--;
      } else if (elseReg = /^;?\s*else(?![\w$])/.exec(f)) {
        foundTrue = part.substring(0, found.end - part.start);
        found.end += elseReg[0].length - 1;
        count--;
        if (!count) found.end -= elseReg[0].length - 1;
      } else {
        foundTrue = foundElse.length ? foundTrue : part.substring(0, found.end - part.start);
        break;
      }
      if (!count) {
        foundElse = extractIfElse(constants, part.substring(found.end - part.start + (/^;?\s*else(?![\w$])/.exec(f)?.[0].length || 0)), depth + 1).all;
        break;
      }
      details = {};
    }
    foundTrue = foundTrue || part.substring(0, found.end - part.start);
    return {
      all: part.substring(0, Math.max(foundTrue.end, foundElse.end) - part.start),
      true: foundTrue,
      false: foundElse
    };
  }
  setLispType2(["if"], (ctx) => {
    const { constants, part, res } = ctx;
    const labels = extractStatementLabels2(res[1]);
    let condition = restOfExp2(constants, part.substring(res[0].length), [], "(");
    const ie = extractIfElse(constants, part.substring(res[1].length));
    const startTrue = res[0].length - res[1].length + condition.length + 1;
    let trueBlock = ie.true.substring(startTrue);
    let elseBlock = ie.false;
    condition = condition.trim();
    trueBlock = trueBlock.trim();
    elseBlock = elseBlock.trim();
    if (!trueBlock.length || /^else(?![\w$])/.test(trueBlock.toString())) throw new SyntaxError("Unexpected token");
    if (trueBlock.char(0) === "{") trueBlock = trueBlock.slice(1, -1);
    if (elseBlock.char(0) === "{") elseBlock = elseBlock.slice(1, -1);
    ctx.lispTree = wrapLabeledStatement2(labels, createLisp2({
      op: LispType.If,
      a: lispifyExpr2(constants, condition, void 0, ctx),
      b: createLisp2({
        op: LispType.IfCase,
        a: lispifyBlock2(trueBlock, constants, false, ctx),
        b: lispifyBlock2(elseBlock, constants, false, ctx)
      })
    }));
  });
  setLispType2(["switch"], (ctx) => {
    const { constants, part, res } = ctx;
    const labels = extractStatementLabels2(res[1]);
    const test = restOfExp2(constants, part.substring(res[0].length), [], "(");
    let start = part.toString().indexOf("{", res[0].length + test.length + 1);
    if (start === -1) throw new SyntaxError("Invalid switch");
    let statement = insertSemicolons2(constants, restOfExp2(constants, part.substring(start + 1), [], "{"));
    let caseFound;
    const caseTest = /^\s*(case\s|default)\s*/;
    const caseNoTestReg = /^\s*case\s*:/;
    const cases = [];
    let defaultFound = false;
    while ((caseFound = caseTest.exec(statement.toString())) || caseNoTestReg.test(statement.toString())) {
      if (!caseFound) throw new SyntaxError("Unexpected end of expression");
      if (caseFound[1] === "default") {
        if (defaultFound) throw new SyntaxError("Only one default switch case allowed");
        defaultFound = true;
      }
      const cond = restOfExp2(constants, statement.substring(caseFound[0].length), [/^:/]);
      if (caseFound[1] !== "default" && !cond.trimStart().length) throw new SyntaxError("Unexpected end of expression");
      let found = emptyString2;
      let i = start = caseFound[0].length + cond.length + 1;
      const bracketFound = /^\s*\{/.exec(statement.substring(i).toString());
      let exprs = [];
      if (bracketFound) {
        i += bracketFound[0].length;
        found = restOfExp2(constants, statement.substring(i), [], "{");
        i += found.length + 1;
        exprs = lispifyBlock2(found, constants, false, ctx);
      } else {
        const notEmpty = restOfExp2(constants, statement.substring(i), [caseTest]);
        if (!notEmpty.trim().length) {
          exprs = [];
          i += notEmpty.length;
        } else {
          while ((found = restOfExp2(constants, statement.substring(i), [semiColon2])).length) {
            i += found.length + (statement.char(i + found.length) === ";" ? 1 : 0);
            if (caseTest.test(statement.substring(i).toString())) break;
          }
          exprs = lispifyBlock2(statement.substring(start, found.end - statement.start), constants, false, ctx);
        }
      }
      statement = statement.substring(i);
      cases.push(createLisp2({
        op: LispType.SwitchCase,
        a: caseFound[1] === "default" ? LispType.None : lispifyExpr2(constants, cond, void 0, ctx),
        b: exprs
      }));
    }
    ctx.lispTree = wrapLabeledStatement2(labels, createLisp2({
      op: LispType.Switch,
      a: lispifyExpr2(constants, test, void 0, ctx),
      b: cases
    }));
  });
}

// node_modules/@nyariv/sandboxjs/dist/esm/parser/lispTypes/control.js
function registerControlLispTypes({ ParseError: ParseError2, createLisp: createLisp2, emptyString: emptyString2, expandDestructure: expandDestructure2, extractStatementLabels: extractStatementLabels2, insertSemicolons: insertSemicolons2, lispify: lispify2, lispifyBlock: lispifyBlock2, lispifyExpr: lispifyExpr2, lispifyReturnExpr: lispifyReturnExpr2, restOfExp: restOfExp2, semiColon: semiColon2, setLispType: setLispType2, startingExecpted: startingExecpted2, wrapLabeledStatement: wrapLabeledStatement2 }) {
  const iteratorRegex = /^((let|var|const|internal)\s+)?\s*([a-zA-Z$_][a-zA-Z\d$_]*)\s+(in|of)(?![\w$])/;
  const iteratorDestructureRegex = /^((let|var|const|internal)\s+)\s*([[{])/;
  const ofStart2 = lispify2({ maxDepth: 10 }, new CodeString("let $$iterator = $$obj[Symbol.iterator]()"), ["initialize"]);
  const ofStart3 = lispify2({ maxDepth: 10 }, new CodeString("let $$next = $$iterator.next()"), ["initialize"]);
  const ofCondition = lispify2({ maxDepth: 10 }, new CodeString("return !$$next.done"), ["initialize"]);
  const ofStep = lispify2({ maxDepth: 10 }, new CodeString("$$next = $$iterator.next()"));
  const asyncOfStart2 = lispify2({ maxDepth: 10 }, new CodeString("let $$iterator = $$obj"), ["initialize"]);
  const asyncOfStart3 = lispify2({ maxDepth: 10 }, new CodeString("let $$next = $$iterator.next()"), ["initialize"]);
  const asyncOfStep = lispify2({ maxDepth: 10 }, new CodeString("$$next = $$iterator.next()"));
  const inStart2 = lispify2({ maxDepth: 10 }, new CodeString("let $$keys = Object.keys($$obj)"), ["initialize"]);
  const inStart3 = lispify2({ maxDepth: 10 }, new CodeString("let $$keyIndex = 0"), ["initialize"]);
  const inStep = lispify2({ maxDepth: 10 }, new CodeString("$$keyIndex++"));
  const inCondition = lispify2({ maxDepth: 10 }, new CodeString("return $$keyIndex < $$keys.length"), ["initialize"]);
  setLispType2([
    "for",
    "do",
    "while"
  ], (ctx) => {
    const { constants, type, part, res } = ctx;
    const labels = extractStatementLabels2(res[1]);
    let i = 0;
    let startStep = LispType.True;
    let startInternal = [];
    let getIterator = LispType.None;
    let beforeStep = LispType.None;
    let checkFirst = LispType.True;
    let condition;
    let step = LispType.True;
    let body;
    const isForAwait = type === "for" && res[2] ? LispType.True : LispType.None;
    switch (type) {
      case "while": {
        i = part.toString().indexOf("(") + 1;
        const extract = restOfExp2(constants, part.substring(i), [], "(");
        if (!extract.trimStart().length) throw new SyntaxError("Unexpected end of expression");
        condition = lispifyReturnExpr2(constants, extract);
        body = restOfExp2(constants, part.substring(i + extract.length + 1)).trim();
        if (body.char(0) === "{") body = body.slice(1, -1);
        break;
      }
      case "for": {
        i = part.toString().indexOf("(") + 1;
        const args = [];
        let extract2 = emptyString2;
        for (let k = 0; k < 3; k++) {
          extract2 = restOfExp2(constants, part.substring(i), [/^[;)]/]);
          args.push(extract2.trim());
          i += extract2.length + 1;
          if (part.char(i - 1) === ")") break;
        }
        let iterator;
        let iteratorDestructure;
        if (args.length === 1 && (iterator = iteratorRegex.exec(args[0].toString()))) {
          const iterableExpr = args[0].substring(iterator[0].length);
          if (!iterableExpr.trimStart().length) throw new SyntaxError("Unexpected end of expression");
          if (iterator[4] === "of") {
            getIterator = lispifyReturnExpr2(constants, iterableExpr);
            startInternal = isForAwait ? [asyncOfStart2, asyncOfStart3] : [ofStart2, ofStart3];
            condition = ofCondition;
            step = isForAwait ? asyncOfStep : ofStep;
            beforeStep = lispify2(constants, new CodeString((iterator[1] || "let ") + iterator[3] + " = $$next.value"), ["initialize"]);
          } else {
            if (isForAwait) throw new SyntaxError("Unexpected token 'in'");
            getIterator = lispifyReturnExpr2(constants, iterableExpr);
            startInternal = [inStart2, inStart3];
            step = inStep;
            condition = inCondition;
            beforeStep = lispify2(constants, new CodeString((iterator[1] || "let ") + iterator[3] + " = $$keys[$$keyIndex]"), ["initialize"]);
          }
        } else if (args.length === 1 && (iteratorDestructure = iteratorDestructureRegex.exec(args[0].toString()))) {
          const keyword = iteratorDestructure[1].trim();
          const openBracket = iteratorDestructure[3];
          const closeBracket = openBracket === "[" ? "]" : "}";
          const keywordPrefixLen = iteratorDestructure[0].length - 1;
          const patternContent = restOfExp2(constants, args[0].substring(keywordPrefixLen + 1), [], openBracket);
          const patternStr = openBracket + patternContent.toString() + closeBracket;
          const afterClose = args[0].substring(keywordPrefixLen + 1 + patternContent.length + 1).trimStart();
          const inOfMatch = /^(in|of)(?![\w$])\s*/.exec(afterClose.toString());
          if (!inOfMatch) throw new SyntaxError("Invalid for loop definition");
          const inOf = inOfMatch[1];
          const iterExpr = afterClose.substring(inOfMatch[0].length);
          if (!iterExpr.trimStart().length) throw new SyntaxError("Unexpected end of expression");
          const tempVarName = "$$_fv";
          if (inOf === "of") {
            getIterator = lispifyReturnExpr2(constants, iterExpr);
            startInternal = isForAwait ? [asyncOfStart2, asyncOfStart3] : [ofStart2, ofStart3];
            condition = ofCondition;
            step = isForAwait ? asyncOfStep : ofStep;
            const stmts = lispifyBlock2(new CodeString(expandDestructure2(keyword, patternStr, tempVarName)), constants, false, ctx);
            beforeStep = createLisp2({
              op: LispType.InternalBlock,
              a: [lispify2(constants, new CodeString(`${keyword} ${tempVarName} = $$next.value`), ["initialize"]), ...stmts],
              b: LispType.None
            });
          } else {
            getIterator = lispifyReturnExpr2(constants, iterExpr);
            startInternal = [inStart2, inStart3];
            step = inStep;
            condition = inCondition;
            const stmts = lispifyBlock2(new CodeString(expandDestructure2(keyword, patternStr, tempVarName)), constants, false, ctx);
            beforeStep = createLisp2({
              op: LispType.InternalBlock,
              a: [lispify2(constants, new CodeString(`${keyword} ${tempVarName} = $$keys[$$keyIndex]`), ["initialize"]), ...stmts],
              b: LispType.None
            });
          }
        } else if (args.length === 3) {
          const [startArg, conditionArg, stepArg] = args;
          if (startArg.length) startStep = lispifyExpr2(constants, startArg, startingExecpted2, ctx);
          condition = conditionArg.length ? lispifyReturnExpr2(constants, conditionArg) : LispType.True;
          if (stepArg.length) step = lispifyExpr2(constants, stepArg, void 0, ctx);
        } else throw new SyntaxError("Invalid for loop definition");
        body = restOfExp2(constants, part.substring(i)).trim();
        if (body.char(0) === "{") body = body.slice(1, -1);
        break;
      }
      case "do": {
        checkFirst = LispType.None;
        const isBlock = !!res[2];
        body = restOfExp2(constants, part.substring(res[0].length), isBlock ? [/^\}/] : [semiColon2]);
        condition = lispifyReturnExpr2(constants, restOfExp2(constants, part.substring(part.toString().indexOf("(", res[0].length + body.length) + 1), [], "("));
        break;
      }
    }
    const a = [
      checkFirst,
      startInternal,
      getIterator,
      startStep,
      step,
      condition,
      beforeStep,
      isForAwait,
      labels
    ];
    ctx.lispTree = createLisp2({
      op: LispType.Loop,
      a,
      b: lispifyBlock2(body, constants, false, ctx)
    });
  });
  setLispType2(["block"], (ctx) => {
    const { constants, part, res } = ctx;
    ctx.lispTree = wrapLabeledStatement2(extractStatementLabels2(res[1]), createLisp2({
      op: LispType.Block,
      a: lispifyBlock2(restOfExp2(constants, part.substring(res[0].length), [], "{"), constants, false, ctx),
      b: LispType.None
    }));
  });
  setLispType2(["loopAction"], (ctx) => {
    const { part, res } = ctx;
    const remaining = part.substring(res[0].length).trimStart();
    if (remaining.length && !res[2]) throw new SyntaxError(`Unexpected token '${remaining.char(0)}'`);
    ctx.lispTree = createLisp2({
      op: LispType.LoopAction,
      a: res[1],
      b: res[2] || LispType.None
    });
  });
  const catchReg = /^\s*(catch\s*(\(\s*([a-zA-Z$_][a-zA-Z\d$_]*)\s*\))?|finally)\s*\{/;
  const catchEmptyBindingReg = /^\s*catch\s*\(\s*\)/;
  const catchReservedBindingReg = /^\s*catch\s*\(\s*(break|case|catch|continue|debugger|default|delete|do|else|finally|for|function|if|in|instanceof|new|return|switch|this|throw|try|typeof|var|void|while|with|class|const|enum|export|extends|implements|import|interface|let|package|private|protected|public|static|super|yield|await)\s*\)/;
  const finallyKeywordReg = /^\s*finally\b/;
  setLispType2(["try"], (ctx) => {
    const { constants, part, res } = ctx;
    const body = restOfExp2(constants, part.substring(res[0].length), [], "{");
    const afterBody = part.substring(res[0].length + body.length + 1).toString();
    if (catchEmptyBindingReg.test(afterBody)) throw new ParseError2("Unexpected token ')'", part.toString());
    const reservedMatch = catchReservedBindingReg.exec(afterBody);
    if (reservedMatch) throw new ParseError2(`Unexpected token '${reservedMatch[1]}'`, part.toString());
    const finallyMatch = finallyKeywordReg.exec(afterBody);
    if (finallyMatch && !/^\s*\{/.test(afterBody.substring(finallyMatch[0].length))) throw new ParseError2("Unexpected token", part.toString());
    let catchRes = catchReg.exec(afterBody);
    let finallyBody;
    let exception = "";
    let catchBody;
    let offset = 0;
    if (!catchRes) throw new ParseError2("Missing catch or finally after try", part.toString());
    if (catchRes[1].startsWith("catch")) {
      catchRes = catchReg.exec(part.substring(res[0].length + body.length + 1).toString());
      exception = catchRes[3] || "";
      catchBody = restOfExp2(constants, part.substring(res[0].length + body.length + 1 + catchRes[0].length), [], "{");
      offset = res[0].length + body.length + 1 + catchRes[0].length + catchBody.length + 1;
      if ((catchRes = catchReg.exec(part.substring(offset).toString())) && catchRes[1].startsWith("finally")) finallyBody = restOfExp2(constants, part.substring(offset + catchRes[0].length), [], "{");
    } else finallyBody = restOfExp2(constants, part.substring(res[0].length + body.length + 1 + catchRes[0].length), [], "{");
    const b = [
      exception,
      lispifyBlock2(insertSemicolons2(constants, catchBody || emptyString2), constants, false, ctx),
      lispifyBlock2(insertSemicolons2(constants, finallyBody || emptyString2), constants, false, ctx)
    ];
    ctx.lispTree = wrapLabeledStatement2(extractStatementLabels2(res[1]), createLisp2({
      op: LispType.Try,
      a: lispifyBlock2(insertSemicolons2(constants, body), constants, false, ctx),
      b
    }));
  });
}

// node_modules/@nyariv/sandboxjs/dist/esm/parser/lispTypes/declarations.js
function registerDeclarationLispTypes({ createLisp: createLisp2, expectTypes: expectTypes2, expandDestructure: expandDestructure2, expandFunctionParamDestructure: expandFunctionParamDestructure2, lispify: lispify2, lispifyBlock: lispifyBlock2, lispifyFunction: lispifyFunction2, restOfExp: restOfExp2, semiColon: semiColon2, setLispType: setLispType2, splitByCommasDestructure: splitByCommasDestructure2 }) {
  setLispType2(["initializeDestructure"], (ctx) => {
    const { constants, part, res } = ctx;
    const keyword = res[1];
    const openBracket = res[2];
    const closeBracket = openBracket === "[" ? "]" : "}";
    const patternContent = restOfExp2(constants, part.substring(res[0].length), [], openBracket);
    const patternStr = openBracket + patternContent.toString() + closeBracket;
    const afterClose = part.substring(res[0].length + patternContent.length + 1).trimStart();
    if (!afterClose.length || afterClose.char(0) !== "=") throw new SyntaxError("Destructuring declaration requires an initializer");
    const stmts = lispifyBlock2(new CodeString(expandDestructure2(keyword, patternStr, restOfExp2(constants, afterClose.substring(1).trimStart(), [semiColon2]).toString())), constants, false, ctx);
    ctx.lispTree = createLisp2({
      op: LispType.InternalBlock,
      a: stmts,
      b: LispType.None
    });
  });
  setLispType2(["initialize"], (ctx) => {
    const { constants, part, res, expect } = ctx;
    const lt = res[1] === "var" ? LispType.Var : res[1] === "let" ? LispType.Let : res[1] === "const" ? LispType.Const : LispType.Internal;
    if (!res[3]) ctx.lispTree = lispify2(constants, part.substring(res[0].length), expectTypes2[expect].next, createLisp2({
      op: lt,
      a: res[2],
      b: LispType.None
    }), false, ctx);
    else {
      const initExpr = part.substring(res[0].length);
      if (!initExpr.trimStart().length) throw new SyntaxError("Unexpected end of expression");
      ctx.lispTree = createLisp2({
        op: lt,
        a: res[2],
        b: lispify2(constants, initExpr, expectTypes2[expect].next, void 0, false, ctx)
      });
    }
  });
  setLispType2([
    "function",
    "inlineFunction",
    "arrowFunction",
    "arrowFunctionSingle"
  ], (ctx) => {
    const { constants, type, part, res, expect, generatorDepth, asyncDepth } = ctx;
    const isArrow = type !== "function" && type !== "inlineFunction";
    const isReturn = isArrow && !res[res.length - 1];
    const isGenerator = !isArrow && res[2] && res[2].trimStart().startsWith("*") ? LispType.True : LispType.None;
    const isAsync = res[1] ? LispType.True : LispType.None;
    let rawArgStr;
    let bodyOffset;
    if (type === "function" || type === "inlineFunction") {
      const argsCode = restOfExp2(constants, part.substring(res[0].length), [], "(");
      rawArgStr = argsCode.toString().trim();
      bodyOffset = res[0].length + argsCode.length + 1;
      const afterParen = part.substring(bodyOffset).trimStart();
      bodyOffset = part.length - afterParen.length + 1;
    } else {
      rawArgStr = res[2] ?? "";
      bodyOffset = 0;
    }
    const args = [...rawArgStr ? splitByCommasDestructure2(rawArgStr).map((a) => a.trim()).filter(Boolean) : []];
    if (!isArrow) args.unshift((res[3] || "").trimStart());
    let ended = false;
    args.forEach((arg) => {
      if (ended) throw new SyntaxError("Rest parameter must be last formal parameter");
      if (arg.startsWith("...")) ended = true;
    });
    const f = restOfExp2(constants, isArrow ? part.substring(res[0].length) : part.substring(bodyOffset), !isReturn ? [/^}/] : [/^[,)}\]]/, semiColon2]);
    let funcBody = isReturn ? "return " + f : f.toString();
    const expanded = expandFunctionParamDestructure2(isArrow ? args : args.slice(1), funcBody);
    const finalArgs = isArrow ? expanded.args : [args[0], ...expanded.args];
    funcBody = expanded.body;
    finalArgs.forEach((arg) => {
      if (reservedWords.has(arg.replace(/^\.\.\./, ""))) throw new SyntaxError(`Unexpected token '${arg}'`);
    });
    const afterFunc = isArrow ? res[0].length + f.length + 1 : bodyOffset + f.length + 1;
    ctx.lispTree = lispify2(constants, part.substring(afterFunc), expectTypes2[expect].next, createLisp2({
      op: isArrow ? LispType.ArrowFunction : type === "function" ? LispType.Function : LispType.InlineFunction,
      a: isArrow ? [isAsync, ...finalArgs] : [
        isAsync,
        isGenerator,
        ...finalArgs
      ],
      b: constants.eager ? lispifyFunction2(new CodeString(funcBody), constants, false, isArrow ? {
        generatorDepth: 0,
        asyncDepth: 0,
        lispDepth: 0
      } : {
        generatorDepth: isGenerator === LispType.True ? generatorDepth + 1 : 0,
        asyncDepth: isAsync === LispType.True ? asyncDepth + 1 : 0,
        lispDepth: 0
      }) : funcBody
    }), false, ctx);
  });
}

// node_modules/@nyariv/sandboxjs/dist/esm/parser/lispTypes/operators.js
function registerOperatorLispTypes({ createLisp: createLisp2, expandDestructure: expandDestructure2, expectTypes: expectTypes2, getDestructurePatternSource: getDestructurePatternSource2, lispifyBlock: lispifyBlock2, restOfExp: restOfExp2, lispify: lispify2, lispifyExpr: lispifyExpr2, setLispType: setLispType2 }) {
  const inlineIfElse = /^:/;
  const modifierTypes = {
    inverse: LispType.Inverse,
    not: LispType.Not,
    positive: LispType.Positive,
    negative: LispType.Negative,
    typeof: LispType.Typeof,
    delete: LispType.Delete
  };
  setLispType2([
    "inverse",
    "not",
    "negative",
    "positive",
    "typeof",
    "delete"
  ], (ctx) => {
    const { constants, type, part, res, expect } = ctx;
    const extract = restOfExp2(constants, part.substring(res[0].length), [/^([^\s.?\w$]|\?[^.])/]);
    if (part.substring(extract.length + res[0].length).trim().toString().startsWith("**")) throw new SyntaxError("Unary operator used immediately before exponentiation expression. Parenthesis must be used to disambiguate operator precedence");
    ctx.lispTree = lispify2(constants, part.substring(extract.length + res[0].length), restOfExp2.next, createLisp2({
      op: modifierTypes[type],
      a: ctx.lispTree,
      b: lispify2(constants, extract, expectTypes2[expect].next)
    }), false, ctx);
  });
  setLispType2(["taggedTemplate"], (ctx) => {
    const { constants, part, res, expect } = ctx;
    const literalIndex = res[1];
    const [, templateStr, jsExprs] = constants.literals[parseInt(literalIndex)];
    const stringParts = [];
    const expressions = [];
    let currentStr = "";
    let i = 0;
    while (i < templateStr.length) if (templateStr.substring(i, i + 2) === "${") {
      let j = i + 2;
      let exprIndex = "";
      let isValidPlaceholder = false;
      while (j < templateStr.length && templateStr[j] !== "}") {
        exprIndex += templateStr[j];
        j++;
      }
      if (j < templateStr.length && templateStr[j] === "}" && /^\d+$/.test(exprIndex)) isValidPlaceholder = true;
      if (isValidPlaceholder) {
        stringParts.push(currentStr);
        currentStr = "";
        expressions.push(jsExprs[parseInt(exprIndex)]);
        i = j + 1;
      } else {
        currentStr += templateStr[i];
        i++;
      }
    } else {
      currentStr += templateStr[i];
      i++;
    }
    stringParts.push(currentStr);
    const stringsArray = stringParts.map((str2) => createLisp2({
      op: LispType.StringIndex,
      a: LispType.None,
      b: String(constants.strings.push(str2) - 1)
    }));
    const stringsArrayLisp = createLisp2({
      op: LispType.CreateArray,
      a: createLisp2({
        op: LispType.None,
        a: LispType.None,
        b: LispType.None
      }),
      b: stringsArray
    });
    ctx.lispTree = lispify2(constants, part.substring(res[0].length), expectTypes2[expect].next, createLisp2({
      op: LispType.Call,
      a: ctx.lispTree,
      b: [stringsArrayLisp, ...expressions]
    }), false, ctx);
  });
  const incrementTypes = {
    "++$": LispType.IncrementBefore,
    "--$": LispType.DecrementBefore,
    "$++": LispType.IncrementAfter,
    "$--": LispType.DecrementAfter
  };
  setLispType2(["incrementerBefore"], (ctx) => {
    const { constants, part, res } = ctx;
    const extract = restOfExp2(constants, part.substring(2), [/^[^\s.\w$]/]);
    ctx.lispTree = lispify2(constants, part.substring(extract.length + 2), restOfExp2.next, createLisp2({
      op: incrementTypes[res[0] + "$"],
      a: lispify2(constants, extract, expectTypes2.incrementerBefore.next),
      b: LispType.None
    }), false, ctx);
  });
  setLispType2(["incrementerAfter"], (ctx) => {
    const { constants, part, res, expect } = ctx;
    if (ctx.lispTree[0] === LispType.Number || ctx.lispTree[0] === LispType.BigInt || ctx.lispTree[0] === LispType.GlobalSymbol || ctx.lispTree[0] === LispType.StringIndex || ctx.lispTree[0] === LispType.LiteralIndex || ctx.lispTree[0] === LispType.RegexIndex) throw new SyntaxError("Invalid left-hand side expression in postfix operation");
    ctx.lispTree = lispify2(constants, part.substring(res[0].length), expectTypes2[expect].next, createLisp2({
      op: incrementTypes["$" + res[0]],
      a: ctx.lispTree,
      b: LispType.None
    }), false, ctx);
  });
  const adderTypes = {
    "&&": LispType.And,
    "||": LispType.Or,
    "??": LispType.NullishCoalescing,
    instanceof: LispType.Instanceof,
    in: LispType.In,
    "=": LispType.Assign,
    "-=": LispType.SubractEquals,
    "+=": LispType.AddEquals,
    "/=": LispType.DivideEquals,
    "**=": LispType.PowerEquals,
    "*=": LispType.MultiplyEquals,
    "%=": LispType.ModulusEquals,
    "^=": LispType.BitNegateEquals,
    "&=": LispType.BitAndEquals,
    "|=": LispType.BitOrEquals,
    ">>>=": LispType.UnsignedShiftRightEquals,
    "<<=": LispType.ShiftLeftEquals,
    ">>=": LispType.ShiftRightEquals,
    "&&=": LispType.AndEquals,
    "||=": LispType.OrEquals,
    "??=": LispType.NullishCoalescingEquals
  };
  setLispType2([
    "assign",
    "assignModify",
    "nullishCoalescing"
  ], (ctx) => {
    const { constants, type, part, res } = ctx;
    if (type !== "nullishCoalescing" && isLisp(ctx.lispTree) && (ctx.lispTree[0] === LispType.PropOptional || ctx.lispTree[0] === LispType.CallOptional)) throw new SyntaxError("Invalid left-hand side in assignment");
    if (res[0] === "=") {
      const patternStr = getDestructurePatternSource2(ctx.lispTree);
      if (patternStr) {
        const rhsStr = part.substring(res[0].length).toString();
        const tempName = `$$_da${Math.random().toString(36).slice(2)}`;
        const expandedCode = `internal ${tempName} = (${rhsStr}); ${expandDestructure2("", patternStr, tempName)}; ${tempName}`;
        ctx.lispTree = createLisp2({
          op: LispType.InternalBlock,
          a: lispifyBlock2(new CodeString(expandedCode), constants, false, ctx),
          b: LispType.None
        });
        return;
      }
    }
    ctx.lispTree = createLisp2({
      op: adderTypes[res[0]],
      a: ctx.lispTree,
      b: lispify2(constants, part.substring(res[0].length), expectTypes2.assignment.next, void 0, false, {
        ...ctx,
        lispDepth: ctx.lispDepth + 1
      })
    });
  });
  const opTypes = {
    "&": LispType.BitAnd,
    "|": LispType.BitOr,
    "^": LispType.BitNegate,
    "<<": LispType.BitShiftLeft,
    ">>": LispType.BitShiftRight,
    ">>>": LispType.BitUnsignedShiftRight,
    "<=": LispType.SmallerEqualThan,
    ">=": LispType.LargerEqualThan,
    "<": LispType.SmallerThan,
    ">": LispType.LargerThan,
    "!==": LispType.StrictNotEqual,
    "!=": LispType.NotEqual,
    "===": LispType.StrictEqual,
    "==": LispType.Equal,
    "+": LispType.Plus,
    "-": LispType.Minus,
    "/": LispType.Divide,
    "**": LispType.Power,
    "*": LispType.Multiply,
    "%": LispType.Modulus,
    "&&": LispType.And,
    "||": LispType.Or,
    instanceof: LispType.Instanceof,
    in: LispType.In
  };
  setLispType2([
    "power",
    "opHigh",
    "op",
    "comparitor",
    "bitwiseShift",
    "bitwiseAnd",
    "bitwiseXor",
    "bitwiseOr",
    "boolOpAnd",
    "boolOpOr"
  ], (ctx) => {
    const { constants, type, part, res } = ctx;
    const next = [expectTypes2.inlineIf.types.inlineIf, inlineIfElse];
    switch (type) {
      case "power":
        break;
      case "opHigh":
        next.push(expectTypes2.splitter.types.opHigh);
      case "op":
        next.push(expectTypes2.splitter.types.op);
      case "comparitor":
        next.push(expectTypes2.splitter.types.comparitor);
      case "bitwiseShift":
        next.push(expectTypes2.splitter.types.bitwiseShift);
      case "bitwiseAnd":
        next.push(expectTypes2.splitter.types.bitwiseAnd);
      case "bitwiseXor":
        next.push(expectTypes2.splitter.types.bitwiseXor);
      case "bitwiseOr":
        next.push(expectTypes2.splitter.types.bitwiseOr);
      case "boolOpAnd":
        next.push(expectTypes2.splitter.types.boolOpAnd);
      case "boolOpOr":
        next.push(expectTypes2.splitter.types.boolOpOr);
    }
    const extract = restOfExp2(constants, part.substring(res[0].length), next);
    ctx.lispTree = lispify2(constants, part.substring(extract.length + res[0].length), restOfExp2.next, createLisp2({
      op: opTypes[res[0]],
      a: ctx.lispTree,
      b: lispify2(constants, extract, expectTypes2.splitter.next)
    }), false, ctx);
  });
  setLispType2(["inlineIf"], (ctx) => {
    const { constants, part, res } = ctx;
    let found = false;
    const extract = part.substring(0, 0);
    let quoteCount = 1;
    while (!found && extract.length < part.length) {
      extract.end = restOfExp2(constants, part.substring(extract.length + 1), [expectTypes2.inlineIf.types.inlineIf, inlineIfElse]).end;
      if (part.char(extract.length) === "?") quoteCount++;
      else quoteCount--;
      if (!quoteCount) found = true;
    }
    extract.start = part.start + 1;
    const falseExpr = part.substring(res[0].length + extract.length + 1);
    if (!falseExpr.trimStart().length) throw new SyntaxError("Unexpected end of expression");
    ctx.lispTree = createLisp2({
      op: LispType.InlineIf,
      a: ctx.lispTree,
      b: createLisp2({
        op: LispType.InlineIfCase,
        a: lispifyExpr2(constants, extract, void 0, {
          ...ctx,
          lispDepth: ctx.lispDepth + 1
        }),
        b: lispifyExpr2(constants, falseExpr, void 0, {
          ...ctx,
          lispDepth: ctx.lispDepth + 1
        })
      })
    });
  });
}

// node_modules/@nyariv/sandboxjs/dist/esm/parser/lispTypes/structures.js
function registerStructureLispTypes({ NullLisp: NullLisp2, createLisp: createLisp2, emptyString: emptyString2, expectTypes: expectTypes2, expandDestructure: expandDestructure2, findPatternEndIdx: findPatternEndIdx2, lispify: lispify2, lispifyBlock: lispifyBlock2, lispifyExpr: lispifyExpr2, restOfExp: restOfExp2, setLispType: setLispType2 }) {
  const closingsCreate = {
    createArray: /^\]/,
    createObject: /^\}/,
    group: /^\)/,
    arrayProp: /^\]/,
    call: /^\)/
  };
  const typesCreate = {
    createArray: LispType.CreateArray,
    createObject: LispType.CreateObject,
    group: LispType.Group,
    arrayProp: LispType.ArrayProp,
    call: LispType.Call,
    prop: LispType.Prop,
    "?prop": LispType.PropOptional,
    "?call": LispType.CallOptional
  };
  setLispType2([
    "createArray",
    "createObject",
    "group",
    "arrayProp",
    "call"
  ], (ctx) => {
    const { constants, type, part, res, expect } = ctx;
    let extract = emptyString2;
    const arg = [];
    const argIsHole = [];
    let end = false;
    let i = res[0].length;
    const start = i;
    while (i < part.length && !end) {
      extract = restOfExp2(constants, part.substring(i), [closingsCreate[type], /^,/]);
      i += extract.length;
      if (extract.trim().length) {
        arg.push(extract);
        argIsHole.push(false);
      } else if (type === "createArray" && part.char(i) === ",") {
        arg.push(extract);
        argIsHole.push(true);
      }
      if (part.char(i) !== ",") end = true;
      else {
        if (!extract.trim().length && type === "call") throw new SyntaxError("Unexpected end of expression");
        if (!extract.trim().length && type === "createObject") throw new SyntaxError("Unexpected token ,");
        i++;
      }
    }
    const next = type === "createArray" || type === "createObject" || type === "group" ? [
      "value",
      "modifier",
      "prop",
      "incrementerBefore",
      "assignment",
      "expEnd"
    ] : [
      "value",
      "modifier",
      "prop",
      "incrementerBefore",
      "expEnd"
    ];
    let l;
    let funcFound;
    switch (type) {
      case "group": {
        const groupContentStr = part.substring(start, i).trim().toString();
        if (groupContentStr.startsWith("{") || groupContentStr.startsWith("[")) {
          const patternEnd = findPatternEndIdx2(groupContentStr);
          const patternStr = groupContentStr.slice(0, patternEnd).trim();
          const afterPattern = groupContentStr.slice(patternEnd).trimStart();
          if (afterPattern.startsWith("=")) {
            const rhsStr = afterPattern.slice(1).trim();
            const tempName = `$$_da${Math.random().toString(36).slice(2)}`;
            const expandedCode = `internal ${tempName} = (${rhsStr}); ${expandDestructure2("", patternStr, tempName)}; ${tempName}`;
            l = createLisp2({
              op: LispType.InternalBlock,
              a: lispifyBlock2(new CodeString(expandedCode), constants, false, ctx),
              b: LispType.None
            });
            break;
          }
        }
      }
      case "arrayProp": {
        const arrayPropExpr = part.substring(start, i);
        if (!arrayPropExpr.trimStart().length) throw new SyntaxError("Unexpected end of expression");
        l = lispifyExpr2(constants, arrayPropExpr, void 0, {
          ...ctx,
          lispDepth: ctx.lispDepth + 1
        });
        break;
      }
      case "call":
        l = arg.map((e) => lispify2(constants, e, [...next, "spreadArray"], void 0, false, ctx));
        break;
      case "createArray":
        l = arg.map((e, idx) => argIsHole[idx] ? createLisp2({
          op: LispType.Hole,
          a: LispType.None,
          b: LispType.None
        }) : lispify2(constants, e, [...next, "spreadArray"], void 0, false, ctx));
        break;
      case "createObject":
        l = arg.map((str2) => {
          str2 = str2.trimStart();
          let value;
          let key = "";
          if (str2.char(0) === "[") {
            const innerExpr = restOfExp2(constants, str2.substring(1), [], "[");
            const afterBracket = str2.substring(1 + innerExpr.length + 1).trimStart();
            key = lispify2(constants, innerExpr, next);
            if (afterBracket.length > 0 && afterBracket.char(0) === ":") value = lispify2(constants, afterBracket.substring(1));
            else if (afterBracket.length > 0 && afterBracket.char(0) === "(") value = lispify2(constants, new CodeString("function" + afterBracket.toString()));
            else throw new SyntaxError("Unexpected token in computed property");
            return createLisp2({
              op: LispType.KeyVal,
              a: key,
              b: value
            });
          }
          funcFound = expectTypes2.expFunction.types.function.exec("function " + str2);
          if (funcFound) {
            key = funcFound[3].trimStart();
            value = lispify2(constants, new CodeString("function " + str2.toString().replace(key, "")));
          } else {
            const extract2 = restOfExp2(constants, str2, [/^:/]);
            key = lispify2(constants, extract2, [...next, "spreadObject"]);
            if (isLisp(key) && key[0] === LispType.SpreadObject) value = NullLisp2;
            else {
              if (key[0] === LispType.Prop) key = key[2];
              if (str2.length > extract2.length && str2.char(extract2.length) === ":") value = lispify2(constants, str2.substring(extract2.length + 1));
              else value = lispify2(constants, extract2, next);
            }
          }
          return createLisp2({
            op: LispType.KeyVal,
            a: key,
            b: value
          });
        });
        break;
    }
    const lisptype = type === "arrayProp" ? res[1] ? LispType.PropOptional : LispType.Prop : type === "call" ? res[1] ? LispType.CallOptional : LispType.Call : typesCreate[type];
    const currentTree = createLisp2({
      op: lisptype,
      a: ctx.lispTree,
      b: l
    });
    if (lisptype === LispType.Group || lisptype === LispType.CreateArray || lisptype === LispType.CreateObject) currentTree.source = part.substring(start, i).toString();
    ctx.lispTree = lispify2(constants, part.substring(i + 1), expectTypes2[expect].next, currentTree, false, ctx);
  });
  setLispType2(["dot", "prop"], (ctx) => {
    const { constants, type, part, res, expect } = ctx;
    let prop = res[0];
    let index = res[0].length;
    let op = "prop";
    if (type === "dot") {
      if (res[1]) op = "?prop";
      const matches = part.substring(res[0].length).toString().match(expectTypes2.prop.types.prop);
      if (matches && matches.length) {
        prop = matches[0];
        index = prop.length + res[0].length;
      } else throw new SyntaxError("Hanging dot");
    } else if (reservedWords.has(prop) && prop !== "this") throw new SyntaxError(`Unexpected token '${prop}'`);
    ctx.lispTree = lispify2(constants, part.substring(index), expectTypes2[expect].next, createLisp2({
      op: typesCreate[op],
      a: ctx.lispTree,
      b: prop
    }), false, ctx);
  });
  setLispType2(["spreadArray", "spreadObject"], (ctx) => {
    const { constants, type, part, res, expect } = ctx;
    ctx.lispTree = createLisp2({
      op: type === "spreadArray" ? LispType.SpreadArray : LispType.SpreadObject,
      a: LispType.None,
      b: lispify2(constants, part.substring(res[0].length), expectTypes2[expect].next)
    });
  });
}

// node_modules/@nyariv/sandboxjs/dist/esm/parser/lispTypes/values.js
function registerValueLispTypes({ createLisp: createLisp2, expectTypes: expectTypes2, lispify: lispify2, lispifyExpr: lispifyExpr2, restOfExp: restOfExp2, semiColon: semiColon2, setLispType: setLispType2 }) {
  setLispType2(["return", "throw"], (ctx) => {
    const { constants, type, part, res } = ctx;
    const expr = part.substring(res[0].length);
    if (type === "throw" && !expr.trimStart().length) throw new SyntaxError("Unexpected end of expression");
    ctx.lispTree = createLisp2({
      op: type === "return" ? LispType.Return : LispType.Throw,
      a: LispType.None,
      b: lispifyExpr2(constants, expr, void 0, ctx)
    });
  });
  setLispType2([
    "number",
    "boolean",
    "null",
    "und",
    "NaN",
    "Infinity"
  ], (ctx) => {
    const { constants, type, part, res, expect } = ctx;
    ctx.lispTree = lispify2(constants, part.substring(res[0].length), expectTypes2[expect].next, createLisp2({
      op: type === "number" ? res[12] ? LispType.BigInt : LispType.Number : LispType.GlobalSymbol,
      a: LispType.None,
      b: res[12] ? res[1] : res[0]
    }), false, ctx);
  });
  setLispType2([
    "string",
    "literal",
    "regex"
  ], (ctx) => {
    const { constants, type, part, res, expect } = ctx;
    ctx.lispTree = lispify2(constants, part.substring(res[0].length), expectTypes2[expect].next, createLisp2({
      op: type === "string" ? LispType.StringIndex : type === "literal" ? LispType.LiteralIndex : LispType.RegexIndex,
      a: LispType.None,
      b: res[1]
    }), false, ctx);
  });
  setLispType2(["void", "await"], (ctx) => {
    const { constants, type, part, res, expect } = ctx;
    const extract = restOfExp2(constants, part.substring(res[0].length), [/^([^\s.?\w$]|\?[^.])/]);
    if (!extract.trimStart().length) throw new SyntaxError("Unexpected end of expression");
    ctx.lispTree = lispify2(constants, part.substring(res[0].length + extract.length), expectTypes2[expect].next, createLisp2({
      op: type === "void" ? LispType.Void : LispType.Await,
      a: lispify2(constants, extract),
      b: LispType.None
    }), false, ctx);
  });
  setLispType2(["yield"], (ctx) => {
    const { constants, part, res, expect, generatorDepth } = ctx;
    if (generatorDepth === 0) throw new SyntaxError("Unexpected token");
    const isDelegate = res[0].trimEnd().endsWith("*");
    const extract = restOfExp2(constants, part.substring(res[0].length), [/^([^\s.?\w$]|\?[^.])/]);
    if (isDelegate && !extract.trimStart().length) throw new SyntaxError("Unexpected end of expression");
    ctx.lispTree = lispify2(constants, part.substring(res[0].length + extract.length), expectTypes2[expect].next, createLisp2({
      op: isDelegate ? LispType.YieldDelegate : LispType.Yield,
      a: lispify2(constants, extract),
      b: LispType.None
    }), false, ctx);
  });
  setLispType2(["new"], (ctx) => {
    const { constants, part, res } = ctx;
    let i = res[0].length;
    const obj = restOfExp2(constants, part.substring(i), [], void 0, "(");
    if (!obj.trimStart().length) throw new SyntaxError("Unexpected end of expression");
    i += obj.length + 1;
    const args = [];
    if (part.char(i - 1) === "(") {
      const argsString = restOfExp2(constants, part.substring(i), [], "(");
      i += argsString.length + 1;
      let found;
      let j = 0;
      while ((found = restOfExp2(constants, argsString.substring(j), [/^,/])).length) {
        j += found.length + 1;
        args.push(found.trim());
      }
    }
    ctx.lispTree = lispify2(constants, part.substring(i), expectTypes2.expEdge.next, createLisp2({
      op: LispType.New,
      a: lispify2(constants, obj, expectTypes2.initialize.next),
      b: args.map((arg) => lispify2(constants, arg, expectTypes2.initialize.next))
    }), false, ctx);
  });
}

// node_modules/@nyariv/sandboxjs/dist/esm/parser/lispTypes/index.js
function registerLispTypes(deps) {
  registerStructureLispTypes(deps);
  registerOperatorLispTypes(deps);
  registerConditionalLispTypes(deps);
  registerValueLispTypes(deps);
  registerDeclarationLispTypes(deps);
  registerControlLispTypes(deps);
}

// node_modules/@nyariv/sandboxjs/dist/esm/parser/parserUtils.js
function createLisp(obj) {
  return [
    obj.op,
    obj.a,
    obj.b
  ];
}
var NullLisp = createLisp({
  op: LispType.None,
  a: LispType.None,
  b: LispType.None
});
var statementLabelRegex = /([a-zA-Z$_][\w$]*)\s*:/g;
function extractStatementLabels(prefix = "") {
  return [...prefix.matchAll(statementLabelRegex)].map((match) => match[1]);
}
function wrapLabeledStatement(labels, statement) {
  return labels.reduceRight((current, label) => createLisp({
    op: LispType.Labeled,
    a: label,
    b: current
  }), statement);
}
var lispTypes = /* @__PURE__ */ new Map();
var ParseError = class extends Error {
  constructor(message, code) {
    super(message + ": " + code.substring(0, 40));
    this.code = code;
  }
};
var lastType;
var space = /^\s/;
var expectTypes = {
  splitter: {
    types: {
      power: /^(\*\*)(?!=)/,
      opHigh: /^(\/|\*(?!\*)|%)(?!=)/,
      op: /^(\+(?!(\+))|-(?!(-)))(?!=)/,
      comparitor: /^(<=|>=|<(?!<)|>(?!>)|!==|!=(?!=)|===|==)/,
      bitwiseShift: /^(<<|>>(?!>)|>>>)(?!=)/,
      bitwiseAnd: /^(&(?!&))(?!=)/,
      bitwiseXor: /^(\^)(?!=)/,
      bitwiseOr: /^(\|(?!\|))(?!=)/,
      boolOpAnd: /^(&&)(?!=)/,
      boolOpOr: /^(\|\|(?!=)|instanceof(?![\w$])|in(?![\w$]))/,
      nullishCoalescing: /^\?\?(?!=)/
    },
    next: [
      "modifier",
      "value",
      "prop",
      "incrementerBefore"
    ]
  },
  inlineIf: {
    types: { inlineIf: /^\?(?!\.(?!\d))/ },
    next: ["expEnd"]
  },
  assignment: {
    types: {
      assignModify: /^(-=|\+=|\/=|\*\*=|\*=|%=|\^=|&=|\|=|>>>=|>>=|<<=|&&=|\|\|=|\?\?=)/,
      assign: /^(=)(?!=)/
    },
    next: [
      "modifier",
      "value",
      "prop",
      "incrementerBefore"
    ]
  },
  incrementerBefore: {
    types: { incrementerBefore: /^(\+\+|--)/ },
    next: ["prop"]
  },
  expEdge: {
    types: {
      call: /^(\?\.)?[(]/,
      incrementerAfter: /^(\+\+|--)/,
      taggedTemplate: /^`(\d+)`/
    },
    next: [
      "splitter",
      "assignment",
      "expEdge",
      "dot",
      "inlineIf",
      "expEnd"
    ]
  },
  modifier: {
    types: {
      not: /^!/,
      inverse: /^~/,
      negative: /^-(?!-)/,
      positive: /^\+(?!\+)/,
      typeof: /^typeof(?![\w$])/,
      delete: /^delete(?![\w$])/
    },
    next: [
      "modifier",
      "value",
      "prop",
      "incrementerBefore"
    ]
  },
  dot: {
    types: {
      arrayProp: /^(\?\.)?\[/,
      dot: /^(\?)?\.(?=\s*[a-zA-Z$_])/
    },
    next: [
      "splitter",
      "assignment",
      "expEdge",
      "dot",
      "inlineIf",
      "expEnd"
    ]
  },
  prop: {
    types: { prop: /^[a-zA-Z$_][a-zA-Z\d$_]*/ },
    next: [
      "splitter",
      "assignment",
      "expEdge",
      "dot",
      "inlineIf",
      "expEnd"
    ]
  },
  value: {
    types: {
      createObject: /^\{/,
      createArray: /^\[/,
      number: /^(0b[01]+(_[01]+)*|0o[0-7]+(_[0-7]+)*|0x[\da-f]+(_[\da-f]+)*|(\d+(_\d+)*(\.\d+(_\d+)*)?|\.\d+(_\d+)*))(e[+-]?\d+(_\d+)*)?(n)?(?!\d)/i,
      string: /^"(\d+)"/,
      literal: /^`(\d+)`/,
      regex: /^\/(\d+)\/r(?![\w$])/,
      boolean: /^(true|false)(?![\w$])/,
      null: /^null(?![\w$])/,
      und: /^undefined(?![\w$])/,
      arrowFunctionSingle: /^(async\s+)?([a-zA-Z$_][a-zA-Z\d$_]*)\s*=>\s*({)?/,
      arrowFunction: /^(async\s*)?\(\s*([^)(]*?)\s*\)\s*=>\s*({)?/,
      inlineFunction: /^(async\s+)?function(\*\s*|\s*)([a-zA-Z$_][a-zA-Z\d$_]*)?\s*\(\s*/,
      yield: /^yield\*(?![\w$])\s*|^yield(?![\w$])\s*/,
      group: /^\(/,
      NaN: /^NaN(?![\w$])/,
      Infinity: /^Infinity(?![\w$])/,
      void: /^void(?![\w$])\s*/,
      await: /^await(?![\w$])\s*/,
      new: /^new(?![\w$])\s*/
    },
    next: [
      "splitter",
      "expEdge",
      "dot",
      "inlineIf",
      "expEnd"
    ]
  },
  initialize: {
    types: {
      initializeDestructure: /^(var|let|const|internal)\s+([{[])/,
      initialize: /^(var|let|const|internal)\s+([a-zA-Z$_][a-zA-Z\d$_]*)\s*(=)?/,
      return: /^return(?![\w$])/,
      throw: /^throw(?![\w$])\s*/
    },
    next: [
      "modifier",
      "value",
      "prop",
      "incrementerBefore",
      "expEnd"
    ]
  },
  spreadObject: {
    types: { spreadObject: /^\.\.\./ },
    next: ["value", "prop"]
  },
  spreadArray: {
    types: { spreadArray: /^\.\.\./ },
    next: ["value", "prop"]
  },
  expEnd: {
    types: {},
    next: []
  },
  expFunction: {
    types: { function: /^(async\s+)?function(\*\s*|\s+)([a-zA-Z$_][a-zA-Z\d$_]*)\s*\(\s*/ },
    next: ["expEdge", "expEnd"]
  },
  expSingle: {
    types: {
      for: /^((?:[a-zA-Z$_][\w$]*\s*:\s*)*)\s*for(\s+await)?\s*\(/,
      do: /^((?:[a-zA-Z$_][\w$]*\s*:\s*)*)\s*do(?![\w$])\s*(\{)?/,
      while: /^((?:[a-zA-Z$_][\w$]*\s*:\s*)*)\s*while\s*\(/,
      loopAction: /^(break|continue)(?![\w$])\s*([a-zA-Z$_][\w$]*)?/,
      if: /^((?:[a-zA-Z$_][\w$]*\s*:\s*)*)\s*if\s*\(/,
      try: /^((?:[a-zA-Z$_][\w$]*\s*:\s*)*)\s*try\s*{/,
      block: /^((?:[a-zA-Z$_][\w$]*\s*:\s*)*)\s*{/,
      switch: /^((?:[a-zA-Z$_][\w$]*\s*:\s*)*)\s*switch\s*\(/
    },
    next: ["expEnd"]
  }
};
var closings = {
  "(": ")",
  "[": "]",
  "{": "}",
  "'": "'",
  '"': '"',
  "`": "`"
};
function testMultiple(str2, tests) {
  let found = null;
  for (let i = 0; i < tests.length; i++) {
    found = tests[i].exec(str2);
    if (found) break;
  }
  return found;
}
var emptyString = new CodeString("");
var okFirstChars = /^[+\-~ !]/;
var aNumber = expectTypes.value.types.number;
var wordReg = /^((if|for|else|while|do|function)(?![\w$])|[\w$]+)/;
var semiColon = /^;/;
var insertedSemicolons = /* @__PURE__ */ new WeakMap();
var quoteCache = /* @__PURE__ */ new WeakMap();
function restOfExp(constants, part, tests, quote, firstOpening, closingsTests, details = {}, depth = 0) {
  if (!part.length) return part;
  if (depth > constants.maxDepth) throw new SandboxCapabilityError("Maximum expression depth exceeded");
  details.words = details.words || [];
  let isStart = true;
  tests = tests || [];
  const hasSemiTest = tests.includes(semiColon);
  if (hasSemiTest) tests = tests.filter((a) => a !== semiColon);
  const insertedSemis = insertedSemicolons.get(part.ref) || [];
  const cache = quoteCache.get(part.ref) || /* @__PURE__ */ new Map();
  quoteCache.set(part.ref, cache);
  if (quote && cache.has(part.start - 1)) return part.substring(0, cache.get(part.start - 1) - part.start);
  let escape2 = false;
  let done = false;
  let lastChar = "";
  let isOneLiner = false;
  let i;
  let lastInertedSemi = false;
  let seenKeyword = false;
  let skipNextWord = false;
  for (i = 0; i < part.length && !done; i++) {
    let char = part.char(i);
    if (quote === '"' || quote === "'" || quote === "`") {
      if (quote === "`" && char === "$" && part.char(i + 1) === "{" && !escape2) {
        const skip = restOfExp(constants, part.substring(i + 2), [], "{", void 0, void 0, {}, depth + 1);
        i += skip.length + 2;
      } else if (char === quote && !escape2) return part.substring(0, i);
      escape2 = !escape2 && char === "\\";
    } else if (closings[char]) {
      if (!lastInertedSemi && insertedSemis[i + part.start]) {
        lastInertedSemi = true;
        if (hasSemiTest) break;
        i--;
        lastChar = ";";
        continue;
      }
      if (isOneLiner && char === "{") isOneLiner = false;
      if (char === firstOpening) {
        done = true;
        break;
      } else {
        const skip = restOfExp(constants, part.substring(i + 1), [], char, void 0, void 0, {}, depth + 1);
        cache.set(skip.start - 1, skip.end);
        i += skip.length + 1;
        isStart = false;
        if (closingsTests) {
          const sub = part.substring(i);
          let found;
          if (found = testMultiple(sub.toString(), closingsTests)) {
            details.regRes = found;
            done = true;
          }
        }
      }
    } else if (!quote) {
      let sub = part.substring(i).toString();
      let foundWord;
      let foundNumber;
      if (closingsTests) {
        let found;
        if (found = testMultiple(sub, closingsTests)) {
          details.regRes = found;
          i++;
          done = true;
          break;
        }
      }
      if (foundNumber = aNumber.exec(sub)) {
        i += foundNumber[0].length - 1;
        sub = part.substring(i).toString();
        if (closingsTests) {
          let found;
          if (found = testMultiple(sub, closingsTests)) {
            details.regRes = found;
            i++;
            done = true;
            break;
          }
        }
      } else if (lastChar != char) {
        let found = null;
        if (char === ";" || insertedSemis[i + part.start] && !isStart && !lastInertedSemi) {
          if (hasSemiTest) found = [";"];
          else if (insertedSemis[i + part.start]) {
            lastInertedSemi = true;
            i--;
            lastChar = ";";
            continue;
          }
          char = sub = ";";
        } else lastInertedSemi = false;
        if (!found) found = testMultiple(sub, tests);
        if (found) done = true;
        if (!done && (foundWord = wordReg.exec(sub))) {
          isOneLiner = true;
          if (foundWord[2]) {
            seenKeyword = true;
            skipNextWord = true;
          } else if (seenKeyword) if (skipNextWord) skipNextWord = false;
          else details.bodyContentAfterKeyword = true;
          if (foundWord[0].length > 1) {
            details.words.push(foundWord[1]);
            details.lastAnyWord = foundWord[1];
            if (foundWord[2]) details.lastWord = foundWord[2];
          }
          if (foundWord[0].length > 2) i += foundWord[0].length - 2;
        }
      }
      if (isStart) if (okFirstChars.test(sub)) done = false;
      else isStart = false;
      if (done) break;
    } else if (char === closings[quote]) return part.substring(0, i);
    lastChar = char;
  }
  if (quote) throw new SyntaxError("Unclosed '" + quote + "'");
  if (details) details.oneliner = isOneLiner;
  return part.substring(0, i);
}
restOfExp.next = [
  "splitter",
  "expEnd",
  "inlineIf"
];
var startingExecpted = [
  "initialize",
  "expSingle",
  "expFunction",
  "value",
  "modifier",
  "prop",
  "incrementerBefore",
  "expEnd"
];
var setLispType = (types, fn) => {
  types.forEach((type) => {
    lispTypes.set(type, fn);
  });
};
registerLispTypes({
  NullLisp,
  ParseError,
  createLisp,
  emptyString,
  expectTypes,
  expandDestructure,
  expandFunctionParamDestructure,
  extractStatementLabels,
  findPatternEndIdx,
  getDestructurePatternSource,
  insertSemicolons,
  lispify,
  lispifyBlock,
  lispifyExpr,
  lispifyFunction,
  lispifyReturnExpr,
  restOfExp,
  semiColon,
  setLispType,
  splitByCommasDestructure,
  startingExecpted,
  wrapLabeledStatement
});
function splitByCommasDestructure(s) {
  const parts = [];
  let depth = 0;
  let cur = "";
  for (let i = 0; i < s.length; i++) {
    const c = s[i];
    if (c === "[" || c === "{" || c === "(") depth++;
    else if (c === "]" || c === "}" || c === ")") depth--;
    if (c === "," && depth === 0) {
      parts.push(cur);
      cur = "";
    } else cur += c;
  }
  parts.push(cur);
  return parts;
}
function findFirstAtTopLevel(s, ch) {
  let depth = 0;
  for (let i = 0; i < s.length; i++) {
    const c = s[i];
    if (c === "[" || c === "{" || c === "(") depth++;
    else if (c === "]" || c === "}" || c === ")") depth--;
    if (c === ch && depth === 0) return i;
  }
  return -1;
}
function findPatternEndIdx(s) {
  let depth = 0;
  for (let i = 0; i < s.length; i++) {
    const c = s[i];
    if (c === "[" || c === "{") depth++;
    else if (c === "]" || c === "}") {
      depth--;
      if (depth === 0) return i + 1;
    }
  }
  return s.length;
}
var validIdentifier = /^[a-zA-Z$_][a-zA-Z\d$_]*$/;
function assertIdentifier(name) {
  if (!validIdentifier.test(name)) throw new SyntaxError(`Invalid destructuring target: '${name}'`);
  return name;
}
function expandDestructure(keyword, patternStr, rhsStr) {
  const stmts = [];
  function genTemp() {
    return `$$_d${Math.random().toString(36).slice(2)}`;
  }
  function processPattern(pattern, src) {
    pattern = pattern.trim();
    if (pattern.startsWith("[")) processArrayPattern(pattern.slice(1, -1), src);
    else if (pattern.startsWith("{")) processObjectPattern(pattern.slice(1, -1), src);
  }
  function processArrayPattern(content, src) {
    const elements = splitByCommasDestructure(content);
    for (let i = 0; i < elements.length; i++) {
      const elem = elements[i].trim();
      if (!elem) continue;
      if (elem.startsWith("...")) {
        const rest = elem.slice(3).trim();
        if (rest.startsWith("[") || rest.startsWith("{")) {
          const t = genTemp();
          stmts.push(`internal ${t} = ${src}.slice(${i})`);
          processPattern(rest, t);
        } else stmts.push(`${keyword} ${assertIdentifier(rest)} = ${src}.slice(${i})`);
        break;
      }
      const eqIdx = findFirstAtTopLevel(elem, "=");
      const target = eqIdx !== -1 ? elem.slice(0, eqIdx).trim() : elem.trim();
      const defaultVal = eqIdx !== -1 ? elem.slice(eqIdx + 1).trim() : void 0;
      if (target.startsWith("[") || target.startsWith("{")) {
        const t = genTemp();
        stmts.push(defaultVal !== void 0 ? `internal ${t} = ${src}[${i}] !== undefined ? ${src}[${i}] : (${defaultVal})` : `internal ${t} = ${src}[${i}]`);
        processPattern(target, t);
      } else stmts.push(defaultVal !== void 0 ? `${keyword} ${assertIdentifier(target)} = ${src}[${i}] !== undefined ? ${src}[${i}] : (${defaultVal})` : `${keyword} ${assertIdentifier(target)} = ${src}[${i}]`);
    }
  }
  function processObjectPattern(content, src) {
    const props = splitByCommasDestructure(content);
    const usedKeys = [];
    for (const prop of props) {
      const p = prop.trim();
      if (!p) continue;
      if (p.startsWith("...")) {
        const restIdx = props.indexOf(prop);
        if (props.slice(restIdx + 1).some((pp) => pp.trim().length > 0)) throw new SyntaxError("Rest element must be last element");
        const rest = p.slice(3).trim();
        const exclTemp = genTemp();
        const keyTemp = genTemp();
        const resTemp = genTemp();
        const exclEntries = usedKeys.map((k) => `${assertIdentifier(k)}:1`).join(",");
        stmts.push(`internal ${exclTemp} = {${exclEntries}}`);
        stmts.push(`internal ${resTemp} = {}`);
        stmts.push(`for (internal ${keyTemp} in ${src}) { if (!(${keyTemp} in ${exclTemp})) { ${resTemp}[${keyTemp}] = ${src}[${keyTemp}] } }`);
        if (rest.startsWith("[") || rest.startsWith("{")) processPattern(rest, resTemp);
        else stmts.push(`${keyword} ${assertIdentifier(rest)} = ${resTemp}`);
        break;
      }
      if (p.startsWith("[")) {
        let closeBracket = -1;
        let depth = 1;
        for (let ci = 1; ci < p.length; ci++) if (p[ci] === "[") depth++;
        else if (p[ci] === "]") {
          depth--;
          if (depth === 0) {
            closeBracket = ci;
            break;
          }
        }
        if (closeBracket !== -1) {
          const computedExpr = p.slice(1, closeBracket);
          const after = p.slice(closeBracket + 1).trim();
          if (after.startsWith(":")) {
            const valueStr = after.slice(1).trim();
            const accessor = `${src}[${computedExpr}]`;
            const eqIdx2 = findFirstAtTopLevel(valueStr, "=");
            const tgt = eqIdx2 !== -1 && !valueStr.slice(0, eqIdx2).trim().match(/^[[{]/) ? valueStr.slice(0, eqIdx2).trim() : valueStr.trim();
            const defVal = eqIdx2 !== -1 && !valueStr.slice(0, eqIdx2).trim().match(/^[[{]/) ? valueStr.slice(eqIdx2 + 1).trim() : void 0;
            if (tgt.startsWith("[") || tgt.startsWith("{")) {
              const t = genTemp();
              stmts.push(defVal !== void 0 ? `internal ${t} = ${accessor} !== undefined ? ${accessor} : (${defVal})` : `internal ${t} = ${accessor}`);
              processPattern(tgt, t);
            } else stmts.push(defVal !== void 0 ? `${keyword} ${assertIdentifier(tgt)} = ${accessor} !== undefined ? ${accessor} : (${defVal})` : `${keyword} ${assertIdentifier(tgt)} = ${accessor}`);
          }
        }
        continue;
      }
      const colonIdx = findFirstAtTopLevel(p, ":");
      if (colonIdx !== -1) {
        const key = assertIdentifier(p.slice(0, colonIdx).trim());
        const valueStr = p.slice(colonIdx + 1).trim();
        usedKeys.push(key);
        const accessor = `${src}.${key}`;
        const eqIdx2 = findFirstAtTopLevel(valueStr, "=");
        const beforeEq = eqIdx2 !== -1 ? valueStr.slice(0, eqIdx2).trim() : valueStr.trim();
        const isNestedPattern = beforeEq.startsWith("[") || beforeEq.startsWith("{");
        const tgt = isNestedPattern ? valueStr.trim() : beforeEq;
        const defVal = !isNestedPattern && eqIdx2 !== -1 ? valueStr.slice(eqIdx2 + 1).trim() : void 0;
        if (tgt.startsWith("[") || tgt.startsWith("{")) {
          const patEnd = findPatternEndIdx(tgt);
          const finalPattern = tgt.slice(0, patEnd);
          const afterPat = tgt.slice(patEnd).trim();
          const nestedDef = afterPat.startsWith("=") ? afterPat.slice(1).trim() : void 0;
          const t = genTemp();
          stmts.push(nestedDef !== void 0 ? `internal ${t} = ${accessor} !== undefined ? ${accessor} : (${nestedDef})` : `internal ${t} = ${accessor}`);
          processPattern(finalPattern, t);
        } else stmts.push(defVal !== void 0 ? `${keyword} ${assertIdentifier(tgt)} = ${accessor} !== undefined ? ${accessor} : (${defVal})` : `${keyword} ${assertIdentifier(tgt)} = ${accessor}`);
        continue;
      }
      const eqIdx = findFirstAtTopLevel(p, "=");
      if (eqIdx !== -1) {
        const name = assertIdentifier(p.slice(0, eqIdx).trim());
        const defaultVal = p.slice(eqIdx + 1).trim();
        usedKeys.push(name);
        stmts.push(`${keyword} ${name} = ${src}.${name} !== undefined ? ${src}.${name} : (${defaultVal})`);
      } else {
        assertIdentifier(p);
        usedKeys.push(p);
        stmts.push(`${keyword} ${p} = ${src}.${p}`);
      }
    }
  }
  const rootTemp = genTemp();
  stmts.unshift(`var ${rootTemp} = (${rhsStr})`);
  processPattern(patternStr, rootTemp);
  return stmts.join("; ");
}
function getDestructurePatternSource(tree) {
  if (!isLisp(tree)) return null;
  const source = tree.source?.trim();
  if (source && (source.startsWith("[") || source.startsWith("{"))) return source;
  if (tree[0] === LispType.Group) return getDestructurePatternSource(tree[2]);
  return null;
}
function expandFunctionParamDestructure(args, funcBody) {
  const injected = [];
  const newArgs = args.map((arg, i) => {
    const a = arg.trim();
    if (a.startsWith("[") || a.startsWith("{")) {
      const tempName = `$$_p${i}`;
      const patEnd = findPatternEndIdx(a);
      const patternOnly = a.slice(0, patEnd);
      const afterPat = a.slice(patEnd).trim();
      if (afterPat.startsWith("=")) {
        const defaultVal = afterPat.slice(1).trim();
        injected.push(expandDestructure("const", patternOnly, `${tempName} !== undefined ? ${tempName} : (${defaultVal})`));
      } else injected.push(expandDestructure("const", patternOnly, tempName));
      return tempName;
    }
    const eqIdx = findFirstAtTopLevel(a, "=");
    if (eqIdx !== -1 && !a.startsWith("...")) {
      const paramName = a.slice(0, eqIdx).trim();
      const defaultVal = a.slice(eqIdx + 1).trim();
      injected.push(`if (${paramName} === undefined) ${paramName} = (${defaultVal})`);
      return paramName;
    }
    return a;
  });
  if (injected.length === 0) return {
    args: newArgs,
    body: funcBody
  };
  return {
    args: newArgs,
    body: injected.join("; ") + "; " + funcBody
  };
}
function lispify(constants, part, expected, lispTree, topLevel = false, depthCtx = {
  generatorDepth: 0,
  asyncDepth: 0,
  lispDepth: 0
}) {
  if (depthCtx.lispDepth > constants.maxDepth) throw new SandboxCapabilityError("Maximum expression depth exceeded");
  const { generatorDepth, asyncDepth, lispDepth } = depthCtx;
  lispTree = lispTree || NullLisp;
  expected = expected || expectTypes.initialize.next;
  if (part === void 0) return lispTree;
  part = part.trimStart();
  const str2 = part.toString();
  if (!part.length && !expected.includes("expEnd")) throw new SyntaxError("Unexpected end of expression");
  if (!part.length) return lispTree;
  const ctx = {
    constants,
    type: "",
    part,
    res: [],
    expect: "",
    lispTree,
    generatorDepth,
    asyncDepth,
    lispDepth
  };
  let res;
  for (const expect of expected) {
    if (expect === "expEnd") continue;
    for (const type in expectTypes[expect].types) {
      if (type === "expEnd") continue;
      if (res = expectTypes[expect].types[type].exec(str2)) {
        lastType = type;
        ctx.type = type;
        ctx.part = part;
        ctx.res = res;
        ctx.expect = expect;
        try {
          lispTypes.get(type)?.(ctx);
        } catch (e) {
          if (topLevel && e instanceof SyntaxError) throw new ParseError(e.message, str2);
          throw e;
        }
        break;
      }
    }
    if (res) break;
  }
  if (!res && part.length) {
    if (topLevel) throw new ParseError(`Unexpected token after ${lastType}: ${part.char(0)}`, str2);
    throw new SyntaxError(`Unexpected token after ${lastType}: ${part.char(0)}`);
  }
  return ctx.lispTree;
}
var startingExpectedWithoutSingle = startingExecpted.filter((r) => r !== "expSingle");
function lispifyExpr(constants, str2, expected, depthCtx = {
  generatorDepth: 0,
  asyncDepth: 0,
  lispDepth: 0
}) {
  if (depthCtx.lispDepth > constants.maxDepth) throw new SandboxCapabilityError("Maximum expression depth exceeded");
  if (!str2.trimStart().length) return NullLisp;
  const subExpressions = [];
  let sub;
  let pos = 0;
  expected = expected || expectTypes.initialize.next;
  if (expected.includes("expSingle")) {
    if (testMultiple(str2.toString(), Object.values(expectTypes.expSingle.types))) return lispify(constants, str2, ["expSingle"], void 0, true, depthCtx);
  }
  if (expected === startingExecpted) expected = startingExpectedWithoutSingle;
  while ((sub = restOfExp(constants, str2.substring(pos), [/^,/])).length) {
    subExpressions.push(sub.trimStart());
    pos += sub.length + 1;
  }
  if (subExpressions.length === 1) return lispify(constants, str2, expected, void 0, true, depthCtx);
  if (expected.includes("initialize")) {
    const defined = expectTypes.initialize.types.initialize.exec(subExpressions[0].toString());
    if (defined) return createLisp({
      op: LispType.InternalBlock,
      a: subExpressions.map((str3, i) => lispify(constants, i ? new CodeString(defined[1] + " " + str3) : str3, ["initialize"], void 0, true, depthCtx)),
      b: LispType.None
    });
    else if (expectTypes.initialize.types.return.exec(subExpressions[0].toString())) return lispify(constants, str2, expected, void 0, true, depthCtx);
  }
  const exprs = subExpressions.map((str3) => lispify(constants, str3, expected, void 0, true, depthCtx));
  return createLisp({
    op: LispType.Expression,
    a: exprs,
    b: LispType.None
  });
}
function lispifyReturnExpr(constants, str2) {
  return createLisp({
    op: LispType.Return,
    a: LispType.None,
    b: lispifyExpr(constants, str2)
  });
}
function lispifyBlock(str2, constants, expression = false, depthCtx = {
  generatorDepth: 0,
  asyncDepth: 0,
  lispDepth: 0
}) {
  str2 = insertSemicolons(constants, str2);
  if (!str2.trim().length) return [];
  const parts = [];
  let part;
  let pos = 0;
  let start = 0;
  let details = {};
  let skipped = false;
  let isInserted = false;
  while ((part = restOfExp(constants, str2.substring(pos), [semiColon], void 0, void 0, void 0, details)).length) {
    isInserted = !!(str2.char(pos + part.length) && str2.char(pos + part.length) !== ";");
    pos += part.length + (isInserted ? 0 : 1);
    if (/^\s*else(?![\w$])/.test(str2.substring(pos).toString())) skipped = true;
    else if (details["words"]?.includes("do") && /^\s*while(?![\w$])/.test(str2.substring(pos).toString())) skipped = true;
    else {
      skipped = false;
      parts.push(str2.substring(start, pos - (isInserted ? 0 : 1)));
      start = pos;
    }
    details = {};
    if (expression) break;
  }
  if (skipped) parts.push(str2.substring(start, pos - (isInserted ? 0 : 1)));
  return parts.map((str3) => str3.trimStart()).filter((str3) => str3.length).map((str3) => {
    return lispifyExpr(constants, str3.trimStart(), startingExecpted, depthCtx);
  });
}
function lispifyFunction(str2, constants, expression = false, depthCtx = {
  generatorDepth: 0,
  asyncDepth: 0,
  lispDepth: 0
}) {
  if (!str2.trim().length) return [];
  const tree = lispifyBlock(str2, constants, expression, depthCtx);
  hoist(tree);
  return tree;
}
function hoist(item, res = []) {
  if (isLisp(item)) {
    if (!isLisp(item)) return false;
    const [op, a, b] = item;
    if (op === LispType.Labeled || op === LispType.Try || op === LispType.If || op === LispType.Loop || op === LispType.Switch) {
      hoist(a, res);
      hoist(b, res);
    } else if (op === LispType.Var) res.push(createLisp({
      op: LispType.Var,
      a,
      b: LispType.None
    }));
    else if (op === LispType.Function && a[2]) {
      res.push(item);
      return true;
    }
  } else if (Array.isArray(item)) {
    const rep = [];
    for (const it of item) if (!hoist(it, res)) rep.push(it);
    if (rep.length !== item.length) {
      item.length = 0;
      item.push(...res, ...rep);
    }
  }
  return false;
}
var closingsNoInsertion = /^(\})\s*(catch|finally|else|while|instanceof)(?![\w$])/;
var colonsRegex = /^((([\w$\])"'`]|\+\+|--)\s*\r?\n\s*([\w$+\-!~]))|(\}\s*[\w$!~+\-{("'`]))/;
function insertSemicolons(constants, str2) {
  let rest = str2;
  let sub = emptyString;
  let details = {};
  let pendingDoWhile = false;
  const inserted = insertedSemicolons.get(str2.ref) || new Array(str2.ref.str.length);
  while ((sub = restOfExp(constants, rest, [], void 0, void 0, [colonsRegex], details)).length) {
    let valid = false;
    let part = sub;
    let edge = sub.length;
    if (details.regRes) {
      valid = true;
      const [, , a, , , b] = details.regRes;
      edge = details.regRes[3] === "++" || details.regRes[3] === "--" ? sub.length + 1 : sub.length;
      part = rest.substring(0, edge);
      if (b) {
        const res = closingsNoInsertion.exec(rest.substring(sub.length - 1).toString());
        if (res) if (res[2] === "while") if (details.lastWord === "do") {
          valid = false;
          pendingDoWhile = true;
        } else valid = true;
        else valid = false;
        else if (details.lastWord === "function" && details.regRes[5][0] === "}" && details.regRes[5].slice(-1) === "(") valid = false;
      } else if (a) {
        if (pendingDoWhile && details.lastWord === "while") {
          valid = true;
          pendingDoWhile = false;
        } else if (details.lastWord === "if" || details.lastWord === "while" || details.lastWord === "for" || details.lastWord === "else") valid = !!details.bodyContentAfterKeyword;
      }
    }
    if (valid) inserted[part.end] = true;
    rest = rest.substring(edge);
    details = {};
  }
  insertedSemicolons.set(str2.ref, inserted);
  return str2;
}
function checkRegex(str2) {
  let i = 1;
  let escape2 = false;
  let done = false;
  let cancel = false;
  while (i < str2.length && !done && !cancel) {
    done = str2[i] === "/" && !escape2;
    escape2 = str2[i] === "\\" && !escape2;
    cancel = str2[i] === "\n";
    i++;
  }
  const after = str2.substring(i);
  cancel = cancel || !done || /^\s*\d/.test(after);
  if (cancel) return null;
  const flags = /^[a-z]*/.exec(after);
  if (/^\s+[\w$]/.test(str2.substring(i + flags[0].length))) return null;
  const regexPattern = str2.substring(1, i - 1);
  const regexFlags = flags && flags[0] || "";
  try {
    new RegExp(regexPattern, regexFlags);
  } catch (e) {
    if (e instanceof SyntaxError) throw e;
  }
  return {
    regex: regexPattern,
    flags: regexFlags,
    length: i + (flags && flags[0].length || 0)
  };
}
var notDivide = /(typeof|delete|instanceof|return|in|of|throw|new|void|do|if)$/;
var possibleDivide = /^([\w$\])]|\+\+|--)[\s/]/;
function extractConstants(constants, str2, currentEnclosure = "", depth = 0) {
  if (depth > constants.maxDepth) throw new SandboxCapabilityError("Maximum expression depth exceeded");
  let quote;
  let extract = [];
  let escape2 = false;
  let regexFound;
  let comment = "";
  let commentStart = -1;
  let currJs = [];
  let char = "";
  const strRes = [];
  const enclosures = [];
  let isPossibleDivide = null;
  let i = 0;
  for (i = 0; i < str2.length; i++) {
    char = str2[i];
    if (comment) {
      if (char === comment) {
        if (comment === "*" && str2[i + 1] === "/") {
          comment = "";
          i++;
        } else if (comment === "\n") {
          comment = "";
          strRes.push("\n");
        }
      }
    } else {
      if (escape2) {
        escape2 = false;
        extract.push(char);
        continue;
      }
      if (quote) if (quote === "`" && char === "$" && str2[i + 1] === "{") {
        const skip = extractConstants(constants, str2.substring(i + 2), "{", depth + 1);
        if (!skip.str.trim().length) throw new SyntaxError("Unexpected end of expression");
        currJs.push(skip.str);
        extract.push("${", currJs.length - 1, `}`);
        i += skip.length + 2;
      } else if (quote === char) {
        if (quote === "`") {
          const li = createLisp({
            op: LispType.Literal,
            a: unraw(extract.join("")),
            b: []
          });
          li.tempJsStrings = currJs;
          constants.literals.push(li);
          strRes.push(`\``, constants.literals.length - 1, `\``);
        } else {
          constants.strings.push(unraw(extract.join("")));
          strRes.push(`"`, constants.strings.length - 1, `"`);
        }
        quote = null;
        extract = [];
      } else extract.push(char);
      else {
        if (char === "'" || char === '"' || char === "`") {
          currJs = [];
          quote = char;
        } else if (closings[currentEnclosure] === char && !enclosures.length) return {
          str: strRes.join(""),
          length: i
        };
        else if (closings[char]) {
          enclosures.push(char);
          strRes.push(char);
        } else if (closings[enclosures[enclosures.length - 1]] === char) {
          enclosures.pop();
          strRes.push(char);
        } else if (char === "/" && (str2[i + 1] === "*" || str2[i + 1] === "/")) {
          comment = str2[i + 1] === "*" ? "*" : "\n";
          commentStart = i;
        } else if (char === "/" && !isPossibleDivide && (regexFound = checkRegex(str2.substring(i)))) {
          constants.regexes.push(regexFound);
          strRes.push(`/`, constants.regexes.length - 1, `/r`);
          i += regexFound.length - 1;
        } else strRes.push(char);
        if (!isPossibleDivide || !space.test(char)) {
          if (isPossibleDivide = possibleDivide.exec(str2.substring(i))) {
            if (notDivide.test(str2.substring(0, i + isPossibleDivide[1].length))) isPossibleDivide = null;
          }
        }
      }
      escape2 = !!(quote && char === "\\");
    }
  }
  if (quote) throw new SyntaxError(`Unclosed '${quote}'`);
  if (comment) {
    if (comment === "*") throw new SyntaxError(`Unclosed comment '/*': ${str2.substring(commentStart)}`);
  }
  return {
    str: strRes.join(""),
    length: i
  };
}
function parse(code, eager = false, expression = false, maxParserRecursionDepth = 256) {
  if (typeof code !== "string") throw new ParseError(`Cannot parse ${code}`, String(code));
  let str2 = " " + code;
  const constants = {
    strings: [],
    literals: [],
    regexes: [],
    eager,
    maxDepth: maxParserRecursionDepth
  };
  str2 = extractConstants(constants, str2).str;
  for (const l of constants.literals) {
    l[2] = l.tempJsStrings.map((js) => lispifyExpr(constants, new CodeString(js)));
    delete l.tempJsStrings;
  }
  return {
    tree: lispifyFunction(new CodeString(str2), constants, expression),
    constants
  };
}

// node_modules/@nyariv/sandboxjs/dist/esm/executor/opsRegistry.js
var ops = /* @__PURE__ */ new Map();
function addOps(type, cb) {
  ops.set(type, cb);
}

// node_modules/@nyariv/sandboxjs/dist/esm/executor/ops/prop.js
addOps(LispType.Prop, ({ done, a, b, obj, context, scope, internal }) => {
  if (a === null) throw new TypeError(`Cannot read properties of null (reading '${b?.toString()}')`);
  if (!isPropertyKey(b)) b = `${b}`;
  if (a === void 0 && obj === void 0 && typeof b === "string") {
    const prop = scope.get(b, internal);
    if (prop.context === void 0) throw new ReferenceError(`${b} is not defined`);
    if (prop.context === context.ctx.sandboxGlobal) {
      if (context.ctx.options.audit) context.ctx.auditReport?.globalsAccess.add(b);
    }
    const val2 = prop.context[prop.prop];
    done(void 0, resolveSandboxProp(val2, context, prop) || prop);
    return;
  } else if (a === void 0) throw new TypeError(`Cannot read properties of undefined (reading '${b.toString()}')`);
  if (!hasPossibleProperties(a)) {
    done(void 0, new Prop(void 0, b));
    return;
  }
  const prototypeAccess = typeof a === "function" || !hasOwnProperty(a, b);
  if (context.ctx.options.audit && prototypeAccess) {
    let prot = Object.getPrototypeOf(a);
    do
      if (hasOwnProperty(prot, b)) {
        if (context.ctx.auditReport && !context.ctx.auditReport.prototypeAccess[prot.constructor.name]) context.ctx.auditReport.prototypeAccess[prot.constructor.name] = /* @__PURE__ */ new Set();
        context.ctx.auditReport?.prototypeAccess[prot.constructor.name].add(b);
      }
    while (prot = Object.getPrototypeOf(prot));
  }
  if (prototypeAccess) {
    if (typeof a === "function") {
      if (hasOwnProperty(a, b)) {
        const whitelist = context.ctx.prototypeWhitelist.get(a.prototype);
        if (!(whitelist && (!whitelist.size || whitelist.has(b))) && !context.ctx.sandboxedFunctions.has(a)) throw new SandboxAccessError(`Static method or property access not permitted: ${a.name}.${b.toString()}`);
      }
    }
    let prot = a;
    while (prot = Object.getPrototypeOf(prot)) if (hasOwnProperty(prot, b) || b === "__proto__") {
      const whitelist = context.ctx.prototypeWhitelist.get(prot);
      if (whitelist && (!whitelist.size || whitelist.has(b)) || context.ctx.sandboxedFunctions.has(prot.constructor)) break;
      if (b === "__proto__") throw new SandboxAccessError(`Access to prototype of global object is not permitted`);
      throw new SandboxAccessError(`Method or property access not permitted: ${prot.constructor.name}.${b.toString()}`);
    }
  }
  if (typeof a === "function") {
    if (b === "prototype" && !context.ctx.sandboxedFunctions.has(a)) throw new SandboxAccessError(`Access to prototype of global object is not permitted`);
    if ([
      "caller",
      "callee",
      "arguments"
    ].includes(b)) throw new SandboxAccessError(`Access to '${b}' property is not permitted`);
  }
  const val = a[b];
  if (b === "__proto__" && !context.ctx.sandboxedFunctions.has(val?.constructor)) throw new SandboxAccessError(`Access to prototype of global object is not permitted`);
  const p = resolveSandboxProp(val, context, new Prop(a, b, false, false));
  if (p) {
    done(void 0, p);
    return;
  }
  const isSandboxGlobal = a === context.ctx.sandboxGlobal;
  const g = !isSandboxGlobal && obj instanceof Prop && obj.isGlobal || typeof a === "function" && !context.ctx.sandboxedFunctions.has(a) || context.ctx.globalsWhitelist.has(a) || isSandboxGlobal && typeof b === "string" && hasOwnProperty(context.ctx.globalScope.globals, b);
  done(void 0, new Prop(a, b, false, g, false));
});
addOps(LispType.StringIndex, ({ done, b, context }) => done(void 0, context.constants.strings[parseInt(b)]));

// node_modules/@nyariv/sandboxjs/dist/esm/executor/ops/call.js
addOps(LispType.Call, (params) => {
  const { done, a, b, obj, context } = params;
  if (context.ctx.options.forbidFunctionCalls) throw new SandboxCapabilityError("Function invocations are not allowed");
  if (typeof a !== "function") throw new TypeError(`${typeof obj?.prop === "symbol" ? "Symbol" : obj?.prop} is not a function`);
  const vals = new Array(b.length);
  let valsLen = 0;
  for (let i = 0; i < b.length; i++) {
    const item = b[i];
    if (item instanceof SpreadArray) {
      const expanded = Array.isArray(item.item) ? item.item : [...item.item];
      if (checkHaltExpectedTicks(params, BigInt(expanded.length))) return;
      for (let j = 0; j < expanded.length; j++) vals[valsLen++] = sanitizeProp(expanded[j], context);
    } else vals[valsLen++] = sanitizeProp(item, context);
  }
  vals.length = valsLen;
  if (a === String) {
    const result = String(vals[0]);
    checkTicksAndThrow(context, BigInt(result.length));
    done(void 0, result);
    return;
  }
  if (typeof obj === "function") {
    const evl2 = context.evals.get(obj);
    const receiver2 = getReplacementReceiver(obj);
    let ret2 = evl2 ? evl2(obj, ...vals) : receiver2 === void 0 ? obj(...vals) : obj.call(receiver2, ...vals);
    ret2 = sanitizeProp(ret2, context);
    if (ret2 !== null && typeof ret2 === "object" && ret2 instanceof DelayedSynchronousResult) Promise.resolve(ret2.result).then((res) => done(void 0, res), (err) => done(err));
    else done(void 0, ret2);
    return;
  }
  const originalFn = obj.context[obj.prop];
  if (originalFn === JSON.stringify && context.getSubscriptions.size) {
    const cache = /* @__PURE__ */ new WeakSet();
    let ticks = 0n;
    const recurse = (x) => {
      if (!x || !(typeof x === "object") || cache.has(x)) return;
      cache.add(x);
      const keys = Object.keys(x);
      ticks += BigInt(keys.length);
      for (const y of keys) {
        context.getSubscriptions.forEach((cb) => cb(x, y));
        recurse(x[y]);
      }
    };
    recurse(vals[0]);
    checkTicksAndThrow(context, ticks);
  }
  if (obj.context instanceof Array && arrayChange.has(originalFn) && (context.changeSubscriptions.get(obj.context) || context.changeSubscriptionsGlobal.get(obj.context))) {
    let change = void 0;
    let changed = false;
    if (obj.prop === "push") {
      change = {
        type: "push",
        added: vals
      };
      changed = !!vals.length;
    } else if (obj.prop === "pop") {
      change = {
        type: "pop",
        removed: obj.context.slice(-1)
      };
      changed = !!change.removed.length;
    } else if (obj.prop === "shift") {
      change = {
        type: "shift",
        removed: obj.context.slice(0, 1)
      };
      changed = !!change.removed.length;
    } else if (obj.prop === "unshift") {
      change = {
        type: "unshift",
        added: vals
      };
      changed = !!vals.length;
    } else if (obj.prop === "splice") {
      change = {
        type: "splice",
        startIndex: vals[0],
        deleteCount: vals[1] === void 0 ? obj.context.length : vals[1],
        added: vals.slice(2),
        removed: obj.context.slice(vals[0], vals[1] === void 0 ? void 0 : vals[0] + vals[1])
      };
      changed = !!change.added.length || !!change.removed.length;
    } else if (obj.prop === "reverse" || obj.prop === "sort") {
      change = { type: obj.prop };
      changed = !!obj.context.length;
    } else if (obj.prop === "copyWithin") {
      const len = vals[2] === void 0 ? obj.context.length - vals[1] : Math.min(obj.context.length, vals[2] - vals[1]);
      change = {
        type: "copyWithin",
        startIndex: vals[0],
        endIndex: vals[0] + len,
        added: obj.context.slice(vals[1], vals[1] + len),
        removed: obj.context.slice(vals[0], vals[0] + len)
      };
      changed = !!change.added.length || !!change.removed.length;
    }
    if (changed) {
      const subs = context.changeSubscriptions.get(obj.context);
      if (subs !== void 0) for (const cb of subs) cb(change);
      const subsG = context.changeSubscriptionsGlobal.get(obj.context);
      if (subsG !== void 0) for (const cb of subsG) cb(change);
    }
  }
  obj.get(context);
  const evl = context.evals.get(originalFn);
  const receiver = getReplacementReceiver(originalFn);
  const thisArg = obj.isVariable && receiver !== void 0 ? receiver : obj.context;
  let ret = evl ? evl.call(thisArg, ...vals) : a.call(thisArg, ...vals);
  ret = sanitizeProp(ret, context);
  if (ret !== null && typeof ret === "object" && ret instanceof DelayedSynchronousResult) Promise.resolve(ret.result).then((res) => done(void 0, res), (err) => done(err));
  else done(void 0, ret);
});
addOps(LispType.New, (params) => {
  const { done, a, b, context } = params;
  if (!context.ctx.globalsWhitelist.has(a) && !context.ctx.sandboxedFunctions.has(a)) throw new SandboxAccessError(`Object construction not allowed: ${a.constructor.name}`);
  const vals = b.map((item) => sanitizeProp(item, context));
  const replacement = context.evals.get(a);
  if (replacement) {
    done(void 0, new replacement(...vals));
    return;
  }
  const expectedTicks = getNewTicks(a, vals);
  if (expectedTicks > 0n && checkHaltExpectedTicks(params, expectedTicks)) return;
  done(void 0, new a(...vals));
});
function getNewTicks(ctor, args) {
  if (ctor === Array) {
    const n = args[0];
    if (typeof n === "number" && args.length === 1) return BigInt(n);
    return BigInt(args.length);
  }
  if (typedArrayProtos.has(Object.getPrototypeOf(ctor.prototype))) {
    const n = args[0];
    if (typeof n === "number") return BigInt(n);
    if (Array.isArray(n) || ArrayBuffer.isView(n)) return BigInt(n.length);
    return 0n;
  }
  if (ctor === Map || ctor === Set) {
    const iterable = args[0];
    if (Array.isArray(iterable)) return BigInt(iterable.length);
    return 0n;
  }
  if (ctor === String || ctor === RegExp) {
    const s = args[0];
    if (typeof s === "string") return BigInt(s.length);
    return 0n;
  }
  return 0n;
}

// node_modules/@nyariv/sandboxjs/dist/esm/executor/ops/object.js
addOps(LispType.CreateObject, (params) => {
  const { done, b } = params;
  let res = {};
  for (const item of b) if (item.key instanceof SpreadObject) {
    const keys = Object.keys(item.key.item);
    if (checkHaltExpectedTicks(params, BigInt(keys.length))) return;
    res = {
      ...res,
      ...item.key.item
    };
  } else res[item.key] = item.val;
  done(void 0, res);
});
addOps(LispType.KeyVal, ({ done, a, b }) => done(void 0, new KeyVal(a, b)));
addOps(LispType.CreateArray, (params) => {
  const { done, b, context } = params;
  const items = [];
  for (const item of b) if (item instanceof SpreadArray) {
    const expanded = Array.isArray(item.item) ? item.item : [...item.item];
    if (checkHaltExpectedTicks(params, BigInt(expanded.length))) return;
    for (const v of expanded) items.push(sanitizeProp(v, context));
  } else if (item instanceof ArrayHole) items.length++;
  else items.push(sanitizeProp(item, context));
  done(void 0, items);
});
addOps(LispType.Hole, ({ done }) => done(void 0, new ArrayHole()));
addOps(LispType.Group, ({ done, b }) => done(void 0, b));
addOps(LispType.GlobalSymbol, ({ done, b }) => {
  switch (b) {
    case "true":
      return done(void 0, true);
    case "false":
      return done(void 0, false);
    case "null":
      return done(void 0, null);
    case "undefined":
      return done(void 0, void 0);
    case "NaN":
      return done(void 0, NaN);
    case "Infinity":
      return done(void 0, Infinity);
  }
  done(/* @__PURE__ */ new Error("Unknown symbol: " + b));
});
addOps(LispType.SpreadArray, ({ done, b }) => {
  done(void 0, new SpreadArray(b));
});
addOps(LispType.SpreadObject, ({ done, b }) => {
  done(void 0, new SpreadObject(b));
});

// node_modules/@nyariv/sandboxjs/dist/esm/executor/ops/literals.js
addOps(LispType.Number, ({ done, b }) => done(void 0, Number(b.replace(/_/g, ""))));
addOps(LispType.BigInt, ({ done, b }) => done(void 0, BigInt(b.replace(/_/g, ""))));
addOps(LispType.RegexIndex, ({ done, b, context }) => {
  const reg = context.constants.regexes[parseInt(b)];
  if (!context.ctx.globalsWhitelist.has(RegExp)) throw new SandboxCapabilityError("Regex not permitted");
  else done(void 0, new (context.evals.get(RegExp) ?? RegExp)(reg.regex, reg.flags));
});
addOps(LispType.LiteralIndex, (params) => {
  const { exec, done, ticks, b, context, scope, internal, generatorYield } = params;
  const [, name, js] = context.constants.literals[parseInt(b)];
  const found = [];
  let f;
  const resnums = [];
  while (f = literalRegex.exec(name)) if (!f[2]) {
    found.push(js[parseInt(f[3], 10)]);
    resnums.push(f[3]);
  }
  exec(ticks, found, scope, context, (...args) => {
    const reses = {};
    if (args.length === 1) {
      done(args[0]);
      return;
    }
    const processed = args[1];
    for (const i of Object.keys(processed)) {
      const num = resnums[i];
      reses[num] = processed[i];
    }
    const result = name.replace(/(\\\\)*(\\)?\${(\d+)}/g, (match, $$, $, num) => {
      if ($) return match;
      const res = reses[num];
      return ($$ ? $$ : "") + `${sanitizeProp(res, context)}`;
    });
    if (checkHaltExpectedTicks(params, BigInt(result.length))) return;
    done(void 0, result);
  }, void 0, internal, generatorYield);
});

// node_modules/@nyariv/sandboxjs/dist/esm/executor/ops/unary.js
addOps(LispType.Not, ({ done, b }) => done(void 0, !b));
addOps(LispType.Inverse, ({ done, b }) => done(void 0, ~b));
addOps(LispType.IncrementBefore, ({ done, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, ++obj.context[obj.prop]);
});
addOps(LispType.IncrementAfter, ({ done, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop]++);
});
addOps(LispType.DecrementBefore, ({ done, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, --obj.context[obj.prop]);
});
addOps(LispType.DecrementAfter, ({ done, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop]--);
});
addOps(LispType.Positive, ({ done, b }) => done(void 0, +b));
addOps(LispType.Negative, ({ done, b }) => done(void 0, -b));
addOps(LispType.Typeof, ({ exec, done, ticks, b, context, scope, internal, generatorYield }) => {
  exec(ticks, b, scope, context, (e, prop) => {
    done(void 0, typeof sanitizeProp(prop, context));
  }, void 0, internal, generatorYield);
});
addOps(LispType.Delete, ({ done, context, bobj }) => {
  if (!(bobj instanceof Prop)) {
    done(void 0, true);
    return;
  }
  assignCheck(bobj, context, "delete");
  if (bobj.isVariable) {
    done(void 0, false);
    return;
  }
  done(void 0, delete bobj.context?.[bobj.prop]);
});
addOps(LispType.Void, ({ done }) => {
  done();
});

// node_modules/@nyariv/sandboxjs/dist/esm/executor/ops/assignment.js
addOps(LispType.Assign, (params) => {
  const { done, b, obj, context, scope, bobj, internal } = params;
  assignCheck(obj, context);
  obj.isGlobal = bobj?.isGlobal || false;
  if (obj.isVariable) {
    const s = scope.getWhereValScope(obj.prop, obj.prop === "this", internal);
    if (s === null) throw new ReferenceError(`Cannot assign to undeclared variable '${obj.prop.toString()}'`);
    s.set(obj.prop, b, internal);
    if (obj.isGlobal) s.globals[obj.prop.toString()] = true;
    else delete s.globals[obj.prop.toString()];
    done(void 0, b);
    return;
  }
  if (obj.prop === "length" && Array.isArray(obj.context) && typeof b === "number") {
    const delta = BigInt(Math.abs(b - obj.context.length));
    if (delta > 0n && checkHaltExpectedTicks(params, delta)) return;
  }
  done(void 0, obj.context[obj.prop] = b);
});
addOps(LispType.AddEquals, (params) => {
  const { done, b, obj, context } = params;
  assignCheck(obj, context);
  const result = obj.context[obj.prop] + b;
  if (typeof result === "string" && checkHaltExpectedTicks(params, BigInt(result.length))) return;
  done(void 0, obj.context[obj.prop] = result);
});
addOps(LispType.SubractEquals, ({ done, b, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop] -= b);
});
addOps(LispType.DivideEquals, ({ done, b, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop] /= b);
});
addOps(LispType.MultiplyEquals, ({ done, b, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop] *= b);
});
addOps(LispType.PowerEquals, ({ done, b, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop] **= b);
});
addOps(LispType.ModulusEquals, ({ done, b, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop] %= b);
});
addOps(LispType.BitNegateEquals, ({ done, b, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop] ^= b);
});
addOps(LispType.BitAndEquals, ({ done, b, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop] &= b);
});
addOps(LispType.BitOrEquals, ({ done, b, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop] |= b);
});
addOps(LispType.ShiftLeftEquals, ({ done, b, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop] <<= b);
});
addOps(LispType.ShiftRightEquals, ({ done, b, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop] >>= b);
});
addOps(LispType.UnsignedShiftRightEquals, ({ done, b, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop] >>>= b);
});
addOps(LispType.AndEquals, ({ done, b, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop] &&= b);
});
addOps(LispType.OrEquals, ({ done, b, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop] ||= b);
});
addOps(LispType.NullishCoalescingEquals, ({ done, b, obj, context }) => {
  assignCheck(obj, context);
  done(void 0, obj.context[obj.prop] ??= b);
});

// node_modules/@nyariv/sandboxjs/dist/esm/executor/ops/comparison.js
addOps(LispType.LargerThan, ({ done, a, b }) => done(void 0, a > b));
addOps(LispType.SmallerThan, ({ done, a, b }) => done(void 0, a < b));
addOps(LispType.LargerEqualThan, ({ done, a, b }) => done(void 0, a >= b));
addOps(LispType.SmallerEqualThan, ({ done, a, b }) => done(void 0, a <= b));
addOps(LispType.Equal, ({ done, a, b }) => done(void 0, a == b));
addOps(LispType.StrictEqual, ({ done, a, b }) => done(void 0, a === b));
addOps(LispType.NotEqual, ({ done, a, b }) => done(void 0, a != b));
addOps(LispType.StrictNotEqual, ({ done, a, b }) => done(void 0, a !== b));
addOps(LispType.And, ({ done, a, b }) => done(void 0, a && b));
addOps(LispType.Or, ({ done, a, b }) => done(void 0, a || b));
addOps(LispType.NullishCoalescing, ({ done, a, b }) => done(void 0, a ?? b));
addOps(LispType.BitAnd, ({ done, a, b }) => done(void 0, a & b));
addOps(LispType.BitOr, ({ done, a, b }) => done(void 0, a | b));
addOps(LispType.Plus, (params) => {
  const { done, a, b } = params;
  const result = a + b;
  if (typeof result === "string" && checkHaltExpectedTicks(params, BigInt(result.length))) return;
  done(void 0, result);
});
addOps(LispType.Minus, ({ done, a, b }) => done(void 0, a - b));
addOps(LispType.Divide, ({ done, a, b }) => done(void 0, a / b));
addOps(LispType.Power, ({ done, a, b }) => done(void 0, a ** b));
addOps(LispType.BitNegate, ({ done, a, b }) => done(void 0, a ^ b));
addOps(LispType.Multiply, ({ done, a, b }) => done(void 0, a * b));
addOps(LispType.Modulus, ({ done, a, b }) => done(void 0, a % b));
addOps(LispType.BitShiftLeft, ({ done, a, b }) => done(void 0, a << b));
addOps(LispType.BitShiftRight, ({ done, a, b }) => done(void 0, a >> b));
addOps(LispType.BitUnsignedShiftRight, ({ done, a, b }) => done(void 0, a >>> b));
addOps(LispType.Instanceof, ({ done, a, b }) => done(void 0, a instanceof b));
addOps(LispType.In, ({ done, a, b }) => done(void 0, a in b));

// node_modules/@nyariv/sandboxjs/dist/esm/executor/ops/variables.js
addOps(LispType.Return, ({ done, b }) => done(void 0, b));
addOps(LispType.Var, ({ done, a, b, scope, bobj, internal }) => {
  done(void 0, scope.declare(a, VarType.var, b, bobj?.isGlobal || false, internal));
});
addOps(LispType.Let, ({ done, a, b, scope, bobj, internal }) => {
  done(void 0, scope.declare(a, VarType.let, b, bobj?.isGlobal || false, internal));
});
addOps(LispType.Const, ({ done, a, b, scope, bobj, internal }) => {
  done(void 0, scope.declare(a, VarType.const, b, bobj?.isGlobal || false, internal));
});

// node_modules/@nyariv/sandboxjs/dist/esm/executor/ops/misc.js
addOps(LispType.Internal, ({ done, a, b, scope, bobj, internal }) => {
  if (!internal) throw new SandboxCapabilityError("Internal variables are not accessible");
  done(void 0, scope.declare(a, VarType.internal, b, bobj?.isGlobal || false, internal));
});
addOps(LispType.LoopAction, ({ done, a, b, context, statementLabels }) => {
  const label = normalizeStatementLabel(b);
  const target = findControlFlowTarget(statementLabels, a, label);
  if (target === null) throw new TypeError("Illegal continue statement");
  if (!target) throw new TypeError(label ? `Undefined label '${label}'` : "Illegal " + a + " statement");
  done(void 0, new ExecReturn(context.ctx.auditReport, void 0, false, {
    type: a,
    label
  }));
});
addOps(LispType.Throw, ({ done, b }) => {
  done(b);
});
addOps(LispType.None, ({ done }) => done());

// node_modules/@nyariv/sandboxjs/dist/esm/executor/ops/functions.js
addOps(LispType.ArrowFunction, ({ done, ticks, a, b, obj, context, scope, internal }) => {
  if (typeof obj[2] === "string" || obj[2] instanceof CodeString) if (context.allowJit && context.evalContext) obj[2] = b = context.evalContext.lispifyFunction(new CodeString(obj[2]), context.constants);
  else throw new SandboxCapabilityError("Unevaluated code detected, JIT not allowed");
  const argNames = a.slice(1);
  if (a[0]) done(void 0, createFunctionAsync(argNames, b, ticks, context, scope, void 0, internal));
  else done(void 0, createFunction(argNames, b, ticks, context, scope, void 0, internal));
});
addOps(LispType.Function, ({ done, ticks, a, b, obj, context, scope, internal }) => {
  if (typeof obj[2] === "string" || obj[2] instanceof CodeString) if (context.allowJit && context.evalContext) obj[2] = b = context.evalContext.lispifyFunction(new CodeString(obj[2]), context.constants, false, {
    generatorDepth: a[1] === LispType.True ? 1 : 0,
    asyncDepth: a[0] === LispType.True ? 1 : 0,
    lispDepth: 0
  });
  else throw new SandboxCapabilityError("Unevaluated code detected, JIT not allowed");
  const isAsync = a[0];
  const isGenerator = a[1];
  const name = a[2];
  const argNames = a.slice(3);
  let func;
  if (isAsync === LispType.True && isGenerator === LispType.True) func = createAsyncGeneratorFunction(argNames, b, ticks, context, scope, name, internal);
  else if (isGenerator === LispType.True) func = createGeneratorFunction(argNames, b, ticks, context, scope, name, internal);
  else if (isAsync === LispType.True) func = createFunctionAsync(argNames, b, ticks, context, scope, name, internal);
  else func = createFunction(argNames, b, ticks, context, scope, name, internal);
  if (name) scope.declare(name, VarType.var, func, false, internal);
  done(void 0, func);
});
addOps(LispType.InlineFunction, ({ done, ticks, a, b, obj, context, scope, internal }) => {
  if (typeof obj[2] === "string" || obj[2] instanceof CodeString) if (context.allowJit && context.evalContext) obj[2] = b = context.evalContext.lispifyFunction(new CodeString(obj[2]), context.constants, false, {
    generatorDepth: a[1] === LispType.True ? 1 : 0,
    asyncDepth: a[0] === LispType.True ? 1 : 0,
    lispDepth: 0
  });
  else throw new SandboxCapabilityError("Unevaluated code detected, JIT not allowed");
  const isAsync = a[0];
  const isGenerator = a[1];
  const name = a[2];
  const argNames = a.slice(3);
  if (name) scope = new Scope(scope, {});
  let func;
  if (isAsync === LispType.True && isGenerator === LispType.True) func = createAsyncGeneratorFunction(argNames, b, ticks, context, scope, name, internal);
  else if (isGenerator === LispType.True) func = createGeneratorFunction(argNames, b, ticks, context, scope, name, internal);
  else if (isAsync === LispType.True) func = createFunctionAsync(argNames, b, ticks, context, scope, name, internal);
  else func = createFunction(argNames, b, ticks, context, scope, name, internal);
  if (name) scope.declare(name, VarType.let, func, false, internal);
  done(void 0, func);
});

// node_modules/@nyariv/sandboxjs/dist/esm/executor/ops/control.js
addOps(LispType.Loop, ({ exec, done, ticks, a, b, context, scope, statementLabels, internal, generatorYield }) => {
  const [checkFirst, startInternal, getIterator, startStep, step, condition, beforeStep, isForAwait, label] = a;
  const loopStatementTargets = [...normalizeStatementLabels(label).map((loopLabel) => createLoopTarget(loopLabel, false)), createLoopTarget()];
  const loopTargets = addControlFlowTargets(statementLabels, loopStatementTargets);
  if (isForAwait === LispType.True && exec !== execAsync) {
    done(/* @__PURE__ */ new SyntaxError("for-await-of loops are only allowed inside async functions"));
    return;
  }
  let loop = true;
  const loopScope = new Scope(scope, {});
  const internalVars = { $$obj: void 0 };
  const interalScope = new Scope(loopScope, internalVars);
  if (exec === execAsync) (async () => {
    let ad;
    ad = asyncDone((d) => exec(ticks, startStep, loopScope, context, d, void 0, internal, generatorYield));
    internalVars["$$obj"] = (ad = asyncDone((d) => exec(ticks, getIterator, loopScope, context, d, void 0, internal, generatorYield))).isInstant === true ? ad.instant : (await ad.p).result;
    if (isForAwait === LispType.True) {
      const obj = internalVars["$$obj"];
      internalVars["$$obj"] = obj[Symbol.asyncIterator] ? obj[Symbol.asyncIterator]() : obj[Symbol.iterator] ? obj[Symbol.iterator]() : obj;
    }
    ad = asyncDone((d) => exec(ticks, startInternal, interalScope, context, d, void 0, internal, generatorYield));
    if (isForAwait === LispType.True) internalVars["$$next"] = await internalVars["$$next"];
    if (checkFirst) loop = (ad = asyncDone((d) => exec(ticks, condition, interalScope, context, d, void 0, internal, generatorYield))).isInstant === true ? ad.instant : (await ad.p).result;
    while (loop) {
      const iterScope = new Scope(interalScope, {});
      ad = asyncDone((d) => exec(ticks, beforeStep, iterScope, context, d, void 0, internal, generatorYield));
      ad.isInstant === true ? ad.instant : (await ad.p).result;
      const res = await executeTreeAsync(ticks, context, b, [iterScope], loopTargets, internal, generatorYield);
      if (res instanceof ExecReturn && res.returned) {
        done(void 0, res);
        return;
      }
      if (res instanceof ExecReturn && res.controlFlow) {
        if (!loopStatementTargets.some((target) => matchesControlFlowTarget(res.controlFlow, target))) {
          done(void 0, res);
          return;
        }
        if (res.breakLoop) break;
      }
      ad = asyncDone((d) => exec(ticks, step, interalScope, context, d, void 0, internal, generatorYield));
      if (isForAwait === LispType.True) internalVars["$$next"] = await internalVars["$$next"];
      loop = (ad = asyncDone((d) => exec(ticks, condition, interalScope, context, d, void 0, internal, generatorYield))).isInstant === true ? ad.instant : (await ad.p).result;
    }
    done();
  })().catch(done);
  else {
    syncDone((d) => exec(ticks, startStep, loopScope, context, d, void 0, internal, generatorYield));
    internalVars["$$obj"] = syncDone((d) => exec(ticks, getIterator, loopScope, context, d, void 0, internal, generatorYield)).result;
    syncDone((d) => exec(ticks, startInternal, interalScope, context, d, void 0, internal, generatorYield));
    if (checkFirst) loop = syncDone((d) => exec(ticks, condition, interalScope, context, d, void 0, internal, generatorYield)).result;
    while (loop) {
      const iterScope = new Scope(interalScope, {});
      syncDone((d) => exec(ticks, beforeStep, iterScope, context, d, void 0, internal, generatorYield));
      const res = executeTree(ticks, context, b, [iterScope], loopTargets, internal, generatorYield);
      if (res instanceof ExecReturn && res.returned) {
        done(void 0, res);
        return;
      }
      if (res instanceof ExecReturn && res.controlFlow) {
        if (!loopStatementTargets.some((target) => matchesControlFlowTarget(res.controlFlow, target))) {
          done(void 0, res);
          return;
        }
        if (res.breakLoop) break;
      }
      syncDone((d) => exec(ticks, step, interalScope, context, d, void 0, internal, generatorYield));
      loop = syncDone((d) => exec(ticks, condition, interalScope, context, d, void 0, internal, generatorYield)).result;
    }
    done();
  }
});
addOps(LispType.If, ({ exec, done, ticks, a, b, context, scope, statementLabels, internal, generatorYield }) => {
  exec(ticks, sanitizeProp(a, context) ? b.t : b.f, scope, context, done, statementLabels, internal, generatorYield);
});
addOps(LispType.InlineIf, ({ exec, done, ticks, a, b, context, scope, internal, generatorYield }) => {
  exec(ticks, sanitizeProp(a, context) ? b.t : b.f, scope, context, done, void 0, internal, generatorYield);
});
addOps(LispType.InlineIfCase, ({ done, a, b }) => done(void 0, new If(a, b)));
addOps(LispType.IfCase, ({ done, a, b }) => done(void 0, new If(a, b)));
addOps(LispType.Labeled, ({ exec, done, ticks, a, b, context, scope, statementLabels, internal, generatorYield }) => {
  const target = createLabeledStatementTarget(normalizeStatementLabel(a));
  exec(ticks, b, scope, context, (...args) => {
    if (args.length === 1) {
      done(args[0]);
      return;
    }
    const res = args[1];
    if (res instanceof ExecReturn && res.controlFlow && target) {
      if (matchesControlFlowTarget(res.controlFlow, target)) {
        done();
        return;
      }
    }
    done(void 0, res);
  }, addControlFlowTarget(statementLabels, target), internal, generatorYield);
});
addOps(LispType.Switch, ({ exec, done, ticks, a, b, context, scope, statementLabels, internal, generatorYield }) => {
  const switchTarget = createSwitchTarget();
  const switchTargets = addControlFlowTarget(statementLabels, switchTarget);
  exec(ticks, a, scope, context, (...args) => {
    if (args.length === 1) {
      done(args[0]);
      return;
    }
    let toTest = args[1];
    toTest = sanitizeProp(toTest, context);
    if (exec === execSync) {
      let res;
      let isTrue = false;
      for (const caseItem of b) if (isTrue || (isTrue = !caseItem[1] || toTest === sanitizeProp(syncDone((d) => exec(ticks, caseItem[1], scope, context, d, void 0, internal, generatorYield)).result, context))) {
        if (!caseItem[2]) continue;
        res = executeTree(ticks, context, caseItem[2], [scope], switchTargets, internal, generatorYield);
        if (res.controlFlow) {
          if (!matchesControlFlowTarget(res.controlFlow, switchTarget)) {
            done(void 0, res);
            return;
          }
          if (res.breakLoop) break;
        }
        if (res.returned) {
          done(void 0, res);
          return;
        }
        if (!caseItem[1]) break;
      }
      done();
    } else (async () => {
      let res;
      let isTrue = false;
      for (const caseItem of b) {
        let ad;
        if (isTrue || (isTrue = !caseItem[1] || toTest === sanitizeProp((ad = asyncDone((d) => exec(ticks, caseItem[1], scope, context, d, void 0, internal, generatorYield))).isInstant === true ? ad.instant : (await ad.p).result, context))) {
          if (!caseItem[2]) continue;
          res = await executeTreeAsync(ticks, context, caseItem[2], [scope], switchTargets, internal, generatorYield);
          if (res.controlFlow) {
            if (!matchesControlFlowTarget(res.controlFlow, switchTarget)) {
              done(void 0, res);
              return;
            }
            if (res.breakLoop) break;
          }
          if (res.returned) {
            done(void 0, res);
            return;
          }
          if (!caseItem[1]) break;
        }
      }
      done();
    })().catch(done);
  }, void 0, internal, generatorYield);
});
addOps(LispType.Try, ({ exec, done, ticks, a, b, context, scope, statementLabels, internal, generatorYield }) => {
  const [exception, catchBody, finallyBody] = b;
  executeTreeWithDone(exec, (...tryArgs) => {
    const tryHadError = tryArgs.length === 1;
    const tryError = tryHadError ? tryArgs[0] : void 0;
    const tryResult = !tryHadError && tryArgs.length > 1 ? tryArgs[1] : void 0;
    const executeFinallyAndComplete = (hadError, errorOrResult) => {
      if (finallyBody && finallyBody.length > 0) executeTreeWithDone(exec, (...finallyArgs) => {
        const finallyHadError = finallyArgs.length === 1;
        const finallyResult = !finallyHadError && finallyArgs.length > 1 ? finallyArgs[1] : void 0;
        if (finallyHadError) {
          done(finallyArgs[0]);
          return;
        }
        if (finallyResult instanceof ExecReturn && (finallyResult.returned || finallyResult.breakLoop || finallyResult.continueLoop)) {
          done(void 0, finallyResult);
          return;
        }
        if (hadError) done(errorOrResult);
        else if (errorOrResult instanceof ExecReturn) if (errorOrResult.returned || errorOrResult.breakLoop || errorOrResult.continueLoop) done(void 0, errorOrResult);
        else done();
        else done();
      }, ticks, context, finallyBody, [new Scope(scope, {})], statementLabels, internal, generatorYield);
      else if (hadError) done(errorOrResult);
      else if (errorOrResult instanceof ExecReturn) if (errorOrResult.returned || errorOrResult.breakLoop || errorOrResult.continueLoop) done(void 0, errorOrResult);
      else done();
      else done();
    };
    if (tryHadError && tryError instanceof SandboxError) {
      done(tryError);
      return;
    }
    if (tryHadError && catchBody && catchBody.length > 0) {
      const sc = {};
      if (exception) sc[exception] = tryError;
      executeTreeWithDone(exec, (...catchArgs) => {
        const catchHadError = catchArgs.length === 1;
        executeFinallyAndComplete(catchHadError, catchHadError ? catchArgs[0] : catchArgs.length > 1 ? catchArgs[1] : void 0);
      }, ticks, context, catchBody, [new Scope(scope, sc)], statementLabels, internal, generatorYield);
    } else executeFinallyAndComplete(tryHadError, tryHadError ? tryError : tryResult);
  }, ticks, context, a, [new Scope(scope)], statementLabels, internal, generatorYield);
});
addOps(LispType.Expression, ({ done, a }) => done(void 0, a.pop()));

// node_modules/@nyariv/sandboxjs/dist/esm/executor/executorUtils.js
var ExecReturn = class {
  constructor(auditReport, result, returned, controlFlow) {
    this.auditReport = auditReport;
    this.result = result;
    this.returned = returned;
    this.controlFlow = controlFlow;
  }
  get breakLoop() {
    return this.controlFlow?.type === "break";
  }
  get continueLoop() {
    return this.controlFlow?.type === "continue";
  }
};
var emptyControlFlowTargets = [];
function normalizeStatementLabel(label) {
  return label === void 0 || label === LispType.None ? void 0 : label;
}
function normalizeStatementLabels(label) {
  if (label === void 0 || label === LispType.None) return [];
  if (Array.isArray(label) && !isLisp(label)) return label.filter((item) => typeof item === "string");
  return [label];
}
function createLoopTarget(label, acceptsUnlabeled = true) {
  return {
    label,
    acceptsBreak: true,
    acceptsContinue: true,
    acceptsUnlabeledBreak: acceptsUnlabeled,
    acceptsUnlabeledContinue: acceptsUnlabeled
  };
}
function createSwitchTarget(label) {
  return {
    label,
    acceptsBreak: true,
    acceptsContinue: false,
    acceptsUnlabeledBreak: true,
    acceptsUnlabeledContinue: false
  };
}
function createLabeledStatementTarget(label) {
  if (!label) return void 0;
  return {
    label,
    acceptsBreak: true,
    acceptsContinue: false,
    acceptsUnlabeledBreak: false,
    acceptsUnlabeledContinue: false
  };
}
function addControlFlowTarget(controlFlowTargets, target) {
  if (!target) return controlFlowTargets;
  return [...controlFlowTargets || emptyControlFlowTargets, target];
}
function addControlFlowTargets(controlFlowTargets, targets) {
  return targets.reduce((currentTargets, target) => addControlFlowTarget(currentTargets, target), controlFlowTargets);
}
function matchesControlFlowTarget(signal, target) {
  if (signal.type === "continue") {
    if (!target.acceptsContinue) return false;
    return signal.label ? target.label === signal.label : target.acceptsUnlabeledContinue;
  }
  if (!target.acceptsBreak) return false;
  return signal.label ? target.label === signal.label : target.acceptsUnlabeledBreak;
}
function findControlFlowTarget(controlFlowTargets, type, label) {
  if (!controlFlowTargets) return void 0;
  for (let i = controlFlowTargets.length - 1; i >= 0; i--) {
    const target = controlFlowTargets[i];
    if (label) {
      if (target.label !== label) continue;
      if (type === "continue" ? target.acceptsContinue : target.acceptsBreak) return target;
      return null;
    }
    if (type === "continue" ? target.acceptsUnlabeledContinue : target.acceptsUnlabeledBreak) return target;
  }
}
function generateArgs(argNames, args) {
  const vars = {};
  argNames.forEach((arg, i) => {
    if (arg.startsWith("...")) vars[arg.substring(3)] = args.slice(i);
    else vars[arg] = args[i];
  });
  return vars;
}
function createFunction(argNames, parsed, ticks, context, scope, name, internal = false) {
  if (context.ctx.options.forbidFunctionCreation) throw new SandboxCapabilityError("Function creation is forbidden");
  let func;
  if (name === void 0) func = (...args) => {
    const vars = generateArgs(argNames, args);
    return executeTree(ticks, context, parsed, scope === void 0 ? [] : [new Scope(scope, vars)], void 0, internal).result;
  };
  else func = function sandboxedObject(...args) {
    const vars = generateArgs(argNames, args);
    return executeTree(ticks, context, parsed, scope === void 0 ? [] : [new Scope(scope, vars, this)], void 0, internal).result;
  };
  context.registerSandboxFunction(func);
  context.ctx.sandboxedFunctions.add(func);
  return func;
}
function createFunctionAsync(argNames, parsed, ticks, context, scope, name, internal = false) {
  if (context.ctx.options.forbidFunctionCreation) throw new SandboxCapabilityError("Function creation is forbidden");
  if (!context.ctx.prototypeWhitelist?.has(Promise.prototype)) throw new SandboxCapabilityError("Async/await not permitted");
  let func;
  if (name === void 0) func = async (...args) => {
    const vars = generateArgs(argNames, args);
    return (await executeTreeAsync(ticks, context, parsed, scope === void 0 ? [] : [new Scope(scope, vars)], void 0, internal)).result;
  };
  else func = async function sandboxedObject(...args) {
    const vars = generateArgs(argNames, args);
    return (await executeTreeAsync(ticks, context, parsed, scope === void 0 ? [] : [new Scope(scope, vars, this)], void 0, internal)).result;
  };
  context.registerSandboxFunction(func);
  context.ctx.sandboxedFunctions.add(func);
  return func;
}
var YieldValue = class {
  constructor(value, delegate) {
    this.value = value;
    this.delegate = delegate;
  }
};
var syncYieldPauseSentinel = Symbol("syncYieldPause");
function asIterableIterator(value) {
  const iterator = value?.[Symbol.iterator]?.() ?? value;
  if (!iterator || typeof iterator.next !== "function") throw new TypeError("yield* target is not iterable");
  if (typeof iterator[Symbol.iterator] === "function") return iterator;
  return {
    next: iterator.next.bind(iterator),
    throw: iterator.throw?.bind(iterator),
    return: iterator.return?.bind(iterator),
    [Symbol.iterator]() {
      return this;
    }
  };
}
function asAsyncIterableIterator(value) {
  const asyncIterator = value?.[Symbol.asyncIterator]?.();
  if (asyncIterator) return {
    next: asyncIterator.next.bind(asyncIterator),
    throw: asyncIterator.throw?.bind(asyncIterator),
    return: asyncIterator.return?.bind(asyncIterator),
    [Symbol.asyncIterator]() {
      return this;
    }
  };
  const iterator = asIterableIterator(value);
  return {
    async next(nextValue) {
      return iterator.next(nextValue);
    },
    async throw(err) {
      if (typeof iterator.throw === "function") return iterator.throw(err);
      throw err;
    },
    async return(valueToReturn) {
      if (typeof iterator.return === "function") return iterator.return(valueToReturn);
      return {
        value: valueToReturn,
        done: true
      };
    },
    [Symbol.asyncIterator]() {
      return this;
    }
  };
}
function* executeGenBody(ticks, tree, scope, context, statementLabels, internal) {
  if (!isLisp(tree) && Array.isArray(tree)) {
    const stmts = tree;
    if (stmts.length === 0 || stmts[0] === LispType.None) return new ExecReturn(context.ctx.auditReport, void 0, false);
    for (const stmt of stmts) {
      const res = yield* executeGenBody(ticks, stmt, scope, context, statementLabels, internal);
      if (res instanceof ExecReturn && (res.returned || res.controlFlow)) return res;
      if (isLisp(stmt) && stmt[0] === LispType.Return) return new ExecReturn(context.ctx.auditReport, res.result, true);
    }
    return new ExecReturn(context.ctx.auditReport, void 0, false);
  }
  const [op, a, b] = tree;
  switch (op) {
    case LispType.Yield: {
      const injected = yield sanitizeProp((yield* executeGenBody(ticks, a, scope, context, statementLabels, internal)).result, context);
      return new ExecReturn(context.ctx.auditReport, injected, false);
    }
    case LispType.YieldDelegate: {
      const result = yield* asIterableIterator(sanitizeProp((yield* executeGenBody(ticks, a, scope, context, statementLabels, internal)).result, context));
      return new ExecReturn(context.ctx.auditReport, result, false);
    }
    case LispType.If: {
      const condResult = yield* executeGenBody(ticks, a, scope, context, statementLabels, internal);
      const ifCase = syncDone((d) => execSync(ticks, b, scope, context, d, statementLabels, internal, void 0)).result;
      const branch = sanitizeProp(condResult.result, context) ? ifCase.t : ifCase.f;
      if (branch) return yield* executeGenBody(ticks, branch, scope, context, statementLabels, internal);
      return new ExecReturn(context.ctx.auditReport, void 0, false);
    }
    case LispType.Loop: {
      const [checkFirst, startInternal, getIterator, startStep, step, condition, beforeStep, isForAwait, label] = a;
      if (isForAwait === LispType.True) throw new SyntaxError("for-await-of loops are only allowed inside async functions");
      const loopStatementTargets = [...normalizeStatementLabels(label).map((loopLabel) => createLoopTarget(loopLabel, false)), createLoopTarget()];
      const loopTargets = addControlFlowTargets(statementLabels, loopStatementTargets);
      const loopScope = new Scope(scope, {});
      const internalVars = { $$obj: void 0 };
      const interalScope = new Scope(loopScope, internalVars);
      syncDone((d) => execSync(ticks, startStep, loopScope, context, d, void 0, internal, void 0));
      internalVars["$$obj"] = syncDone((d) => execSync(ticks, getIterator, loopScope, context, d, void 0, internal, void 0)).result;
      syncDone((d) => execSync(ticks, startInternal, interalScope, context, d, void 0, internal, void 0));
      let loop = true;
      if (checkFirst) loop = syncDone((d) => execSync(ticks, condition, interalScope, context, d, void 0, internal, void 0)).result;
      while (loop) {
        const iterScope = new Scope(interalScope, {});
        syncDone((d) => execSync(ticks, beforeStep, iterScope, context, d, void 0, internal, void 0));
        const res = yield* executeGenBody(ticks, b, iterScope, context, loopTargets, internal);
        if (res.returned) return res;
        if (res.controlFlow) {
          if (!loopStatementTargets.some((t) => matchesControlFlowTarget(res.controlFlow, t))) return res;
          if (res.breakLoop) break;
        }
        syncDone((d) => execSync(ticks, step, interalScope, context, d, void 0, internal, void 0));
        loop = syncDone((d) => execSync(ticks, condition, interalScope, context, d, void 0, internal, void 0)).result;
      }
      return new ExecReturn(context.ctx.auditReport, void 0, false);
    }
    case LispType.Try: {
      const [exception, catchBody, finallyBody] = b;
      let result;
      let finalOverride;
      try {
        result = yield* executeGenBody(ticks, a, scope, context, statementLabels, internal);
      } catch (e) {
        if (exception && catchBody?.length > 0) result = yield* executeGenBody(ticks, catchBody, new Scope(scope, { [exception]: e }), context, statementLabels, internal);
        else throw e;
      } finally {
        if (finallyBody?.length > 0) {
          const fr = yield* executeGenBody(ticks, finallyBody, scope, context, statementLabels, internal);
          if (fr.returned || fr.controlFlow) finalOverride = fr;
        }
      }
      if (finalOverride) return finalOverride;
      return result;
    }
    case LispType.Labeled: {
      const target = createLabeledStatementTarget(normalizeStatementLabel(a));
      const res = yield* executeGenBody(ticks, b, scope, context, addControlFlowTargets(statementLabels, target ? [target] : []), internal);
      if (res.controlFlow && target && matchesControlFlowTarget(res.controlFlow, target)) return new ExecReturn(context.ctx.auditReport, res.result, false);
      return res;
    }
    default: {
      let completedYields = 0;
      const yieldResults = [];
      while (true) {
        let currentYieldIdx = 0;
        let capturedValue = void 0;
        let capturedDelegate = false;
        let yielded = false;
        const captureYieldFn = (yv, continueDone) => {
          if (currentYieldIdx < completedYields) {
            continueDone(void 0, yieldResults[currentYieldIdx]);
            currentYieldIdx++;
            return;
          }
          capturedValue = yv.value;
          capturedDelegate = yv.delegate;
          yielded = true;
          currentYieldIdx++;
          throw syncYieldPauseSentinel;
        };
        try {
          const result = syncDone((d) => execSync(ticks, tree, scope, context, d, statementLabels, internal, captureYieldFn)).result;
          if (result instanceof ExecReturn) return result;
          return new ExecReturn(context.ctx.auditReport, result, false);
        } catch (e) {
          if (!yielded || e !== syncYieldPauseSentinel) throw e;
          const resumedValue = capturedDelegate ? yield* asIterableIterator(capturedValue) : yield capturedValue;
          yieldResults.push(resumedValue);
          completedYields++;
        }
      }
    }
  }
}
function createGeneratorFunction(argNames, parsed, ticks, context, scope, name, internal = false) {
  if (context.ctx.options.forbidFunctionCreation) throw new SandboxCapabilityError("Function creation is forbidden");
  const makeGen = (thisArg, args) => {
    const vars = generateArgs(argNames, args);
    const executionGen = executeGenBody(ticks, parsed, scope === void 0 ? new Scope(null, vars, thisArg) : new Scope(scope, vars, thisArg), context, void 0, internal);
    let isDone = false;
    function drive(action) {
      if (isDone) return {
        value: void 0,
        done: true
      };
      try {
        const r = action();
        if (r.done) {
          isDone = true;
          return {
            value: r.value instanceof ExecReturn ? r.value.result : r.value,
            done: true
          };
        }
        return {
          value: r.value,
          done: false
        };
      } catch (e) {
        isDone = true;
        throw e;
      }
    }
    return {
      next(value) {
        return drive(() => executionGen.next(value));
      },
      return(value) {
        return drive(() => executionGen.return(value));
      },
      throw(err) {
        return drive(() => executionGen.throw(err));
      },
      [Symbol.iterator]() {
        return this;
      }
    };
  };
  const func = function sandboxedObject(...args) {
    return makeGen(this, args);
  };
  Object.setPrototypeOf(func, GeneratorFunction.prototype);
  context.registerSandboxFunction(func);
  context.ctx.sandboxedFunctions.add(func);
  return func;
}
function createAsyncGeneratorFunction(argNames, parsed, ticks, context, scope, name, internal = false) {
  if (context.ctx.options.forbidFunctionCreation) throw new SandboxCapabilityError("Function creation is forbidden");
  if (!context.ctx.prototypeWhitelist?.has(Promise.prototype)) throw new SandboxCapabilityError("Async/await not permitted");
  const makeGen = (thisArg, args) => {
    const vars = generateArgs(argNames, args);
    const genScope = scope === void 0 ? [new Scope(null, vars, thisArg)] : [new Scope(scope, vars, thisArg)];
    return async function* sandboxedAsyncGenerator() {
      const yieldQueue = [];
      let resolveYield = null;
      const yieldFn = (yv, continueDone) => {
        yieldQueue.push({
          yieldValue: yv,
          continueDone
        });
        if (resolveYield) {
          resolveYield();
          resolveYield = null;
        }
      };
      const bodyPromise = executeTreeAsync(ticks, context, parsed, genScope, void 0, internal, yieldFn);
      let bodyDone = false;
      let bodyResult;
      let bodyError;
      bodyPromise.then((r) => {
        bodyDone = true;
        bodyResult = r;
        resolveYield?.();
      }, (e) => {
        bodyDone = true;
        bodyError = e;
        resolveYield?.();
      });
      while (true) {
        if (yieldQueue.length === 0 && !bodyDone) await new Promise((res) => {
          resolveYield = res;
        });
        while (yieldQueue.length > 0) {
          const { yieldValue, continueDone } = yieldQueue.shift();
          try {
            const resumedValue = yieldValue.delegate ? yield* asAsyncIterableIterator(yieldValue.value) : yield yieldValue.value;
            continueDone?.(void 0, resumedValue);
          } catch (err) {
            continueDone?.(err);
          }
        }
        if (bodyDone) break;
      }
      if (bodyError !== void 0) throw bodyError;
      return bodyResult?.result;
    }();
  };
  const func = function sandboxedObject(...args) {
    return makeGen(this, args);
  };
  Object.setPrototypeOf(func, AsyncGeneratorFunction.prototype);
  context.registerSandboxFunction(func);
  context.ctx.sandboxedFunctions.add(func);
  return func;
}
function assignCheck(obj, context, op = "assign") {
  if (obj.context === void 0) throw new ReferenceError(`Cannot ${op} value to undefined.`);
  if (obj.isConst) throw new TypeError(`Assignment to constant variable.`);
  if (obj.isGlobal) throw new SandboxAccessError(`Cannot ${op} property '${obj.prop.toString()}' of a global object`);
  if (obj.context === null) throw new TypeError("Cannot set properties of null");
  if (typeof obj.context[obj.prop] === "function" && !hasOwnProperty(obj.context, obj.prop)) throw new SandboxAccessError(`Override prototype property '${obj.prop.toString()}' not allowed`);
  if (op === "delete") {
    if (hasOwnProperty(obj.context, obj.prop)) {
      context.changeSubscriptions.get(obj.context)?.forEach((cb) => cb({
        type: "delete",
        prop: obj.prop.toString()
      }));
      context.changeSubscriptionsGlobal.get(obj.context)?.forEach((cb) => cb({
        type: "delete",
        prop: obj.prop.toString()
      }));
    }
  } else if (hasOwnProperty(obj.context, obj.prop)) {
    context.setSubscriptions.get(obj.context)?.get(obj.prop.toString())?.forEach((cb) => cb({ type: "replace" }));
    context.setSubscriptionsGlobal.get(obj.context)?.get(obj.prop.toString())?.forEach((cb) => cb({ type: "replace" }));
  } else {
    context.changeSubscriptions.get(obj.context)?.forEach((cb) => cb({
      type: "create",
      prop: obj.prop.toString()
    }));
    context.changeSubscriptionsGlobal.get(obj.context)?.forEach((cb) => cb({
      type: "create",
      prop: obj.prop.toString()
    }));
  }
}
var arrayChange = /* @__PURE__ */ new Set([
  [].push,
  [].pop,
  [].shift,
  [].unshift,
  [].splice,
  [].reverse,
  [].sort,
  [].copyWithin
]);
var KeyVal = class {
  constructor(key, val) {
    this.key = key;
    this.val = val;
  }
};
var SpreadObject = class {
  constructor(item) {
    this.item = item;
  }
};
var SpreadArray = class {
  constructor(item) {
    this.item = item;
  }
};
var ArrayHole = class {
};
var If = class {
  constructor(t, f, label) {
    this.t = t;
    this.f = f;
    this.label = label;
  }
};
var literalRegex = /(\$\$)*(\$)?\${(\d+)}/g;
var prorptyKeyTypes = [
  "string",
  "number",
  "symbol"
];
function isPropertyKey(val) {
  return prorptyKeyTypes.includes(typeof val);
}
function hasPossibleProperties(val) {
  return val !== null && val !== void 0;
}
function execMany(ticks, exec, tree, done, scope, context, statementLabels, internal, generatorYield) {
  if (exec === execSync) _execManySync(ticks, tree, done, scope, context, statementLabels, internal, generatorYield);
  else _execManyAsync(ticks, tree, done, scope, context, statementLabels, internal, generatorYield).catch(done);
}
function _execManySync(ticks, tree, done, scope, context, statementLabels, internal, generatorYield) {
  const ret = [];
  for (let i = 0; i < tree.length; i++) {
    let res = syncDone((d) => execSync(ticks, tree[i], scope, context, d, statementLabels, internal, generatorYield)).result;
    if (res instanceof ExecReturn && (res.returned || res.breakLoop || res.continueLoop)) {
      done(void 0, res);
      return;
    }
    if (isLisp(tree[i]) && tree[i][0] === LispType.Return) {
      done(void 0, new ExecReturn(context.ctx.auditReport, res, true));
      return;
    }
    ret.push(res);
  }
  done(void 0, ret);
}
async function _execManyAsync(ticks, tree, done, scope, context, statementLabels, internal, generatorYield) {
  const ret = [];
  for (let i = 0; i < tree.length; i++) {
    let res;
    try {
      let ad;
      res = (ad = asyncDone((d) => execAsync(ticks, tree[i], scope, context, d, statementLabels, internal, generatorYield))).isInstant === true ? ad.instant : (await ad.p).result;
    } catch (e) {
      done(e);
      return;
    }
    if (res instanceof ExecReturn && (res.returned || res.breakLoop || res.continueLoop)) {
      done(void 0, res);
      return;
    }
    if (isLisp(tree[i]) && tree[i][0] === LispType.Return) {
      done(void 0, new ExecReturn(context.ctx.auditReport, res, true));
      return;
    }
    ret.push(res);
  }
  done(void 0, ret);
}
function asyncDone(callback) {
  let isInstant = false;
  let instant;
  const p = new Promise((resolve, reject) => {
    callback((...args) => {
      if (args.length === 1) reject(args[0]);
      else {
        isInstant = true;
        instant = args[1];
        resolve({ result: args[1] });
      }
    });
  });
  return {
    isInstant,
    instant,
    p
  };
}
function syncDone(callback) {
  let result;
  let err;
  callback((...args) => {
    err = args.length === 1 ? { error: args[0] } : void 0;
    result = args[1];
  });
  if (err) throw err.error;
  return { result };
}
async function execAsync(ticks, tree, scope, context, doneOriginal, statementLabels, internal, generatorYield) {
  let done = doneOriginal;
  const p = new Promise((resolve) => {
    done = (...args) => {
      doneOriginal(...args);
      resolve();
    };
  });
  if (!_execNoneRecurse(ticks, tree, scope, context, done, true, statementLabels, internal, generatorYield) && isLisp(tree)) {
    let op = tree[0];
    let obj;
    try {
      let ad;
      obj = (ad = asyncDone((d) => execAsync(ticks, tree[1], scope, context, d, statementLabels, internal, generatorYield))).isInstant === true ? ad.instant : (await ad.p).result;
    } catch (e) {
      done(e);
      return;
    }
    let a = obj;
    try {
      a = obj instanceof Prop ? obj.get(context) : obj;
    } catch (e) {
      done(e);
      return;
    }
    if (op === LispType.PropOptional || op === LispType.CallOptional) {
      if (a === void 0 || a === null) {
        done(void 0, optional);
        return;
      }
      op = op === LispType.PropOptional ? LispType.Prop : LispType.Call;
    }
    if (a === optional) if (op === LispType.Prop || op === LispType.Call) {
      done(void 0, a);
      return;
    } else a = void 0;
    if (op === LispType.NullishCoalescing && a !== void 0 && a !== null) {
      done(void 0, a);
      return;
    }
    let bobj;
    try {
      let ad;
      bobj = (ad = asyncDone((d) => execAsync(ticks, tree[2], scope, context, d, statementLabels, internal, generatorYield))).isInstant === true ? ad.instant : (await ad.p).result;
    } catch (e) {
      done(e);
      return;
    }
    let b = bobj;
    try {
      b = bobj instanceof Prop ? bobj.get(context) : bobj;
    } catch (e) {
      done(e);
      return;
    }
    if (b === optional) b = void 0;
    performOp({
      op,
      exec: execAsync,
      done,
      ticks,
      a,
      b,
      obj,
      context,
      scope,
      bobj,
      statementLabels,
      internal,
      generatorYield,
      tree
    });
  }
  await p;
}
function execSync(ticks, tree, scope, context, done, statementLabels, internal, generatorYield) {
  if (!_execNoneRecurse(ticks, tree, scope, context, done, false, statementLabels, internal, generatorYield) && isLisp(tree)) {
    let op = tree[0];
    let obj = syncDone((d) => execSync(ticks, tree[1], scope, context, d, statementLabels, internal, generatorYield)).result;
    let a = obj instanceof Prop ? obj.get(context) : obj;
    if (op === LispType.PropOptional || op === LispType.CallOptional) {
      if (a === void 0 || a === null) {
        done(void 0, optional);
        return;
      }
      op = op === LispType.PropOptional ? LispType.Prop : LispType.Call;
    }
    if (a === optional) if (op === LispType.Prop || op === LispType.Call) {
      done(void 0, a);
      return;
    } else a = void 0;
    if (op === LispType.NullishCoalescing && a !== void 0 && a !== null) {
      done(void 0, a);
      return;
    }
    let bobj = syncDone((d) => execSync(ticks, tree[2], scope, context, d, statementLabels, internal, generatorYield)).result;
    let b = bobj instanceof Prop ? bobj.get(context) : bobj;
    if (b === optional) b = void 0;
    performOp({
      op,
      exec: execSync,
      done,
      ticks,
      a,
      b,
      obj,
      context,
      scope,
      bobj,
      statementLabels,
      internal,
      generatorYield,
      tree
    });
  }
}
function checkHaltExpectedTicks(params, expectTicks = 0n) {
  const sandbox = params.context.ctx.sandbox;
  const { ticks, scope, context } = params;
  if (sandbox.halted) {
    const sub = sandbox.subscribeResume(() => {
      sub.unsubscribe();
      performOp(params, false);
    });
    return true;
  } else if (ticks.tickLimit !== void 0 && ticks.tickLimit <= ticks.ticks + expectTicks) {
    const error = new SandboxExecutionQuotaExceededError("Execution quota exceeded");
    if (context.ctx.options.haltOnSandboxError) {
      const sub = sandbox.subscribeResume(() => {
        sub.unsubscribe();
        performOp(params);
      });
      sandbox.haltExecution({
        type: "error",
        error,
        ticks,
        scope,
        context
      });
    } else params.done(error);
    return true;
  } else if (ticks.nextYield && ticks.ticks > ticks.nextYield) {
    const sub = sandbox.subscribeResume(() => {
      sub.unsubscribe();
      performOp(params, false);
    });
    ticks.nextYield += NON_BLOCKING_THRESHOLD;
    sandbox.haltExecution({ type: "yield" });
    setTimeout(() => sandbox.resumeExecution());
    return true;
  }
  ticks.ticks += expectTicks;
  return false;
}
function performOp(params, count = true) {
  const { done, op, ticks, context, scope } = params;
  if (count) ticks.ticks++;
  const sandbox = context.ctx.sandbox;
  try {
    if (checkHaltExpectedTicks(params)) return;
    const o = ops.get(op);
    if (o === void 0) {
      done(new SandboxExecutionTreeError("Unknown operator: " + op));
      return;
    }
    o(params);
  } catch (err) {
    if (context.ctx.options.haltOnSandboxError && err instanceof SandboxError) {
      const sub = sandbox.subscribeResume(() => {
        sub.unsubscribe();
        done(err);
      });
      sandbox.haltExecution({
        type: "error",
        error: err,
        ticks,
        scope,
        context
      });
    } else done(err);
  }
}
var unexecTypes = /* @__PURE__ */ new Set([
  LispType.ArrowFunction,
  LispType.Function,
  LispType.InlineFunction,
  LispType.Loop,
  LispType.Try,
  LispType.Switch,
  LispType.IfCase,
  LispType.InlineIfCase,
  LispType.Labeled,
  LispType.Typeof
]);
function _execNoneRecurse(ticks, tree, scope, context, done, isAsync, statementLabels, internal, generatorYield) {
  const exec = isAsync ? execAsync : execSync;
  if (tree instanceof Prop) done(void 0, tree.get(context));
  else if (tree === optional) done();
  else if (Array.isArray(tree) && !isLisp(tree)) if (tree[0] === LispType.None) done();
  else execMany(ticks, exec, tree, done, scope, context, statementLabels, internal, generatorYield);
  else if (!isLisp(tree)) done(void 0, tree);
  else if (tree[0] === LispType.Block) execMany(ticks, exec, tree[1], done, new Scope(scope), context, statementLabels, internal, generatorYield);
  else if (tree[0] === LispType.InternalBlock) execMany(ticks, exec, tree[1], done, scope, context, statementLabels, true, generatorYield);
  else if (tree[0] === LispType.Await) if (!isAsync) done(/* @__PURE__ */ new SyntaxError("Illegal use of 'await', must be inside async function"));
  else if (context.ctx.prototypeWhitelist?.has(Promise.prototype)) execAsync(ticks, tree[1], scope, context, async (...args) => {
    if (args.length === 1) done(args[0]);
    else try {
      done(void 0, await sanitizeProp(args[1], context));
    } catch (err) {
      done(err);
    }
  }, statementLabels, internal, generatorYield).catch(done);
  else done(new SandboxCapabilityError("Async/await is not permitted"));
  else if (tree[0] === LispType.Yield || tree[0] === LispType.YieldDelegate) {
    const yieldFn = generatorYield;
    if (!yieldFn) {
      done(/* @__PURE__ */ new SyntaxError("Illegal use of 'yield', must be inside a generator function"));
      return true;
    }
    const isDelegate = tree[0] === LispType.YieldDelegate;
    if (isAsync) execAsync(ticks, tree[1], scope, context, async (...args) => {
      if (args.length === 1) {
        done(args[0]);
        return;
      }
      try {
        yieldFn(new YieldValue(await sanitizeProp(args[1], context), isDelegate), done);
      } catch (err) {
        done(err);
      }
    }, statementLabels, internal, generatorYield).catch(done);
    else try {
      const val = syncDone((d) => execSync(ticks, tree[1], scope, context, d, statementLabels, internal, generatorYield)).result;
      yieldFn(new YieldValue(sanitizeProp(val, context), isDelegate), done);
    } catch (err) {
      if (err === syncYieldPauseSentinel) throw err;
      done(err);
    }
  } else if (unexecTypes.has(tree[0])) performOp({
    op: tree[0],
    exec,
    done,
    ticks,
    a: tree[1],
    b: tree[2],
    obj: tree,
    tree,
    context,
    scope,
    bobj: void 0,
    statementLabels,
    internal,
    generatorYield
  });
  else return false;
  return true;
}
function executeTree(ticks, context, executionTree, scopes = [], statementLabels, internal, generatorYield) {
  return syncDone((done) => executeTreeWithDone(execSync, done, ticks, context, executionTree, scopes, statementLabels, internal, generatorYield)).result;
}
async function executeTreeAsync(ticks, context, executionTree, scopes = [], statementLabels, internal, generatorYield) {
  let ad;
  return (ad = asyncDone((done) => executeTreeWithDone(execAsync, done, ticks, context, executionTree, scopes, statementLabels, internal, generatorYield))).isInstant === true ? ad.instant : (await ad.p).result;
}
function executeTreeWithDone(exec, done, ticks, context, executionTree, scopes = [], statementLabels, internal, generatorYield) {
  if (!executionTree) {
    done();
    return;
  }
  if (!(executionTree instanceof Array)) throw new SyntaxError("Bad execution tree");
  let scope = context.ctx.globalScope;
  let s;
  while (s = scopes.shift()) {
    if (typeof s !== "object") continue;
    if (s instanceof Scope) scope = s;
    else scope = new Scope(scope, s, s instanceof LocalScope ? void 0 : null);
  }
  if (context.ctx.options.audit && !context.ctx.auditReport) context.ctx.auditReport = {
    globalsAccess: /* @__PURE__ */ new Set(),
    prototypeAccess: {}
  };
  if (exec === execSync) _executeWithDoneSync(done, ticks, context, executionTree, scope, statementLabels, internal, generatorYield);
  else _executeWithDoneAsync(done, ticks, context, executionTree, scope, statementLabels, internal, generatorYield).catch(done);
}
function _executeWithDoneSync(done, ticks, context, executionTree, scope, statementLabels, internal, generatorYield) {
  if (!(executionTree instanceof Array)) throw new SyntaxError("Bad execution tree");
  let i = 0;
  for (i = 0; i < executionTree.length; i++) {
    let res;
    let err;
    const current = executionTree[i];
    try {
      execSync(ticks, current, scope, context, (...args) => {
        if (args.length === 1) err = { error: args[0] };
        else res = args[1];
      }, statementLabels, internal, generatorYield);
    } catch (e) {
      err = { error: e };
    }
    if (err) {
      done(err.error);
      return;
    }
    if (res instanceof ExecReturn) {
      done(void 0, res);
      return;
    }
    if (isLisp(current) && current[0] === LispType.Return) {
      done(void 0, new ExecReturn(context.ctx.auditReport, res, true));
      return;
    }
  }
  done(void 0, new ExecReturn(context.ctx.auditReport, void 0, false));
}
async function _executeWithDoneAsync(done, ticks, context, executionTree, scope, statementLabels, internal, generatorYield) {
  if (!(executionTree instanceof Array)) throw new SyntaxError("Bad execution tree");
  let i = 0;
  for (i = 0; i < executionTree.length; i++) {
    let res;
    let err;
    const current = executionTree[i];
    try {
      await execAsync(ticks, current, scope, context, (...args) => {
        if (args.length === 1) err = { error: args[0] };
        else res = args[1];
      }, statementLabels, internal, generatorYield);
    } catch (e) {
      err = { error: e };
    }
    if (err) {
      done(err.error);
      return;
    }
    if (res instanceof ExecReturn) {
      done(void 0, res);
      return;
    }
    if (isLisp(current) && current[0] === LispType.Return) {
      done(void 0, new ExecReturn(context.ctx.auditReport, res, true));
      return;
    }
  }
  done(void 0, new ExecReturn(context.ctx.auditReport, void 0, false));
}

// node_modules/@nyariv/sandboxjs/dist/esm/eval/index.js
function createEvalContext() {
  return {
    sandboxFunction,
    sandboxAsyncFunction,
    sandboxGeneratorFunction,
    sandboxAsyncGeneratorFunction,
    sandboxedSymbol,
    sandboxedEval,
    sandboxedSetTimeout,
    sandboxedSetInterval,
    sandboxedClearTimeout,
    sandboxedClearInterval,
    lispifyFunction
  };
}
function sandboxedSymbol(context) {
  return getSandboxSymbolCtor(context.ctx.sandboxSymbols);
}
function SB() {
}
function sandboxFunction(context) {
  SandboxFunction.prototype = SB.prototype;
  return SandboxFunction;
  function SandboxFunction(...params) {
    const parsed = parse(params.pop() || "", false, false, context.ctx.options.maxParserRecursionDepth);
    return createFunction(params, parsed.tree, context.ctx.ticks, {
      ...context,
      constants: parsed.constants,
      tree: parsed.tree
    }, void 0, "anonymous");
  }
}
function SAF() {
}
function sandboxAsyncFunction(context) {
  SandboxAsyncFunction.prototype = SAF.prototype;
  return SandboxAsyncFunction;
  function SandboxAsyncFunction(...params) {
    const parsed = parse(params.pop() || "", false, false, context.ctx.options.maxParserRecursionDepth);
    return createFunctionAsync(params, parsed.tree, context.ctx.ticks, {
      ...context,
      constants: parsed.constants,
      tree: parsed.tree
    }, void 0, "anonymous");
  }
}
function SGF() {
}
function sandboxGeneratorFunction(context) {
  SandboxGeneratorFunction.prototype = SGF.prototype;
  return SandboxGeneratorFunction;
  function SandboxGeneratorFunction(...params) {
    const parsed = parse(params.pop() || "", false, false, context.ctx.options.maxParserRecursionDepth);
    return createGeneratorFunction(params, parsed.tree, context.ctx.ticks, {
      ...context,
      constants: parsed.constants,
      tree: parsed.tree
    }, void 0, "anonymous");
  }
}
function SAGF() {
}
function sandboxAsyncGeneratorFunction(context) {
  SandboxAsyncGeneratorFunction.prototype = SAGF.prototype;
  return SandboxAsyncGeneratorFunction;
  function SandboxAsyncGeneratorFunction(...params) {
    const parsed = parse(params.pop() || "", false, false, context.ctx.options.maxParserRecursionDepth);
    return createAsyncGeneratorFunction(params, parsed.tree, context.ctx.ticks, {
      ...context,
      constants: parsed.constants,
      tree: parsed.tree
    }, void 0, "anonymous");
  }
}
function SE() {
}
function sandboxedEval(func, context) {
  sandboxEval.prototype = SE.prototype;
  return sandboxEval;
  function sandboxEval(code) {
    const parsed = parse(code, false, false, context.ctx.options.maxParserRecursionDepth);
    const tree = wrapLastStatementInReturn(parsed.tree);
    return createFunction([], tree, context.ctx.ticks, {
      ...context,
      constants: parsed.constants,
      tree
    }, void 0, "anonymous")();
  }
}
function wrapLastStatementInReturn(tree) {
  if (tree.length === 0) return tree;
  const newTree = [...tree];
  const lastIndex = newTree.length - 1;
  const lastStmt = newTree[lastIndex];
  if (Array.isArray(lastStmt) && lastStmt.length >= 1) {
    const op = lastStmt[0];
    if (op === LispType.Return || op === LispType.Throw) return newTree;
    if ([
      LispType.Let,
      LispType.Const,
      LispType.Var,
      LispType.Function,
      LispType.If,
      LispType.Loop,
      LispType.Try,
      LispType.Switch,
      LispType.InternalBlock,
      LispType.Expression
    ].includes(op)) return newTree;
    newTree[lastIndex] = [
      LispType.Return,
      LispType.None,
      lastStmt
    ];
  }
  return newTree;
}
function sST() {
}
function sandboxedSetTimeout(func, context) {
  sandboxSetTimeout.prototype = sST.prototype;
  return sandboxSetTimeout;
  function sandboxSetTimeout(handler, timeout, ...args) {
    const sandbox = context.ctx.sandbox;
    const exec = (...a) => {
      const h = typeof handler === "string" ? func(handler) : handler;
      haltsub.unsubscribe();
      contsub.unsubscribe();
      sandbox.setTimeoutHandles.delete(sandBoxhandle);
      return h(...a);
    };
    const sandBoxhandle = ++sandbox.timeoutHandleCounter;
    let start = Date.now();
    let handle = setTimeout(exec, timeout, ...args);
    let elapsed = 0;
    const haltsub = sandbox.subscribeHalt(() => {
      elapsed = Date.now() - start + elapsed;
      clearTimeout(handle);
    });
    const contsub = sandbox.subscribeResume(() => {
      start = Date.now();
      const remaining = Math.floor((timeout || 0) - elapsed);
      handle = setTimeout(exec, remaining, ...args);
      sandbox.setTimeoutHandles.set(sandBoxhandle, {
        handle,
        haltsub,
        contsub
      });
    });
    sandbox.setTimeoutHandles.set(sandBoxhandle, {
      handle,
      haltsub,
      contsub
    });
    return sandBoxhandle;
  }
}
function sCT() {
}
function sandboxedClearTimeout(context) {
  sandboxClearTimeout.prototype = sCT.prototype;
  return sandboxClearTimeout;
  function sandboxClearTimeout(handle) {
    const sandbox = context.ctx.sandbox;
    const timeoutHandle = sandbox.setTimeoutHandles.get(handle);
    if (timeoutHandle) {
      clearTimeout(timeoutHandle.handle);
      timeoutHandle.haltsub.unsubscribe();
      timeoutHandle.contsub.unsubscribe();
      sandbox.setTimeoutHandles.delete(handle);
    }
  }
}
function sCI() {
}
function sandboxedClearInterval(context) {
  sandboxClearInterval.prototype = sCI.prototype;
  return sandboxClearInterval;
  function sandboxClearInterval(handle) {
    const sandbox = context.ctx.sandbox;
    const intervalHandle = sandbox.setIntervalHandles.get(handle);
    if (intervalHandle) {
      clearInterval(intervalHandle.handle);
      clearTimeout(intervalHandle.handle);
      intervalHandle.haltsub.unsubscribe();
      intervalHandle.contsub.unsubscribe();
      sandbox.setIntervalHandles.delete(handle);
    }
  }
}
function sSI() {
}
function sandboxedSetInterval(func, context) {
  sandboxSetInterval.prototype = sSI.prototype;
  return sandboxSetInterval;
  function sandboxSetInterval(handler, timeout, ...args) {
    const sandbox = context.ctx.sandbox;
    const h = typeof handler === "string" ? func(handler) : handler;
    const exec = (...a) => {
      start = Date.now();
      elapsed = 0;
      return h(...a);
    };
    const sandBoxhandle = ++sandbox.timeoutHandleCounter;
    let start = Date.now();
    let handle = setInterval(exec, timeout, ...args);
    let elapsed = 0;
    const haltsub = sandbox.subscribeHalt(() => {
      elapsed = Date.now() - start + elapsed;
      clearInterval(handle);
      clearTimeout(handle);
    });
    const contsub = sandbox.subscribeResume(() => {
      start = Date.now();
      handle = setTimeout(() => {
        start = Date.now();
        elapsed = 0;
        handle = setInterval(exec, timeout, ...args);
        handlObj.handle = handle;
        exec(...args);
      }, Math.floor((timeout || 0) - elapsed), ...args);
      handlObj.handle = handle;
    });
    const handlObj = {
      handle,
      haltsub,
      contsub
    };
    sandbox.setIntervalHandles.set(sandBoxhandle, handlObj);
    return sandBoxhandle;
  }
}

// node_modules/@nyariv/sandboxjs/dist/esm/SandboxExec.js
function subscribeSet(obj, name, callback, context) {
  const names = context.setSubscriptions.get(obj) || /* @__PURE__ */ new Map();
  context.setSubscriptions.set(obj, names);
  const callbacks = names.get(name) || /* @__PURE__ */ new Set();
  names.set(name, callbacks);
  callbacks.add(callback);
  let changeCbs;
  const val = obj[name];
  if (val instanceof Object) {
    changeCbs = context.changeSubscriptions.get(val) || /* @__PURE__ */ new Set();
    changeCbs.add(callback);
    context.changeSubscriptions.set(val, changeCbs);
  }
  return { unsubscribe: () => {
    callbacks.delete(callback);
    changeCbs?.delete(callback);
  } };
}
var SandboxExec = class SandboxExec2 {
  constructor(options, evalContext) {
    this.evalContext = evalContext;
    this.setSubscriptions = /* @__PURE__ */ new WeakMap();
    this.changeSubscriptions = /* @__PURE__ */ new WeakMap();
    this.sandboxFunctions = /* @__PURE__ */ new WeakMap();
    this.haltSubscriptions = /* @__PURE__ */ new Set();
    this.resumeSubscriptions = /* @__PURE__ */ new Set();
    this.halted = false;
    this.timeoutHandleCounter = 0;
    this.setTimeoutHandles = /* @__PURE__ */ new Map();
    this.setIntervalHandles = /* @__PURE__ */ new Map();
    const opt = Object.assign({
      audit: false,
      forbidFunctionCalls: false,
      forbidFunctionCreation: false,
      globals: SandboxExec2.SAFE_GLOBALS,
      symbolWhitelist: SandboxExec2.SAFE_SYMBOLS,
      prototypeWhitelist: SandboxExec2.SAFE_PROTOTYPES,
      maxParserRecursionDepth: 256,
      nonBlocking: false,
      functionReplacements: /* @__PURE__ */ new Map()
    }, options || {});
    this.context = createContext(this, opt);
  }
  static get SAFE_GLOBALS() {
    return {
      globalThis,
      Function,
      eval,
      console: {
        debug: console.debug,
        error: console.error,
        info: console.info,
        log: console.log,
        table: console.table,
        warn: console.warn
      },
      isFinite,
      isNaN,
      parseFloat,
      parseInt,
      decodeURI,
      decodeURIComponent,
      encodeURI,
      encodeURIComponent,
      escape,
      unescape,
      Boolean,
      Number,
      BigInt,
      String,
      Object,
      Array,
      Symbol,
      Error,
      EvalError,
      RangeError,
      ReferenceError,
      SyntaxError,
      TypeError,
      URIError,
      Int8Array,
      Uint8Array,
      Uint8ClampedArray,
      Int16Array,
      Uint16Array,
      Int32Array,
      Uint32Array,
      Float32Array,
      Float64Array,
      Map,
      Set,
      WeakMap,
      WeakSet,
      Promise,
      Intl,
      JSON,
      Math,
      Date,
      RegExp
    };
  }
  static get SAFE_SYMBOLS() {
    const safeSymbols = {};
    for (const key of [
      "asyncIterator",
      "iterator",
      "match",
      "matchAll",
      "replace",
      "search",
      "split"
    ]) {
      const value = Symbol[key];
      if (typeof value === "symbol") safeSymbols[key] = value;
    }
    return safeSymbols;
  }
  static get SAFE_PROTOTYPES() {
    const protos = [
      Function,
      Boolean,
      Number,
      BigInt,
      String,
      Date,
      Error,
      Array,
      Int8Array,
      Uint8Array,
      Uint8ClampedArray,
      Int16Array,
      Uint16Array,
      Int32Array,
      Uint32Array,
      Float32Array,
      Float64Array,
      Map,
      Set,
      WeakMap,
      WeakSet,
      Promise,
      Symbol,
      Date,
      RegExp
    ];
    const map = /* @__PURE__ */ new Map();
    protos.forEach((proto) => {
      map.set(proto, /* @__PURE__ */ new Set());
    });
    map.set(Object, /* @__PURE__ */ new Set([
      "constructor",
      "name",
      "entries",
      "fromEntries",
      "getOwnPropertyNames",
      "is",
      "keys",
      "hasOwnProperty",
      "isPrototypeOf",
      "propertyIsEnumerable",
      "toLocaleString",
      "toString",
      "valueOf",
      "values"
    ]));
    return map;
  }
  subscribeGet(callback, context) {
    context.getSubscriptions.add(callback);
    return { unsubscribe: () => context.getSubscriptions.delete(callback) };
  }
  subscribeSet(obj, name, callback, context) {
    return subscribeSet(obj, name, callback, context);
  }
  subscribeSetGlobal(obj, name, callback) {
    return subscribeSet(obj, name, callback, this);
  }
  subscribeHalt(cb) {
    this.haltSubscriptions.add(cb);
    return { unsubscribe: () => {
      this.haltSubscriptions.delete(cb);
    } };
  }
  subscribeResume(cb) {
    this.resumeSubscriptions.add(cb);
    return { unsubscribe: () => {
      this.resumeSubscriptions.delete(cb);
    } };
  }
  haltExecution(haltContext = { type: "manual" }) {
    if (this.halted) return;
    this.halted = true;
    for (const cb of this.haltSubscriptions) cb(haltContext);
  }
  resumeExecution() {
    if (!this.halted) return;
    if (this.context.ticks.tickLimit !== void 0 && this.context.ticks.ticks >= this.context.ticks.tickLimit) throw new SandboxExecutionQuotaExceededError("Cannot resume execution: tick limit exceeded");
    this.halted = false;
    for (const cb of this.resumeSubscriptions) cb();
  }
  getContext(fn) {
    return this.sandboxFunctions.get(fn);
  }
  executeTree(context, scopes = []) {
    return executeTree(context.ctx.ticks, context, context.tree, scopes, void 0, false);
  }
  executeTreeAsync(context, scopes = []) {
    return executeTreeAsync(context.ctx.ticks, context, context.tree, scopes, void 0, false);
  }
};

// node_modules/@nyariv/sandboxjs/dist/esm/Sandbox.js
var Sandbox = class extends SandboxExec {
  constructor(options) {
    super(options, createEvalContext());
  }
  static audit(code, scopes = []) {
    const globals = {};
    for (const i of Object.getOwnPropertyNames(globalThis)) globals[i] = globalThis[i];
    const sandbox = new SandboxExec({
      globals,
      audit: true
    });
    return sandbox.executeTree(createExecContext(sandbox, parse(code, true, false, sandbox.context.options.maxParserRecursionDepth), createEvalContext()), scopes);
  }
  static parse(code) {
    return parse(code, true);
  }
  get Function() {
    return createExecContext(this, {
      tree: [],
      constants: {
        strings: [],
        eager: true,
        literals: [],
        maxDepth: this.context.options.maxParserRecursionDepth,
        regexes: []
      }
    }, this.evalContext).evals.get(Function);
  }
  get AsyncFunction() {
    return createExecContext(this, {
      tree: [],
      constants: {
        strings: [],
        eager: true,
        literals: [],
        maxDepth: this.context.options.maxParserRecursionDepth,
        regexes: []
      }
    }, this.evalContext).evals.get(AsyncFunction);
  }
  get eval() {
    return createExecContext(this, {
      tree: [],
      constants: {
        strings: [],
        eager: true,
        literals: [],
        maxDepth: this.context.options.maxParserRecursionDepth,
        regexes: []
      }
    }, this.evalContext).evals.get(eval);
  }
  compile(code, optimize = false) {
    if (this.context.options.nonBlocking) throw new SandboxCapabilityError("Non-blocking mode is enabled, use Sandbox.compileAsync() instead.");
    const parsed = parse(code, optimize, false, this.context.options.maxParserRecursionDepth);
    const context = createExecContext(this, parsed, this.evalContext);
    const exec = (...scopes) => {
      sanitizeScopes(scopes, context);
      return {
        context,
        run: () => this.executeTree(context, [...scopes]).result
      };
    };
    return exec;
  }
  compileAsync(code, optimize = false) {
    const parsed = parse(code, optimize, false, this.context.options.maxParserRecursionDepth);
    const context = createExecContext(this, parsed, this.evalContext);
    const exec = (...scopes) => {
      sanitizeScopes(scopes, context);
      return {
        context,
        run: () => this.executeTreeAsync(context, [...scopes]).then((ret) => ret.result)
      };
    };
    return exec;
  }
  compileExpression(code, optimize = false) {
    const parsed = parse(code, optimize, true, this.context.options.maxParserRecursionDepth);
    const context = createExecContext(this, parsed, this.evalContext);
    const exec = (...scopes) => {
      sanitizeScopes(scopes, context);
      return {
        context,
        run: () => this.executeTree(context, [...scopes]).result
      };
    };
    return exec;
  }
  compileExpressionAsync(code, optimize = false) {
    const parsed = parse(code, optimize, true, this.context.options.maxParserRecursionDepth);
    const context = createExecContext(this, parsed, this.evalContext);
    const exec = (...scopes) => {
      return {
        context,
        run: () => this.executeTreeAsync(context, [...scopes]).then((ret) => ret.result)
      };
    };
    return exec;
  }
};

// node_modules/async-mutex/index.mjs
var E_TIMEOUT = new Error("timeout while waiting for mutex to become available");
var E_ALREADY_LOCKED = new Error("mutex already locked");
var E_CANCELED = new Error("request for lock canceled");
var __awaiter$2 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var Semaphore = class {
  constructor(_value, _cancelError = E_CANCELED) {
    this._value = _value;
    this._cancelError = _cancelError;
    this._queue = [];
    this._weightedWaiters = [];
  }
  acquire(weight = 1, priority = 0) {
    if (weight <= 0)
      throw new Error(`invalid weight ${weight}: must be positive`);
    return new Promise((resolve, reject) => {
      const task = { resolve, reject, weight, priority };
      const i = findIndexFromEnd(this._queue, (other) => priority <= other.priority);
      if (i === -1 && weight <= this._value) {
        this._dispatchItem(task);
      } else {
        this._queue.splice(i + 1, 0, task);
      }
    });
  }
  runExclusive(callback_1) {
    return __awaiter$2(this, arguments, void 0, function* (callback, weight = 1, priority = 0) {
      const [value, release] = yield this.acquire(weight, priority);
      try {
        return yield callback(value);
      } finally {
        release();
      }
    });
  }
  waitForUnlock(weight = 1, priority = 0) {
    if (weight <= 0)
      throw new Error(`invalid weight ${weight}: must be positive`);
    if (this._couldLockImmediately(weight, priority)) {
      return Promise.resolve();
    } else {
      return new Promise((resolve) => {
        if (!this._weightedWaiters[weight - 1])
          this._weightedWaiters[weight - 1] = [];
        insertSorted(this._weightedWaiters[weight - 1], { resolve, priority });
      });
    }
  }
  isLocked() {
    return this._value <= 0;
  }
  getValue() {
    return this._value;
  }
  setValue(value) {
    this._value = value;
    this._dispatchQueue();
  }
  release(weight = 1) {
    if (weight <= 0)
      throw new Error(`invalid weight ${weight}: must be positive`);
    this._value += weight;
    this._dispatchQueue();
  }
  cancel() {
    this._queue.forEach((entry) => entry.reject(this._cancelError));
    this._queue = [];
  }
  _dispatchQueue() {
    this._drainUnlockWaiters();
    while (this._queue.length > 0 && this._queue[0].weight <= this._value) {
      this._dispatchItem(this._queue.shift());
      this._drainUnlockWaiters();
    }
  }
  _dispatchItem(item) {
    const previousValue = this._value;
    this._value -= item.weight;
    item.resolve([previousValue, this._newReleaser(item.weight)]);
  }
  _newReleaser(weight) {
    let called = false;
    return () => {
      if (called)
        return;
      called = true;
      this.release(weight);
    };
  }
  _drainUnlockWaiters() {
    if (this._queue.length === 0) {
      for (let weight = this._value; weight > 0; weight--) {
        const waiters = this._weightedWaiters[weight - 1];
        if (!waiters)
          continue;
        waiters.forEach((waiter) => waiter.resolve());
        this._weightedWaiters[weight - 1] = [];
      }
    } else {
      const queuedPriority = this._queue[0].priority;
      for (let weight = this._value; weight > 0; weight--) {
        const waiters = this._weightedWaiters[weight - 1];
        if (!waiters)
          continue;
        const i = waiters.findIndex((waiter) => waiter.priority <= queuedPriority);
        (i === -1 ? waiters : waiters.splice(0, i)).forEach((waiter) => waiter.resolve());
      }
    }
  }
  _couldLockImmediately(weight, priority) {
    return (this._queue.length === 0 || this._queue[0].priority < priority) && weight <= this._value;
  }
};
function insertSorted(a, v) {
  const i = findIndexFromEnd(a, (other) => v.priority <= other.priority);
  a.splice(i + 1, 0, v);
}
function findIndexFromEnd(a, predicate) {
  for (let i = a.length - 1; i >= 0; i--) {
    if (predicate(a[i])) {
      return i;
    }
  }
  return -1;
}
var __awaiter$1 = function(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
};
var Mutex = class {
  constructor(cancelError) {
    this._semaphore = new Semaphore(1, cancelError);
  }
  acquire() {
    return __awaiter$1(this, arguments, void 0, function* (priority = 0) {
      const [, releaser] = yield this._semaphore.acquire(1, priority);
      return releaser;
    });
  }
  runExclusive(callback, priority = 0) {
    return this._semaphore.runExclusive(() => callback(), 1, priority);
  }
  isLocked() {
    return this._semaphore.isLocked();
  }
  waitForUnlock(priority = 0) {
    return this._semaphore.waitForUnlock(1, priority);
  }
  release() {
    if (this._semaphore.isLocked())
      this._semaphore.release();
  }
  cancel() {
    return this._semaphore.cancel();
  }
};

// src-web/dsl.ts
var DslErrors;
((DslErrors2) => {
  class PerfectHedgeError extends Error {
    state;
    amount;
    partyIdx;
    pair;
    constructor(msg, st, amount, partyIdx, pair) {
      super(msg);
      this.state = st;
      this.amount = amount;
      this.partyIdx = partyIdx;
      this.pair = pair;
    }
  }
  DslErrors2.PerfectHedgeError = PerfectHedgeError;
  class InfinityError extends Error {
    state = void 0;
    constructor(msg, st) {
      super(msg);
      this.state = st;
    }
  }
  DslErrors2.InfinityError = InfinityError;
  class InfinityCountError extends Error {
  }
  DslErrors2.InfinityCountError = InfinityCountError;
  class PartyAtAdvantage extends Error {
    amount;
    partyIdx;
    pair;
    constructor(msg, amount, partyIdx, pair) {
      super(msg);
      this.amount = amount;
      this.partyIdx = partyIdx;
      this.pair = pair;
    }
  }
  DslErrors2.PartyAtAdvantage = PartyAtAdvantage;
  class OnePayPerCondition extends Error {
    amount;
    partyIdx;
    pair;
    state;
    constructor(msg, amount, partyIdx, pair, state) {
      super(msg);
      this.amount = amount;
      this.partyIdx = partyIdx;
      this.pair = pair;
      this.state = state;
    }
  }
  DslErrors2.OnePayPerCondition = OnePayPerCondition;
  class ComplexConditions extends Error {
    amount;
    partyIdx;
    pair;
    constructor(msg, amount, partyIdx, pair) {
      super(msg);
      this.amount = amount;
      this.partyIdx = partyIdx;
      this.pair = pair;
    }
  }
  DslErrors2.ComplexConditions = ComplexConditions;
  class SafeModeError extends Error {
  }
  DslErrors2.SafeModeError = SafeModeError;
  class EmptyDslOutput extends Error {
  }
  DslErrors2.EmptyDslOutput = EmptyDslOutput;
})(DslErrors || (DslErrors = {}));
var Dsl = class {
  state = {};
  template() {
    const model = {
      id: "",
      bet: [0, 0],
      betOn: void 0,
      oracles: [],
      question: "",
      status: "matching",
      blockchain: "",
      role: "initiator"
    };
    return model;
  }
  root = this.template();
  cursor = this.root;
  prev = void 0;
  lastOutcome = void 0;
  flag = false;
  alicePayCounter = 0;
  bobPayCounter = 0;
  aliceTrackers = {};
  bobTrackers = {};
  pay(idx, amount) {
    if (this.flagSameAssetSwap) {
      throw Error(`Cannot swap same type of asset between ${this.selected[0]} and ${this.selected[1]}`);
    }
    if (this.unssafeInifnityCtx) {
      throw new Error("Payouts are disabled in unsafe infinity context. Specify cashflows in return instead");
    }
    if (idx === void 0) {
      throw new Error("party undefined");
    }
    if (!this.protect) {
      throw new Error("should not call outside of body; use `new Dsl((dsl) => handler).enumerate()`");
    }
    if (this.prev === void 0 || this.lastOutcome === void 0) {
      new Error("cannot pay unconditionally!");
    }
    if (amount <= 0) {
      throw Error("Pay amount must be positive! found: " + amount);
    }
    if (this.lastOutcome !== null) {
      if (this.lastOutcome && idx === 0) {
        if (this.prev.betOn === void 0 || this.prev.betOn === true) {
          this.prev.betOn = true;
        } else {
          throw new DslErrors.PerfectHedgeError("Perfect hedge! Trader not allowed to benefit regardless of outcome. Your trade is overcollaterized!", this.state, amount, idx, this.selected);
        }
      }
      if (!this.lastOutcome && idx === 0) {
        if (this.prev.betOn === void 0 || this.prev.betOn === false) {
          this.prev.betOn = false;
        } else {
          throw new DslErrors.PerfectHedgeError("Perfect hedge! Trader not allowed to benefit regardless of outcome. Your trade is overcollaterized!", this.state, amount, idx, this.selected);
        }
      }
    }
    if (this.lastOutcome !== null) {
      if (this.lastOutcome && idx === 1) {
        if (this.prev.betOn === void 0 || this.prev.betOn === false) {
          this.prev.betOn = false;
        } else {
          throw new DslErrors.PerfectHedgeError("Perfect hedge! Trader not allowed to benefit regardless of outcome. Your trade is overcollaterized!", this.state, amount, idx, this.selected);
        }
      }
      if (!this.lastOutcome && idx === 1) {
        if (this.prev.betOn === void 0 || this.prev.betOn === true) {
          this.prev.betOn = true;
        } else {
          throw new DslErrors.PerfectHedgeError("Perfect hedge! Trader not allowed to benefit regardless of outcome. Your trade is overcollaterized!", this.state, amount, idx, this.selected);
        }
      }
    }
    if (this.flag) {
      throw new Error(`one pay per condition check! and pay before checking out next condition too, please!
at amount = ${amount}; idx = ${idx}; pair = ${this.selected}`);
    }
    const id = this.megaMode ? this.currentPub : this.currentPub + JSON.stringify(this.currentArgs);
    if (idx == 0) {
      this.alicePayCounter++;
      if (!this.aliceTrackers[id]) {
        this.aliceTrackers[id] = 0;
      }
      this.aliceTrackers[id] += 1;
      this.prev.bet[1] = Math.round(amount);
    } else {
      if (!this.bobTrackers[id]) {
        this.bobTrackers[id] = 0;
      }
      this.bobTrackers[id] += 1;
      this.bobPayCounter++;
      this.prev.bet[0] = Math.round(amount);
    }
    this.flag = true;
    if (idx === 0) {
      this.collateral1 += amount;
      if (this.collateral1 > this.budgetBound1) {
        const party = this.selected[0] ?? "Alice";
        throw new Error(`exceeded budget ${this.budgetBound1} for ${party}, for outcomes:` + JSON.stringify(this.state));
      }
    }
    if (idx === 1) {
      this.collateral2 += amount;
      if (this.collateral2 > this.budgetBound2) {
        const party = this.selected[1] ?? "Bob";
        throw new Error(`exceeded budget ${this.budgetBound2} for ${party}, for outcomes:` + JSON.stringify(this.state));
      }
    }
  }
  currentPub = void 0;
  currentArgs = void 0;
  enrichAndProgress(aliceOutcome, pubkey, yes, no, args = {}) {
    this.lastOutcome = aliceOutcome;
    this.flag = false;
    if (aliceOutcome === false) {
      if (this.cursor.ifCounterPartyWins === void 0) {
        this.cursor.ifCounterPartyWins = this.template();
      } else {
        const x = this.cursor.ifCounterPartyWins.bet;
        this.cursor.ifCounterPartyWins.bet = [x[0] === null ? 0 : x[0], x[0] === null ? 0 : x[0]];
      }
      this.prev = this.cursor;
      this.cursor = this.cursor.ifCounterPartyWins;
    } else if (aliceOutcome === true) {
      if (this.cursor.ifPartyWins === void 0) {
        this.cursor.ifPartyWins = this.template();
      } else {
        const x = this.cursor.ifPartyWins.bet;
        this.cursor.ifPartyWins.bet = [x[0] === null ? 0 : x[0], x[0] === null ? 0 : x[0]];
      }
      this.prev = this.cursor;
      this.cursor = this.cursor.ifPartyWins;
    }
    if (this.prev) {
      this.prev.oracles[0] = {
        capabilityPub: pubkey.replaceAll(/-###-.*/g, ""),
        params: args
      };
      this.prev.yesOutcomes = yes;
      this.prev.noOutcomes = no;
    }
  }
  counter = 0;
  memoize = [];
  checked = [];
  superMode = false;
  safeMode = false;
  megaMode = false;
  safeModeStarted = false;
  megaModeStarted = false;
  superModeStarted = false;
  fairModeStarted = false;
  strictModeStarted = false;
  security = {
    startSafeMode: () => {
      if (this.collateral1 > 0 && this.collateral2 > 0 && this.counter > 0) {
        throw new Error("Safe mode has to be started before any payouts or observations happen!");
      }
      if (this.safeMode) {
        throw new Error("Safe mode already enabled manually!");
      }
      this.safeModeStarted = true;
      this.safeMode = true;
    },
    startMegaMode: () => {
      if (this.collateral1 > 0 && this.collateral2 > 0 && this.counter > 0) {
        throw new Error("Mega mode has to be started before any payouts or observations happen!");
      }
      if (this.megaMode) {
        throw new Error("Mega mode already enabled manually!");
      }
      this.megaModeStarted = true;
      this.megaMode = true;
    },
    startSuperMode: () => {
      if (this.collateral1 > 0 && this.collateral2 > 0 && this.counter > 0) {
        throw new Error("Super mode has to be started before any payouts or observations happen!");
      }
      if (this.superMode) {
        throw new Error("Super mode already enabled manually!");
      }
      this.superModeStarted = true;
      this.superMode = true;
    },
    startFairMode: () => {
      if (this.collateral1 > 0 && this.collateral2 > 0 && this.counter > 0) {
        throw new Error("Fair mode has to be started before any payouts or observations happen!");
      }
      if (this.megaMode) {
        throw new Error("Fair mode already enabled manually!");
      }
      this.fairModeStarted = true;
      this.strictlyFair = true;
    },
    startStrictMode: () => {
      if (this.collateral1 > 0 && this.collateral2 > 0 && this.counter > 0) {
        throw new Error("Strict mode has to be started before any payouts or observations happen!");
      }
      if (this.megaMode) {
        throw new Error("Strict mode already enabled manually!");
      }
      this.strictModeStarted = true;
      this.strictlyStrict = true;
    }
  };
  insecurity = {
    open: {
      disableSafeMode: () => {
        if (this.safeModeStarted) {
          throw new Error("Safe mode has to be started with `startSafeMode`!");
        }
        if (!this.safeMode) {
          throw new Error("Safe mode has to be enabled first!");
        }
        this.safeMode = false;
      },
      disableMegaMode: () => {
        if (this.megaModeStarted) {
          throw new Error("Mega mode has to be started with `startMegaMode`!");
        }
        if (!this.megaMode) {
          throw new Error("Mega mode has to be enabled first!");
        }
        this.megaMode = false;
      },
      disableSuperMode: () => {
        if (this.superModeStarted) {
          throw new Error("Super mode has to be started with `startSuperMode`!");
        }
        if (!this.superMode) {
          throw new Error("Super mode has to be enabled first!");
        }
        this.superMode = false;
      }
    },
    close: {
      reEnableSafeMode: () => {
        if (this.safeModeStarted) {
          throw new Error("Safe mode has to be started with `startSafeMode`!");
        }
        if (this.safeMode) {
          throw new Error("Safe mode has to be disabled first!");
        }
        this.safeMode = false;
      },
      reEnableMegaMode: () => {
        if (this.megaModeStarted) {
          throw new Error("Mega mode has to be started with `startMegaMode`!");
        }
        if (this.megaMode) {
          throw new Error("Mega mode has to be disabled first!");
        }
        this.megaMode = false;
      },
      reEnableSuperMode: () => {
        if (this.superModeStarted) {
          throw new Error("Mega mode has to be started with `startSuperMode`!");
        }
        if (this.superMode) {
          throw new Error("Mega mode has to be disabled first!");
        }
        this.superMode = false;
      }
    }
  };
  strictlyOneLeafPays = false;
  strictlyOneLeafPairPays = false;
  flagSameAssetSwap = false;
  outcome(pubkey, yes, no, args = {}, allowTruth = false, strict = true, ignoreObserveChecksSuperUnsafe = false) {
    this.currentPub = pubkey;
    this.currentArgs = args;
    const pubkeyUnique = pubkey + "-###-" + JSON.stringify(yes) + JSON.stringify(no) + JSON.stringify(args);
    if (ignoreObserveChecksSuperUnsafe) {
      if (this.state[pubkeyUnique] === void 0) {
        const memoized = this.memoize.find((x) => x.id === pubkey && JSON.stringify(x.yes.sort()) === JSON.stringify(yes.sort()) && JSON.stringify(x.args) === JSON.stringify(args));
        const pubkeyUnique2 = pubkey + "-###-" + JSON.stringify(yes) + JSON.stringify(memoized.no) + JSON.stringify(args);
        if (!this.state[pubkeyUnique2]) {
          throw new Error("Did not find memoized state " + pubkeyUnique2 + "\n" + this.state);
        }
        this.enrichAndProgress(this.state[pubkeyUnique2][1], pubkeyUnique2, yes, no, args);
        return this.state[pubkeyUnique2][1];
      }
      this.enrichAndProgress(this.state[pubkeyUnique][1], pubkeyUnique, yes, no, args);
      return this.state[pubkeyUnique][1];
    }
    if (strict) {
      this.alicePayCounter = 0;
      this.bobPayCounter = 0;
    }
    if (allowTruth && this.selected !== void 0) {
      const tokens1 = this.selected[0].split("_");
      const asset1 = tokens1.length === 1 ? "btc" : tokens1[tokens1.length - 1];
      const tokens2 = this.selected[1].split("_");
      const asset2 = tokens2.length === 1 ? "btc" : tokens2[tokens2.length - 1];
      if (asset1 === asset2) {
        this.flagSameAssetSwap = true;
      }
    }
    yes.sort();
    no.sort();
    this.counter++;
    if (new Set(yes).size !== yes.length) {
      throw Error("Duplicate outcomes:" + JSON.stringify(yes) + "; pubkey: " + pubkey);
    }
    if (new Set(no).size !== no.length) {
      throw Error("Duplicate outcomes: " + JSON.stringify(no)) + "; pubkey: " + pubkey;
    }
    if (strict && (yes.length === 0 || no.length === 0)) {
      throw Error("One of the outcome sets is empty. Trader would possibly benefit regardless of outcome. Use `dsl.unsafe` to allow!; pubkey: " + pubkey);
    }
    if (yes.length === 0 && no.length === 0) {
      throw Error("Transaction race! Outcomes are empty. Trader cannot benefit regardless of outcome. Use `dsl.unsafe` with synthetic outcome (convention: btc script generated outcomes should start with $, e.g. $(thisTx.utxo[0])) to allow!; pubkey: " + pubkey);
    }
    if (JSON.stringify(yes) === JSON.stringify(no) && !allowTruth) {
      throw Error("Contradiction! Outcomes are not mutually exclusive!; pubkey: " + pubkey);
    }
    const yesSet = new Set(yes);
    const noSet = new Set(no);
    if (yes.find((x) => noSet.has(x)) || no.find((x) => yesSet.has(x))) {
      if (strict) {
        throw Error("Partial contradiction! Some outcomes are not mutually exclusive!; pubkey: " + pubkey);
      }
    }
    if (!this.protect) {
      throw "should not call outside of body; use `new Dsl((dsl) => handler).enumerate()`; pubkey: " + pubkey;
    }
    if (this.state[pubkeyUnique] === void 0) {
      const max = Object.values(this.state).length === 0 ? -1 : Math.max(...Object.values(this.state).map((x) => x[0]));
      this.state[pubkeyUnique] = [max + 1, null, args];
      this.checked.push(this.state[pubkeyUnique][0]);
      this.memoize.push({
        id: pubkey,
        yes,
        no,
        args,
        pubkeyUnique
      });
      throw "uninitialized";
    } else {
      this.checked.push(this.state[pubkeyUnique][0]);
      if (this.memoize.find((x) => x.id === pubkey && JSON.stringify(x.yes.sort()) === JSON.stringify(yes.sort()) && JSON.stringify(x.no.sort()) === JSON.stringify(no.sort()) && JSON.stringify(x.args) === JSON.stringify(args)) !== void 0) {
        throw new Error("Cannot query same observation twice. Save it into const instead: const obs1 = outcome(...); args=" + JSON.stringify(args) + "; pubkey: " + pubkey);
      }
      const sameQuery = this.memoize.find((x) => x.id === pubkey);
      if (strict && sameQuery && JSON.stringify(sameQuery.yes.concat(sameQuery.no).sort()) !== JSON.stringify(yes.concat(no).sort())) {
        throw new Error("Set of overall outcomes must be same, regardless of parameters! " + sameQuery.yes.concat(sameQuery.no).sort() + " != " + yes.concat(no).sort() + "; pubkey: " + pubkey);
      }
      if (this.superMode && sameQuery && JSON.stringify(sameQuery.args) === JSON.stringify(sameQuery.args)) {
        throw new Error("Cannot query same observation twice! Super strictly! Arguments are allowed to vary; pubkey: " + pubkey);
      }
      if (this.megaMode && sameQuery) {
        throw new Error("Cannot query same observation twice! MEGA strictly!");
      }
      const contradiction = this.memoize.find((x) => x.id === pubkey && JSON.stringify(x.yes.sort()) === JSON.stringify(no.sort()) && JSON.stringify(x.no.sort()) === JSON.stringify(yes.sort()) && JSON.stringify(x.args) === JSON.stringify(args));
      if (contradiction !== void 0) {
        throw new Error("Cannot query the opposite of checked observation. Save it into const and inverse instead: const obs1 = outcome(...); const obs2 = !obs1; pubkey: " + pubkey);
      }
      this.enrichAndProgress(this.state[pubkeyUnique][1], pubkeyUnique, yes, no, args);
      this.memoize.push({
        id: pubkey,
        yes,
        no,
        args,
        pubkeyUnique
      });
      return this.state[pubkeyUnique][1];
    }
  }
  next() {
    this.cursor = this.root;
    this.prev = void 0;
    let i = 0;
    let cursor = true;
    let entry = void 0;
    while (cursor) {
      if (Object.values(this.state).find((x) => x[0] === i) === void 0) {
        return false;
      }
      entry = Object.values(this.state).find((x) => x[0] === i);
      if (entry[1] === null && cursor) {
        entry[1] = false;
        return true;
      }
      if (!cursor) {
        return true;
      }
      if (entry[1] === true) {
        if (cursor === true) {
          entry[1] = false;
        }
      } else {
        entry[1] = true;
        cursor = false;
      }
      i++;
    }
    return true;
  }
  body;
  constructor(body) {
    this.body = body;
  }
  protect = false;
  static Party = 0;
  static CounterParty = 1;
  static Alice = 0;
  static Bob = 1;
  collateral1 = 0;
  collateral2 = 0;
  budgetBound1 = 0;
  budgetBound2 = 0;
  leafsFiltered = false;
  strictlyStrict = false;
  filterLeafs(model, assertNoZeros = false) {
    if (model === void 0) {
      throw new DslErrors.EmptyDslOutput("Empty DSL model output!");
    }
    if (!model.bet[0] && !model.bet[1] && !model.ifPartyWins && !model.ifCounterPartyWins) {
      this.leafsFiltered = true;
      return void 0;
    }
    if (this.strictlyFair) {
      if (!model.bet[0] && model.bet[1]) {
        throw new DslErrors.PartyAtAdvantage("Party at advantage - no premium/discount introduced", model.bet[1], 0, this.selected);
      }
      if (model.bet[0] && !model.bet[1]) {
        throw new DslErrors.PartyAtAdvantage("Party at advantage - no premium/discount introduced", model.bet[0], 1, this.selected);
      }
    }
    if (assertNoZeros) {
      if (!model.bet[0] && !model.bet[1]) {
        throw new DslErrors.ComplexConditions("Strict Semantics of observation: complex conditions are disallowed!", model.bet[0], 1, this.selected);
      }
    }
    if (model.ifPartyWins) {
      model.ifPartyWins = this.filterLeafs(model.ifPartyWins);
    }
    if (model.ifCounterPartyWins) {
      model.ifCounterPartyWins = this.filterLeafs(model.ifCounterPartyWins);
    }
    if (!model.ifPartyWins) {
      delete model.ifPartyWins;
    }
    if (!model.ifCounterPartyWins) {
      delete model.ifCounterPartyWins;
    }
    return model;
  }
  multiparty = [];
  selected = [void 0, void 0];
  isSelected0 = (party) => {
    return this.selected[0] && this.selected[0] === party;
  };
  isSelected1 = (party) => {
    return this.selected[0] && this.selected[1] === party;
  };
  multiple = (...parties) => {
    if (this.multiparty.length > 0) {
      throw Error("parties can be specified only once! Adding more parties ad-hoc is equivalent to early termination since consensus is required for that! \n It is advised to create a new contract. \n Note: you can freely parametrize Discreet contracts (terms) and use factories to easily spawn contracts with additional parties.");
    }
    if (parties.length < 2) {
      throw Error("Commmon sense! Minimum of 2 parties required for a contract!");
    }
    const set = [...new Set(parties)];
    if (set.length < parties.length) {
      throw Error("Tractable relations! parties have to be unique!");
    }
    this.multiparty = parties;
    return this;
  };
  party = (partyName, partyAsset) => ({
    pays: (counterpartyName, counterpartyAsset) => ({
      amount: (amount, asset) => {
        if (partyName === void 0) {
          throw Error("Party undefined");
        }
        if (counterpartyName === void 0) {
          throw Error("Counter-party undefined");
        }
        const party = partyName + (partyAsset ? "_" + partyAsset : "");
        const counterparty = counterpartyName + (counterpartyAsset ? "_" + counterpartyAsset : "");
        if (partyAsset !== asset) {
          throw Error(`Trying to pay ${asset} from collateral denominated in ${partyAsset}`);
        }
        if (!this.multiparty.find((x) => x === party)) {
          throw Error("party " + party + " not registered! Use .multiple to register parties");
        }
        if (!this.multiparty.find((x) => x === counterparty)) {
          throw Error("counterparty " + counterparty + " not registered! Use .multiple to register parties");
        }
        if (this.isSelected0(party) && this.isSelected1(counterparty)) {
          this.pay(0, amount);
        } else if (this.isSelected0(counterparty) && this.isSelected1(party)) {
          this.pay(1, amount);
        }
      }
    })
  });
  static account(partyName, partyAsset) {
    return partyName + "_" + partyAsset;
  }
  static mutual(...parties) {
    return parties.join("+");
  }
  static refund(party) {
    return party + "_refund";
  }
  unfinalized = 0;
  static track = {};
  static recurse = {
    bounded: (fn) => ({
      attempts: (attempts) => ({
        otherwiseYield: (defaultValue) => {
          if (!this.track[fn.toString()]) {
            this.track[fn.toString()] = 0;
          }
          return () => {
            this.track[fn.toString()] = this.track[fn.toString()] + 1;
            if (this.track[fn.toString()] > attempts) {
              return defaultValue;
            }
            return fn();
          };
        }
      })
    })
  };
  strictlyFair = false;
  unssafeInifnityCtx = false;
  atomicSwap = {
    ifTruth: (lock = "TRUTH", unlockOutcome = "true", args = {}, allowMisplacedPay = true) => {
      return this.if(lock, [unlockOutcome], [unlockOutcome], args, true, allowMisplacedPay, false);
    },
    truth: (lock = "TRUTH", unlockOutcome = "true", args = {}) => {
      return this.outcome(lock, [unlockOutcome], [unlockOutcome], args, true);
    }
  };
  unsafe = {
    if: (pubkey, yes, no, args = {}, allowSwaps = false, allowMisplacedPay = true, strict = false, ignoreObserveChecksSuperUnsafe = false) => {
      if (this.safeMode) {
        throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
      }
      return this.if(pubkey, yes, no, args, allowSwaps, allowMisplacedPay, strict, ignoreObserveChecksSuperUnsafe);
    },
    numeric: {
      outcome: (pubkey, from, to, step = 1, args = {}, allowMisplacedPay = true, allowUnsafe = true, ignoreObserveChecksSuperUnsafe = false) => {
        if (this.safeMode) {
          throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
        }
        return this.numeric.outcome(pubkey, from, to, step, args, allowMisplacedPay, allowUnsafe, ignoreObserveChecksSuperUnsafe);
      },
      infinity: {
        bounded: (maxInfinity = 1e7, maxCount = 1e9) => ({
          progress: (start, forward = (x) => x + 1) => ({
            perpetual: (init, step) => {
              if (this.safeMode) {
                throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
              }
              this.unsafe.infinity.bounded(maxInfinity, maxCount).compare((a, b) => a - b).progress(start, forward).perpetual(init, step);
            }
          }),
          perpetual: (init, step) => {
            if (this.safeMode) {
              throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
            }
            this.unsafe.infinity.bounded(maxInfinity, maxCount).compare((a, b) => b - a).progress(0, (x) => x + 1).perpetual(init, step);
          }
        })
      }
    },
    set: {
      outcome: (pubkey, set, args = {}, allowMisplacedPay = true, allowUnsafe = true) => {
        if (this.safeMode) {
          throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
        }
        return this.set.outcome(pubkey, set, args, allowMisplacedPay, allowUnsafe);
      },
      outcomeT: (pubkey, set, renderer, args = {}, allowMisplacedPay = true, allowUnsafe = true) => {
        if (this.safeMode) {
          throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
        }
        return this.set.outcomeT(pubkey, set, renderer, args, allowMisplacedPay, allowUnsafe);
      }
    },
    outcome: (pubkey, yes, no, args = {}, allowTruth = false, strict = false, ignoreObserveChecksSuperUnsafe = false) => {
      if (this.safeMode) {
        throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
      }
      return this.outcome(pubkey, yes, no, args, allowTruth, strict, ignoreObserveChecksSuperUnsafe);
    },
    infinity: {
      move: (x) => {
        if (x === void 0) {
          throw new Error("Cannot move with undefined state!");
        }
        return x;
      },
      stop: (cashflows) => [void 0, cashflows],
      bounded: (maxInfinity, maxCount = 1e4) => ({
        compare: (cmp) => ({
          progress: (start, forward) => ({
            perpetual: (init, step) => {
              if (this.safeMode) {
                throw new DslErrors.SafeModeError("safe mode is activated! unsafe is disallowed!");
              }
              let cursor = start;
              let counter = 0;
              let state = init;
              this.unssafeInifnityCtx = true;
              try {
                while (cmp(cursor, maxInfinity) > 0 && counter < maxCount) {
                  const saveState = state;
                  let cashflows = void 0;
                  [state, cashflows] = step(cursor, state);
                  cashflows.forEach((cashflow) => {
                    try {
                      this.party(
                        cashflow.from[0],
                        cashflow.from[1]
                      ).pays(
                        cashflow.to[0],
                        cashflow.to[1]
                      ).amount(
                        cashflow.amount[0],
                        cashflow.amount[1]
                      );
                    } catch (e) {
                      if (e instanceof DslErrors.PerfectHedgeError) {
                        const party = e.pair[e.partyIdx];
                        state[party] = saveState[party];
                      }
                    }
                  });
                  if (state === void 0) {
                    return;
                  }
                  if (state === saveState) {
                    throw new DslErrors.InfinityError("Infinity Inferred! State did not progress! Collaterals are not decreasing?", state);
                  }
                  cursor = forward(cursor);
                  counter++;
                }
                if (counter >= maxCount) {
                  throw new DslErrors.InfinityCountError("Max count reached!");
                }
                if (cmp(cursor, maxInfinity) <= 0) {
                  throw new DslErrors.InfinityError("Infinity Reached! Collaterals are not decreasing?", state);
                }
              } finally {
                this.unssafeInifnityCtx = false;
              }
            }
          })
        })
      })
    }
  };
  infinity = {
    move: (x) => {
      if (x === void 0) {
        throw new Error("Cannot move with undefined state!");
      }
      return x;
    },
    stop: void 0,
    bounded: (maxInfinity, maxCount = 1e4) => ({
      compare: (cmp) => ({
        progress: (start, forward) => ({
          perpetual: (init, step) => {
            let cursor = start;
            let counter = 0;
            let state = init;
            while (cmp(cursor, maxInfinity) > 0 && counter < maxCount) {
              const saveState = state;
              state = step(cursor, state);
              if (state === void 0) {
                return;
              }
              if (state === saveState) {
                throw new DslErrors.InfinityError("Infinity Inferred! State did not progress! Collaterals are not decreasing?", state);
              }
              cursor = forward(cursor);
              counter++;
            }
            if (counter >= maxCount) {
              throw new DslErrors.InfinityCountError("Max count reached!");
            }
            if (cmp(cursor, maxInfinity) <= 0) {
              throw new DslErrors.InfinityError("Infinity Reached! Collaterals are not decreasing?", state);
            }
          }
        })
      })
    })
  };
  bool = {
    safe: {
      outcome: (pubkey, yes, no, args = {}) => {
        return this.outcome(pubkey, [yes], [no], args);
      }
    }
  };
  numeric = {
    infinity: {
      bounded: (maxInfinity = 1e7, maxCount = 1e9) => ({
        progress: (start, forward = (x) => x + 1) => ({
          perpetual: (init, step) => {
            this.infinity.bounded(maxInfinity, maxCount).compare((a, b) => a - b).progress(start, forward).perpetual(init, step);
          }
        }),
        perpetual: (init, step) => {
          this.infinity.bounded(maxInfinity, maxCount).compare((a, b) => b - a).progress(0, (x) => x + 1).perpetual(init, step);
        }
      })
    },
    safe: {
      outcome: (pubkey, yes, no, args = {}) => {
        if (this.outcome(pubkey, [`${yes}`], [`${no}`], args)) {
          return yes;
        } else {
          return no;
        }
      },
      if: (pubkey, yes, no, args = {}, allowSwaps = false, allowMisplacedPay = false, strict = true) => {
        const iff = this.if(pubkey, [`${yes}`], [`${no}`], args, allowSwaps, allowMisplacedPay, strict);
        return {
          then: (handler) => {
            const thenn = iff.then((h) => handler(yes, h));
            return {
              else: (handler2) => {
                return thenn.else((h) => handler2(no, h));
              }
            };
          }
        };
      }
    },
    outcome: (pubkey, from, to, step = 1, args = {}, allowMisplacedPay = false, allowUnsafe = false, ignoreObserveChecksSuperUnsafe = false) => ({
      evaluate: (handler) => {
        let numbers = [];
        for (let i = from; i <= to; i += step) {
          numbers.push(i);
        }
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          if (this.outcome(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args, false, false)) {
            if (l.length === 1) {
              if (this.outcome(pubkey, l.map((x) => x.toString()), [], args, false, false, true)) {
                handler(l[0]);
              }
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          } else {
            if (r.length === 1) {
              handler(r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          }
        };
        recurse(numbers.slice(0, numbers.length / 2), numbers.slice(numbers.length / 2));
        const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
        if (!allowUnsafe) {
          if (this.aliceTrackers[id] > numbers.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
          }
          if (this.bobTrackers[id] > numbers.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
          }
        }
        if (this.strictlyOneLeafPays && (this.aliceTrackers[id] > 1 || this.bobPayCounter[id] > 1)) {
          throw new DslErrors.OnePayPerCondition("Only one leaf in a tree is alowed to pay", void 0, void 0, this.selected, this.state);
        }
        if (this.strictlyOneLeafPairPays && (this.aliceTrackers[id] > 1 || this.bobPayCounter[id] > 1)) {
          throw new DslErrors.PerfectHedgeError("Only one pair in a tree is alowed to pay", this.state, void 0, void 0, this.selected);
        }
      },
      evaluateWithPaymentCtx: (payhandler) => {
        let numbers = [];
        for (let i = from; i <= to; i += step) {
          numbers.push(i);
        }
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          this.unsafe.if(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args, false, allowMisplacedPay).then((h) => {
            if (l.length === 1) {
              this.unsafe.if(pubkey, l.map((x) => x.toString()), [], args, false, allowMisplacedPay, false, true).then((h2) => {
                payhandler(h2, l[0]);
              });
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          }).else((h) => {
            if (r.length === 1) {
              payhandler(h, l[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          });
        };
        recurse(numbers.slice(0, numbers.length / 2), numbers.slice(numbers.length / 2));
        const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
        if (!allowUnsafe) {
          if (this.aliceTrackers[id] > numbers.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
          }
          if (this.bobTrackers[id] > numbers.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
          }
        }
      },
      value: () => {
        let numbers = [];
        for (let i = from; i <= to; i += step) {
          numbers.push(i);
        }
        const recurse = (l, r) => {
          if (l.length === 1 && r.length === 0) {
            return l[0];
          }
          if (r.length === 1 && l.length === 0) {
            return r[0];
          }
          if (this.unsafe.outcome(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args)) {
            if (l.length === 1) {
              if (this.unsafe.outcome(pubkey, l.map((x) => x.toString()), [], args, false, false, true)) {
                return l[0];
              } else {
                throw "skip";
              }
            } else {
              return recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          } else {
            if (r.length === 1) {
              return r[0];
            } else {
              return recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          }
        };
        if (!allowUnsafe) {
          throw new Error("Only available in unsafe mode");
        }
        return recurse(numbers.slice(0, numbers.length / 2), numbers.slice(numbers.length / 2));
      },
      valueWithPaymentCtxUnsafe: () => {
        let numbers = [];
        for (let i = from; i <= to; i += step) {
          numbers.push(i);
        }
        let nn = numbers[0];
        let hh = void 0;
        let payhandler = (h, n) => {
          nn = n;
          hh = h;
        };
        let argument = args;
        const recurse = (l, r, payhandlerOut) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          this.unsafe.if(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), argument, false, allowMisplacedPay).then((h) => {
            if (l.length === 1) {
              this.unsafe.if(pubkey, l.map((x) => x.toString()), [], args, false, allowMisplacedPay, false, true).then((h2) => {
                payhandler(h2, l[0]);
              }).else((_) => {
                throw "skip";
              });
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2), h);
            }
          }).else((h) => {
            if (r.length === 1) {
              payhandler(h, r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2), h);
            }
          });
        };
        recurse(numbers.slice(0, numbers.length / 2), numbers.slice(numbers.length / 2), null);
        this.unfinalized++;
        const saveRelease = hh.release;
        hh.release = () => {
          const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
          if (!allowUnsafe) {
            if (this.aliceTrackers[id] > numbers.length - 1) {
              throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
            }
            if (this.bobTrackers[id] > numbers.length - 1) {
              throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
            }
          }
          saveRelease();
        };
        if (hh === void 0) {
          this.unfinalized--;
          throw "skip";
        }
        return [nn, hh];
      }
    })
  };
  set = {
    safe: {
      outcome: (pubkey, yes, no, args = {}, allowMisplacedPay = false) => {
        if (this.outcome(pubkey, [yes], [no], args)) {
          return yes;
        } else {
          return no;
        }
      },
      outcomeT: (pubkey, yes, no, renderer = (x) => x.toString(), args = {}) => {
        if (this.outcome(pubkey, [renderer(yes)], [renderer(no)], args)) {
          return yes;
        } else {
          return no;
        }
      },
      if: (pubkey, yes, no, args = {}) => {
        const iff = this.if(pubkey, [yes], [no], args);
        return {
          then: (handler) => {
            const thenn = iff.then((h) => handler(yes, h));
            return {
              else: (handler2) => {
                return thenn.else((h) => handler2(no, h));
              }
            };
          }
        };
      },
      ifT: (pubkey, yes, no, renderer = (x) => x.toString(), args = {}) => {
        const iff = this.if(pubkey, [renderer(yes)], [renderer(no)], args);
        return {
          then: (handler) => {
            const thenn = iff.then((h) => handler(yes, h));
            return {
              else: (handler2) => {
                return thenn.else((h) => handler2(no, h));
              }
            };
          }
        };
      }
    },
    outcome: (pubkey, set, args = {}, allowMisplacedPay = false, allowUnsafe = true) => ({
      evaluate: (handler) => {
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          if (this.unsafe.outcome(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args)) {
            if (l.length === 1) {
              if (this.unsafe.outcome(pubkey, l, [], args, false, true, true)) {
                handler(l[0]);
              }
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          } else {
            if (r.length === 1) {
              handler(r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          }
        };
        recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
        const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
        if (!allowUnsafe) {
          if (this.aliceTrackers[id] > set.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
          }
          if (this.bobTrackers[id] > set.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
          }
        }
        if (this.strictlyOneLeafPays && (this.aliceTrackers[id] > 1 || this.bobPayCounter[id] > 1)) {
          throw new DslErrors.OnePayPerCondition("Only one leaf in a tree is alowed to pay", void 0, void 0, this.selected, this.state);
        }
        if (this.strictlyOneLeafPairPays && (this.aliceTrackers[id] > 1 || this.bobPayCounter[id] > 1)) {
          throw new DslErrors.PerfectHedgeError("Only one pair in a tree is alowed to pay", this.state, void 0, void 0, this.selected);
        }
      },
      evaluateWithPaymentCtx: (payhandler) => {
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          this.if(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args, false, allowMisplacedPay).then((h) => {
            if (l.length === 1) {
              this.if(pubkey, l.map((x) => x.toString()), [], args, false, allowMisplacedPay, false, true).then((h2) => {
                payhandler(h2, l[0]);
              });
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          }).else((h) => {
            if (r.length === 1) {
              payhandler(h, r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          });
        };
        if (this.strictlyOneLeafPays || this.strictlyOneLeafPairPays) {
          throw Error("Leaf strictness tracking is not available in this mode. Use `evaluate`");
        }
        recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
        const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
        if (!allowUnsafe) {
          if (this.aliceTrackers[id] > set.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
          }
          if (this.bobTrackers[id] > set.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
          }
        }
      },
      value: () => {
        const recurse = (l, r) => {
          if (l.length === 1 && r.length === 0) {
            return l[0];
          }
          if (r.length === 1 && l.length === 0) {
            return r[0];
          }
          if (this.outcome(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args)) {
            if (l.length === 1) {
              if (this.outcome(pubkey, l.map((x) => x.toString()), [], args, false, false, true)) {
                return l[0];
              } else {
                throw "skip";
              }
            } else {
              return recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          } else {
            if (r.length === 1) {
              return r[0];
            } else {
              return recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          }
        };
        if (this.strictlyOneLeafPays || this.strictlyOneLeafPairPays) {
          throw Error("Leaf strictness tracking is not available in this mode. Use `evaluate`");
        }
        return recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
      },
      valueWithPaymentCtxUnsafe: () => {
        let nn = set[0];
        let hh = void 0;
        const payhandler = (h, n) => {
          nn = n;
          hh = h;
        };
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          this.if(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args, false, allowMisplacedPay).then((h) => {
            if (l.length === 1) {
              this.if(pubkey, l.map((x) => x.toString()), [], args, false, allowMisplacedPay, true).then((h2) => {
                payhandler(h2, l[0]);
              });
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          }).else((h) => {
            if (r.length === 1) {
              payhandler(h, r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          });
        };
        if (this.strictlyOneLeafPays || this.strictlyOneLeafPairPays) {
          throw Error("Leaf strictness tracking is not available in this mode. Use `evaluate`");
        }
        recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
        this.unfinalized++;
        const saveRelease = hh.release;
        hh.release = () => {
          const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
          if (!allowUnsafe) {
            if (this.aliceTrackers[id] > set.length - 1) {
              throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
            }
            if (this.bobTrackers[id] > set.length - 1) {
              throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
            }
          }
          saveRelease();
        };
        if (hh === void 0) {
          this.unfinalized--;
          throw "skip";
        }
        return [nn, hh];
      }
    }),
    outcomeT: (pubkey, set, renderer, args = {}, allowMisplacedPay = false, allowUnsafe = false) => ({
      evaluate: (handler) => {
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          if (this.unsafe.outcome(pubkey, l.map((x) => renderer(x)), r.map((x) => renderer(x)), args)) {
            if (l.length === 1) {
              if (this.unsafe.outcome(pubkey, l.map((x) => renderer(x)), [], args, false, false, true)) {
                handler(l[0]);
              }
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          } else {
            if (r.length === 1) {
              handler(r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          }
        };
        recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
        const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
        if (!allowUnsafe) {
          if (this.aliceTrackers[id] > set.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
          }
          if (this.bobTrackers[id] > set.length - 1) {
            throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
          }
        }
        if (this.strictlyOneLeafPays && (this.aliceTrackers[id] > 1 || this.bobPayCounter[id] > 1)) {
          throw new DslErrors.OnePayPerCondition("Only one leaf in a tree is alowed to pay", void 0, void 0, this.selected, this.state);
        }
        if (this.strictlyOneLeafPairPays && (this.aliceTrackers[id] > 1 || this.bobPayCounter[id] > 1)) {
          throw new DslErrors.PerfectHedgeError("Only one pair in a tree is alowed to pay", this.state, void 0, void 0, this.selected);
        }
      },
      evaluateWithPaymentCtx: (payhandler) => {
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          this.if(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args, false, allowMisplacedPay).then((h) => {
            if (l.length === 1) {
              this.if(pubkey, l.map((x) => x.toString()), [], args, false, allowMisplacedPay, false, true).then((h2) => {
                payhandler(h2, l[0]);
              });
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          }).else((h) => {
            if (r.length === 1) {
              payhandler(h, r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          });
        };
        if (this.strictlyOneLeafPays || this.strictlyOneLeafPairPays) {
          throw Error("Leaf strictness tracking is not available in this mode. Use `evaluate`");
        }
        recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
      },
      value: () => {
        const recurse = (l, r) => {
          if (l.length === 1 && r.length === 0) {
            return l[0];
          }
          if (r.length === 1 && l.length === 0) {
            return r[0];
          }
          if (this.outcome(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args)) {
            if (l.length === 1) {
              if (this.outcome(pubkey, l.map((x) => x.toString()), [], args, false, false, true)) {
                return l[0];
              }
            } else {
              return recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          } else {
            if (r.length === 1) {
              return r[0];
            } else {
              return recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          }
        };
        if (this.strictlyOneLeafPays || this.strictlyOneLeafPairPays) {
          throw Error("Leaf strictness tracking is not available in this mode. Use `evaluate`");
        }
        return recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
      },
      valueWithPaymentCtxUnsafe: () => {
        let nn = set[0];
        let hh = void 0;
        let payhandler = (h, n) => {
          nn = n;
          hh = h;
        };
        const recurse = (l, r) => {
          if (l.length === 0) {
            return;
          }
          if (r.length === 0) {
            return;
          }
          const rt = this.if(pubkey, l.map((x) => x.toString()), r.map((x) => x.toString()), args, false, allowMisplacedPay).then((h) => {
            if (l.length === 1) {
              this.if(pubkey, l.map((x) => x.toString()), [], args, false, allowMisplacedPay, false, true).then((h2) => {
                payhandler(h2, l[0]);
              });
            } else {
              recurse(l.slice(0, l.length / 2), l.slice(l.length / 2));
            }
          }).else((h) => {
            if (r.length === 1) {
              payhandler(h, r[0]);
            } else {
              recurse(r.slice(0, r.length / 2), r.slice(r.length / 2));
            }
          });
        };
        if (this.strictlyOneLeafPays || this.strictlyOneLeafPairPays) {
          throw Error("Leaf strictness tracking is not available in this mode. Use `evaluate`");
        }
        recurse(set.slice(0, set.length / 2), set.slice(set.length / 2));
        this.unfinalized++;
        const saveRelease = hh.release;
        hh.release = () => {
          const id = this.megaMode ? pubkey : pubkey + JSON.stringify(args);
          if (!allowUnsafe) {
            if (this.aliceTrackers[id] > set.length - 1) {
              throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 0, this.selected);
            }
            if (this.bobTrackers[id] > set.length - 1) {
              throw new DslErrors.PerfectHedgeError("Party cannot benefit regardless of outcome", this.state, void 0, 1, this.selected);
            }
          }
          saveRelease();
        };
        if (hh === void 0) {
          this.unfinalized--;
          throw "skip";
        }
        return [nn, hh];
      }
    })
  };
  assert = {
    sum: {
      budget: (amount) => {
        if (this.collateral1 + this.collateral2 > amount) {
          throw Error(`Max mutual budget assertion: ${this.collateral1} > ${amount}`);
        }
      },
      parties: (party1, party2) => ({
        budget: (amount) => {
          if (this.isSelected0(party1) && this.isSelected1(party2)) {
            if (this.collateral1 + this.collateral2 > amount) {
              throw Error(`Max mutual budget assertion: ${this.collateral1} > ${amount}`);
            }
          }
          if (this.isSelected0(party2) && this.isSelected1(party1)) {
            if (this.collateral1 + this.collateral2 > amount) {
              throw Error(`Max mutual budget assertion: ${this.collateral1} > ${amount}`);
            }
          }
        }
      })
    },
    budget: (idx, amount) => {
      if (idx == 0) {
        if (this.collateral1 > amount) {
          throw Error(`Max Alice budget assertion: ${this.collateral1} > ${amount}`);
        }
      } else {
        if (this.collateral1 > amount) {
          throw Error(`Max Bob budget assertion: ${this.collateral2} > ${amount}`);
        }
      }
    },
    parties: (party1, party2) => ({
      budget: (idx, amount) => {
        if (this.isSelected0(party1) && this.isSelected1(party2)) {
          if (idx == 0) {
            if (this.collateral1 > amount) {
              throw Error(`Max ${party1} budget assertion (against ${party2}): ${this.collateral1} > ${amount}`);
            }
          } else {
            if (this.collateral1 > amount) {
              throw Error(`Max ${party2} budget assertion (against ${party1}): ${this.collateral2} > ${amount}`);
            }
          }
        }
        if (this.isSelected0(party2) && this.isSelected1(party1)) {
          if (idx == 0) {
            if (this.collateral1 > amount) {
              throw Error(`Max ${party2} budget assertion (against ${party1}): ${this.collateral1} > ${amount}`);
            }
          } else {
            if (this.collateral1 > amount) {
              throw Error(`Max ${party1} budget assertion (against ${party2}): ${this.collateral2} > ${amount}`);
            }
          }
        }
      }
    })
  };
  disablePartyRoleReversal = false;
  if = (pubkey, yes, no, args = {}, allowSwaps = false, allowMisplacedPay = false, strict = true, ignoreObserveChecksSuperUnsafe = false) => {
    let contradiction = false;
    const yesSet = new Set(yes);
    const noSet = new Set(no);
    if (yes.find((x) => noSet.has(x)) || no.find((x) => yesSet.has(x))) {
      contradiction = true;
    }
    const observation = this.outcome(pubkey, yes, no, args, allowSwaps, strict, ignoreObserveChecksSuperUnsafe);
    const currentNode = this.cursor;
    const currentPrevNode = this.prev;
    const currentLastOutcome = this.lastOutcome;
    return {
      then: (handler) => {
        let party = void 0;
        let sum = 0;
        const funds = {
          pay: (idx, amount) => {
            if (party === void 0 || idx === party) {
              sum += amount;
              party = idx;
            } else {
              if (party !== void 0 && idx !== party) {
                if (this.disablePartyRoleReversal) {
                  throw new Error("Party role reversal is disabled!");
                }
                sum -= amount;
              } else {
                throw new DslErrors.PerfectHedgeError("Perfect Hedge! Party cannot benefit regardless of outcome!", this.state, amount, idx, this.selected);
              }
            }
          },
          party: (partyName, partyAsset) => ({
            pays: (counterpartyName, counterpartyAsset) => ({
              amount: (amount, asset) => {
                const party2 = partyName + (partyAsset ? "_" + partyAsset : "");
                const counterparty = counterpartyName + (counterpartyAsset ? "_" + counterpartyAsset : "");
                if (currentNode !== this.cursor && !allowMisplacedPay) {
                  throw Error("Possibly trying to pay nondeterministically! You tried to use outer account context to pay: use the closest `if(...).then/else(account => ...)` please! This also happens when you pay after checking unrelated observations: pay before checking next outcome! You can turn this off by using `dsl.unsafe`");
                }
                if (partyAsset !== asset) {
                  throw Error(`Trying to pay ${asset} from collateral denominated in ${partyAsset}`);
                }
                if (!this.multiparty.find((x) => x === party2)) {
                  throw Error("party " + party2 + " not registered! Use .multiple to register parties");
                }
                if (!this.multiparty.find((x) => x === counterparty)) {
                  throw Error("counterparty " + counterparty + " not registered! Use .multiple to register parties");
                }
                if (this.isSelected0(party2) && this.isSelected1(counterparty)) {
                  funds.pay(0, amount);
                } else if (this.isSelected0(counterparty) && this.isSelected1(party2)) {
                  funds.pay(1, amount);
                }
                if (sum < 0 && partyAsset !== counterpartyAsset) {
                  throw new Error(`Semantics: ${partyName} cannot pay negative amount of ${partyAsset} units. It is only allowed if assets are of the same type.`);
                }
                if (contradiction && partyAsset === counterpartyAsset) {
                  throw Error("Contradiction! Outcomes are not mutually exclusive! Cannot allow swaps in same currency!");
                }
              }
            })
          }),
          release: () => {
            if (currentNode !== this.cursor && !allowMisplacedPay) {
              throw Error("Trying to release nondeterministically! You tried to release using outer account context: use the closest `if(...).then/else(account => ...)` please! This also happens if you release after checking unrelated observations: release before next `outcome; use `allowMisplacedPay` in `dsl.if` to disable this check`");
            }
            const saveCursor = this.cursor;
            const savePrev = this.prev;
            const saveFlag = this.flag;
            const saveLastOutcome = this.lastOutcome;
            if (allowMisplacedPay) {
              this.cursor = currentNode;
              this.prev = currentPrevNode;
              this.flag = false;
              this.lastOutcome = currentLastOutcome;
            }
            this.unfinalized--;
            funds.party = void 0;
            funds.pay = void 0;
            if (party !== void 0 && sum !== 0) {
              if (sum > 0) {
                this.pay(party, sum);
              } else if (sum < 0) {
                this.pay(party === 0 ? 1 : 0, -sum);
              }
            }
            this.cursor = saveCursor;
            this.prev = savePrev;
            this.flag = saveFlag;
            this.lastOutcome = saveLastOutcome;
          }
        };
        const finalizeUnsafeInternal = () => {
          if (observation) {
            handler(funds);
            const saveCursor = this.cursor;
            const savePrev = this.prev;
            const saveFlag = this.flag;
            const saveLastOutcome = this.lastOutcome;
            if (allowMisplacedPay) {
              this.cursor = currentNode;
              this.prev = currentPrevNode;
              this.flag = false;
              this.lastOutcome = currentLastOutcome;
            }
            if (party !== void 0 && sum !== 0) {
              if (sum > 0) {
                this.pay(party, sum);
              } else if (sum < 0) {
                this.pay(party === 0 ? 1 : 0, -sum);
              }
            }
            this.cursor = saveCursor;
            this.prev = savePrev;
            this.flag = saveFlag;
            this.lastOutcome = saveLastOutcome;
          }
        };
        finalizeUnsafeInternal();
        return {
          else: (handler2) => {
            let counterparty = void 0;
            let sum2 = 0;
            const funds2 = {
              pay: (idx, amount) => {
                if (counterparty === void 0 || idx === counterparty) {
                  if (party !== void 0 && counterparty !== void 0 && party === counterparty) {
                    throw new DslErrors.PerfectHedgeError("Perfect Hedge! Party cannot benefit regardless of outcome!", this.state, amount, idx, this.selected);
                  }
                  counterparty = idx;
                  sum2 += amount;
                } else {
                  if (counterparty !== void 0 && idx !== counterparty) {
                    if (this.disablePartyRoleReversal) {
                      throw new Error("Party role reversal is disabled!");
                    }
                    sum2 -= amount;
                  } else {
                    throw new DslErrors.PerfectHedgeError("Perfect Hedge! Party cannot benefit regardless of outcome!", this.state, amount, idx, this.selected);
                  }
                }
              },
              party: (partyName, partyAsset) => ({
                pays: (counterpartyName, counterpartyAsset) => ({
                  amount: (amount, asset) => {
                    const party2 = partyName + (partyAsset ? "_" + partyAsset : "");
                    const counterparty2 = counterpartyName + (counterpartyAsset ? "_" + counterpartyAsset : "");
                    if (currentNode !== this.cursor && !allowMisplacedPay) {
                      throw Error("Possibly trying to pay nondeterministically! You tried to use outer account context to pay: use the closest `if(...).then/else(account => ...)` please! This also happens when you pay after checking unrelated observations: pay before checking next outcome! You can turn this off by using `dsl.unsafe`");
                    }
                    if (partyAsset !== asset) {
                      throw Error(`Trying to pay ${asset} from collateral denominated in ${partyAsset}`);
                    }
                    if (!this.multiparty.find((x) => x === party2)) {
                      throw Error("party " + party2 + " not registered! Use .multiple to register parties");
                    }
                    if (!this.multiparty.find((x) => x === counterparty2)) {
                      throw Error("counterparty " + counterparty2 + " not registered! Use .multiple to register parties");
                    }
                    if (!this.multiparty.find((x) => x === party2)) {
                      throw Error("party " + party2 + " not registered! Use .multiple to register parties in " + this.multiparty);
                    }
                    if (!this.multiparty.find((x) => x === counterparty2)) {
                      throw Error("counterparty " + counterparty2 + " not registered! Use .multiple to register parties in " + this.multiparty);
                    }
                    if (this.isSelected0(party2) && this.isSelected1(counterparty2)) {
                      funds2.pay(0, amount);
                    } else if (this.isSelected0(counterparty2) && this.isSelected1(party2)) {
                      funds2.pay(1, amount);
                    }
                  }
                })
              }),
              release: () => {
                if (currentNode !== this.cursor && !allowMisplacedPay) {
                  throw Error("Possibly trying to release nondeterministically! You tried to release using outer account context: use the closest `if(...).then/else(account => ...)` please! This also happens if you release after checking unrelated observations: release before next `outcome`; use `allowMisplacedPay` in `dsl.if` to disable this check");
                }
                const saveCursor = this.cursor;
                const savePrev = this.prev;
                const saveFlag = this.flag;
                const saveLastOutcome = this.lastOutcome;
                if (allowMisplacedPay) {
                  this.cursor = currentNode;
                  this.prev = currentPrevNode;
                  this.flag = false;
                  this.lastOutcome = currentLastOutcome;
                }
                this.unfinalized--;
                funds2.party = void 0;
                funds2.pay = void 0;
                if (counterparty !== void 0 && sum2 !== 0) {
                  if (sum2 > 0) {
                    this.pay(counterparty, sum2);
                  } else if (sum2 < 0) {
                    this.pay(counterparty === 0 ? 1 : 0, -sum2);
                  }
                }
                this.cursor = saveCursor;
                this.prev = savePrev;
                this.flag = saveFlag;
                this.lastOutcome = saveLastOutcome;
              }
            };
            const finalizeUnsafeInternal2 = () => {
              if (!observation) {
                handler2(funds2);
                const saveCursor = this.cursor;
                const savePrev = this.prev;
                const saveFlag = this.flag;
                const saveLastOutcome = this.lastOutcome;
                if (allowMisplacedPay) {
                  this.cursor = currentNode;
                  this.prev = currentPrevNode;
                  this.flag = false;
                  this.lastOutcome = currentLastOutcome;
                }
                if (counterparty !== void 0 && sum2 !== 0) {
                  if (sum2 > 0) {
                    this.pay(counterparty, sum2);
                  } else if (sum2 < 0) {
                    this.pay(counterparty === 0 ? 1 : 0, -sum2);
                  }
                }
                this.cursor = saveCursor;
                this.prev = savePrev;
                this.flag = saveFlag;
                this.lastOutcome = saveLastOutcome;
              }
            };
            finalizeUnsafeInternal2();
          }
        };
      }
    };
  };
  ifAtomicSwapLeg1(lock = "TRUTH", unlockOutcome = "true", allowMisplacedPay = false) {
    return this.if(lock, [unlockOutcome], [unlockOutcome], {}, true, allowMisplacedPay);
  }
  multiflag = false;
  async enumerateWithBoundMulti(collateralBounds) {
    this.multiflag = true;
    const pairs = this.multiparty.map((x) => this.multiparty.map((y) => [x, y]).filter((pair) => pair[0] !== pair[1])).flat().map((x) => x.sort());
    const ids = /* @__PURE__ */ new Set();
    const uniquepairs = pairs.filter((id) => !ids.has(JSON.stringify(id)) && ids.add(JSON.stringify(id)));
    const mutex = new Mutex();
    const res = await Promise.all(uniquepairs.map(async (pair, i) => {
      return await mutex.runExclusive(async () => {
        this.selected = pair;
        if (!collateralBounds[i]) {
          throw Error("Specify bounds for a pair " + pair + " at index: " + i);
        }
        try {
          const subcontract = await this.enumerateWithBound(collateralBounds[i][0], collateralBounds[i][1]);
          return [pair[0], pair[1], subcontract];
        } catch (e) {
          if (e instanceof DslErrors.EmptyDslOutput) {
            return [pair[0], pair[1], void 0];
          } else {
            throw e;
          }
        }
      });
    }));
    this.multiflag = false;
    return res;
  }
  async enumerateWithBound(collateralBound1, collateralBound2) {
    if (this.multiparty.length > 0 && !this.multiflag) {
      throw Error("use `enumerateWithBoundMulti` for multiparty contracts!");
    }
    if (this.protect) {
      throw "Don't call enumerate inside of the body of your script!";
    }
    this.protect = true;
    let next = true;
    const mutex = new Mutex();
    this.aliceTrackers = {};
    this.bobTrackers = {};
    while (next) {
      try {
        this.flagSameAssetSwap = false;
        this.collateral1 = 0;
        this.collateral2 = 0;
        this.budgetBound1 = collateralBound1;
        this.budgetBound2 = collateralBound2;
        this.memoize = [];
        this.checked = [];
        this.counter = 0;
        this.lastOutcome = void 0;
        this.flag = false;
        await mutex.runExclusive(async () => await this.body(this));
        if (this.unfinalized !== 0) {
          throw new Error("" + this.unfinalized + " resource locks are not released! Every `[v, payments] = valueWithPaymentCtxUnsafe` must have a corresponding `payments.release()`");
        }
        if (this.safeModeStarted && !this.safeMode) {
          throw new Error("All insecure safe mode escape sections must be closed! Forgot `insecure.close.reEnableMode`?");
        }
        if (this.megaModeStarted && !this.megaMode) {
          throw new Error("All insecure mega mode escape sections must be closed! Forgot `insecure.close.reEnableMode`?");
        }
        if (this.strictModeStarted && !this.strictlyStrict) {
          throw new Error("All insecure strict mode escape sections must be closed! Forgot `insecure.close.reEnableMode`?");
        }
        if (this.superModeStarted && !this.superMode) {
          throw new Error("All insecure super mode escape sections must be closed! Forgot `insecure.close.reEnableMode`?");
        }
        if (this.fairModeStarted && !this.strictlyFair) {
          throw new Error("All insecure fair mode escape sections must be closed! Forgot `insecure.close.reEnableMode`?");
        }
      } catch (e) {
        if (e === "uninitialized" || e === "skip") {
        } else {
          throw e;
        }
      }
      next = this.next();
    }
    this.protect = false;
    this.leafsFiltered = true;
    let result = this.root;
    while (this.leafsFiltered) {
      this.leafsFiltered = false;
      result = this.filterLeafs(result);
    }
    if (this.strictlyStrict) {
      this.filterLeafs(result, true);
    }
    return result;
  }
};
if (typeof window === "undefined" && __require.main === module) {
  (async () => {
    const model = await new Dsl(async (dsl) => {
      const a = 60;
      if (dsl.outcome("really?", ["YES"], ["NO"])) {
        dsl.pay(Dsl.Bob, a + 100);
        const out1 = dsl.outcome("is it?", ["YES"], ["NO", "DON'T KNOW"]);
        if (out1) {
          dsl.pay(Dsl.Alice, 40);
          if (dsl.outcome("is it?", ["DON'T KNOW"], ["NO", "YES"])) {
            dsl.pay(Dsl.Bob, 40);
            dsl.if("lol?", ["yup"], ["nope"]).then((funds) => {
              funds.pay(Dsl.Bob, 20);
              funds.pay(Dsl.Alice, 30);
            }).else((funds) => {
              funds.pay(Dsl.Bob, 40);
              funds.pay(Dsl.Alice, 10);
            });
          }
        } else {
          dsl.pay(Dsl.Bob, 50);
        }
      } else {
        dsl.pay(Dsl.Alice, 20);
      }
    }).multiple("alice", "bob").enumerateWithBoundMulti([[1e3, 2e4]]);
    console.log(model);
    const multi = await new Dsl(async (dsl) => {
      if (dsl.outcome("really?", ["YES"], ["NO"])) {
        dsl.party("alice").pays("bob").amount(100);
        dsl.party("bob").pays("carol").amount(20);
      } else {
        dsl.party("carol").pays("alice").amount(40);
        dsl.party("bob").pays("alice").amount(40);
        dsl.if("wow?", ["yup"], ["nope"]).then((account) => {
          account.party("alice").pays("carol").amount(30);
          account.party("carol").pays("alice").amount(5);
        }).else((account) => {
          account.party("carol").pays("alice").amount(30);
        });
      }
    }).multiple("alice", "bob", "carol").enumerateWithBoundMulti([[1e3, 2e3], [1e3, 2e3], [1e3, 2e3]]);
    console.log(multi);
    const multi2 = await new Dsl(async (dsl) => {
      const dates = ["today", "tomorrow", "next week", "next month"];
      const capitalisationDates = /* @__PURE__ */ new Set(["next week"]);
      const notional = 1e4;
      const floatingLegIndex = "interest rate index?";
      const fixedRate = 0.990382835;
      const quantisationStep = 1;
      dates.reduce(([capitalisation1, capitalisation2], date) => {
        const [floatingRate, accounts] = dsl.unsafe.numeric.outcome(floatingLegIndex, 0, 1, quantisationStep, { date }).valueWithPaymentCtxUnsafe();
        if (capitalisationDates.has(date)) {
          const floatingPayout = (notional + capitalisation1) * (floatingRate / 100);
          const fixedPayout = (notional + capitalisation2) * (fixedRate / 100);
          accounts.party("alice").pays("bob").amount(floatingPayout);
          accounts.party("bob").pays("alice").amount(fixedPayout);
          console.log("!!!!!" + (floatingPayout - fixedPayout) + " ==> " + JSON.stringify(dsl["aliceTrackers"]));
          accounts.release();
          return [0, 0];
        } else {
          accounts.release();
          return [
            notional * (floatingRate / 100) + capitalisation1,
            notional * (fixedRate / 100) + capitalisation2
          ];
        }
      }, [0, 0]);
    }).multiple("alice", "bob").enumerateWithBoundMulti([[5e4, 2e4]]);
    console.log(multi2);
    const assets = await new Dsl(async (dsl) => {
      if (dsl.outcome("really?", ["YES"], ["NO"])) {
        dsl.party("alice", "usd").pays("bob", "btc").amount(1e7, "usd");
      } else {
        dsl.party("bob", "btc").pays("alice", "usd").amount(10, "btc");
      }
    }).multiple(Dsl.account("alice", "usd"), Dsl.account("bob", "btc")).enumerateWithBoundMulti([[1e9, 2e4]]);
    console.log(assets);
    const swap = await new Dsl(async (dsl) => {
      dsl.atomicSwap.ifTruth("lock1", "allowed").then((pay) => {
        dsl.atomicSwap.ifTruth("lock12", "allowed").then((pay2) => {
          pay2.party("bob", "btc").pays("alice", "usd").amount(10, "btc");
        }).else(() => {
        });
        pay.party("alice", "usd").pays("bob", "btc").amount(1e7, "usd");
      }).else((pay) => {
        pay.party("bob", "btc").pays("alice", "usd").amount(10, "btc");
      });
      dsl.unsafe.numeric.outcome("???", 0, 3).evaluate((x) => {
        dsl.pay(Dsl.Alice, 10);
      });
      dsl.numeric.infinity.bounded(100).perpetual(0, (x, st) => {
        return dsl.infinity.stop;
      });
      dsl.unsafe.numeric.infinity.bounded(100).perpetual({ "alice": 100, "bob": 100 }, (x, st) => {
        const shouldnot = dsl.unsafe.infinity.move([{
          alice: st.alice - 1,
          bob: st.bob - 2
        }, [
          {
            from: ["alice", "usd"],
            to: ["bob", "btc"],
            amount: 1
          },
          {
            from: ["bob", "btc"],
            to: ["alice", "usd"],
            amount: 2
          }
        ]]);
        return dsl.unsafe.infinity.stop([]);
      });
    }).multiple(Dsl.account("alice", "usd"), Dsl.account("bob", "btc")).enumerateWithBoundMulti([[1e10, 2e5]]);
    console.log(swap);
    const turing = (a) => () => {
      if (a > 5) {
        return 7;
      } else {
        return Dsl.recurse.bounded(turing(a)).attempts(30).otherwiseYield(50)();
      }
    };
    console.log(turing(7)());
    console.log(turing(1)());
    console.log("OK!");
  })();
}

// src-web/util/dsl-runtime.ts
var evalDiscreet = async (expression, parties, bounds) => {
  const model = await new Dsl(async (dsl) => {
    const prototypeWhitelist = Sandbox.SAFE_PROTOTYPES;
    const globals = { ...Sandbox.SAFE_GLOBALS, alert };
    prototypeWhitelist.set(Dsl, /* @__PURE__ */ new Set());
    const sandbox = new Sandbox({ globals, prototypeWhitelist });
    const exec = sandbox.compile(expression);
    exec({ dsl, Dsl }).run();
  }).multiple(...parties).enumerateWithBoundMulti(bounds);
  return model;
};
window.evalDiscreet = evalDiscreet;
export {
  evalDiscreet
};
//# sourceMappingURL=discreet-eval.min.js.map
